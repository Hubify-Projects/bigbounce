# Ledger row 19 — the joint (r, f_NL) no-go for general P(X) k-essence with λ ≠ 0

**Status:** IN PROGRESS 2026-09-04.
**Owner lane:** `research/track_a3_multichannel/row19_lambda/`
(does NOT touch `research/track_a3_multichannel/paper/`).
**Artifacts (planned):** `row19_lambda.py`, `results.json`, `row19_lambda.log`,
`row19_lambda.png`, manifest `reproducibility/manifests/experiments/a3-row19-lambda.json`.

## Why

Row 14 (`../row14_cs_window/ROW14_CS_WINDOW_2026-09-04.md`) closed the `c_s` window
as a NULL: `r = 24 c_s` forces `c_s < 1.5e-3` for BICEP/Keck, while
`f_NL^{pre}(c_s) = -165/16 + 65/(8c_s^2)` (Li, Quintin, Wang & Cai 2016,
arXiv:1612.02036 Eq. 4.19, squeezed isoceles) forces `c_s > 0.44` for Planck —
disjoint by ~296x on the Quintin background. **That derivation was done at
`λ = 0`.** R8 audit item R8-01 flagged that Li+2016's Eq. (4.19) carries the
cubic-action coefficient `λ` (the `X^3`/`\dot\zeta^3` coefficient of
Chen, Huang, Kachru & Shiu 2007, hep-th/0605045), so a nonzero `λ` changes the
`c_s`-dependence of `f_NL^{pre}` and could in principle re-open the window.
Decision D-A3-13 states the no-go for `λ = 0` only; row 19 must either
generalise it or find the `λ` that breaks it.

## Plan

1. **Fetch and transcribe** Li+2016 Eq. (4.19) with its full `λ` (and, if carried,
   sound-speed running `s`) dependence; state the mapping between Li's `λ`
   convention and CHKS's `λ/Σ`.
2. **`f_NL^{pre}(c_s, λ)`** squeezed and equilateral by sympy from the transcribed
   shape function. Validation gates: at `λ = 0` it must reproduce row 14's
   `-165/16 + 65/(8c_s^2)` (squeezed) and `5(9c_s^4 - 268c_s^2 + 208)/(128c_s^2)`
   (equilateral); at `c_s = 1, λ = 0` it must give `-35/16`.
3. **Does `r` depend on `λ`?** `r = 16 ε c_s^{2ν-2} = 24 c_s` follows from the
   QUADRATIC action; `λ` lives only in the cubic action. Verify explicitly.
4. **Bounce term with the λ vertex.** Extend row 18b's S1 in-in integrator
   (`../../cubic_bounce_transmission/row18b_cs_bounce_cubic/`) with the λ-vertex
   coefficient from lane (a)'s vertex table
   (`../../cubic_bounce_transmission/lane_a_vertex_table/cubic_vertex_table.py`)
   at the same `kη_B = 1e-3` on the Quintin background, scanning
   `λ/Σ ∈ {-1, -0.5, 0, 0.5, 1}` plus the DBI physical line (`λ/Σ` fixed by `c_s`).
5. **The window.** For each `λ`, find the `c_s` range with `|f_NL^{after}| ≤ 5.1`
   (Planck 1σ) and the `r` there. Report `min r` over the whole scan and where it
   sits. Verdict: does ANY physical `λ` make `r < 0.036` and `|f_NL^{after}| ≤ 5.1`
   coincide?

## Ground rules

Never tune. Every number in the results table comes from the committed script.
Claims stated at exactly their evidential strength; a null is published as a null.
The DBI line is included because it is the one case where `λ` is not a free knob.
