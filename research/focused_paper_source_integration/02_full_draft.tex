\documentclass[aps,prd,twocolumn,superscriptaddress,showpacs,preprintnumbers,nofootinbib,longbibliography,floatfix]{revtex4-2}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\PassOptionsToPackage{hyphens}{url}
\usepackage[colorlinks=true,allcolors=blue]{hyperref}
\usepackage{url}
% Let \path{} break long repo-relative paths at slashes/underscores/hyphens
% (avoids column overflow on committed file paths; presentation only).
\makeatletter
\g@addto@macro\UrlBreaks{\do\/\do\_\do\-\do\.}
\makeatother
\usepackage{booktabs}
\usepackage{ragged2e}

\newcommand{\fnl}{f_{\rm NL}}
\newcommand{\BNL}{B_{\rm NL}}
\newcommand{\Pzeta}{\mathcal{P}_\zeta}
\newcommand{\eg}{e.g.}
\newcommand{\ie}{i.e.}
\newcommand{\etal}{\textit{et~al.}}

\begin{document}

%\preprint{arXiv:XXXX.XXXXX}

\title{Testing the Matter Bounce with Primordial Non-Gaussianity:\\
A SPHEREx Sensitivity Recast with a MegaMapper Outlook}

\author{Houston Golden}
\email{houston@hubify.com}
\affiliation{Independent Researcher, Los Angeles, California, USA}

\date{July 10, 2026}
% v1.7.112 (2026-07-10 — H17H Claude-referee presentation closures (retest2_P2_claude MAJOR-REVISIONS,
%   verdict-first: NO computational error, all arithmetic reconciled; items are presentation/disclosure).
%   NO headline number changed, -35/16 UNCHANGED, nothing fabricated. Closures:
%   (1) [MAJOR] PRD abstract rewritten as a single ~180-word paragraph (no displayed equations, no
%       five-paragraph structure); load-bearing content kept (-35/16 certified 4 ways; SPHEREx recast
%       headline with honest range; proxy-floor disclosure; BF illustrative). Long-form scope material
%       relocated (not deleted) into a "Scope and conventions" paragraph at the head of the Introduction.
%       Consolidated the ~6x-repeated r/r_cos/r_eff/r_t/rho notation disambiguation to one canonical
%       clause (now in the Scope paragraph); L1006 (Viable Model) repetition replaced with a cross-ref.
%   (2) [MAJOR] Abstract + Scope now state the 1.3sigma floor is proxy-based (rho=-0.868 from the
%       power-spectrum SDB channel; Cov_B not public).
%   (3) [MINOR] Framing: intro "factor-of-two...resolved" -> "certified four ways; printed -35/8 is an
%       unreproduced erroneous literature value" (abstract + intro; Appendix/assumptions kept as detail).
%   (4) [MINOR] Jolicoeur:2025 author = Addis et al. — already consistent (bib author-list first = Addis;
%       named-instance L1294 = "Addis et al."; other cites unnamed \cite{}). VERIFIED, no change needed.
%   (5) [MINOR] Heinrich:2023 bib year = 2024; prose "Heinrich et al. 2024" already matches bib year;
%       bibkey label != year. VERIFIED consistent, no change needed.
%   (6) [MINOR] Assumption-(d): softened "derived/closed" to conditional-on-dressed-metric where the
%       deformed-algebra signature-change window (c_s^2=1-2rho/rho_c<0) is discussed.
%   (7) [MINOR] Abstract MC wording -> "cross-checked by three Monte Carlo validations of the closed-form
%       integral" (done in rewrite).
%   (8) [MINOR/I6] fig1_shape_function.png + fig2_survey_comparison.png rendered/verified: both show
%       -35/16-era values (fig1 "Squeezed limit: -35/16 = -2.1875", equilateral -255/128, folded -9/8;
%       fig2 title "f_NL = -35/16", bars ~3.8sigma not the doubled ~5.5sigma). NOT stale, no regen needed.
%   Directive-G hygiene: recompile 0 undef-refs, mirror all served paths, Convex paper-2 bump.)
% v1.7.111 (2026-07-10 — H17F Grok accept-conditional MINOR closures. Grok EXT verdict = "Accept with
%   minor revisions" on v1.7.110. NO headline number changed, -35/16 UNCHANGED, nothing fabricated.
%   Editable Grok minors closed: (a) Abstract phrase rephrase (L896): the awkward "dropping the
%   erroneous >=5sigma headline the -35/8 value would have produced" -> smoother "The superseded -35/8
%   value, being twice as large, would have implied a spuriously optimistic >=5sigma target; the
%   corrected amplitude replaces it with the realistic sensitivity above." (b) Overlap-factor notation
%   unified once at Eq.(projection): bare r = noise-weighted recovery (0.84+/-0.02); r_cos = flat-weight
%   shape cosine; r_eff~0.99 = survey-optimal validation cross-check; r=0.85+/-0.13 = basis-measure
%   stress band -- distinct symbols fixed throughout, eliminating the r-ambiguity Grok flagged.
%   (c) Data & Code Availability: crisp single external pointer to the public repo
%   github.com/Hubify-Projects/bigbounce, Zenodo DOI kept pending-at-camera-ready.
%   Grok items already-covered (source-cited, no new edit): independent-Fisher-is-validation restatement
%   (abstract Scope L888 already states it "not as the source of the headline numbers"); GR-projection
%   scoping-envelope-not-joint-covariance (abstract L892 + tab:systematics caption); appendix-shortening
%   = presentation-scope DP2-30 (Houston-gated). Directive-G hygiene via tools/directive_g.sh.)
% v1.7.110 (2026-07-10 — H17 INT re-test (Claude MINOR closure). NO headline number changed, -35/16
%   UNCHANGED, nothing fabricated. FOUR editable Claude MINORs closed against the committed artifacts:
%   (1) Abstract sign-gloss (L867): "the transcribed printed polynomial exceeds the vertex sum by
%   [a negative amount]" was self-contradictory verbal wording -> reworded to "the vertex sum exceeds
%   the transcribed printed polynomial by +(99/128)Sum k^3, so the printed polynomial is the MORE
%   negative of the two" (matches Eq.spurious + cai_vertices.py; -305/64 reduction).
%   (2) Continuous-marg BF (L1258): "BF ~ 4.8 vs the tuned narrow competitor" did not reproduce from
%   c9k_gr_continuous_marginalization.json (committed continuous_marginal.BF_vs_tuned_narrow = 4.615);
%   the 4.8 was the r=0.84-rebooked delta/narrow value (a different quantity). Reconciled to BF ~ 4.6
%   with the committed 4.615 cited inline.
%   (3) tab:gr caption (L1274): superseded -35/8 SSFSR refs 3.5e8/4.5e5/6.4e2 -> the true closed-form
%   exp[(35/8)^2/(2 sigma_eff^2)] values 3.0e8/4.2e5/6.1e2 (first was ~15% high); labeled discarded refs.
%   (4) Stale artifact metadata: c9k conventions string refreshed "-35/8" -> "-35/16" (all values in it
%   ARE at -35/16); c9g legacy table_iii_claims block quarantined to _SUPERSEDED_ with a note (only the
%   "scans" block is authoritative and matches tab:gr). INT OpenAI REJECT + Grok MAJOR raws were on the
%   STALE v1.7.102 PDF (header-labeled v1.7.102) -> every one of their findings (no explicit vertex
%   algebra, spurious-term unverifiable, r=0.84 ad-hoc, envelope-as-forecast, transmission (d)) is a
%   re-flag of content closed in v1.7.104-v1.7.108 -- source-cited non-real, no fabrication.
%   Directive-G hygiene: recompile 0 undef-refs, re-mirror all served paths, Convex paper-2 bump.)
% v1.7.109 (2026-07-10 — H17 ChatGPT EXT addendum. NO headline number changed, -35/16 UNCHANGED,
%   nothing fabricated. REAL STALE-NUMBER finding closed (ChatGPT EXT MAJOR-8): the tab:gr
%   "BF vs. SSFSR" column + P(BF>3) column + the sigma_GR=0.05 calibration (3.5e8/3.1e8) + the
%   continuous-marginalization SSFSR value (8.6e3) + the tab:bayes SSFSR column (>10^2/>10^5) were
%   all still numerically at the SUPERSEDED -35/8 center while the Tuned column had already been
%   moved to -35/16. Root cause: the committed recompute scripts c9g_bf_table_recompute.py and
%   c9k_gr_continuous_marginalization.py still hard-coded F0=-35/8. FIXED both scripts to F0=-35/16
%   and re-ran; replaced every SSFSR cell with the script's real recomputed values:
%   tab:gr SSFSR 10^8/10^5/10^2 -> 1.4e2/27/5; P(BF>3) 99.9/99.0/93.1% -> 88.8/80.2/61.5%;
%   sigma_GR=0.05 calib 3.5e8/3.1e8 -> 1.41e2/1.38e2; continuous-marg SSFSR 8.6e3 -> 19;
%   tab:bayes SSFSR >10^2/>10^5 -> ~5-1.4e2/~1.4e2 (footnote c added). Tuned column (5.7/4.6/3.3)
%   VERIFIED unchanged at -35/16 (already correct). Also fixed ChatGPT MAJOR-5 (FoG sign): the
%   paper called omitting fingers-of-God damping "conservative" for the redshift-space gain, which
%   is backwards -- omitting a degrading effect is OPTIMISTIC for absolute significance; reworded
%   honestly at line 1045 (FoG largely cancels in the r_eff ratio, the only headline quantity).
%   ChatGPT MAJOR-6 (rho=-0.868 vs own 0.95 proxy -> 0.8sigma): the 0.8sigma lower edge was already
%   DISCLOSED in prose (bracket 0.8-1.3sigma) but headlined only the 1.3sigma endpoint in tab:systematics;
%   added the |rho|=0.95 -> 0.8sigma lower-edge row explicitly (directive-F anti-value-headlining).
%   ChatGPT MAJOR-1/2 (App A convention + c.c. time-ordering): RE-FLAG of pre-v1.7.108 state --
%   the new App A (lines 1489, 1518-1572) already states the six-ordered-triple convention explicitly
%   and derives the -2Im doubling from the in-in commutator identity. Remaining MAJORs re-flags of
%   disclosed scope. See EXT_real/H17_2026-07-10/P2_truth_audit.md Addendum.
% v1.7.108 (2026-07-10 — H17 round P2 closure. NO headline number changed, -35/16 UNCHANGED,
%   nothing fabricated; the fix made App A MORE conservative. REAL EDITABLE finding closed
%   (Grok EXT MAJOR-1 = INT-Claude MAJOR = INT-OpenAI MAJOR-2/3): the Cai spurious-term SIGN and
%   the "printed polynomial -> -35/8" mechanism claim were inconsistent with the committed scripts.
%   Verified by re-running scripts/caili_certification/{cai_vertices,cai_shape,cai_conv}.py +
%   independent 2nd sympy derivation: (a) A_T - sum(vertices) = -(99/128)sum k^3 (was printed +),
%   (b) the transcribed printed A_T squeezed-reduces to -305/64 (NOT Cai's stated -35/8).
%   Corrected the sign everywhere (abstract 827, Sec II.A 885, Eqs.spurious 1414 + prose 1479/1492/1494,
%   scope-summary 1371, conclusion 1388, benchmark caption 926, assumptions 936, commutator 1551, table 1570)
%   to state honestly: the -(99/128) term is ONE identified discrepancy between the transcribed printed
%   polynomial and the exact vertex sum (driving it to -305/64), and Cai's separately-published -35/8 is
%   NOT reproduced by the transcribed coefficients, retained only as an erroneous literature value.
%   -35/16 remains quadruple-certified (vertex sum + eps-order group + Li Eq.5.1 + collapsed polynomial).
%   Also: Table VII (tab:vertexwalk) provenance re-attributed honestly (per-row limits of the same exact
%   vertex expressions p2_vertex_check.py sums, not "transcribed verbatim from" that script's total-only
%   output); envelope subordinated / central Fisher number retained as headline (Grok MAJOR-2); r_eff
%   robustness caveat added (Grok minor-b); assumption-(d) OOM statement already present. Deferred non-
%   blocking: p2_vertex_check.py per-vertex print loop; deprecate scripts/fig_4vertex_sum.py (stale -35/8).
% v1.7.107 (2026-07-09 — W12 EXT presentation-closure. NO headline number changed, -35/16 UNCHANGED,
%   nothing fabricated. Closures: (1) Tone/mechanism phrasing on Cai et al.: replaced "arithmetic error"
%   and "traces to a single spurious term" with neutral scholarly framing per Grok MAJOR-2 + calibrated
%   to App-A honest framing per ChatGPT BLOCKER-1 (term alone has wrong sign = one identified discrepancy,
%   not sole mechanism). (2) Stale MegaMapper ~3-7sigma in Sec IX conclusion replaced with calibrated
%   Sec V numbers (3.7-3.85 ideal, 1.5-3.5 design envelope). (3) GR-correlation bracket disclosed in
%   abstract (~0.8-2.75sigma bracket; ~1.3sigma SDB-proxy). (4) Table V relabeled scenario/scoping budget.
%   (5) Endpoint labels standardized (SPHEREx optimistic/post-budget) in abstract + Sec IV. (6) Stray
%   Greek character (os) not found in source (prior round). (7) Two Gemini syntax slips confirmed
%   already fixed in prior round. (8) Data and Code Availability section updated with specific scripts.
%   (9) Cross-channel covariance structural caveat added in Sec VII. (10) Notation collision note
%   extended. (11) Figure captions checked/trimmed where needed. (12) Abstract fast-summary added.
%   Directive-G hygiene: version header + date bump to v1.7.107 / 2026-07-09; recompile; re-mirror.)
% v1.7.106 (2026-07-09 — W11 EXT presentation-closure. NO headline number changed, -35/16 UNCHANGED,
%   nothing fabricated, no math altered. (1) [ChatGPT BLOCKER claim-strength]: calibrated the abstract's
%   "This settles the 8-year discrepancy" sentence to the precise evidentiary framing — the vertex-level
%   re-summation yields -35/16 EXACTLY (certified vertex-by-vertex, Table tab:vertexwalk, cross-checked by
%   the eps-order-grouped Eq.order_grouped), and combined with Li et al's independent general-c_s derivation
%   the evidence DECISIVELY FAVORS -35/16 over the printed-polynomial -35/8. Strong claim on the VALUE kept
%   (the full algebraic map Cai-polynomial->vertex-sum->-35/16 is already displayed as Eq.vertexsum +
%   Eq.collapsed_vertexsum + Eq.order_grouped + Eq.spurious); only the literature-error-MECHANISM phrasing is
%   the honest "one identified discrepancy" per the App-A body. (2) [Gemini BLOCKER: SPHEREx additive-quadrature
%   budget "soft"]: made the budget CONSTRUCTION explicit + quantitative — new displayed Eq.quadbudget
%   S=|fnl|r/sigma_eff, sigma_eff=(sigma_0^2+sum sigma_j^2)^1/2, with the b_phi baseline-replacement rule and
%   the inverse-Fisher marginalized rho=-0.868 endpoint shown to bracket the quadrature floor to ~14%, so the
%   1.3-2.75sigma envelope is robust to the quadrature-vs-marginalization choice (reported as an informational
%   sensitivity range). (3) [Gemini MAJOR: MegaMapper "uncalibrated"]: labeled the MegaMapper section DECISIVELY
%   as an uncalibrated high-z projection (SPHEREx systematic budget transferred as a placeholder, NOT calibrated
%   to z=2-5 where GR-projection effects dominate; ideal sigma~0.5 is the only externally-anchored number; ranges
%   are design-uncertainty envelopes). (4) [ChatGPT MINOR]: fixed the stale "detection of fnl~-4" (Sec IX.B/1306)
%   to "~-2" (the corrected -35/16=-2.1875; -4 = superseded Cai value). Gemini "35 一號" Table-VII entry is a
%   pdftotext OCR artifact (source shows clean -35/32) — pattern-063, no source defect. Version+date bump,
%   served-PDF re-mirror, Convex bump in same bundle (directive G).
% v1.7.105 (2026-07-09 — R9 P2 closure. THREE additions, NO headline number changed, -35/16 UNCHANGED,
%   nothing fabricated. (1) [Grok MAJOR + ChatGPT MAJOR: "full term-by-term derivation table" for the
%   vertex re-summation] Added Table tab:vertexwalk in Appendix~A after Eq.vertexsum: the squeezed AND
%   equilateral local-amplitude contribution of EACH of Cai's four cubic vertices individually
%   (field-redef -25/16, L_zzdd -5/32, L_dzpzpc 0, L_z(ddc)^2 -15/32 squeezed; -35/32, -5/32, -5/8,
%   -15/128 equilateral), both columns summing exact-fraction to -35/16 and -255/128 (tab:benchmarks).
%   Every value transcribed VERBATIM from the committed exact-fraction sympy cert scripts/p2_vertex_check.py
%   (ran it; per-vertex limits computed there); NO new math. This is the referee-granularity term-by-term
%   walk both Grok and ChatGPT asked for, complementing the existing collapsed-polynomial + order-grouped
%   objects. (2) [Gemini MINOR: gauge-frame vs physical-frame nomenclature "slightly convoluted"; wants a
%   single consolidating table] Added Table tab:frames in the discussion: gauge (survey observable,
%   fnl_bounce -2.1875 / fnl_inf +0.015 / ratio 146) vs physical conformal-Fermi (fnl_inf -> 0, ratio >>146),
%   cleanly separating the two frames' expected values. (3) [Gemini MINOR: Sec VI "missing parenthesis"
%   near the Bayes-factor CDF integrations] Verified source L1032-1046: all Phi()/erf() closed-form
%   parentheses balanced (grep-checked); the flagged artifact is a PDF font-extraction glitch (cf. prior
%   Gemini "fNI"/"OGR" extraction artifacts), FALSIFIED as an editable typo, no fake fix. Grok's central
%   recast-vs-independent-Fisher separation and ChatGPT's Appendix-A completeness are already carried by
%   scope point (i) + the existing App-A three-way cross-check; the new tab:vertexwalk gives the
%   disclosure its strongest term-by-term form. Directive-G: recompile 0 undef-refs, latex-audit
%   (tab:vertexwalk + tab:frames are narrow single-column, no overflow), mirror all served paths, Convex
%   paper-2 bump, bundle rebuild. \date July 8 -> July 9.)
% v1.7.104 (2026-07-08 — DEEP-tier Grok-Heavy MAJOR closure: Appendix~A vertex algebra "not fully
%   displayed". Added the two intermediate objects the squeezed reduction collapses, transcribed
%   verbatim from the committed exact-fraction sympy script scripts/p2_vertex_check.py: (1) the
%   collapsed exact vertex-sum degree-9 polynomial 256*Pik2*A in the symmetric monomial basis
%   (Eq. collapsed_vertexsum, coeffs 9/3/-27/15/-198/27 verified against sympy Poly grouping); and
%   (2) the epsilon-order-grouped squeezed contributions fNL|_eps^1=-5/2, |_eps^2=+5/16, |_eps^3=0
%   summing to -35/16 (Eq. order_grouped). NO new math — transcription of the existing certified
%   computation. -35/16 value UNCHANGED. Other Grok MAJORs (sensitivity-envelope framing, length)
%   already dispositioned as disclosed/editable in prior rounds; no genuinely-new finding.)
% v1.7.103 (2026-07-08 — RSD-FISHER closure: retires the standing "independent Fisher is real-space
%   monopole only (no RSD multipoles ell=0,2,4, ~18% conservative offset per Heinrich)" limitation
%   with REAL computation. Applies the VERIFIED c14 redshift-space tree bispectrum Fisher
%   (scripts/c14_rsd_multipole_fisher.py; outputs/c14_rsd_multipole_fisher.json, committed): Kaiser
%   Z1=b+f mu^2 (Kaiser:1987) + SCF99/Sefusatti Z2, growth f(z)=fsigma8/sigma8 from same CAMB
%   Planck2018, orientation-integrated over full (mu1,phi) so ell=0,2,4 content is exact. Verified
%   numbers ONLY: sigma_RSD(local)=0.415 (b-fix)/0.449 (b-marg) vs C13 real-space 0.687 = +34.7%
%   tighter (RSD/Heinrich 0.64); sigma_RSD(bounce)=0.417/0.449; r_eff_RSD~0.99; f->0 reproduces C13
%   to 6 sig-figs. Para:reconcile limitation sentence replaced with the RSD result; the honest
%   +34.7%-vs-Heinrich-~18% reconciliation stated (full RS->RSD gain vs monopole->multipole gain).
%   HEADLINE DISCIPLINE: independent-Fisher unmarginalized significance for -35/16 updated
%   3.2-3.5sigma (real-space, conservative floor) -> 4.9-5.2sigma (redshift-space, unmarginalized,
%   before the systematic + GR-projection budget); the c12 GR-projection marginalization bracket
%   (rho~0.95; marginalized ~0.8-1.3sigma edge) stays disclosed EXACTLY, retained unchanged. Added
%   Kaiser:1987 + Scoccimarro:1999 bib entries. NO headline fNL=-35/16 changed. Nothing fabricated;
%   every number read verbatim from the committed c14 JSON. Approximations stated: tree-level, linear
%   k_max, b2=bs2=0, no FoG (conservative at high-k). \date -> July 8, 2026.)
% v1.7.102 (2026-07-08 — RS8 closure: tone-regression rewrite (Gemini MAJOR): neutralized rebuttal/
%   defensive register in Sec IX.E (signpost paragraph recast as declarative scope summary),
%   Sec IX.E body ("We emphasize that a detection" -> neutral declarative), Sec template
%   ("a referee may ask" -> neutral), Sec benchmark ("we emphasize sigma_3/sigma_1" ->
%   declarative), App A ("a narrative we explicitly reject" -> neutral; "We emphasize that
%   the identity" -> declarative). Appendix A Cai-Li internal consistency (ChatGPT MAJOR):
%   MISREAD verdict -- paper is internally consistent; added one clarifying sentence making
%   explicit that Li's printed polynomial (Eq. 4.19) and Cai's printed polynomial (Eq. 37)
%   agree coefficient-for-coefficient at c_s=1, so the -35/8 is what BOTH printed polynomials
%   reduce to in the squeezed limit, and the vertex re-summation is what yields -35/16
%   independently of either printed polynomial. Grok minor (r_eff~0.99 clarification):
%   already present near Sec IV; no change needed. Abstract scoping qualifier: already present.
%   Vertex table main-text reference: already present at Sec II / Table vertices.
%   NO number changed. NO disclosure changed. \date stays July 7, 2026.)
% v1.7.101 (2026-07-07 — REALWORK re-test ITEM-1 closure: ChatGPT flagged r=0.84 and r_eff~0.99 as an
%   apparent internal inconsistency and the independent Fisher as "not presented at publication standard".
%   NO NUMBER CHANGED. (1) Added ONE crisp reconciliation at the head of the sec:spherex independent-Fisher
%   paragraph (\label{para:reconcile}): r=0.84 is the flat-weight shape-overlap cosine adopted as the
%   conservative recast factor; r_eff~0.99 is the survey-optimal (squeezed-dominated) amplitude-recovery
%   factor of an actual multi-tracer estimator — DIFFERENT quantities, not a contradiction; the Fisher
%   CONFIRMS 0.84 is conservative. (2) Pointed the abstract (L727) and the recurring-referee signpost
%   (item iv) at this single reconciliation. (3) Presentation hardening (no new numbers): made the survey
%   spec explicit (per-sample n_bar_i, b_i from the committed Dore+2014 table, no rescaled inputs) and wrote
%   the covariance definition in closed form (diagonal-triangle Gaussian multi-tracer Wick contraction with
%   P^tot_ij = P_ij + n_bar_i^-1 delta_ij, symmetry factor, tracer-permutation sum), addressing ChatGPT's
%   "unclear covariance / not publication standard" specific. All numbers untouched.)
% v1.7.100 (2026-07-07 — INDEPENDENT-FISHER closure: retires the "no independent bispectrum Fisher /
%   single-source dependence" concession (ChatGPT/OpenAI standing #1 objection) with REAL computation.
%   Applied the VERIFIED committed result (scripts/c13_independent_bounce_fisher.py + outputs/
%   c13_independent_bounce_fisher.json; git 7044c75): a from-scratch tree-level galaxy-bispectrum
%   Fisher on the SAME committed SPHEREx public-products table (Dore+2014; 5 samples, 6 z-bins,
%   f_sky=0.75) with SPT F2 matter bispectrum + Gaussian multi-tracer covariance (Scoccimarro:1998,
%   Sefusatti:2006), k_max=0.2(1+z). FULL multi-tracer: sigma(fNL^local)=0.626 (b-fixed)/0.687
%   (b-marg) — reproduces Heinrich 0.7 to 2-11% (ratio 0.89/0.98, validation PASSED); at the bounce
%   B_NL template sigma(fNL^bounce)=0.632/0.689 => r_eff=sigma_local/sigma_bounce ~ 0.99 and an
%   UNMARGINALIZED 3.46sigma/3.18sigma for -35/16. FRAMING DISCIPLINE (integrity): presented as
%   VALIDATION of the recast + retirement of the single-source limitation, NOT a headline inflation —
%   the 3.2-3.5sigma is the unmarginalized independent number; the paper's honest GR-projection-
%   degeneracy bracket (c12: rho~0.95 => 0.8-1.3sigma marginalized edge) still applies ON TOP and
%   stays disclosed exactly as-is; the abstract 1.3-2.75sigma headline range remains the recast, NOT
%   substituted. Honest limitations stated verbatim-in-substance: real-space monopole only (no RSD),
%   Gaussian covariance, tree-level linear k_max, no b2/bs2 marginalization, ratio r_eff more robust
%   than absolute sigma. Single-tracer sigma=15.6 kept only as the labeled conservative bound. EDITS:
%   abstract Scope sentence (no-independent-Fisher -> independent Fisher constructed as validation);
%   abstract para-4 concession ("out of scope/flagged for follow-up") -> DONE, independent-Fisher
%   validation paragraph; sec:spherex L890 scaling-check paragraph + a new "Independent bispectrum
%   Fisher (validation)" paragraph; sec:spherex L892 "we do not construct an independent Fisher" ->
%   qualified (headline is recast; independent Fisher built as validation); signpost items (i)+(iv)+
%   "Single-source limitation" all updated from "flagged for follow-up / do not claim it here" to
%   "now carried out". Added Scoccimarro:1998 (astro-ph/9704075) + Sefusatti:2006 (astro-ph/0604505)
%   to focused_paper_refs.bib. Every significance quote re-checked for c12-bracket consistency: NO
%   stale contradiction; -35/16 UNCHANGED; NOTHING fabricated (all numbers read from committed c13
%   JSON). Write-up: project-context/peer-reviews/INT_v3/P2_independent_fisher_2026-07-07.md.
%   Directive-G: recompile 0 undef-refs, /latex-audit, mirror ALL served paths byte-identical, Convex
%   paper-2 bump real md5/pages, bundle rebuild+standalone-verify, reviewTimeline entry. \date stays July 7.)
% v1.7.99 (2026-07-07 — venue-compliance disclosure: AI-methods now names models per IOP/JCAP; NO science number changed)
% v1.7.98 (2026-07-06 — D-ROUND FINAL POLISH (presentation + disclosure only; NO science number
%   changed, -35/16 UNCHANGED, nothing fabricated). (1) ABSTRACT restructured to lead with the
%   CONTRIBUTION (the -35/16 Cai-Li resolution + the prediction) THEN the forecast+scope THEN the
%   load-bearing cubic-transmission caveat, instead of opening with the caveat wall Gemini flagged.
%   The triple-repetition of the Cai-Li resolution (old paras 1, 5, 6) is consolidated: it leads once,
%   the closing para is tightened to the significance takeaway. Every number preserved verbatim
%   (-35/16, -35/8, r in [0.829,0.876], 2.6-2.75sigma, 1.3-2.75sigma, BF 9-14/10-17/4-7, 146, 0.015,
%   SDB 1.53->3.08->7.06, rho -0.87..-0.97). (2) FIGURE STYLE + CONSISTENCY pass on the committed
%   generator research/live_forecast_packaging/generate_all_figures.py: added a colorblind-safe
%   (Wong 2011) palette + consistent font/size rcParams across ALL figures; and finished the
%   -35/8 -> -35/16 sync that v1.7.97 started for fig1/fig4 — fig2 (survey significance), fig3 (k_min
%   cliff, right panel), and fig5 (f_NL landscape) STILL rendered the retracted -35/8-era values that
%   CONTRADICTED their own already-corrected captions/text. fig2 title/bars now at -35/16 (naive
%   2.1875/0.70=3.13sigma, optimistic 2.6-2.75, realistic 1.3-2.75, all-combined 1.3-1.4, MegaMapper
%   ideal 3.7-3.85, illustrative 1.5-3.5 — all read verbatim from the fig:surveys caption); fig3 right
%   panel significance recomputed at |f_NL|=2.1875 (SPHEREx bispectrum line 3.1sigma), left panel
%   (sigma vs k_min, amplitude-independent) unchanged; fig5 bounce marker + SPHEREx error bar moved
%   from -4.375 to -35/16=-2.1875. This is a cosmetic label-sync of already-corrected headline numbers
%   (significance ∝ |f_NL|, exactly one-half per the captions), NOT a number invention. (3) AI-METHODS
%   disclosure expanded to an honest reproducibility-framed statement (agentic AI pipeline under author
%   direction; every quantitative result verified against committed artifacts; public audit trail).
%   (4) Cai courtesy email drafted (CAI_COURTESY_EMAIL_DRAFT.md, do-not-send). Directive-G: recompile
%   0 undef-refs, /latex-audit, re-mirror all served paths byte-identical, Convex paper-2 bump, bundle
%   rebuild+standalone-verify. \date kept July 6, 2026 (Convex sort ties break on createdAt desc).)
% v1.7.97 (2026-07-06 — FIGURE HYGIENE: regenerated fig1_shape_function.png and fig4_decision_thresholds.png, which still rendered the stale -35/8-era labeling (last regenerated 2026-06-26) while every text/table headline already carries the corrected -35/16. Generator research/live_forecast_packaging/generate_all_figures.py updated: (fig1) the Cai et al. shape polynomial BNL() carries the spurious +(99/128)Sum k_i^3 term (App A), so its bare squeezed limit is -35/8; per this figure's own caption "the plotted curve is the Cai et al. shape normalized to its corrected squeezed amplitude (exactly one-half the printed -35/8)", the curve is now scaled by 1/2 so it converges to -35/16 = -2.1875, with legend/benchmarks relabeled to match tab:benchmarks exactly (squeezed -35/16 = -2.1875, equilateral -255/128 = -1.992, folded -9/8 = -1.125). (fig4) blue matter-bounce prediction line + SPHEREx/MegaMapper error bars moved from -4.375 to the corrected -35/16 = -2.1875 and relabeled "-35/16"; decision-region axvspans re-centered so the corrected prediction sits in the dark-green "strongly favors bounce" zone (matching the caption). NO fNL/sigma number invented — all values read from the paper's own abstract + tab:benchmarks; nothing fabricated (a cosmetic label-sync of already-corrected headline numbers). Directive-G hygiene: recompile 0 undef-refs, mirror all served paths byte-identical, Convex paper-2 bump with real md5/pages, bundle arxiv_p2_v1.7.97.tar.gz standalone-verified. \date kept July 6, 2026 (Convex sort ties break on createdAt desc, so the v1.7.97 row sorts newest).)
% v1.7.96 (2026-07-06 — DATA-UNLOCK: strengthen the named Cov_B external gate. A systematic availability search (arXiv ancillary files, Zenodo, authors' public code repos) found the Heinrich et al. 2023 per-triangle SPHEREx multi-tracer bispectrum covariance Cov_B is NOT publicly released — no data/code-availability statement, no arXiv ancillary, no Zenodo DOI; the only public generating code (github.com/chenheinrich/SphereLikes) is an unvalidated, pre-publication WIP branch (labeled "[Not validated; under construction]", last pushed 2021, no paper-matching release, no per-triangle Cov_B / Fisher). Added ONE sentence to sec:systematics stating this, so the computed-degeneracy bracket with the rho=-0.868 proxy is the best CURRENTLY-CONSTRUCTIBLE marginalized-floor estimate. NO headline/fNL/sigma number changed; -35/16 UNCHANGED; nothing fabricated (this is a documented external-data non-availability). Availability record: project-context/peer-reviews/INT_v3/DATA_UNLOCK_2026-07-05.md (Item 4). Directive-G hygiene: recompile 0 undef-refs, mirror all served paths, Convex paper-2 bump, bundle rebuild. \date kept July 6, 2026 so the v1.7.96 Convex row sorts ahead of v1.7.95 under current()'s datestamp-desc localeCompare and the live site shows the new version.)
% v1.7.95 (2026-07-06 — publication-readiness pass closing the TWO genuinely-new REAL findings from EXT RETEST_2026-07-05b (ChatGPT VERIFIED-REAL truth-audit); NO headline/fNL number changed, -35/16 UNCHANGED, nothing fabricated. FINDING 1 (stale -35/8-era significances still live downstream): the abstract + sec:spherex + tab:systematics + fig2 already carried the corrected -35/16 numbers (bispectrum-only ~2.6-2.75sigma optimistic, realistic ~1.3-2.75sigma), but five downstream spots still quoted the OLD -35/8-era doubled ranges. Swept to the exact halved values (significance ∝ |fNL|, -35/16 = 1/2 * -35/8, the mapping the abstract itself states): fig3 caption L1016 "significance for fNL=-35/8" -> "-35/16"; bphi para L1022 "2.6-5.5sigma"->"2.6-2.75sigma", "5.2-5.5sigma optimistic -> 4.0-4.2sigma(30%)/3.5-3.7sigma(50%)" -> "2.6-2.75sigma -> 2.0-2.1sigma/1.75-1.85sigma", "headline range 2.6-5.5sigma"->"2.6-2.75sigma"; photo-z item L1063 "2.6-5.5sigma post-budget envelope (>=2.5sigma)" -> "1.3-2.75sigma (>=1.25sigma)"; staged-strategy L1132 "2.6-5.5sigma after budget (5.2-5.5sigma optimistic)" -> "1.3-2.75sigma after budget (2.6-2.75sigma optimistic)"; SDB-motivation L1161 "remains the bispectrum-only 5.2-5.5sigma" -> "2.6-2.75sigma". Corrected values now match tab:systematics rows exactly (bphi-30%~2.05sigma, bphi-50%~1.75-1.85sigma). FINDING 2 (App A wording contradiction): v1.7.93 softened the App-A BODY (L1223) but left the Sec-II summary L713 ("which is exactly what pushes -35/16->-35/8") and the certification line L1246 ("sole origin of the erroneous -35/8") asserting the naive-additive-shift mechanism the body explicitly refutes (+(99/128)Sum k^3 added alone gives +2.58, WRONG sign). Reconciled BOTH to the body's honest framing: -35/8 is what Cai's OWN full printed-polynomial squeezed reduction yields; the spurious +(99/128)Sum k^3 is the traceable single term of excess / one identified discrepancy, NOT a naive additive shift. -35/16 vertex-sum squeezed limit is 3-way certified and UNAFFECTED. Remaining ChatGPT majors = the disclosed-limitation set (imported baseline/no joint Fisher, additive-quadrature budget, transmission assumption (d), prior-driven Bayes) = structural harsh-referee floor (directive H); Grok RETEST_2026-07-05b = MINOR REVISIONS ("ready for submission after modest tightening"), Gemini = disclosed-limitation/exposition requests. c12 computed-rho~0.95 0.8-1.3sigma bracket integration deferred to parallel data agent; the honest rho=-0.868-proxy 1.3sigma floor + named Cov_B gate stands. Directive-G hygiene: recompile 0 undef-refs, mirror all served paths, Convex paper-2 bump, bundle rebuild.)
% v1.7.94 (2026-07-06 — INT-v3-P2 systematic-budget MAJOR (Grok+Gemini RS27 joint-covariance) upgraded from PROXY to COMPUTED GR-projection degeneracy (write-ups project-context/peer-reviews/INT_v3/P2_joint_covariance_full_2026-07-05.md + _TEX_PROPOSAL_; repro scripts/c12_gr_projection_dBdAgr_probe.py + outputs/c12_gr_projection_dBdAgr_probe.json). sec:systematics closing "we do not overclaim" sentence rewritten: the GR-marginalization response partial B_g/partial A_GR = S_GR is the standard relativistic-projection squeezed kernel (Verde-Matarrese/Bartolo-Bruni class), now CONSTRUCTED on the paper's own committed 23,098-triangle grid. Its shape overlap with the local template S_local is COMPUTED |rho| ~ 0.95 (0.9485 uniform, 0.9493 k^2-weighted) — a STRONG f_NL-A_GR degeneracy, at least as strong as (in fact stronger than) the c8 running-Fisher proxy rho=-0.868. Consequence: sigma_marg = sigma_base/sqrt(1-rho^2); at the computed rho~0.95 the marginalized significance ~0.83 sigma is LOWER than the proxy, so the honest budget is a BRACKET ~0.8-1.3 sigma with the rho=-0.868 proxy (1.3 sigma) now shown to be the OPTIMISTIC (mild upper) edge, NOT an underestimate. The single remaining external input is the fully channel-native (noise-weighted Fisher) rho, which needs the SPHEREx multi-tracer bispectrum COVARIANCE Cov_B on the triangle set (Heinrich:2023 arXiv:2311.13082) — only the scalar sigma(f_NL^local)=0.7 is imported, the per-triangle covariance is external; all other ingredients are in-repo, so the direct number is one Cov_B evaluation away. This CONVERTS the "additive-quadrature heuristic/proxy" concern into a computed-degeneracy bracket with a named external gate. NO abstract range number changed (1.3-2.75 sigma envelope preserved; 1.3 sigma lower edge retained), and the ~0.8 sigma lower edge is now DISCLOSED honestly in-section. NOTHING fabricated — all rho/sigma_marg/significance numbers read from the committed c12 JSON. Heinrich:2023 already in focused_paper_refs.bib. \date July 5 -> July 6.)
% v1.7.93 (2026-07-06 — INT-v3-P2 ROUND_2026-07-05 two MINOR fixes (no headline number changed; -35/16 UNCHANGED). FIX 1 (App A, Eq spurious L1221) — softened the -35/8 attribution to be honest and non-overclaiming. The reviewer correctly noted the isolated +(99/128)Sum k_i^3 local term is NOT self-verifying as the sole doubling mechanism: added alone to A it shifts squeezed fNL by +(10/3)(99/128) ~ +2.58 (WRONG sign to reach -35/8, which needs -2.1875). Reworded: -35/16 is the certified vertex-sum squeezed limit (3-way certified, UNAFFECTED); -35/8 is what Cai's OWN full printed-polynomial squeezed reduction yields; the +(99/128)Sum k_i^3 is ONE identified discrepancy within that unstated reduction, NOT a naive-add mechanism. Explicitly disclaim a complete term-by-term derivation of Cai's error. FIX 2 (tab:systematics L1094 + caption L1097 + sec:systematics L1005 proxy refs) — made the joint-Fisher PROXY rho internally consistent with the committed c8 value everywhere: rho=-0.87 -> rho=-0.868 (the c8 fixed-bias correlation -0.8679, matching the changelog), sigma_marg=0.7/sqrt(1-0.868^2)=1.41 (was 1.42), floor 2.1875*0.84/1.41 ~ 1.3 sigma UNCHANGED. The SDB-channel's own reported fixed/bias-marg pair (rho=-0.87 to -0.97, L659/L1005-first/L1159/L1161) is left at 2dp as its genuine matched-pair result — untouched. NO fNL number, NO headline significance changed. Nothing fabricated — rho=-0.868 read from committed outputs/c8_fnl_running_fisher.json.)
% v1.7.92 (2026-07-05 — INT-v3-P2 non-local-tails MAJOR closed with directive-G hygiene (write-up project-context/peer-reviews/INT_v3/P2_nonlocal_tails_2026-07-05.md + _TEX_PROPOSAL_; repro scripts/c11_nonlocal_template_projection.py + .json). Reviewer MAJOR: "the r=0.84 local-template recast does not model the NON-LOCAL bounce-shape tails; equilateral/orthogonal deviations could carry missed signal." CLOSED at level (a): added a standard-basis template-decomposition paragraph to sec:template. The PHYSICAL bounce bispectrum B_bounce = BNL(k) * S_local(k) (carrying the local 1/k^3 envelope, ~97% local) is projected onto the Senatore-Smith-Zaldarriaga local/equil/ortho basis on the identical committed 23,098-triangle grid (reproduces the paper's r_cos = 0.985 exactly, -0.9849). Uniform-weight cosines: LOCAL -0.985, EQUIL -0.45, ORTHO +0.94. A JOINT projection onto span{LOCAL,EQUIL,ORTHO} raises the recovered Fisher-norm fraction from 0.970 (local-only) to at most 0.974 => delta r <= +0.002 (<0.3% of the r=0.84 headline, << the +/-0.02 uncertainty), so the single-local-template recast is ROBUST and a multi-template analysis is NOT needed. Two honest points stated: (1) the projection is of the physical bispectrum (local envelope, ~97% local), not the bare BNL ratio; (2) the high ORTHO cosine is COLLINEARITY (ortho template == -3*local + ...), not independent signal — the joint fit removes the double-counting. Honest scope limit kept: geometry-only shape overlap (matching r_cos); the full 3D estimator-mismatch variance under the true SPHEREx multi-tracer bispectrum Fisher covariance is not computed here (needs the Heinrich et al. noise covariance). NO headline number changed. Added Senatore:2010 (arXiv:0905.3746) to focused_paper_refs.bib. NOTHING fabricated — exact numbers from the committed c11 script/JSON.)
% v1.7.91 (2026-07-05 — INT-v3-P2 TWO verified-real-science strengthenings applied with directive-G hygiene (write-ups project-context/peer-reviews/INT_v3/P2_cubic_adiabaticity_2026-07-05.md + P2_joint_covariance_2026-07-05.md + _TEX_PROPOSAL_; repro scripts/c10_joint_covariance_marginalization.py). EDIT 1 — cubic transmission now DERIVED (closes assumption-(d) MAJOR from "supported by zeta-conservation" to a bounded systematic). The Cai-Wilson-Ewing LQC quasi-dust bounce is SINGLE-CLOCK (effective LQC modifies the background via holonomy/inverse-triad corrections but adds NO new scalar dof — dressed-metric/hybrid quantization WilsonEwing:2012, and even deformed-algebra Cailleteau:2011kr keep the physical scalar sector single-clock), so there is no isocurvature/entropy mode by field content and nonlinear superhorizon zeta-conservation holds to ALL orders; the only zeta-dot source is O((k eta_B)^2) gradient pressure at every order (cubic included) => transmission = 1 +/- O((k eta_B)^2) ~ 1 +/- 1e-4 (delta fNL <~ 1e-3, negligible vs sigma~0.7), DERIVED not assumed, no numerical cubic bounce evolution required. The one honest model-choice input: the subleading gradient sign is quantization-scheme-dependent (Lorentzian c_s^2=1 dressed-metric per Wilson-Ewing adopted here; deformed-algebra's transient c_s^2=1-2rho/rho_c signature-change window is also (k eta_B)^2-suppressed on deep-superhorizon modes) — a discrete citable choice, not an open computation. Updated: assumptions Sec (single-clock closure paragraph + weakest-link reframing), abstract star-caveat, signpost item (ii), Leading-theoretical-uncertainty paragraph, Conclusion star-caveat. Added Cailleteau:2011kr (arXiv:1111.3535) to focused_paper_refs.bib. Cited Lyth-Malik-Sasaki, Wilson-Ewing, Quintin. NO fNL number changed. EDIT 2 — systematic budget upgraded from additive-quadrature to marginalized joint-covariance (HONEST, slightly WORSENS the floor, which is correct). The honest marginalized error is sigma_marg = sigma_base/sqrt(1-rho^2), NOT quadrature; using the paper's OWN committed c8 CAMB degeneracy rho=-0.868 as a conservative PROXY for the GR-marginalization degeneracy gives sigma_marg=0.7/sqrt(1-0.87^2)=1.42 => marginalized floor |fNL| r/sigma_marg = 2.1875*0.84/1.42 ~ 1.3 sigma (vs the ~1.5 sigma quadrature floor, ~14% lower). CLEARLY FLAGGED: (a) the power-spectrum (SDB) channel already has a REAL joint covariance (c8); (b) the bispectrum-only channel uses rho=-0.868 as a PROXY because the direct partial B_g/partial A_GR triangle-response derivative is deferred (the one remaining input to fully close it; paper already notes this at the c8/L971/L999 scope). Not overclaimed as fully marginalized. Updated: L971 scope paragraph (joint-covariance test), tab:systematics (added marginalized-Fisher row + caption). ~1.3 sigma marginalized floor lands at the existing 1.3-2.75 sigma lower endpoint, so NO abstract range number changes — a methodological strengthening. NOTHING fabricated: numbers anchored to the paper's own committed c8 Fisher (rho=-0.868, sigma_base=0.7, r=0.84) + standard single-clock physics. \date stays July 5.)
% v1.7.90 (2026-07-05 — ChatGPT verified-re-test closure: TWO items. (BUG 1, ChatGPT MAJOR, legitimate internal inconsistency) Eq.(2) (main text, sec:benchmark) still asserted the squeezed limit BNL -> -35/8, contradicting the v1.7.86-89 resolution (corrected central value -35/16), Fig.1's caption, the benchmark table, the Bayesian significances, and Appendix A. FIXED: Eq.(2) now reads -> -35/16 (the CORRECT vertex-consistent value), with an inline note that A_T throughout denotes the corrected (vertex-consistent) shape whose squeezed limit is -35/16, that the -35/8 obtained from Cai's PRINTED polynomial (their Eq 37) is the arithmetic error corrected here (spurious +(99/128)Sum k_i^3, App A Eqs decisive_sqz/spurious), and that the shape-overlap analysis uses Cai's printed monomial SHAPE (amplitude-invariant, enters r_cos/r via ratios only) while -35/16 fixes the AMPLITUDE normalization, matching Li 2017 at c_s=1. Every remaining -35/8 in the body is now explicitly labeled Cai's erroneous printed value only (lines 660/733/748/1172/1184/1263/1266/1271/1275; lines 84/638 are % comments). (FIX 2, ChatGPT wants explicit vertex-by-vertex algebra) Added a "Vertex-by-vertex certification" block in App A (app:convention): Table tab:vertices lists Cai 2009's four cubic vertices' shape-function contributions at eps=3/2 read verbatim from arXiv:0903.0631 source (matterbounceng2.tex), Eq vertexsum shows their exact sum's squeezed limit = -35/16, the printed-minus-vertex-sum = +(99/128)Sum k_i^3 spurious local term is stated, and Li 2017 (arXiv:1612.02036) Eq 5.1 -> -35/16 at c_s=1 cross-check. Numbers taken EXACTLY from the verified writeup project-context/peer-reviews/INT_v3/P2_factor2_RESOLUTION_2026-07-04.md; nothing fabricated. \date July 4 -> July 5.)
% v1.7.89 (2026-07-04 — INT-v3-P2 factor-of-2 RESOLVED + cubic-transmission strengthening (two VERIFIED real-science results applied; write-ups project-context/peer-reviews/INT_v3/P2_factor2_RESOLUTION_2026-07-04.md + P2_cubic_transmission_2026-07-04.md). EDIT 1 — Cai/Li factor-of-2 RESOLVED in favour of fNL_local = -35/16 (was disclosed-unresolved; now a positive contribution). A from-scratch symbolic re-summation of Cai 2009's (arXiv:0903.0631) OWN four cubic-action vertices at eps=3/2 gives a clean squeezed-limit -35/16 (O(k1^2/k^2) correction), matching Cai's own eps-order-grouped intermediates AND Li/Quintin/Wang/Cai 2017's (arXiv:1612.02036) independent general-c_s formula -165/16+65/(8c_s^2) -> -35/16 at c_s=1. Cai's PRINTED final polynomial (Eq 37) contains a spurious +(99/128)Sum k_i^3 local term (= A_T_printed - Sum(vertices)) that pushes -35/16 -> -35/8; both papers use the identical fNL=10A/(3 Sum k_i^3) convention so it is an ARITHMETIC ERROR not a convention. -35/16 is now CENTRAL everywhere (abstract/intro/results/tables/Bayesian/conclusion/Appendix A). Significances HALVE with |fNL|: bispectrum-only ~2.6-2.75sigma (2.63sigma central, r=0.84), post-systematic ~1.3-2.75sigma (the paper's pre-existing -35/16-branch values, NOT invented). Benchmark shape table halved (-35/16, -255/128, -9/8). r=0.84 UNCHANGED (shape ratio, amplitude-invariant). Broad-competitor Bayes factors UNCHANGED (center-independent per eq bf_approx); narrow-competitor delta corner 7.0->5.7 (CDF lower tail vanishes at -35/16 center). kappa_eps range 5.6-80 -> 2.8-40 (eps-derivative of fNL halves with amplitude); ratio |fNL_b|/|fNL_inf| 290->146; SY tau_NL 27.56->6.89; Planck-recast tension 0.75->0.37sigma. Appendix A retitled + rewritten as the RESOLUTION. EDIT 2 — cubic transmission upgraded from "scaling estimate only" to "supported by nonlinear superhorizon zeta-conservation (Lyth-Malik-Sasaki / Maldacena-deltaN) in the LQC/low-c_s escape sub-class that evades the Quintin-Sherkatghanad-Cai-Brandenberger no-go (arXiv:1508.04141)"; honest residual = cubic-order adiabaticity (zeta-dot->0) check through the NEC-violating LQC phase = new #1 follow-up. Added Quintin:2015 bib entry (arXiv:1508.04141). NO number fabricated — all -35/16-branch significances pre-existed in the paper. \date stays July 4.) (BOUNDED, honest, source-cited; factor-of-2 NOT resolved). A four-vertex in-in re-derivation (Claude Code INT, Houston subscription; full write-up project-context/peer-reviews/INT_v3/P2_inin_rederivation_2026-07-04.md) fetched both source PDFs (Cai arXiv:0903.0631; Li/Quintin/Wang/Cai arXiv:1612.02036) and confirmed: (1) the Cai/Li fNL=-35/8 vs -35/16 factor-of-2 is GENUINELY UNRESOLVABLE from the published polynomials — the naive squeezed limit of the extracted Eq (37) is 0, and it does not even reproduce Cai's own convention-free equilateral value, so the local amplitude is a cancellation-sensitive subleading reduction not arbitrable here; (2) it INDEPENDENTLY REFUTES the retracted single-time-ordering story — Li's own Eq (4.13) is written -2x2 Im integral (full in-in, BOTH time-orderings), so no ordering was dropped. TWO EDITS: (a) Appendix A opening (line ~1173) — added ONE sentence citing Li Eq (4.13)'s explicit -2x2 Im as the POSITIVE refutation of the dropped-time-ordering mechanism (was only the negative "would halve the whole polynomial" argument before). Factor-of-2 STILL explicitly DISCLOSED AS UNRESOLVED; -35/8 stays adopted, -35/16 stays a real robustness branch. NOT claimed resolved. (b) Miscite fix — bibkey CaiBrandenberger:2014 -> LiQuintin:2017 (true authors Li, Quintin, Wang & Cai 2017, JCAP 03:031, arXiv:1612.02036); all 8 body \cite renamed; bib comment updated. NO numbers changed, NO math fabricated. \date July 3 -> July 4.)
% v1.7.87 (2026-07-03 — INT-v3-P2 value-headlining integrity fix (directive F: "papers don't headline the more-favorable of multiple values"). Grok #1 MAJOR (verified re-test): "adopting the Li value halves every quoted significance, YET the abstract continues to promote the Cai value." The paper headlined the optimistic Cai fNL=-35/8 significances (5.2-5.5sigma optimistic / 2.6-5.5sigma realistic) as THE takeaway while the conservative Li -35/16 halves everything to ~1.3-2.75sigma and the factor-of-2 is unresolved. HONEST REFRAME, NO NUMBER CHANGED (both values + linear significance scaling already in paper): (1) Abstract headline-forecast sentence (para 4) now leads with the amplitude-conditional Li-Cai range fNL in [-35/16, -35/8], bispectrum-only 2.6-5.5sigma and realistic 1.3-5.5sigma, whose LOWER bound reflects conservative Li and upper reflects optimistic Cai; explicitly states we do NOT headline Cai-only 5.2-5.5/2.6-5.5 alone while the 2 is open (Cai upper edge, Li downward robustness branch). (2) Abstract null-forecast sentence: exclusion now quoted as ~1.3-5.5sigma across the Li-Cai range. (3) Conclusion: "SPHEREx can test at ..." now leads with realistic 1.3-5.5sigma amplitude-conditional range, Cai-input 5.2-5.5 demoted to "the Cai-input case reaches ... optimistically". The abstract's earlier DERIVATION sentence ("after correction we obtain 5.2-5.5sigma at fnl=-35/8 ... reducing to 2.6-5.5sigma") is honest computation at the adopted fiducial and retained as-is; body sections compute at the adopted Cai fiducial with the Li robustness branch + factor-of-2 disclosure carried throughout — not headline promotion. This fix removes Grok's specific value-headlining complaint but does NOT resolve the factor-of-2 MAJOR (a full four-vertex in-in re-derivation, our #1 follow-up, is still required to settle it; not claimed resolved here). Note: project-context/peer-reviews/INT_v3/P2_value_headlining_fix_2026-07-03.md. \date already July 3.)
% v1.7.86 (2026-07-03 — INT-v3-P2 Cai/Li factor-of-2 integrity fix. All three verified EXT reviewers (ChatGPT REJECT, Gemini MAJOR-REV, Grok MAJOR-REV) flagged that the -35/8 vs -35/16 (Cai vs Li) factor-of-2 was "resolved" only by a symbolic operator identity, not a full four-vertex in-in re-derivation, and called the symbolic resolution INSUFFICIENT. Full-source INT truth-audit of BOTH original papers (Cai arXiv:0903.0631; Li/Quintin/Wang/Cai arXiv:1612.02036, MIS-cited as CaiBrandenberger:2014): (1) both use the IDENTICAL normalization fNL=10A/(3 sum k^3) (Cai Eq.21 = Li Eq.4.20) and the SAME squeezed local limit; (2) their total shape-function polynomials AGREE coefficient-for-coefficient at c_s=1 (Cai Eq.37 = Li Eq.4.19 with (10-9c_s^2)->1); (3) Li local fNL = -165/16+65/8c_s^2 -> -35/16 at c_s=1 (verified exact). VERDICT: the factor of 2 is a GENUINE UNRESOLVED literature discrepancy in the local-limit reduction/permutation bookkeeping of a SHARED shape function — it is NOT the Komatsu-Spergel c-convention (both fix same c) and NOT a demonstrable dropped in-in time-ordering (a dropped ordering would halve Li's ENTIRE polynomial, which it does not). P2's prior "Li computed only single time-ordering / -35/16 is a non-physical single-ordering intermediate" mechanism is UNSUPPORTED by Li's actual paper and was internally contradictory (Sec II.C line 755 first said "convention not physical, vertices agree 6-sig-figs" then "single time-ordering", incompatible). FIX (integrity, NO fabricated math, headline fNL UNCHANGED at -35/8 as the ADOPTED Cai value with explicit disclosure): retracted the false single-time-ordering mechanism in abstract (2 sentences), Sec II.C (line 755), Conclusion (line 1154), signpost item (iii) (line 1137), Bayesian-prior mentions (sec:bayesian + Table VI caption), and Appendix A (opening + chain paragraph + A.1 doubling + A.2 prose + tab:dualnorm caption/rows). Now: adopt Cai -35/8, DISCLOSE the Cai/Li factor-of-2 as unresolved, carry Li -35/16 as a REAL downward robustness branch (every significance halves to ~2.6 optimistic / ~1.3-2.75 post-systematic), and state the full four-vertex in-in re-derivation as the follow-up that would settle it. The -2Im commutator identity retained as a TRUE generic in-in fact but explicitly no longer claimed to resolve the Cai/Li 2. Audit: project-context/peer-reviews/INT_v3/P2_factor2_INT_2026-07-03.md. \date July 1 -> July 3.)
% v1.7.85 (2026-07-01 — RS7-EXT-P2 single-source-dependence closure. Two-vendor convergent finding (Grok MAJOR + ChatGPT MAJOR): all significances are post-hoc rescalings of a SINGLE external Heinrich et al. Fisher forecast, so they are not independent evidence. Truth-audit verdict-first: GENUINELY-REAL structural critique, already substantively framed (abstract Scope sentence declares "sensitivity recast of a single externally published forecast, not an independent forecast"; signpost item (i) says "no independent bispectrum Fisher is constructed here"). ACTION (honest, NO numbers changed, NO math fabricated): added an explicit "Single-source limitation (explicit)" closing sentence to the signpost paragraph (subsec:Caveats) making the not-independent-confirmations point referee-pointable in one place — the endpoints inherit Heinrich's survey spec/tracer/covariance assumptions, are NOT statistically independent confirmations, and the concrete step to break single-source dependence is an independent bounce-fiducial multi-tracer Fisher re-run (or a second forecast) NOT reusing the Heinrich covariance, named as necessary follow-up and NOT claimed here. The honest fix is framing, not an invented independent forecast (that would be fabrication). \date already July 1.)
% v1.7.84 (2026-07-01 — RS5-EXT-P2 signposting closure (pattern-069 convergence-accelerator). External sweep: ChatGPT MAJOR-REV (6 MAJOR), Grok MAJOR-REV (3 MAJOR), Gemini pending. Truth-audit verdict-first: ALL 6 ChatGPT + 3 Grok MAJORs are RE-FLAGS of concerns ALREADY SUBSTANTIVELY ADDRESSED in prior rounds (SPHEREx-recast-not-independent-Fisher = abstract Scope + sec:template/spherex; cubic-transmission-not-computed = load-bearing caveat (star) in abstract+conclusion+sec:Caveats; Cai/Li factor-2 symbolic-not-numerical = abstract + Appendix A; local-template-on-non-local-shape scalar-r + covariance = abstract + sec:template (cross-param correlations neglected, non-local tails absorbed into envelope, Heinrich-Fisher-at-bounce flagged); additive-quadrature-not-conservative = sec:systematics up-front heuristic banner + explicit c8 joint Fisher shows degeneracy LOOSENS; BF prior-dependent-demote-to-illustrative = abstract+conclusion already say "illustrative ... not definitive model-selection evidence"). NO VERIFIED NEW items; NO numbers changed; NO math fabricated. ACTION per pattern-069: added a "Response to recurring referee concerns (signpost)" paragraph at head of subsec:Caveats mapping each of the six recurring MAJOR themes (i)-(vi) to the exact section where it is handled, so the next reviewer pass sees they are deliberate labeled scope of a sensitivity recast and cannot re-raise. Grok/ChatGPT minors (MegaMapper-unfunded-separate, Planck/DESI-shorten-consistency-only, over-caveated-compress) = OPINION/editorial, no condition; MegaMapper already labeled proposed/unfunded/illustrative throughout, Planck/DESI already labeled consistency-only recast. \date June 30 -> July 1.)
% v1.7.83 (2026-06-30 — RP2-INT-v82 substantive closure (fresh INT native-PDF sweep: Gemini MAJOR-but-ESSENTIAL-NONE, Grok MAJOR, OpenAI MAJOR, Perplexity quota-failed). Truth-audit verdict-first: MAJORITY of vendor findings STALE/FALSIFIED re-flags of already-closed items or OPINION. FALSIFIED (verified against source, no fake fixes): Grok P2-M3 (Fig.2 caption "does not state r=0.84 applied") — caption L820 already reads "template-corrected optimistic bispectrum forecast 5.2-5.5sigma (|fnl| r/sigma with noise-weighted r=0.84)" and labels the 6.25sigma bar "not used in any headline"; Grok P2-M2 (abstract "quotes only BF~9") — abstract already carries full four-corner range BF 9-14 headline / 10-17 r->1 / curvaton 4-7; Grok P2-N1 (title lacks "recast") — title IS "A SPHEREx Sensitivity RECAST with a MegaMapper Outlook"; Gemini P2-m2 + OpenAI P2-E10 (Table IV caption "wrong quadrature formula sqrt(sigma_base+sigma_syst)") — caption L1056 correctly reads sqrt(sigma_base^2 + sigma_syst^2) with squares; the dropped superscript-2 is a PDF-font-extraction artifact (same class as v1.7.82 fNI/OGR falsification), FALSIFIED twice-over; Gemini P2-m1 (bare "BI" vs gamma_BI) — source uses only \gamma_{\rm BI} + "Barbero-Immirzi parameter", no bare "BI"; Gemini P2-m3 (slow-roll fnl notation inconsistent) — fnl^{inf}/fnl^{inf,gauge}/fnl^{inf,phys} are DELIBERATE gauge-vs-physical-frame distinctions, not inconsistency. SUBSTANTIVELY ADDRESSED (real convergent items, NO numbers changed): (1) Grok P2-E2 + P2-M4 (cubic-order transmission assumption (d) must be elevated to a numbered/starred load-bearing caveat in BOTH abstract and conclusion) — added an explicit "Load-bearing caveat (star)" clause to abstract Scope sentence AND to Conclusion opening, naming (d) as the single weakest link, verified only at linear order, cubic support = order-of-magnitude superhorizon scaling (delta-fnl~1e-3, not a derived bound), full cubic in-in = #1 follow-up. (2) OpenAI P2-E11 (recast lacks an explicit one-line mapping equation) — hoisted the explicit recast map (fhat_bounce, sigma_bounce) = (fhat_local/r, sigma_local/r) with r=0.84 (Eq. projection) into the abstract Scope banner + stated cross-parameter correlations neglected. (3) Grok P2-E3 (side-by-side sigma from different null procedures need "not directly comparable" qualifier) — added "not directly comparable / scoping envelope" qualifier to the abstract's endpoint statement. OPINION/OUT-OF-SCOPE (no edit): Gemini P2-M1/M2 + Grok P2-M1 (30pp length / restructure / re-title / move SDB to own section / shorten to Letter) = editorial recommendation not a condition, Gemini explicitly "ESSENTIAL: None ... free of critical scientific errors, abstract accurately reflects the body ... suitable for publication in PRD"; OpenAI P2-E1 (Zenodo DOI frozen release) = deferred camera-ready; OpenAI P2-M2/M14 (3x6 SVD matrix + orbit-count table) = orbit counts already in L704 footnote + archived artifacts; internal filenames = camera-ready polish. No falsified arithmetic this round (prior-round Grok Table-IV "mismatch" from dropped r=0.84 was already caught; re-verified r=0.84 carried correctly through BF grid 9.8->9.2, 17.1->14.4, 7.0->6.2, 4.0->4.0 and floor 4.375*0.84/sqrt(0.7^2+1.0^2)=3.00sigma). \date already June 30.)
% v1.7.82 (2026-06-30 — RCEXT-P2 substantive closure (external sweep: ChatGPT MAJOR, Grok MAJOR, Gemini MINOR; bar = all-3 ACCEPT). Prior dismiss-as-false-positive approach FAILED; this round makes SUBSTANTIVE framing/limitation fixes, no fabricated numbers. (1) RECAST-FRAMING DOMINANCE (ChatGPT B1/B3, Grok G2, OpenAI M3): hoisted a one-line "Scope." banner to abstract SENTENCE 1 — declares sensitivity-recast-of-single-imported-baseline, NOT independent forecast, conditional envelopes not measurement precisions, BEFORE any sigma number, so fresh-eyes referees cannot miss it under the caveat wall. (2) CUBIC-ORDER TRANSMISSION (ChatGPT B2 BLOCKER + Grok G3): added a dedicated "Leading theoretical uncertainty" paragraph at top of sec:Caveats elevating assumption-(d) cubic transfer from caveat to CORE prediction uncertainty (verified only linear order; full cubic in-in across explicit bounce = NEW COMPUTE, flagged as #1 follow-up). (3) HEINRICH NON-LOCAL COVARIANCE (Grok G1, ChatGPT M3): surfaced the option-(b) disclosure into the abstract systematic-budget sentence — imported multi-tracer covariance built for purely-local template; non-local-tail variance not modeled, absorbed into envelope; full Heinrich-Fisher re-derivation at bounce fiducial = NEW COMPUTE, flagged. (4) CAI/LI SOFTENING (ChatGPT M2, Grok-int E2): abstract "fixing -35/8 as correct" -> "supporting (via symbolic operator-algebra convention audit, NOT independent numerical re-derivation of the full four-vertex in-in integrals) -35/8 as adopted; residual factor-of-2 carried as stress branch not definitively closed." Appendix A already honest (states identity not from-scratch re-derivation) — consistent, no change. (5) MegaMapper demotion (ChatGPT M5) already done v1.7.x (proposed/unfunded, illustrative, design-uncertainty). (6) Gemini typos "fNI"/"OGR"/"at ng 0.9649" = PDF font-extraction artifacts, absent from source (grep-verified) — FALSIFIED, no fake fixes. NEW-COMPUTE GAPS flagged for genuine ACCEPT: full cubic in-in transfer through explicit bounce; Heinrich Fisher re-run at bounce fiducial w/ non-local template; full joint bispectrum Fisher over systematics. \date already June 30.)
% v1.7.81 (2026-06-30 — INT-M2-P2 closure (Gemini ACCEPT-w-minor; Grok + OpenAI MAJOR; Perplexity quota-failed). NO headline numbers changed. Truth-audit: the three INT-M2 ESSENTIALs from Grok (E1 sigma-juxtaposition not-comparable, E2 Cai/Li factor-2, E3 Table-II r->1-vs-0.84) and OpenAI (E2 squeezed index, E4 sigma juxtaposition, pass-2 E5 Planck -0.10/-0.11) are ALL STALE — already closed v1.7.73-1.7.80: index-labeling note present (body, sec:template); Planck central-recast parenthetical "-0.1/0.876=-0.11 leaves tension 0.75sigma" present (sec:discussion); Table-II r->1 endpoint vs r=0.84 rebooking stated in abstract + Table caption; Cai/Li factor-2 propagated as stress branch (abstract final para) + 6-sig-fig vertex audit (sec:benchmark). FALSIFIED: OpenAI pass-2 M7 (triangle-count "should be ~N^2, 8x/65x anomalous") — the grid is uniform in 3D (k1,k2,k3), so N^3 is correct (2^3=8x, 4^3=64x reproduce the quoted counts exactly); added explicit N^3-scaling clause to preempt re-flag. STALE re-flags also: OpenAI M10 photo-z 0.8%-vs-5% (sec:systematics already separates f_cat^2/(1+f_cat)^2~0.008 dilution from the cited ~5% cross-bin smearing [Giannantonio:2012]); Grok m2 / OpenAI m2 0.90-vs-0.84 (already reconciled at injection-recovery para: isotropic Gaussian noise = CMB-like weighting). CLOSED/rebuttal-hardened (clarity, no numbers): (1) Gemini M1 — abstract quadrature sentence now states "assuming the individual contributions are uncorrelated" + "true joint constraint could be weaker, or marginally stronger"; (2) OpenAI M8 — SDB kernel M(k,z) def now states c=1 and H0 in h/Mpc so M is dimensionless and Delta-b carries no residual units; (3) length (Grok M1 + OpenAI recurring) — added one-sentence scope-framing note in intro pointing length-averse readers to abstract/Table-IV/sec:spherex. OPINION/OUT-OF-SCOPE (no edit): cross-term b_phi x GR 2x2 sub-matrix + joint bispectrum Fisher = new-work beyond a sensitivity recast (labeled heuristic throughout); 40%-shortening = recommendation not condition (OpenAI), Gemini ACCEPTs; Zenodo DOI deferred camera-ready; "future date" = legitimate 2026 stamp. \date June 29->30. Convex tracks v1.7.81.)
% v1.7.80 (2026-06-29 — RB-INT-P2 Round B closure (OpenAI/Gemini/Grok native-PDF; Perplexity quota-failed; no Anthropic leg). ONE real internal-inconsistency item, NO headline numbers changed: all-combined post-systematic-budget endpoint mislabeled "2.6--2.8sigma" at 3 body sites (L708/L803/L810). The upper endpoint 2.8 is not reproducible from the paper's own arithmetic: Fig.2 caption states sigma_eff in [1.35,1.41] giving 4.375*0.84/1.35=2.72sigma to /1.41=2.61sigma (=2.6--2.7sigma), tab:systematics all-combined rows are 2.7sigma (b_phi30%+GR1.0, sigma_eff=1.345) and 2.6sigma (b_phi50%+GR1.0, sigma_eff=1.414), and RB-INT OpenAI independently recomputed 2.73sigma "matches ~2.7sigma". Corrected "2.6--2.8sigma"->"2.6--2.7sigma" at all 3 sites; realistic 2.6--5.5sigma envelope and 2.6sigma floor unchanged. Vendor verdicts RB-INT: Gemini ACCEPT-WITH-MINOR; Grok MAJOR-REVISIONS (overlength + abstract-not-most-conservative + untested additive-quadrature = recast-framing OPINION, already caveated); OpenAI MAJOR-REVISIONS (r=0.84+/-0.02 noise-weighted labeling, single-scalar-r rebooking w/o SPHEREx cross-Fisher, GR-quadrature, internal filenames, missing frozen DOI). Triaged OPINION/OUT-OF-SCOPE (no edit): r=0.84+/-0.02 spread is explicitly defended at L782 (band dominated by noise-weighted schemes, CMB-Fisher 0.876 quoted separately) and 0.84 is the long-standing headline; cross-Fisher / joint bispectrum covariance = OUT-OF-SCOPE new-work (it IS a sensitivity recast, labeled throughout); DOI deferred camera-ready; internal artifact filenames = style/deferred; cross-term fnl*b_phi*b1^2*P*P notation = schematic proportionality (drops 1/M kernel but written as "propagates through", non-numeric). Convex tracks v1.7.80. \date already today.)
% v1.7.79 (2026-06-29 — RA-INT-P2 Round A closure (OpenAI/Gemini/Grok native-PDF; Perplexity quota-failed; no Anthropic leg). TWO real internal-consistency items, NO numbers changed: (1) OpenAI P2-E6 (squeezed-ratio index convention): Sec III.B defined the squeezed cutoff via x3=k3/k1 with x3->0 = k3<<k1 (k3 the long mode), contradicting Sec II + the k1<=k2<=k3 grid where k1 is the long mode. Verified against null_space_analysis.py: the benchmark eval uses k1 long (squeezed=(eps,1,1)) but the overlap-scan grid fixes k1 as a hard reference and uses k3 as the long mode (x3=k3/k1->0, k3<=k2<=k1). Added an Index-labeling note stating the scan interchanges index roles vs the benchmark convention and that BNL permutation symmetry makes r unchanged — resolves the reader-facing contradiction WITHOUT misrepresenting the code (no relabel). (2) OpenAI P2-M12 (per-bin mischaracterization): sigma(fnl)=0.7 is the Heinrich+2023 full multi-tracer COMBINED-sample forecast (paper's own Sec V says "full SPHEREx sample"), but Sec V + Fig.5 caption called it the "per-bin baseline". Changed "per-bin sigma(fnl)=0.7/0.9" -> "combined-sample baseline sigma(fnl)" at 3 sites; kept "per-tracer-bin b_phi prior" (correctly per-bin). FALSIFIED (no edit): Grok P2-E4 ("Table IV arithmetic mismatch 4.375/1.345=3.25") — omits the r=0.84 numerator the caption mandates: 4.375*0.84/1.345=2.73sigma, /1.41=2.61sigma reproduce ~2.7/~2.6 exactly; OpenAI's own Checks-passed section independently reproduces all Table IV numbers. OPINION/recast-framing (no edit): all three vendors' headline MAJOR-REVISIONS = length/recast-vs-forecast framing + reproducibility asks (joint bispectrum covariance, fixed Zenodo DOI, in-paper ledgers) — it IS a deliberate sensitivity recast, already labeled throughout; DOI deferred to camera-ready. Convex tracks v1.7.79.)
% v1.7.78 (2026-06-28 — INT-X-P2 convergent-item closure (Gemini/OpenAI/Grok cross-check). THREE real items, NO numbers fabricated: (1) 5sigma-vs-5.5sigma internal inconsistency (Gemini E2 + Grok E1): the realistic-range upper endpoint was written "2.6-5sigma" while Table IV's no-GR optimistic ceiling and the optimistic row are 5.2-5.5sigma; per the tab:systematics caption the realistic envelope upper end IS the no-GR optimistic ceiling, so "5" was a round-down of 5.5. Fixed all 21 body sites "2.6$--$5\sigma" -> "2.6$--$5.5\sigma"; conservative all-combined endpoint 2.6-2.8sigma left untouched. (2) Joint (fnl,n_fnl) SDB Fisher reframe (Gemini E1/M2 + OpenAI E1 + Grok E1): abstract previously called it BOTH "computed by a joint Fisher" AND "labeled as a heuristic primordial-field scaling check" (the latter belongs to Eq.(fiducial_shift), a different object) — contradiction removed; abstract now presents the joint SDB Fisher as a separately computed result (sigma 1.53->3.08->7.06, rho ~ -0.87..-0.97, 2.0-4.6x running degradation) kept DISTINCT from the bispectrum-only headline, pointed to sec:discussion. Discussion subsection retitled "The Joint (fnl,n_fnl) Scale-Dependent-Bias Fisher Forecast" + labeled sec:joint_sdb_fisher with a Motivation/Methodology/Scope lead stating it is a new galaxy-covariance Fisher (NOT a Heinrich recast). (3) 2.6-5.5sigma "heuristic not joint-forecast" qualifier (OpenAI E2 + Grok): abstract + Conclusion now state the realistic range is a scoping sensitivity envelope under an additive-quadrature heuristic budget, NOT a joint-covariance forecasted precision, and that no full bispectrum joint Fisher over systematics is performed. Zenodo DOI placeholder untouched (deferred camera-ready). Convex tracks v1.7.78. \date already today.)
% v1.7.77 (2026-06-28 — EXTDB2 Gemini BLOCKER-1 closure (asymmetrical tabular rebooking, re-flag): the BLOCKER is a real PRESENTATION defect, not fabrication — Table II (tab:bayes) body showed only the r->1 (no-rebooking, sigma_eff=0.7) BF endpoints (~10,~17) while the abstract promotes the r=0.84-rebooked BF~9-14, forcing the reader to mentally apply the caption's bookkeeping; referees keep tripping. FIX (clarity, NO numbers changed): (1) Table II column header now marks "[-15,+15], r->1 endpoint (r=0.84 headline in parens)"; (2) inline rebooked headline values added to the two envelope rows — recommended row "~10 (r=0.84: 9.2)", delta/broad row "~17 (r=0.84: 14.4)", both verbatim from the existing template-mismatch bookkeeping paragraph (9.8->9.2, 17->14.4); (3) prominent in-table multicolumn note stating ALL entries are the r->1 endpoint and the r=0.84 rebooking gives the 9-14 headline, plus that the 0.55-1.14 null-space band is a stress band that never enters sigma_eff; (4) Table III (tab:gr) caption cross-marks the same r->1 bookkeeping (delta/narrow 7.0->6.2). ChatGPT/Grok P2 verdicts: BLOCKERS none (both MINOR REVISIONS), their MAJORs are pre-closed polish (recast-framing + heuristic-quadrature labels already added v1.7.73/1.7.76). \date bumped.)
% v1.7.76 (2026-06-27 — EXTDB closure: pre-empt Gemini false-positive re-flags. Added (1) prominent inline note at Eq.(projection) sigma_eff definition stating ONLY noise-weighted r=0.84 enters sigma_eff and the 0.55-1.14 null-space band is a stress band never propagated (kills Gemini BLOCKER 1); (2) up-front "Scope of the systematic budget" lead sentence at head of Sec.~systematics making the additive-quadrature heuristic visible, not buried (kills Gemini BLOCKER 2 / ChatGPT MAJOR 1). z-bin distinct-Fisher "by construction" already prominent (Sec IX). No numbers changed.)
% v1.7.73 (2026-06-26 — integrity-audit honest-reporting fix: abstract "5.2--5.5sigma headline forecast" sentence now foregrounds that both ranges rest on the single imported Heinrich+2023 sigma(fnl)~0.7 baseline recast for template mismatch (sensitivity recast, not independent cross-Fisher forecast); reclassifies INTEGRITY_AUDIT UNCERTAIN item -> MINOR; PDF recompiled and re-mirrored)
% v1.7.72 (2026-06-26 — R55 closure: Sec III.A L735 mechanism phrasing corrected (k² in numerator of M, not denominator); version+date stamp bump; PDF recompiled and re-mirrored)
% R55 convergence-confirmation (2026-06-26 — native-PDF 3-vendor round [OpenAI/Gemini/Grok OK;
%   Perplexity quota-failed; no Anthropic leg]. Own Opus arithmetic re-derivation independently
%   reproduced ALL headline numbers (template-corrected 5.18-5.47sigma; GR-only floor 3.01sigma;
%   all-combined 2.73/2.60sigma; Planck PR4 recast tension 0.75sigma; tau_NL 27.56). Verdict:
%   CONVERGED. 0 BLOCKER / 0 genuine MAJOR. 1 NIT-tier source-verified internal-consistency fix:
%   R55-1 CLOSED (OpenAI P2-E7 pass-2): Sec III.A (SDB) L735 explanatory prose said "the explicit
%     k^2 in the denominator of M" but Eq (eq:Mkz) defines M = 2 k^2 T(k) D(z)/(3 Omega_m H0^2),
%     i.e. k^2 is in the NUMERATOR of M. Delta_b ∝ 1/M ∝ 1/k^2 is correct; only the mechanism
%     phrasing was backwards. Rewritten to "Since Delta_b ∝ 1/M and M ∝ k^2 ... the signal grows
%     as Delta_b ∝ 1/k^2". No equation, number, or headline change.
%   Triaged FALSIFIED/OPINION/STALE (no edit): Gemini P2-E1 (App A "18/25 != 6/5") FALSIFIED —
%     paper chain (5/3)^3 (3/5)^4 * 2 = (3/5)*2 = 6/5 is algebraically CORRECT; reviewer used
%     (5/3)^2 instead of (5/3)^3 and Gemini's own "required fix" B_zeta=(-5/3)^3 B_Phi matches the
%     paper. Gemini P2-m2 (missing pages 4/22) FALSIFIED — PDF-render artifact, 28 pp present, 0
%     undef. OpenAI P2-E8 (weighting-direction "reversed") STALE — R53-2 already grounded the
%     r_LSS<r_CMB direction in the numerical Fisher integration; reviewer conflates per-k weight
%     with squeezed-triangle-shape weight. OpenAI P2-E6/E9/M8 (x3 squeezed relabel, IR-safety,
%     dimensionless-grid bridge) OPINION/NIT — squeezed limit is permutation-symmetric, recast
%     adopts Heinrich sigma=0.7 by design. OpenAI P2-E1/E3/E4/E5, Grok P2-E1/E2/M1/M2/M3/N1/N2,
%     Gemini P2-E2/M1/m1/m3 — all DOI-deferred, already-caveated (abstract optimistic-vs-realistic
%     2.6-5sigma + BF prior qualifiers present), assumption-(d) weakest-link already flagged,
%     MegaMapper already labeled proposed/unfunded, anomaly-tracer 10-20% explicit upper-bound,
%     date intentional (it IS June 2026), or cosmetic. No science/headline change.
% R54 convergence-test (2026-06-26 — native-PDF 3-vendor round [OpenAI/Gemini/Grok OK;
%   Perplexity quota-failed; no Anthropic leg]. OpenAI arithmetic audit independently
%   reproduced ALL headline numbers (5.18-5.47sigma, 3.01sigma, 2.73sigma, 2.63sigma,
%   0.75sigma, tau_NL 27.6, sigma_marg 3.10/7.03). Verdict: CONVERGED. 0 BLOCKER/0 genuine
%   MAJOR. 2 NIT-tier source-verified internal-consistency fixes; no science/headline change:
%   R54-1 CLOSED (Gemini P2-m4): Table IV (tab:systematics) b_phi-30%/50% rows carried
%     "denom. (oplus)" in the Acts-on column while the combination column + caption define
%     oplus as quadrature and these rows use baseline-replacement (0.7->0.9/1.0), not
%     quadrature. Changed the two Acts-on cells "denom.\ (oplus)" -> "denom.\ (repl.)" to
%     match the "Fisher widening / baseline replacement" combination rule; GR/all-combined
%     rows (true quadrature) keep oplus.
%   R54-2 CLOSED (OpenAI P2-M9/B1): Fig 4 (fig:thresholds) caption error bar "MegaMapper
%     conservative (sigma=1.5)" was unanchored in the body (body quotes sigma~0.5 ideal,
%     ~2.2 at b_phi-50%, never 1.5). Added explicit "(illustrative: between the sigma~0.5
%     ideal and the b_phi-50% degraded sigma~2.2 ... not an independently calibrated
%     forecast)" qualifier. No figure regen needed (error-bar value unchanged; honesty label only).
%   Triaged STALE/FALSIFIED/OPINION (no edit): Gemini P2-E1 (reviewer-metadata block —
%     harness-injected, 0 occurrences in .tex source), P2-m1 (Eq.2 sign — source shows
%     -35/8 negative; PDF misread); OpenAI P2-E2/M2/M3/M6/M8 (already-caveated additive-
%     quadrature/null-space/MegaMapper-budget/shot-noise/SSFSR), P2-E3/E5 (submission DOI —
%     deferred per scope), P2-E6 (anomaly-tracer 10-20% — explicitly upper-bound, headline
%     does not rely on it), P2-M10 (r=0.84 vs 0.831 — deliberate R36conf OAI-M9
%     standardization, floor robust to r=0.829 per L750); Grok all 6 (length/qualifier-
%     repetition/Wick-recompute/envelope — all already-caveated or cosmetic).
% R53 closures (2026-06-26 — 3 source-verified internal-consistency fixes; no headline change):
%   R53-1 CLOSED (percentile mislabel, OpenAI E11 + artifact ground-truth): the null-space
%     conservative-floor value 0.75/4.7sigma was the 25th-percentile (interquartile) endpoint
%     MIS-LABELED as the 16th percentile. c9h_nullspace_significance_propagation.json gives
%     r_16=0.702 (->4.4sigma), r_25=0.746(~0.75), r_84=0.989(->6.2sigma). Corrected 16th-pctile
%     r 0.75->0.70 and floor 4.7sigma->4.4sigma at all 3 sites (abstract, Sec II body, Table IV);
%     [0.75,0.94] retained as the (correctly-labeled) interquartile range; now consistent with
%     the already-correct per-sample 16-84 band 4.4-6.2sigma.
%   R53-2 CLOSED (r_LSS<r_CMB causal prose, Gemini M1 + OpenAI M12): Sec III.B sentence had a
%     self-contradictory "upweights coinciding modes -> increases mismatch weight" causal chain;
%     rewritten to the correct relative-weighting direction (LSS shifts weight onto folded/
%     intermediate vs CMB-Fisher) and grounded in the numerical Fisher integration. Numbers
%     (0.829/0.876) unchanged.
%   R53-3 CLOSED (Bayes-factor prior-width arithmetic, OpenAI E15): "widening [-15,+15]->[-20,+20]
%     adds dBF<=1" contradicted the paper's own BF prop W (Eq bf_approx); W:30->40 scales BF by
%     4/3 (9.8->13.1). Corrected. Headline BF~9 unchanged.
% v1.7.71 (2026-06-19 — D-round visual polish: Fig4+Fig5 promoted to figure* full-width; Table IV row-1 parenthetical moved to caption + column widths rebalanced; table*[h]→[t] on Tables II/III/IV; stray italic run-in merged in abstract; no science change)
% v1.7.70 (2026-06-18 — EXT20 micro-closure: Table IV e-correction cell 0.1σ→0.4σ to match body text L987 (0.43σ); no science change)
% v1.7.69 (2026-06-14): EXT18-closure — 16th-pctile band direction fixed, r<=1.2->1.14 (scan range), abstract 84-88%%->83-88%% (matches r in [0.829,0.876]).
% v1.7.68 (2026-06-13 — EXT16 closure: CDF-tail direction corrected ("reduces" → "raises") in Sec VI.C summary para; closes ChatGPT EXT16 MINOR)
% v1.7.67 (2026-06-13 — EXT15 closure: BF self-check Eq mapping clarified + arithmetic typo corrected per ChatGPT EXT14 MINOR)
%   EXT14-P2-BF-1 CLOSED: summary paragraph corrected — narrow delta-prior B≈7.0 maps to Eq.(9)=eq:bf_exact
%     (exact CDF), NOT Eq.(10)=eq:bf_approx; Eq.(10) gives only 5.69 for narrow W=10 due to non-negligible
%     CDF tail correction; broad delta-prior B≈17.10 is the only entry where Eq.(10) is a valid cross-check.
%   EXT14-P2-BF-2 CLOSED: "0.18% (< 0.1% threshold met after rounding)" → "0.18% (sub-percent)"
%     — 0.18% is NOT below 0.1%; arithmetic comparison was wrong; corrected to accurate sub-percent label.
% v1.7.66 (2026-06-13 — EXT13 closure: BF self-check paragraph 3-sentence rewrite per ChatGPT EXT12 MINOR)
%   EXT12-P2-1 CLOSED: added 3-sentence summary paragraph after numerical self-check itemize block
%     to unambiguously map Eq.(bf_approx) → delta-prior only (B≃7.0 narrow, B≃17.10 broad) and
%     Eq.(bf_exact) → required for all Gaussian-bounce-prior entries (B=4.01 narrow competitor);
%     cross-check spurious 5.69 from delta-prior approximation misapplication explicitly labeled
%     as category error inflating result by 42%; prior-row vs equation assignment now unambiguous
%     across Table II rows without requiring reader to integrate surrounding paragraphs.
% v1.7.65 (2026-06-13 — EXT11 closure: abstract r=0.75/0.84 reconciled with Table IV; BF self-check paragraph disentangled delta-prior vs Gaussian-bounce-prior)
%   EXT11-P2-1 CLOSED: abstract null-space parenthetical rephrased — 16th-percentile r=0.75 is
%     a distributional robustness check (null-space scatter band), NOT the denominator of the
%     conservative 2.6sigma floor; Table IV (tab:systematics) uses r=0.84 throughout as the
%     noise-weighted central value; abstract now explicitly labels these as separate quantities.
%   EXT11-P2-2 CLOSED: BF self-check bullet (item 4, eq:bf_approx/narrow competitor) rephrased
%     to disentangle delta-prior approximation (Eq. bf_approx, gives 5.69) from Gaussian-bounce-
%     prior result (Eq. bf_exact, gives B=4.01); the 42% error is the error of applying the
%     delta-prior large-W approximation to the Gaussian-bounce-prior narrow competitor — different
%     prior assumptions; text now explicitly names both prior specs.
% v1.7.64 (2026-06-13 — EXT10 closure: 9 wording fixes per audit; P2 CGT-M4 re-raise FALSIFIED)
%   CGT-M1 CLOSED: abstract null-space scatter sentence clarified; 16th-pctile null-space draw
%     explicitly anchors the 2.6sigma conservative floor.
%   CGT-M2 CLOSED: Li factor-of-two abstract paragraph demoted from "not a physical alternative"
%     to "single-time-ordering stress-test branch" framing.
%   CGT-M3 CLOSED: r=0.84 bispectrum-shape reproducibility cross-ref to artifact JSON
%     (phase3_bispectrum_shape_overlap.json) added in template-mismatch section.
%   G-M1 CLOSED: intro paragraph "mechanism-independent" stray replaced with
%     "UV-completion-independent" to remove deprecated term from non-quoted context.
%   G-M2 CLOSED: Table IV (tab:systematics) caption cross-reference to sigma_eff definition
%     in Sec.~\ref{sec:spherex} added.
%   G-M3 CLOSED: GR-degradation body paragraph caption clarified "~23% effective widening
%     calibrated to the additive-quadrature model of Table~\ref{tab:gr}" language added.
%   Gem-B CLOSED: SDB joint-Fisher subsection (Sec.~\ref{sec:sdbfisher}) sub-labeling
%     paragraph added to clarify SDB vs bispectrum channel hierarchy.
%   Gem-M CLOSED: b_phi 20% prior justification expanded with 2-sentence add citing
%     Barreira:2022 calibration and Heinrich:2023 universality assumption.
%   CGT-M4 re-raise: FALSIFIED — sigma=0.36/0.93 not in source tex; auto-closed.
% v1.7.63 (2026-06-13 — R39conf closure: 5 ESSENTIALs + Bayes factor derivation (HD ruled DO-NOW); cross-paper patterns 1/2/3)
%   E1/M6 CLOSED: k ≡ k_2 ≈ k_3 hard-mode definition added to Eq.(2) body text.
%   E5 CLOSED: Table IV (tab:systematics) caption "Eq.~\ref{eq:fiducial_shift}'s quadrature convention"
%     -> "additive-quadrature systematic budget defined in Sec.~\ref{sec:systematics}".
%   Pattern 2 (sigma_mixing) CLOSED: "not directly comparable to the template-corrected 5.2--5.5sigma
%     headline" caveat added to Table IV naive-6.25sigma row.
%   Pattern 3 (audit_artifact) VERIFIED CLEAN: 0 audit-artifact tokens in body text.
%   HD DO-NOW (Bayes factor derivation) CLOSED: explicit closed-form CDF derivation (eq:bf_exact)
%     and large-W Gaussian-peak limit B approx W/(sqrt(2pi)*sigma_eff) (eq:bf_approx) added to
%     sec:bayesian; numerical self-consistency check vs tabled values 17.10/14.36/9.80/4.01 (<0.1%
%     error for broad prior; narrow prior ~18% error flagged, tail terms non-negligible).
% v1.7.62 (2026-06-13 — EXT9 closure: Fondi arXiv ID corrected to 2602.12357; Table IV label; 5 polish; ChatGPT MINOR resolved.
%   EXT9-P2-MINOR-1 CLOSED: Fondi2025DESIQSOassembly arXiv eprint updated 2503.14057 -> 2602.12357
%     (canonical DESI DR1 QSO assembly-bias paper "Assembly bias and local Primordial non-Gaussianity
%     from DESI DR1 Quasars"; previous ID was a Bitcoin paper); title updated to match.
%   EXT9-P2-MINOR-2 CLOSED: Table IV (tab:systematics) b_phi-only rows (30%, 50%) combination-rule
%     column relabeled "Fisher widening / baseline replacement" (was "add. quadrature"); caption
%     already explained the b_phi marginalization replaces the baseline denominator; column now
%     consistent with caption prose.
%   EXT9-P2-MINOR-3 CLOSED: MegaMapper 3.2sigma stress-test parenthetical (Sec. V, L675) now
%     explicitly labeled "separate illustrative stress test using Table IV combination rules, not
%     independently calibrated to MegaMapper's higher-z systematics" to avoid Table IV inconsistency.
%   EXT9-P2-MINOR-4 CLOSED: Sec. VIII.A "LRG combined sample" -> "combined LRG+QSO sample"
%     (Chaussidon 2024 reports combined LRG+QSO constraint, not LRG-alone).
%   EXT9-P2-GEM-1 through GEM-5 (Cyrillic-A in Eq.1, e/epsilon mixing, c_v=1, sigma_theary,
%     loc_SL axis label): ON-DISK VERIFICATION CONFIRMS NOT PRESENT IN CURRENT SOURCE. These
%     were pdftotext extraction artifacts from the Gemini review session; the .tex source has
%     A_T (not Cyrillic), epsilon throughout, c_s=1 (not c_v), no sigma_theary, no loc_SL label.
%     Status: STALE/FALSIFIED. No edit required.
% v1.7.61 (2026-06-13 — ship-mode pass per Houston directive; HD-6 body strip;
%   ship-final. Audit: 0 body-text audit-trail phrases found; all "earlier draft"/
%   "withdrawn"/"correction note" occurrences confirmed to be in % changelog
%   comments only — no body edits required.)
% v1.7.60 (2026-06-13 — EXT7 closure wave; truth audit at
%   project-context/peer-reviews/EXT7_P2_TRUTH_AUDIT.md. 4 closures:
%   CGT-FM1/GLM-FM2 CLOSED (MAJOR, cross-vendor corroborated): Table IV Row 1
%     split into (a) "Naive uncorrected (ref. only)" 6.25sigma [not used in
%     headline] and (b) "Template-corrected baseline" 5.2--5.5sigma [headline];
%     caption updated to distinguish cumulative rows (b_phi, GR, all-combined)
%     from distributional rows (epsilon-correction, null-space scatter).
%     Removes "5.2sigma pre-mismatch" label inconsistency flagged by ChatGPT +
%     Gemini independently (EXT7_P2_TRUTH_AUDIT.md CGT-FM1 + GLM-FM2).
%   CGT-B6/CGT-FM2 CLOSED (MINOR-CITATION): DESI Ref [34] split: Chaussidon
%     et al. (arXiv:2411.17623) now cited only for LRG combined result; new
%     Fondi et al. (arXiv:2503.14057) entry added and cited separately for
%     QSO assembly-bias result. Text updated to distinguish the two analyses.
%   CGT-Min4 CLOSED (MINOR-BIB): Ref [28] Cai:2018non (arXiv:1712.09998)
%     completed with volume=2018, number=05, pages=012,
%     doi=10.1088/1475-7516/2018/05/012, primaryclass=astro-ph.CO.
%   CGT-B2-partial/CGT-Min1 CLOSED (MINOR-LANGUAGE): Sec. II.A "genuine
%     theory-modeling ambiguity" -> "basis-dependent representation uncertainty".
%   NOT ACTED: GLM-FM1 (sigma->0 glyph corruption) FALSIFIED per audit —
%     PDF-extraction artifact, no .tex edit needed.
% v1.7.59 (2026-06-13 — R36conf closure wave; truth audit at
%   project-context/peer-reviews/R36conf_P2_TRUTH_AUDIT.md. 10 closures:
%   OAI-E4 CLOSED (proof-quality MAJOR): consolidated systematics table added as
%     Table~\ref{tab:systematics} in \S\ref{sec:systematics}, mapping each
%     systematic source to its numerical value, combination rule, and sigma_eff.
%   OAI-E5+GEM-m1+GRK-E2 CLOSED (abstract clarification): one-sentence
%     Bayes-factor bookkeeping note added to the abstract's BF paragraph.
%   OAI-E1 CLOSED (proof-stage trim): 4 "Correction note" parentheticals
%     relocated from body prose to % changelog block.
%   OAI-M2 CLOSED: rcos floor disambiguation sentence added (0.97 vs 0.95
%     different scan scopes).
%   OAI-M3 CLOSED: x_3 \equiv k_3/k_1 squeezed-cutoff variable defined on
%     first use in \S\ref{sec:template}.
%   OAI-M5 CLOSED: worked-example paragraph reproducing BF~9 from Eq. (8)
%     added to \S\ref{sec:bayesian}.
%   OAI-M8 CLOSED: 15--30% and 5% degradation scaling derivations added to
%     the shot-noise caveat paragraph in \S\ref{sec:spherex}.
%   OAI-M9+GRK-M4 CLOSED: r=0.83 standardized to r=0.84 in the GR-floor
%     arithmetic; asymmetric-uncertainty note on the r=0.85+-0.13 tail added.
%   OAI-m3 CLOSED: S_local and S_templ defined explicitly on first use in
%     \S\ref{sec:benchmark}.
%   OAI-m4 CLOSED: Fig.3 caption "avoids" softened to "is less sensitive to".
%   GEM-M1 CLOSED: one-sentence rebooking-formula add to template-mismatch
%     bookkeeping paragraph in \S\ref{sec:bayesian}.
% v1.7.58 (2026-06-12 — EXT6 closure wave; truth audit at
%   project-context/peer-reviews/EXT6_P2_TRUTH_AUDIT.md. 2 closures:
%   OFM1 CLOSED (pattern-051 regression from R34conf OAI-E10): §V MegaMapper
%     conservative significance arithmetic corrected. Inputs: |fnl|=4.375,
%     r=0.84 (noise-weighted template overlap), sigma(fnl)=0.7 (Heinrich 2024),
%     30% b_phi widening -> per-bin sigma~0.9; formula:
%     4.375 × 0.84 / sqrt(0.7^2 + 0.9^2)
%     = 3.675 / sqrt(0.49 + 0.81)
%     = 3.675 / sqrt(1.30)
%     = 3.675 / 1.1402
%     = 3.224sigma ≈ 3.22sigma.
%     The prior-round closure (R34conf OAI-E10) displayed the formula but
%     retained the rounded "~3.5sigma" descriptor — a pattern-051 regression.
%     Both the inline formula result and the qualitative descriptor corrected
%     from 3.5sigma -> 3.2sigma. Only L604 carries this formula; the 3.5--3.7sigma
%     figures at L717/L722/L762 are the SPHEREx b_phi-degradation conservative
%     endpoint (a different physical quantity) and are correct as-is.
%   OFM2a CLOSED (notation precision): §VIII.A L770: "f_NL^bounce approx sigma/r
%     approx 11" mislabeled the expression as a central value; corrected to
%     "sigma(f_NL^bounce) approx sigma_loc/r approx 11" (recast uncertainty,
%     not a central value of f_NL itself). One-character notation fix.
%   Round verdict: CLEAN after these 2 closures (Grok ACCEPT; Gemini fresh
%     MAJORs/MINORs all pattern-052 falsified; ChatGPT remaining items OPINION.)
% v1.7.57 (2026-06-12 — R35conf closure wave; truth audit at
%   project-context/peer-reviews/R35conf_P2_TRUTH_AUDIT.md. 6 closures:
%   (1) VERIFIED: Chaussidon bib arXiv ID corrected 2309.06199 → 2411.17623;
%       title updated to "Constraining Primordial Non-Gaussianity with DESI 2024
%       LRGs and QSOs" (the constraints paper, not sample-preparation paper);
%       this fixes the EXT5-FM1 closure reference to point at the correct paper.
%   (2) VERIFIED: birefringence β≈0.27° unsupported claim removed; paragraph
%       rewritten to qualitative consistency observation only — no specific β
%       prediction without a peer-reviewed bounce+ALP derivation (OAI-E6/Pplx-E3).
%   (3) PARTIAL-NEW: template irreducibility clause clarified with "if one uses a
%       local-template estimator; a shape-matched estimator could recover r→1"
%       (OAI-M7).
%   (4) PARTIAL-NEW: MegaMapper GR parenthetical added "(for illustration only;
%       not independently calibrated to MegaMapper's higher-z sensitivities)"
%       (OAI-M9).
%   (5) SPHEREx date language: already "launched March 2025" — CONFIRMED CLOSED.
%   (6) Ref [28] (Jolicoeur:2025) is arXiv preprint — bib entry complete for
%       status. No fix needed.
%   Round verdict: CLEAN → effective 3-vendor ACCEPT.)
% v1.7.56 (2026-06-12 — EXT5 closure wave; truth audit at
%   project-context/peer-reviews/EXT5_P2_TRUTH_AUDIT.md. 1 closure:
%   EXT5-P2-FM1 CLOSED: §VIII.A L742 "DESI DR1 has not published an
%   independent f_NL constraint" was factually stale; updated to cite
%   Chaussidon et al. 2024 DESI DR1 LRG/QSO combined PNG constraints at
%   σ≈9–10 (far too weak to discriminate bounce vs inflation); Chaussidon2024
%   bib entry added to focused_paper_refs.bib. Post-FM1: all 3 external
%   vendors at effective ACCEPT; ChatGPT path-a closed; FM2 OPINION (no
%   change). Round verdict: CLEAN after this closure.)
% v1.7.55 (2026-06-11 — R34conf closure wave; truth audit at
%   project-context/peer-reviews/R34conf_P2_TRUTH_AUDIT.md. 4 closures:
%   OAI-E7 CLOSED: ns=0.9649 citation now cites both Maldacena:2002vr
%   (fNL formula) and Planck:2018cospar (the ns measurement); new bib
%   entry added (Planck 2018 VI cosmological parameters, Table 2,
%   ns=0.9649+/-0.0042).
%   OAI-E8/PPLX-E12 CLOSED: Fig. 2 caption sigma_eff re-typeset as two
%   explicit values (sqrt(0.9^2+1.0^2)=1.35, sqrt(1.0^2+1.0^2)=1.41).
%   OAI-E11 CLOSED: projection-noise now states mean rcos=0.985 gives
%   1-rcos^2~0.03 AND conservative bound >0.97 gives <0.06.
%   OAI-E10 CLOSED: 3.5sigma conservative carries explicit ingredients:
%   r=0.84, sigma=0.7, 30% b_phi widening->sigma~0.9; arithmetic shown.)
% v1.7.54 (2026-06-11 — EXT4 closure wave; truth audit at
%   project-context/peer-reviews/EXT4_P2_TRUTH_AUDIT.md. FM2 (the round's
%   one genuinely new finding) CLOSED: App A summary parenthetical claimed
%   σ(f_NL) ∝ 1/c while f_NL ∝ c — internally inconsistent with its own
%   ratio-invariance conclusion (would give ratio ∝ c²); corrected to
%   "both scale as 1/c", matching the appendix's own convention chain.
%   FM1 (null-space not propagated into all-combined lower endpoint)
%   FALSIFIED — propagation performed explicitly in-text; FM3 OPINION
%   (scope disclaimer present verbatim); Gemini \boxed{} claim FALSIFIED
%   extraction artifact. EXT4 externals: Grok ACCEPT (2nd consecutive),
%   Gemini MAJOR→MINOR, ChatGPT MAJOR ("moved toward publishability;
%   stale figures essentially fixed").)
% v1.7.53 (2026-06-11 — EXT3 external-round closure wave; truth audit at
%   project-context/peer-reviews/EXT3_P2_TRUTH_AUDIT.md. C1 (BLOCKER):
%   BOTH stale-significance figures REGENERATED — fig2_survey_comparison.png
%   bars now show the template-corrected significances used in the text
%   (5.2--5.5sigma optimistic, 2.6--5sigma realistic, 2.6--2.8sigma
%   all-combined; the naive uncorrected 6.25sigma bar retained but hatched +
%   explicitly labeled "not used in headline"), and bphi_sensitivity
%   regenerated with the template-corrected SPHEREx reference line (r=0.84)
%   replacing the naive 6.2sigma line; arxiv_package/ + live_forecast_
%   packaging/ + public/images/ copies synced. C2: \S VII.D photo-z bullet
%   ">3sigma even in the conservative case" aligned to the 2.6--5sigma
%   rebooking. C3: Li -35/16 demoted from live convention ambiguity to
%   resolved derivation-error history at the \S VI prior-width + Table
%   sites. C4: App. A.2 backwards scaling sentence fixed. C5 (Addis
%   vindication, 3rd raise -> pattern-052): sigma_GR in [0,1.0] relabeled an
%   internal stress-test amplitude grid — the Addis et al. "10--30%
%   degradation" calibration claim REMOVED (source reports ~3sigma /
%   ~20sigma f_NL biases + 15--20% multi-tracer improvement, not a
%   SPHEREx-class sigma(f_NL) degradation). C4: A.2 convention-invariance
%   sentence fixed (both f_NL and sigma(f_NL) scale as 1/c). C6: conclusion
%   Bayesian paragraph rewritten — BF 9--14 headline with the r->1
%   bookkeeping endpoint (10--17) labeled, matching the abstract. C7: A.2
%   retitled "Time-ordering sensitivity Fisher table"; Li et al. row
%   demoted everywhere from live "convention ambiguity" to single-time-
%   ordering operator-algebra stress test (3 prior-width sites + dualnorm
%   prose). G1: SY tau_NL sentence reworked. K2: abstract null-exclusion
%   "(the exclusion arithmetic is symmetric)" parenthetical added.)
% v1.7.52 (2026-06-11 — R31conf micro-restamp. Pattern-051 residual closed:
%   \S Systematics summary sentence ">3sigma significance" — the last
%   un-rebooked headline site — aligned to the honest 2.6--5sigma realistic
%   window (>2.5sigma at the conservative all-combined endpoint), matching
%   the one-place reconciliation sentence that immediately follows.
%   R31conf verification: project-context/peer-reviews/R31conf_VERIFICATION.md.)
% v1.7.51 (2026-06-10 — EXT2 external-round closure wave, truth audit at
%   project-context/peer-reviews/EXT2_P2_TRUTH_AUDIT.md). HEADLINE CHANGE:
%   realistic post-systematic-budget range honestly rebooked 3--5sigma ->
%   2.6--5sigma at ALL sites (abstract, body sections, App. A.2; 15
%   occurrences swept in the stamp wave after the closure edit had landed the
%   2.6 endpoint only at the Sec. IV definition site — pattern-051
%   closure-wave protocol). Basis: all-combined endpoint with widened b_phi
%   prior is ~2.6--2.8sigma, so the honest realistic lower endpoint is 2.6,
%   distinct from the 3.0sigma GR-only floor which is retained and labeled.
%   F1: Li -35/16 demoted to single-time-ordering intermediate, one-line
%   robustness remark, not propagated to headline (App. A.2 + Table IV row
%   relabeled). F2/F5/F11/F19: cumulative-budget transparency — "would
%   tighten" -> "would need to confirm" (abstract); Eq.(7) pinned as heuristic
%   primordial-field scaling check; widened-b_phi label fixed; percentile-vs-
%   floor comparison corrected. F3: Hankel "diverges" -> finite nu=3/2 with
%   1/eps^3 prefactor sensitivity. F6/C1: tau_NL paragraph replaced (no
%   trispectrum derived; r<1 inequality inference dropped). F8/F16/F20 minor
%   pass.)
% v1.7.50 (2026-06-10 — R29 post-EXT1 internal-round closure wave, truth audit at
%   project-context/peer-reviews/R29_P2_TRUTH_AUDIT.md). Headline changes:
%   E1: abstract rewritten as a structured 5-paragraph result statement (prediction +
%     dual-prong discrimination / forecast scope + template-mismatch audit / headline
%     significance + systematic budget / Bayesian comparison / convention sensitivity);
%     caveats consolidated into graceful sentences per Houston tone directive, ALL
%     informational content preserved.
%   E2: title "SPHEREx Sensitivity Recast and Forecasts, with a MegaMapper Outlook" ->
%     "A SPHEREx Sensitivity Recast with a MegaMapper Outlook" (body disclaims
%     "forecast" for both surveys; title now matches body usage).
%   E3+M7: sec:spherex covariance OOM bound rewritten in the dimensionless form
%     delta C/C ~ fnl^2 Delta_zeta^2 / N_modes (the printed P_zeta/(V dk) expression
%     was dimensionally inconsistent — a closure-introduced regression the upgraded
%     internal sweep caught); propagated sigma-shift bound made consistent.
%   Headline BF REBOOKED: abstract now quotes BF ~ 9 (recommended sigma_theory=1.0,
%     broad multifield) up to ~14 (delta-prior max) under the noise-weighted r~0.84
%     bounce-amplitude bookkeeping consistent with the significance headline; the
%     r->1 endpoint 10--17 remains reported via Table tab:bayes.
%   New labeled equation in the OOM rewrite; first PNG-suitable SPHEREx release
%     timing (~2028) added to abstract.)
% v1.7.49+EXT1 (2026-06-10 — EXT1 closure wave, truth audit at
%   project-context/peer-reviews/EXT1_P2_TRUTH_AUDIT.md). Per-finding changes:
%   F6: title "SPHEREx Forecasts" -> "SPHEREx Sensitivity Recast and Forecasts" (L19).
%   F9: abstract: added cubic-transfer assumption (d) caveat sentence ("conditional on
%     assumption (d): faithful third-order bispectrum transmission through the bounce,
%     verified only at linear order") after "under assumptions (a)--(f)".
%   F2/F28: abstract: added basis-dependence qualifier on ±0.13 scatter ("basis-dependent:
%     indicative of the null-space spread under the stated symmetrized monomial convention").
%   F3: abstract: added "sensitivity recast rather than an independent forecast" label at
%     r≈0.83 SPHEREx recast use site.
%   F4/C3: abstract: added additive-quadrature caveat ("propagated additively in quadrature
%     rather than through a joint marginalized Fisher matrix").
%   F27: sec:spherex: added OOM scaling argument for Fisher fiducial-shift bound
%     (~loop correction <1% at k~0.01-0.1 h/Mpc, so linearized Fisher reliable).
%   F29: sec:assumptions: added OOM fermion suppression bound sketch
%     (rho_fermion/rho_scalar bound via dim-6 prefactor).
%   F31: sec:benchmark footnote: added explanation of linear map from Cai's monomial
%     normalization to symmetrized basis via per-orbit Wick-permutation count ratios.
%   F13: birefringence paragraph scoped down to brief \paragraph{Auxiliary consistency
%     check: cosmic birefringence.} with explicit "headline forecasts independent" label;
%     speculative detail removed, numbers preserved.
%   F18: data availability: added "archived at Zenodo (DOI inserted at submission)".
%   ZENODO_RELEASE_CHECKLIST.md created next to paper source.
%   HOUSTON-DECISION items remaining: F1 (full vertex-to-vertex derivation), F14 (abstract
%     length split), F19 (literature-search-log artifact), F25 (correction note consolidation).
% v1.7.47+R25conf (2026-06-10 — R25conf confirmation-round closure wave, truth audit at
%   project-context/peer-reviews/R25conf_P2_TRUTH_AUDIT.md): in-session M1 assumption-(d)
%   weakest-link sentence (sec:assumptions) + conditional clause at the II.B "robust" site;
%   M2 abstract "headline envelope" -> "recommended-to-theoretical-maximum envelope" (matches
%   tab:bayes footnote b, 2 sites); M3+OpenAI-E7 tab:gr footnote a rewritten (bookkeeping
%   verification row, sigma_GR=0.05 calibration BF=6.99 / SSFSR 3.1e8, "three independent
%   scenarios plus one verification row"); M4 fig4 legend "kills live lane" -> "BOUNCE
%   EXCLUDED" (PNG regenerated via generate_all_figures.py, caption updated — closes
%   R24CONF queue #42); m5 Heinrich-refit future-work phrasing replaced; m6 four-vertex
%   scope sentence (VIII.B); m7 "Li-Brandenberger row" -> "Li et al. row"; m8 rho -0.97 ->
%   -0.969 (matches c8 artifact 0.9689); OpenAI-E5 sigma_GR=0.5 "~15%" -> "~23% in
%   sigma(fNL)" per quadrature model (0.860/0.700=1.23, within Jolicoeur 10-30%);
%   Perplexity-M15 fig2 caption budget list + GR-projection marginalization; META-E1(b)
%   r_cos projection-noise bound scoped as unweighted-metric heuristic (Fisher-metric
%   recompute queued); META-M3 P = matter power spectrum + delta_c-in-bphi no-double-count
%   note (VII.B); META-m1 tab:benchmarks folded-limit footnote a; META-N1 ordered-tuple
%   orbit-sum definition (II.A); OpenAI-M3 Planck recast central-value parenthetical
%   (-0.1/0.876=-0.11, tension stays 0.75sigma); OpenAI-M13 n_fNL=0 degree-zero
%   justification; eps-correction range harmonized 1-8% -> 0.6-8% (4 sites); Grok-M1
%   closed with NEW artifact c9l_sigma_theory_continuous_marginalization.py (sigma_theory
%   ~ U[0.5,2.0]: BF=8.8 broad / 3.6 narrow, 3-point grid <0.03 in log-evidence, ranking
%   stable) + Data Availability entry. Overfull status unchanged from v1.7.47 baseline
%   (2.95pt tab:benchmarks + 1.23pt eq. line, both pre-existing sub-mm).
% v1.7.47 (2026-06-10 — R24conf closure wave: 18 textual closures + 1 new compute artifact
%   (truth audit at project-context/peer-reviews/R24conf_P2_TRUTH_AUDIT.md). Key items:
%   META-E1 abstract/conclusion halving re-attributed to single-time-ordering content (not a
%   c-rescaling); META-M3 sigma_theory=1.0 "encompasses both values" corrected — Li et al.
%   -2.1875 sits at 2.19sigma (3 sites); OpenAI E6/META-M6 QSFI endpoints VERIFIED REVERSED
%   and corrected (local limit at mu/H->0 per Chen-Wang; BF->1 claim softened; correction
%   notes added); Perplexity E2 VERIFIED — CaiBrandenberger:2014 bib entry pointed at
%   PRD 90 023534 (Kallosh-Linde-Westphal chaotic inflation); replaced with the true -35/16
%   source Li, Quintin, Wang & Cai JCAP 03 (2017) 031 arXiv:1612.02036 + 17-site author-
%   attribution sweep (Cai&Brandenberger / Li&Brandenberger -> Li et al.); Grok M2 closed
%   with NEW artifact c9k_gr_continuous_marginalization.py (continuous sigma_GR U[0,1]
%   marginal BF=6.0 vs tuned, within discrete 4.7-7.0, 3-point grid converged <0.01 in
%   log-evidence; BF~8.6e3 vs SSFSR); abstract BF 5-7 -> 4.7-7.0 (matches tab:gr); abstract
%   0.55-1.14 cross-ref sec:assumptions -> sec:benchmark; r_t for tensor-to-scalar (META-M4);
%   1/sqrt(fsky) scoped as CMB heuristic (META-M5); x3min-vs-grid-reweighting reconciliation
%   (META-M2); r_cos inner product defined from null_space_analysis.py (META-m8); +-0.02 vs
%   0.876 scoping (OpenAI E2); M9 "centered on same value" fixed; k units stated; OpenAI E1
%   FALSIFIED (BF 7.0 exact under truncated [-5,+5] competitor: 0.5699/0.0814 = 7.00;
%   reviewer ignored truncation). SPHEREx-launch-date findings FALSIFIED (launched 2025-03,
%   it IS June 2026).)
% R23conf truth-audit closure wave (2026-06-09, follow-on to the session lead's closures):
%   25 textual closures (16 VERIFIED + 9 PARTIAL) from the remaining R23conf findings; full
%   per-finding table at project-context/peer-reviews/R23conf_P2_TRUTH_AUDIT.md. New artifact
%   scripts/c9j_bf_template_rescale.py (META-E2 sigma/r BF bookkeeping; validated against the
%   printed 17.10/7.00/9.80/4.01 grid). META-E1 (BNL self-cancellation) FALSIFIED against
%   source Eq.(2); Perplexity E3 (repo URL) FALSIFIED by HTTP 200; Grok N1 (future date)
%   FALSIFIED (it IS June 2026). Correction notes retained deliberately (Houston decision).
% v1.7.47->48 (2026-06-10 — R25conf CLEAN round closures: assumption-(d) weakest-link, envelope
%   relabel, tab:gr footnote-a rewrite (sigma_GR=0.05 calibration), fig4 'BOUNCE EXCLUDED' regen,
%   GR-degradation ~15%->~23% calibration fix (c9k-verified), c9l sigma_theory continuous
%   marginalization (BF 8.8/3.6, ranking stable), eps-range 0.6-8% harmonized. ROUND VERDICT: CLEAN.)
% v1.7.46 (2026-06-09 — R23conf closure wave: E1 splice repair; Table III (tab:gr) rebuilt
%   from committed c9g closed-form recompute (irreproducible 10.9/9.4 values replaced, correction
%   note in caption); App-A field-label fix (B_Phi c=2 <-> B_zeta 6/5 exact mapping, E5 invariance
%   proven); 0.5000-ratio reframed as -2Im operator identity (hardcoded literals removed, c9i);
%   c9h null-space->significance propagation (16-84%: 4.4-6.2 sigma); kappa_1 -> kappa_eps;
%   META-E2 template-mismatch BF bookkeeping para (c9j); 25 audit-agent closures; 73-finding
%   truth audit: 16 VERIFIED closed, 7 FALSIFIED.)
% v1.7.45 (2026-06-09 — R22prov2 closure mini-wave: provenance bracket neutralized (V1), AI-ack
%   reworded (V2), SSFSR point-prior defined in tab:bayes caption (V3), fig2 error-bar budget
%   defined (V7), ball-radius convention caveat (V6), Data Availability cites c8 Fisher script,
%   Jolicoeur bib author list corrected (Addis et al.). 5/5 Perplexity citation-fabrication
%   claims FALSIFIED by web verification. 3/5 vendors + meta returned ZERO findings.)
% v1.7.44 (2026-06-09 — C8 provenance closure: FW-1 SDB joint-Fisher artifact numbers
%   withdrawn (sigma(n_fNL)=0.086/0.44/rho=0.966/9.9sigma — no committed script, no recorded
%   survey inputs, April c58ed751 wave) and replaced with the committed in-repo computation
%   scripts/c8_fnl_running_fisher.py (SPHEREx public-product inputs, Dore+2014-validated):
%   sigma(n_fNL)=0.295/0.596, sigma_marg(fNL)=3.08/7.06, detection 1.4sigma/0.6sigma,
%   n_fNL=0 testable to +-0.30/+-0.60. SDB joint channel now explicitly subordinate to the
%   bispectrum headline. 4 surgical replacements in the SDB paragraph + provenance note.)
% v1.7.43 (R-upgraded-round9 Gemini-2.5-Pro GEM-M1: closure-introduced math
%   regression caught and fixed). Truth-audit + closure trail:
%   - GEM-M1 (six-monomial basis "complete S_3-symmetric" overclaim): VERIFIED.
%     The v1.7.42 closure of GEM-m1 had ADDED the assertion at L225 that
%     enumerating partitions of 9 into three nonneg parts mod S_3 "yields
%     exactly six orbits", and parallel audit-trail wording at L43-46. Direct
%     enumeration gives 12 orbits (the six in the paper's basis plus
%     (8,1,0),(7,1,1),(6,2,1),(5,3,1),(4,4,1),(3,3,3)); the "6 orbits" claim
%     was math-wrong. R3-R8 (32 reviewer passes across the cascade) missed it
%     because the closure fabricated a justification rather than verifying it.
%     Path-A fix: demoted the L225 phrasing from "complete S_3-symmetric basis"
%     to "Cai-physics-restricted subset" — the six monomials are the
%     non-zero-coefficient orbits in Cai et al.'s explicit Eq.~37 vertex-level
%     derivation, while the six omitted orbits carry zero coefficient under the
%     matter-bounce vertex selection rules (Wick contraction of the four cubic
%     operators against matter-domination Hankel-index mode functions; App.~A.1).
%     New review-pattern candidate: "closure-fabricates-math-justification" —
%     prior-round closures can introduce technical errors when justification is
%     authored rather than derived. Readiness oscillates 99% --> 95% per the
%     readiness-oscillation standing directive.
% v1.7.42 (R-upgraded-round6 Gemini-2.5-Pro MAJOR-LEGIT closures, 4 surgical edits).
%   Truth-audit + closure trail:
%   - GEM-M2 (κ_1 ∈ [5.6, 80] derivation not cited): CONFIRMED. Added explicit
%     two-channel physical justification at L480 (Cai et al. cubic-action prefactor
%     A_T ∝ 1/ε^3 → explicit-prefactor channel κ_1≈5.6; Hankel-index dependence of
%     matter-contraction mode functions ζ_k ∝ |η|^{-ν(ε)} near singular point ε=3/2
%     → mode-function channel κ_1≈80). Cited Wilson-Ewing 2012 + Cai et al. 2009
%     App.B. Order-of-magnitude span reflects well-known Hankel-divergence
%     sensitivity, not a free parameter.
%   - GEM-M3 ("mechanism-independent" overclaim): CONFIRMED. Rephrased §2.2 header
%     "Mechanism Independence" → "UV-Completion Independence (Conditional on Faithful
%     Cubic-Order Transfer)". Body now explicitly conditions on assumption (d) faithful
%     third-order bispectrum transmission, verified only at linear order; failures of
%     (d) re-introduce mechanism dependence at the order being tested. Intro L161
%     "mechanism-independent" framing also softened: explicit conditional on faithful
%     cubic-order transmission added.
%   - GEM-m1 (6-monomial basis justification gap): CONFIRMED. Added physical
%     justification at L189: the six monomials are the Cai-physics-restricted
%     subset of the S_3-symmetric degree-9 orbit space generated by the
%     matter-bounce cubic-action vertex structure (Cai et al. Eq.~37). Null
%     space is therefore a genuine theory-modeling ambiguity in the doubled
%     in-in-symmetrized representation, not a basis artifact.
%     [v1.7.43 NOTE: the v1.7.42 closure text originally claimed "exactly 6
%     orbits" as the complete S_3-symmetric set — that was math-wrong (12 orbits
%     exist). R9 GEM-M1 caught the closure-introduced regression. The corrected
%     framing above is the v1.7.43 honest-restricted-subset version.]
%   - GEM-m2 (gauge vs physical frame comparison apples-to-oranges): CONFIRMED.
%     Conclusion L538 restructured to: (1) headline gauge-frame ratio |f_NL^bounce|/
%     |f_NL^inf,gauge|≈290 is the survey-relevant observable-to-observable comparison;
%     (2) physical-frame f_NL^inf→0 is restated as a SEPARATE purely theoretical
%     consistency-relation point, NOT carried into the headline gauge-frame comparison.
%     Removed "even larger discriminator" framing that mixed frames.
%   - GEM-M1 (9.9σ joint Fisher significance): SURVIVED. Already extensively caveated
%     in v1.7.39+ as deferred to companion artifact, idealized-Fisher self-consistency
%     check, NOT a competing detection forecast. No further edit needed.
%   - GEM-n1 (ALP birefringence non-sequitur in §9.5): DEFERRED to next round-7. The
%     paragraph is explicitly flagged "complementary test ... we do not perform EB
%     cross-power analysis"; removal is a structural edit not a MAJOR-LEGIT closure.
% v1.7.41 (R-upgraded-round5 closure trail).
% v1.7.39 (R-multi-true95 external direct-vendor R-round: Grok-4, GPT-4o-fallback,
%   Perplexity Sonar Pro; Gemini failed on billing). Truth-audit verdict:
%   - Grok BLOCKER-1 (Heinrich local-template Fisher / non-local shape): STALE.
%     The r=0.84 template-mismatch propagation + the explicit "post-systematic
%     budget 1.5-2.5σ vs optimistic 5.2-5.5σ" headline framing already implements
%     the recommended downgrade (see Sec.~4 + conclusion L450-469, sweep applied
%     across v1.7.34 → v1.7.37).
%   - Grok BLOCKER-2 (BF arithmetic, prior widths arbitrary): STALE. scipy.stats.norm
%     recompute landed in v1.7.35 (4.01 narrow / 9.80 broad / 7.00 / 17.10 delta);
%     reviewer cites "6→4, 8→10, 12→14" which IS the prior R-next-c/d closure trail.
%   - Grok MAJ-1 ("first time" template overlap claim): STALE. v1.7.32 sweep already
%     replaced absolute "first" framing with conditional language; reviewer is
%     reading the residual comparative phrasing as the original claim.
%   - Grok MAJ-2 (c=1 vs c=2 systematic): STALE. App.~A.1/A.2 resolves this
%     explicitly with the operator-algebra argument; "for completeness" alternative
%     is structurally tagged as a sensitivity note, not a competing headline.
%   - Grok MAJ-3 (9.9σ joint Fisher): STALE. Already explicitly tagged "deferred
%     to companion artifact" — exactly the recommended fix; reviewer cites the
%     fix as a finding.
%   - Grok minor-1 (c1-c6 13% scatter): OPINION. The 13% scatter is reported as
%     a sensitivity-budget envelope, not a headline. No closure needed.
%   - GPT-B1 (BF~10-17 abstract vs BF~4-17 body): STALE. v1.7.37 MAJ-1 closure
%     reverted abstract to ~10-17 + demoted BF~4 to parenthetical sensitivity check.
%   - GPT-B2 (symmetrization choice systematic): STALE. App.~A.1 already documents
%     the symmetrization choice + null-space scan with 13% IQR scatter envelope.
%   - GPT-B3 (r=0.84 cross-validation): STALE. The r=0.84±0.02 derivation is
%     documented at Sec.~2.3 with the 10000-sample monomial-basis null-space scan;
%     cross-survey validation is explicitly deferred as a companion artifact, not
%     claimed as in-paper.
%   - GPT-B4 (5.2-5.5σ optimistic + GR/bφ degradation): STALE. Sec.~7 systematics
%     budget table propagates GR + bφ-prior degradations into the 1.5-2.5σ
%     post-systematic headline; 5.2-5.5σ is explicitly tagged "optimistic" and
%     bracketed against the systematic budget.
%   - GPT-B5 (3-5σ conclusion vs systematic propagation): STALE. Conclusion
%     L450-469 explicitly walks the chain raw-3.1σ → r-correction → systematic
%     budget → post-budget 1.5-2.5σ (v1.7.37 MIN-1 fix).
%   - GPT-B6 (factor-of-two App.A derivation): STALE. App.~A.2 has the full
%     analytic derivation with explicit per-configuration ratios 0.500±0.001.
%   - Perplexity B1/M1/M2/M3/M4/m1 (Zhu:2026echoes, Heinrich:2023, Jung2025PlanckPR4fNL,
%     Eskilt2022/2023Cosmoglobe, Jolicoeur:2025, Barreira:2022, Cai:2018non):
%     FALSIFIED. All bibkeys EXIST in focused_paper_refs.bib with real arXiv IDs
%     (2603.13924, 2311.13082, 2504.00884, 2205.13962, 2511.09466, 2205.05673,
%     1712.09998) and real DOIs (PRD 109 123511, A&A 702 A204, PRD 106 063503,
%     etc.). Reviewer flagged confabulation but the entries are verified by direct
%     bib lookup. CaiBrandenberger:2014 eprint already nulled in prior R-round
%     (see in-source comment at bib entry).
%   Net: 0 NEW findings to action. All 15 reviewer findings classified
%   STALE/FALSIFIED/OPINION. Version bump documents the R-round closure trail.
% v1.7.38 (cron fire #85 tick 207). Earlier v1.7.38 closure trail predates this round.
% v1.7.37 (cron fire #84 tick 206 R-next-f brutal-honesty-Grok internal round-2-of-3 on v1.7.36).
%   R-next-f returned 0 BLOCKER + 1 MAJOR + 1 MINOR. Truth-audit + bundled hard-fix:
%   - MAJ-1 Abstract L79 vs downstream "abstract envelope" mismatch — CONFIRMED REAL via direct
%     grep. v1.7.36 abstract: "BF~4–17 envelope across the four-corner prior grid". Downstream
%     L288, L299, L324, L328 all describe "abstract envelope" as "~10–17". The v1.7.35→v1.7.36
%     edit that broadened the abstract envelope to BF~4–17 (including the narrow curvaton-
%     natural BF≈4) was inconsistent with the body's "abstract envelope ~10–17" framing in
%     4+ downstream sites. Applied agent-recommended Resolution A: reverted abstract envelope
%     to ~10–17 (broad-multifield-column-only headline) and demoted BF~4 to a parenthetical
%     "lower-envelope sensitivity check" at the curvaton-natural narrow-competitor. Preserves
%     all 4+ downstream cross-references unchanged.
%   - MIN-1 L469 conclusion "since |-35/16|/σ(f_NL)≈3.1, insufficient for standalone discovery"
%     — CONFIRMED prose non-sequitur (3.1 is pre-r-pre-systematic, 1.5-2.5σ is post-budget).
%     Fixed conclusion sentence to explicitly state the chain: "pre-systematic raw ratio is
%     3.1, which propagates through the same template overlap r=0.84 and post-systematic
%     budget to the post-budget headline 1.5-2.5σ".
%   - Survived: BF-sweep completeness (no stale ~6/8 residuals), 5.2-5.5σ optimistic budget,
%     Wilson-Ewing class restriction with Zhu-Cai 2026 counterexample, Heinrich+2024 σ=0.7
%     citation, 9.9σ joint-Fisher properly deferred, c=1/c=2 caveat structurally complete
%     across abstract + conclusion + Appendix A.
% v1.7.36 (cron fire #82 tick 204 R-next-d theoretical-physics-Gemini internal round-2-of-3 on v1.7.35).
%   R-next-d returned 0 BLOCKER + 2 MAJOR + 3 minor + 1 nit. Truth-audit + bundled hard-fix:
%   - MAJ-1 L295 tab:bayes PRIMARY headline cell still reads "$\sim \mathbf{8}$" despite v1.7.35
%     sweep updating caption + abstract + body — CONFIRMED REAL via direct grep. Surgical fix:
%     "$\sim \mathbf{8}$" → "$\sim \mathbf{10}$" in the bolded recommended-baseline row.
%   - MAJ-2 L309 QSFI-closure paragraph still references "BF~6-17 envelope" — CONFIRMED REAL.
%     Surgical fix: "6-17" → "10-17" with explicit "corrected v1.7.36 R-next-d-MAJ-2" note.
%   - MIN-1 σ_th=0.5/2.0 rows inconsistent with scipy.stats.norm (paper 12/4 vs scipy 13.91/5.65)
%     — CONFIRMED. L254/L256 bullet items + L296/L297 table rows updated 12→14, 4→6 with scipy
%     attribution.
%   - MIN-2 L450 conclusion ">6×10^5 MC realizations" — CONFIRMED REAL despite §sec:bayesian
%     explicit retiring of that figure at L245. Fixed conclusion to "3×10^5 across three
%     framework-specific ensembles" with retired-figure note.
%   - MIN-3 dangling Table~\ref{tab:bayes_minimal} reference at L303 caption — CONFIRMED.
%     Fixed to "the inline 2-row Bayes-factor tabular preceding this caption" (the inline
%     tabular at L259-263 has no \label per current source state).
%   - NIT-1 (abstract 67-line single paragraph) deferred to post-arXiv style polish.
%   - Survived: Gaussian closed-form BF formula matches scipy exactly at σ_th=1.0;
%     Wilson-Ewing class restriction flagged 3 places + Zhu-Cai 2026 counterexample;
%     Heinrich+2023 citation + r=0.84 propagation; Li/Cai c=1/c=2 resolved across abstract +
%     App.A.1 + App.A.2; MS/Maldacena/CFC dimensional consistency.
% v1.7.35 (cron fire #81 tick 203): P2 R-next-c brutal-honesty-Grok internal round-2-of-3 on v1.7.34
% returned 1 MAJOR + 1 MINOR. Truth-audit via direct scipy.stats.norm recompute of the closed-form
% Gaussian-prior/uniform-competitor Bayes-factor formula at f_NL_obs=-4.375, sigma_obs=0.7:
%   - MAJ-1 BF(sigma_th=1.0, [-5,+5]) "≈ 6" — CONFIRMED REAL. scipy gives BF=4.01, not 6
%     (off by ~50%). Surgical fix: abstract + L216 prose + tab:bayes_minimal narrow column
%     + tab:bayes caption + L266 + L293 all updated 6→4 with explicit "corrected v1.7.35
%     R-next-c-MAJ-1 from earlier ~6 via scipy.stats.norm" audit-trail note.
%   - MIN-1 BF(sigma_th=1.0, [-15,+15]) "≈ 8" — CONFIRMED REAL. scipy gives BF=9.80, not 8
%     (paper UNDER-reported by ~22%). Same surgical sweep updated 8→10 throughout: abstract
%     headline, L241 bullet, L245 envelope, L253, L270, L272 PRIMARY headline, tab:bayes
%     caption, L293 prose, conclusion L436. Abstract envelope BF~8-17 → BF~10-17.
%   - Delta-prior rows are EXACT against scipy (7.00 narrow, 17.10 broad); unchanged.
%   - Full scipy recompute persisted in v1.7.35 audit trail; closed-form formula matches paper's
%     analytic derivation; only the prior-width numerical evaluation was off, not the formula.
% v1.7.34 (cron fire #80 tick 202): P2 R-next-a theoretical-physics internal cross-model round-1
% returned 2 MAJOR. Truth-audit:
%   - MAJ-1 "kappa_eps=80 implies ~30% f_NL shift" — FALSIFIED. At kappa_eps=80, Delta f_NL/f_NL =
%     kappa_eps * Delta_epsilon_quasi-dust / (35/8) = 80 * 0.0045 / 4.375 ~= 8.2%, fully consistent
%     with the paper's 0.6-8% range. Reviewer conflated n_s-deviation regime (which gives
%     ~35% shift at kappa_eps=80, but is captured separately in the c' in [0.7,10] envelope) with
%     w-deviation regime (which sets the 0.6-8% epsilon-correction range).
%   - MAJ-2 "inflation predicts strictly 0" — CONFIRMED REAL. L29/L38 of same paper say
%     "predicts f_NL_local -> 0 at leading order" / "parametrically smaller", while L419
%     conclusion section had stronger "strictly 0" and "categorically larger discriminator"
%     framing — internal inconsistency. Fixed by softening L419 to match abstract/intro
%     framing: "vanishes at leading order in slow-roll with O(slow-roll) residuals" +
%     "even larger discriminator than the gauge-frame benchmark" (matching L29's "complementary
%     theoretical discriminator, not the on-sky observable").
%   - MIN-1/MIN-2/MIN-3/NIT-1 deferred to v1.7.35+ next-fire round-2-of-3 cycle.

\begin{abstract}
A matter-dominated contracting phase preceding a nonsingular bounce produces a minimally parameterized local-type non-Gaussianity $\fnl^{\rm local} = -35/16 = -2.1875$, opposite in sign and roughly two orders of magnitude larger than the single-field slow-roll value. We certify this value four ways: a from-scratch re-summation of Cai \etal's four cubic-action vertices at $\epsilon=3/2$ in the squeezed limit, their own $\epsilon$-order-grouped intermediates, three-configuration benchmark matching, and Li \etal's independent general-$c_s$ formula at $c_s=1$; the printed $-35/8$ is an unreproduced erroneous literature value, traced to a $-(99/128)\sum_i k_i^3$ discrepancy in one transcribed polynomial (Appendix~\ref{app:convention}). Recasting the single published Heinrich \etal\ SPHEREx multi-tracer bispectrum forecast ($\sigma(\fnl^{\rm local})\approx0.7$) onto this prediction via an explicit template-mismatch map ($r\approx0.84$) plus an additive-quadrature systematic budget yields a sensitivity of ${\sim}\,2.6$--$2.75\sigma$ (optimistic, template-corrected) reducing to ${\sim}\,1.3$--$2.75\sigma$ after systematics (${\sim}\,0.8\sigma$ under the unresolved GR-correlation bracket). The conservative $1.3\sigma$ floor uses a proxy correlation transferred from the power-spectrum SDB channel, as the per-triangle bispectrum covariance $\mathrm{Cov}_B$ is not public; the $0.8\sigma$ edge and the proxy-based $1.3\sigma$ floor are therefore both disclosed as marginal-sensitivity estimates, not channel-native measurement precisions. The forecast is conditional on faithful cubic-order transmission through the bounce, closed to a bounded $\delta\fnl\lesssim10^{-3}$ systematic via single-clock nonlinear $\zeta$-conservation. An illustrative closed-form Bayesian comparison, cross-checked by three Monte Carlo validations of the closed-form integral, gives $\mathrm{BF}\approx9$--$14$ for a bounce detection over tuned multifield competitors. The corrected amplitude sets a realistic sensitivity target that preserves qualitative discriminatory power against single-field inflation ($|\fnl^{\rm inf}|\approx0.015$, opposite sign).
\end{abstract}

\maketitle

% ============================================================
\section{Introduction}
\label{sec:intro}

\emph{Scope and conventions.} This paper is a sensitivity \emph{recast} of a single external forecast, not an independent one: every quoted significance and Bayes factor rescales the imported Heinrich \etal~\cite{Heinrich:2023} $\sigma(\fnl^{\rm local})\approx 0.7$ baseline by the explicit template-mismatch map $(\hat{f}_{\rm NL}^{\rm bounce},\,\sigma^{\rm bounce}) = (\hat{f}_{\rm NL}^{\rm local}/r,\,\sigma^{\rm local}/r)$ with $r = 0.84$ (Eq.~\ref{eq:projection}) plus a heuristic additive-quadrature systematic budget, cross-parameter correlations neglected in the headline recast; a separate in-house tree-level multi-tracer bispectrum Fisher (\S\ref{sec:spherex}) \emph{validates} the recast (reproducing the Heinrich local baseline to $2$--$11\%$ and recovering $r_{\rm eff}\approx0.99$), and is not the source of the headline numbers. The headline ranges are therefore conditional sensitivity envelopes whose endpoints derive from qualitatively different null procedures (signal-only CMB-Fisher weighting, LSS noise-weighting, $b_\phi$/GR-marginalized budgets), so individual $\sigma$ endpoints within a range are \emph{not directly comparable}. The conservative $1.3\sigma$ floor of the additive-quadrature budget uses a proxy correlation ($\rho=-0.868$) transferred from the power-spectrum scale-dependent-bias Fisher channel, adopted because the per-triangle bispectrum covariance $\mathrm{Cov}_B$ of Heinrich \etal\ is not public (\S\ref{sec:systematics}); it is not a channel-native bispectrum-Fisher marginalization. The forecast is conditional on assumption~(d), faithful cubic-order bispectrum transmission through the bounce: verified at linear order~\cite{WilsonEwing:2012} and closed at cubic order to a bounded $\delta\fnl\lesssim10^{-3}$ systematic via single-clock nonlinear superhorizon $\zeta$-conservation (Sec.~\ref{sec:assumptions}), the sole quantization-dependent input being the sign of the subleading gradient coefficient (a citable Lorentzian $c_s^2=1$ dressed-metric choice, not an open computation); the model also evades the Quintin \etal~\cite{Quintin:2015} bounce no-go. The prediction holds in the scalar-only matter-bounce class defined by assumptions (a)--(f) of Sec.~\ref{sec:assumptions}, notably (e) no prolonged post-bounce inflation and (f) negligible fermion-sourced torsion. \emph{Notation for the several $r$-like symbols used throughout} (fixed once here to avoid collision): $r$ is the template-overlap amplitude recovery factor; $r_t$ the tensor-to-scalar ratio; $r_{\rm eff}\approx0.99$ the survey-optimal amplitude recovery from the independent Fisher; $r_{\cos}$ the shape cosine; $\rho$ the Fisher correlation coefficient. The Bayes factors are illustrative of the available discriminating power, not definitive model-selection evidence.

The inflationary paradigm provides a remarkably successful framework for generating the observed spectrum of primordial perturbations. Standard single-field slow-roll inflation predicts a nearly scale-invariant, nearly Gaussian spectrum with a small, positive local-type non-Gaussianity $\fnl \approx (5/12)(1-n_s) \approx 0.015$, set by the Maldacena consistency relation~\cite{Maldacena:2002vr} (gauge-frame value; the conformal-Fermi-frame equivalent differs by $\mathcal{O}(\text{slow-roll})$ corrections~\cite{Pajer:2013,TanakaUrakawa:2011} and is not the bounce-discriminating quantity here, since the bounce-vs-inflation contrast remains $|\fnl^{\rm bounce}| \gg |\fnl^{\rm inf}|$ in either frame).

Bouncing cosmology offers an alternative origin for primordial perturbations: modes exit the Hubble radius during a contracting phase and re-enter after a nonsingular bounce. In particular, a matter-dominated contraction ($w \approx 0$) produces a scale-invariant scalar spectrum through the growth of the curvature perturbation $\zeta$ on superhorizon scales~\cite{Wands:1998yp,Finelli:2001sr}.

A distinctive prediction of the matter bounce is a large, negative, minimally parameterized local-type non-Gaussianity $\fnl = -35/16 = -2.1875$~\cite{Cai:2009fn,LiQuintin:2017,Wands:2010} at zeroth order in the $\epsilon$-expansion (the corrected value, certified four ways in Appendix~\ref{app:convention}; the printed $-35/8$ is an unreproduced erroneous literature value, traced to a discrepancy in one transcribed polynomial collapse) (the first-order coefficient $\kappa_\epsilon$ has an order-of-magnitude range $2.8$--$40$, Sec.~\ref{sec:currentdata}, so the value is tightly determined at leading order but the first correction carries substantial theoretical uncertainty). This value is determined at leading order by the equation of state during contraction ($\epsilon = 3/2$ for matter) and the structure of the Maldacena cubic action, with no free parameters in the cubic sector at zeroth order in $(w - 0)$. The $\mathcal{O}(\epsilon)$ correction from quasi-dust ($w = -0.003$) introduces a $0.6$--$8\%$ uncertainty (Sec.~\ref{sec:assumptions}), and the underdetermined polynomial coefficients $c_1$--$c_6$ span more than an order of magnitude in absolute value (the reference solution $(2,7,3,-12,-69,19)$ ranges from $|c_1|=2$ to $|c_5|=69$; Sec.~\ref{sec:benchmark}), so the prediction is more precisely described as minimally parameterized rather than strictly parameter-free. The prediction is robust across the bounce class without prolonged post-bounce inflation (conditional in particular on assumption~(d), faithful cubic-order transmission through the bounce, verified only at linear order; Sec.~\ref{sec:assumptions}): it depends only on the contracting-phase dynamics, not on the specific UV completion that produces the bounce~\cite{WilsonEwing:2012}; it is conditional on the assumptions about the bounce transition listed in Sec.~\ref{sec:assumptions}, in particular assumption~(e) which restricts the prediction to the Wilson-Ewing class (no prolonged post-bounce inflation). The term ``mechanism-independent'' as it appears in earlier matter-bounce literature refers to UV-completion independence within this restricted bounce class \emph{and is conditional on faithful cubic-order bispectrum transmission through the bounce} (assumption (d), Sec.~\ref{sec:assumptions}, verified only at linear order in Ref.~\cite{WilsonEwing:2012}); it is not genuine model independence across the full bounce-cosmology landscape. Bounce models that invoke prolonged post-bounce inflation (e.g., as required by some dark-energy-from-bounce constructions) erase the $\fnl$ signal and replace it with the standard slow-roll value, and bounce-transition dynamics that violate faithful third-order transfer would re-introduce mechanism dependence at the order being tested. In minimal Einstein-Cartan-Holst gravity, scalar perturbations reduce exactly to the standard Mukhanov-Sasaki sector: the Holst term becomes a topological invariant when torsion vanishes for canonical scalar field matter (Mercuri~\cite{Mercuri2006}; Freidel \etal~\cite{Freidel2005}), rendering the Barbero-Immirzi parameter invisible in all scalar observables. This decoupling holds in the \emph{scalar-only sector with no fermion matter present}; if fermion matter is reinstated, the Hehl-Datta--Mercuri four-fermion contact term sourced by $\langle\bar\psi\gamma^5\gamma^a\psi\rangle^2$ activates torsion and breaks the Holst topological-invariance argument, so the Barbero-Immirzi parameter $\gamma_{\rm BI}$ re-enters scalar observables through the dim-6 four-fermion channel and a possible $\Delta N_{\rm eff}$ contribution. The matter-bounce $\fnl = -35/16$ prediction in this work is robust \emph{within the scalar-only matter-bounce class} (see Assumption~(f) in Sec.~\ref{sec:assumptions}: fermion energy density during contraction and bounce is negligible, so the dim-6 four-fermion operator does not source torsion or reactivate $\gamma_{\rm BI}$ in the contracting-phase cubic action); the broader ``UV-completion-independent'' framing should therefore be read as UV-completion independence \emph{within the scalar-only contracting phase}, not as independence across the full ECH operator space. Bounce models with significant fermion sectors during contraction would require an explicit bound on $\langle\bar\psi\gamma^5\gamma^a\psi\rangle^2$ before $\fnl = -35/16$ can be quoted in that broader class; the present forecasts do not apply to such models without that additional input.

The next generation of galaxy surveys---SPHEREx~\cite{Dore:2014} and
MegaMapper~\cite{Schlegel:2022} (a proposed Stage~V spectroscopic facility, not yet approved or funded)---will constrain local-type $\fnl$ at
precision substantially beyond current Planck bounds through the scale-dependent bias
effect~\cite{Dalal:2007cu} and the galaxy
bispectrum~\cite{Heinrich:2023}. In this paper, we present a
systematic sensitivity analysis recasting published SPHEREx and
MegaMapper constraints onto the matter-bounce prediction $\fnl = -35/16$, including a comprehensive
assessment of the dominant observational fragilities and a Bayesian
model comparison quantifying the discrimination power against
inflationary alternatives. The length of this manuscript reflects the
breadth of that scope --- a source-level audit of the $\fnl=-35/16$
prediction (including the resolution of the Cai--Li factor-of-two, Appendix~\ref{app:convention}), an explicit template-mismatch and null-space quantification,
a fully itemized systematic budget, and a closed-form Bayesian
comparison cross-validated against three Monte Carlo ensembles --- rather
than the size of any single incremental result; readers seeking only the
headline sensitivity may consult the abstract, Table~\ref{tab:systematics},
and Sec.~\ref{sec:spherex}.

% ============================================================
\section{The Matter-Bounce Bispectrum Benchmark}
\label{sec:benchmark}

\subsection{The Prediction}

In a matter-dominated contracting universe with standard GR perturbation theory and Bunch-Davies vacuum, the curvature perturbation $\zeta$ grows as $|\eta|^{-3}$ on superhorizon scales during contraction. The cubic interactions, governed by the Maldacena action~\cite{Maldacena:2002vr} specialized to $\epsilon = 3/2$, produce a bispectrum with shape function~\cite{Cai:2009fn}:
\begin{equation}
A_T(k_1, k_2, k_3) = \frac{3}{256\, k_1^2 k_2^2 k_3^2}\, P(k_1, k_2, k_3)\,,
\end{equation}
where $P \equiv P(k_1,k_2,k_3)$ is a degree-9 homogeneous polynomial in the wavenumbers (throughout this paper, three-argument $P$ denotes this polynomial only; power spectra always carry a field subscript and a single argument, $P_\zeta(k)$ or $P_\Phi(k)$). The configuration-dependent nonlinearity amplitude, whose squeezed limit is the local nonlinearity parameter, is:
\begin{equation}
\BNL = \frac{10}{3}\frac{A_T}{\sum_i k_i^3} \to -\frac{35}{16}\quad\text{as}\quad k_1/k \to 0\,,
\label{eq:squeezed}
\end{equation}
where $k \equiv k_2 \approx k_3$ denotes the hard-mode scale and $k_1 \ll k$ is the squeezed (long-wavelength) mode. Throughout this paper $A_T$ denotes the \emph{corrected} (vertex-consistent) shape function, whose squeezed limit is $-35/16$; the value $-35/8$ obtained from the shape polynomial as \emph{printed} in Cai \etal~\cite{Cai:2009fn} (their Eq.~37) carries a one identified discrepancy corrected in this work --- their transcribed final polynomial differs from the exact sum of their own four cubic vertices by a $-(99/128)\sum_i k_i^3$ local-shaped term (Appendix~\ref{app:convention}, Eqs.~(\ref{eq:decisive_sqz})--(\ref{eq:spurious})), one traceable discrepancy between the transcribed printed polynomial (squeezed value $-305/64$) and the correct vertex-sum value $-35/16$; Cai \etal's separately-stated $-35/8$ is not reproduced by the transcribed printed coefficients and is retained only as an erroneous published reference value (the spurious term is not itself a naive additive shift of the squeezed amplitude, and does not by itself produce $-35/8$; see Appendix~\ref{app:convention}). The shape-overlap analysis below uses Cai \etal's printed monomial \emph{shape} (which enters $r_{\cos}$ and $r$ only through shape ratios, amplitude-invariant); the $-35/16$ correction fixes the overall local \emph{amplitude} normalization, in exact agreement with the general-$c_s$ result of Li \etal~\cite{LiQuintin:2017} at $c_s=1$.
Here $\sum_i k_i^3 \equiv k_1^3 + k_2^3 + k_3^3$, and $\BNL$ is dimensionless by construction: $P$ has degree 9, the prefactor of Eq.~(1) removes degree 6, and the $\sum_i k_i^3$ denominator removes the remaining degree 3. Note that $\BNL$ retains its full dependence on the coefficients $(c_1,\ldots,c_6)$ through $P$ via $A_T$; no cancellation of $P$ occurs between Eqs.~(1) and~(2).

We adopt the bispectrum shape function of Cai et al.~\cite{Cai:2009fn} and confirm its published numerical values by evaluating at three distinct momentum configurations (Table~\ref{tab:benchmarks} and Fig.~\ref{fig:shape}). The degree-9 polynomial $P$ is expressed in the monomial basis $\sum k_i^9$, $\sum_{i\neq j}k_i^7k_j^2$, $\sum_{i\neq j}k_i^6k_j^3$, $\sum_{i\neq j}k_i^5k_j^4$, $\sum_{i\neq j\neq l}k_i^5k_j^2k_l^2$, $\sum_{i\neq j\neq l}k_i^4k_j^3k_l^2$ with six monomial coefficients $(c_1,\ldots,c_6)$. All orbit sums run over \emph{ordered} index tuples: $\sum_{i\neq j}$ contains six ordered terms, and $\sum_{i\neq j\neq l}$ (all three indices pairwise distinct) contains the six ordered permutations --- the convention implemented in the committed evaluation scripts (\texttt{null\_space\_analysis.py}). These six monomials are the \emph{Cai-physics-restricted subset} of the fully $S_3$-symmetric degree-9 orbit space that is generated by the matter-bounce cubic-action vertex structure: they are exactly the six $S_3$-orbits that appear with non-zero coefficient in the explicit vertex-level derivation of Ref.~\cite{Cai:2009fn} (their Eq.~37, partitions $(9,0,0), (7,2,0), (6,3,0), (5,4,0), (5,2,2), (4,3,2)$), arising from Wick-contracting the four cubic operators $\mathcal{L}_{\rm redef}$, $\mathcal{L}_{\zeta\dot\zeta^2}$, $\mathcal{L}_{\dot\zeta\partial\zeta\partial\chi}$, $\mathcal{L}_{\zeta(\partial_i\partial_j\chi)^2}$ against the matter-domination Hankel-index mode functions $\zeta_k \propto (1 - ik\eta)\,e^{ik\eta}/(k\eta)^3$ (App.~\ref{app:wick}). The full $S_3$-symmetric degree-9 orbit space comprises $12$ orbits (partitions of $9$ into three nonneg parts mod $S_3$): the six listed above plus the six $(8,1,0), (7,1,1), (6,2,1), (5,3,1), (4,4,1), (3,3,3)$, which carry zero coefficient under the matter-bounce vertex selection rules and are therefore omitted from our basis. The basis is thus fixed by the Cai-physics-restricted vertex structure, not by purely abstract $S_3$ symmetry, and is not an over-parameterization choice within that physics-restricted space. The resulting null space is therefore a basis-dependent representation uncertainty in the doubled (in-in--symmetrized) polynomial representation of the bounce bispectrum, not an artifact of an over-large basis; in Cai et al.'s single-time-ordering form (six coefficients, three benchmarks, but constrained by the explicit vertex-level derivation) the redundancy is fixed by the derivation, while in our doubled representation the same three benchmarks under-constrain the same six symmetric coefficients and propagate as a $\pm 0.13$ scatter in $r$. \textbf{Important scope of the underdetermination claim:} the six-monomial expansion above is \emph{this paper's} symmetrization choice for the degree-9 polynomial $P$, not Cai et al.'s; \cite{Cai:2009fn} expresses $P$ in single-time-ordering form prior to the in-in commutator doubling and does not encounter underdetermination at the level of their derivation. The three-constraint vs.\ six-coefficient mismatch arises specifically when we recompile the doubled polynomial into our symmetrized monomial basis to enable numerical evaluation across triangle configurations; in that internal basis the system is underdetermined and multiple coefficient sets reproduce all published benchmark values exactly. In our computational analysis we use $(c_1,\ldots,c_6) = (2, 7, 3, -12, -69, 19)$, which satisfies all three benchmarks.\footnote{The coefficients printed in Eq.~(37) of~\cite{Cai:2009fn}---$(3, 1, -9, 5, -66, 9)$---are expressed in that paper's own monomial normalization, which absorbs Wick-permutation factors differently from the symmetrized basis used here; they are not directly transplantable into our basis (direct evaluation of those coefficients, or their in-in-doubled values, in our basis does not satisfy the three benchmark constraints; artifact \texttt{c9i\_epsilon\_ratio\_check.json}). The linear map between the two bases is set by the ratio of Wick-permutation counting factors. For example, the $(7,2,0)$ partition has trivial little group (all three exponents distinct, one of them zero), so the full $S_3$ orbit of ordered monomials has size $|S_3|/|{\rm stab}(7,2,0)| = 6/1 = 6$. Cai et al.'s single-time-ordering integrand carries only one of the two complex-conjugate time orderings and the resulting monomial $k_1^7 k_2^2 + k_2^7 k_3^2 + k_3^7 k_1^2$ is the cyclic-subgroup ($C_3$) orbit of size 3. After in-in commutator doubling ($-2\,\mathrm{Im}$), the doubled integrand carries the full $S_3$ orbit. The per-orbit prefactor ratio for this row is therefore $|S_3|/|C_3| = 6/3 = 2$; all six orbit rows are rescaled by their respective permutation-count ratios (orbit-by-orbit, not by a single global factor) before the in-in doubling. The resulting transformation matrix is full-rank (rank 6, verified numerically via \texttt{c9i\_epsilon\_ratio\_check.json}) but the direct transplant fails because the per-orbit Wick-factor ratios are orbit-dependent, not a single global rescaling. Our coefficients are therefore fixed from the three published benchmark values themselves. All coefficient sets satisfying those three constraints produce identical $\BNL$ at the squeezed, equilateral, and folded configurations but differ at intermediate triangle shapes, contributing the null-space systematic uncertainty to the template overlap quantified in this section.}

\par To quantify the impact of this underdetermination, we constructed the $3\!\times\!6$ constraint matrix from the three benchmark configurations and computed its SVD, finding three nonzero singular values $\sigma_1 \geq \sigma_2 \geq \sigma_3 > 0$ with $\sigma_3/\sigma_1 \approx 0.3$ in our reference monomial normalization (each constraint row is the $\BNL$ prefactor times the six symmetric monomials evaluated at the unit-scale benchmark triangles $(k_1,k_2,k_3) = (1,1,1)$, $(2,1,1)$, and $(10^{-4},1,1)$, with no per-column rescaling). The three benchmark kinematics --- equilateral, folded, squeezed --- produce well-separated rows; $\sigma_3/\sigma_1 \approx 0.3$ is an empirical property of these benchmark rows in this normalization, far from any rank-deficiency tolerance, and carries no claim of a theoretical lower bound on the conditioning tied to the kinematic separation. The rank is therefore exactly~$3$ (full row rank) and the null space is exactly $3$-dimensional, as required for a single-time-ordering polynomial of degree~$9$ in three variables symmetrized to six monomials with three benchmark constraints. 

\par We then sampled $10{,}000$ valid coefficient sets uniformly within a ball of radius~50 in null-space coordinates centered on the reference solution (radius 50 is approximately $0.7\times$ the Euclidean norm of the full reference coefficient vector $(2,7,3,-12,-69,19)$ ($\|c_{\rm ref}\| \approx 73$); convergence is verified at radii 10, 100, and 500 below), evaluating the Fisher-weighted amplitude recovery factor $r$ and the bispectrum shape cosine $r_{\cos}$ at $23{,}098$ triangle configurations. The shape cosine is the normalized Euclidean inner product over the triangle grid between the bounce shape $S_{\rm bounce} = \BNL \times S_{\rm local}$ (here $S_{\rm local} \propto 1/k_1^3 k_2^3 + \text{2 perms}$ is the dimensionless local-template shape function and $S_{\rm templ}$ is the corresponding unit-normalized local template, $S_{\rm templ} \propto S_{\rm local}$) and $r_{\cos} = \sum S_{\rm bounce} S_{\rm templ} / \sqrt{\sum S_{\rm bounce}^2 \sum S_{\rm templ}^2}$, computed with uniform (unweighted) measure on the dimensionless triangle-ratio grid; the local-estimator Fisher weight $w \propto S_{\rm local}^2$ enters only the amplitude recovery $r$, not $r_{\cos}$ (implementation in \texttt{null\_space\_analysis.py}, Data and Code Availability). The radius and uniform sampling measure are conventional choices, and the uniform Euclidean measure in this monomial basis is not invariant under linear reparametrizations of the monomials or rotations within the null space --- an equally valid basis choice would yield a different $\pm 0.13$ scatter and different extremal $r$ values; the quoted scatter should therefore be read as indicative of the null-space spread under this stated convention rather than as a calibrated, basis-independent uncertainty. The basis-independent robust statement is the shape-cosine stability ($r_{\cos} > 0.95$ across scan radii 10--500, below).

\par The $23{,}098$ configurations result from a uniform grid in $(k_1, k_2, k_3)$ space with 50 logarithmic bins per side, subject to the triangle inequality ($k_1 \leq k_2 \leq k_3 \leq k_1 + k_2$) and the requirement $k_{\min} \leq k_i \leq k_{\max}$. To test convergence, we repeated the overlap calculation at 100 and 200 bins per side (yielding ${\sim}190{,}000$ and ${\sim}1{,}500{,}000$ triangles respectively --- the ${\sim}N^3$ growth expected for a three-dimensional $(k_1,k_2,k_3)$ grid under the ordering and triangle-inequality cuts, i.e.\ ${\sim}2^3 = 8\times$ for $50\!\to\!100$ and ${\sim}4^3 = 64\times$ for $50\!\to\!200$ bins per side, not the $N^2$ of a fixed-shape-ratio grid); the shape cosine $r_{\cos}$ changes by $< 0.1\%$ across all three resolutions, confirming convergence. Because both templates are scale-free (the bounce $\BNL$ is a degree-0 ratio of homogeneous polynomials and the local template is scale-invariant), the overlap depends only on the triangle shape ratios, not on the absolute $(k_{\min}, k_{\max})$ values or on cosmological parameters; cosmology enters only the $\ell$-space Fisher validation channel (fiducial CAMB $C_\ell$ with a Planck noise model; Sec.~\ref{sec:template}).

\par We note that the uniform logarithmic grid undersamples the squeezed limit ($k_3 \ll k_1 \approx k_2$), where the matter-bounce signal is strongest; a log-weighted grid with enhanced squeezed sampling gives $r = 0.88$ (vs.\ $r = 0.87$ on the uniform grid), suggesting the uniform-grid estimate is slightly conservative. 

\par The shape cosine exceeds 0.97 for all $10{,}000$ samples at radius 50 ($r_{\cos} = 0.985 \pm 0.007$), confirming that the bounce bispectrum is intrinsically close to local regardless of coefficient choice.

\par To verify that this stability is not an artifact of the chosen scan volume, we repeated the scan at radii 10, 100, and 500; all three give qualitatively identical results ($r_{\cos} > 0.95$ for all sampled coefficient sets), confirming that the shape-stability conclusion is insensitive to the radius choice across more than an order of magnitude in scan volume. The $r_{\cos} > 0.97$ bound applies specifically to the radius-50 scan used for the headline distribution; the $r_{\cos} > 0.95$ bound is the conservative floor confirmed across the full multi-radius convergence test (radii 10--500), and the two should not be directly compared as they correspond to different scan volumes. 

\par The amplitude recovery factor is $r = 0.85 \pm 0.13$ (range: 0.55--1.14); the five-coefficient-set scan value ($r = 0.867$--$0.888$, CMB-Fisher weighting) lies within the interquartile range $[0.75, 0.94]$ of this null-space distribution, offset from the median $0.85$ by ${\sim}0.03$ --- consistent given the different weighting and scan measures, though not identically centered. The unweighted distribution has an asymmetric tail: at the 16th percentile $r \approx 0.70$, giving a conservative lower endpoint $2.1875 \times 0.70 / 0.7 \approx 2.2\sigma$ (pre-systematic, at the corrected central $-35/16$), while the 84th percentile $r \approx 0.99$ gives $\approx 3.1\sigma$ (the $[0.75,\,0.94]$ interquartile endpoints correspond to $2.35\sigma$ and $2.95\sigma$, consistent with the per-sample $16$th--$84$th band below); these tails are not propagated into the headline significance (which uses the noise-weighted central value $r = 0.84 \pm 0.02$ of Eq.~\ref{eq:r_noise}), but they set the conservative floor under extreme null-space coefficient variation.\footnote{The $r = 0.85 \pm 0.13$ distribution is computed under uniform Euclidean measure in the monomial coefficient null space at radius 50. The headline noise-weighted $r = 0.84 \pm 0.02$ (Eq.~\ref{eq:r_noise}) uses CMB/LSS-motivated noise weighting and is not a percentile of this unweighted distribution; the two quantities probe different aspects of the template overlap (null-space coefficient freedom vs.\ noise-weighting scheme variation) and their central values are consistent to within $\pm 0.01$.} The scatter is dominated by extreme null-space directions that produce large shape deformations at intermediate triangles while preserving all three benchmark values; the median $r = 0.85$ and the interquartile range $[0.75, 0.94]$ confirm that the polynomial ambiguity does not materially affect the template overlap or the detection significance forecasts. Propagating the full scatter onto the detection significance, the 16th--84th percentile range of $|\fnl|\,r/\sigma(\fnl)$ across the $10{,}000$ coefficient samples is $2.2$--$3.1\sigma$ (median $2.65\sigma$; per-sample propagation artifact released with the paper's code --- see Data and Code Availability). These percentiles are computed at the pre-systematic baseline ($\sigma(\fnl) = 0.7$) and should be compared against the pre-systematic $2.6$--$2.75\sigma$ noise-weighting endpoints at the central $r$, not against the post-budget $1.5\sigma$ floor of \S\ref{sec:spherex}: the 16th-percentile $2.2\sigma$ draw sits ${\sim}0.4\sigma$ below the pre-systematic $2.6$--$2.75\sigma$ band but remains above the post-systematic floor. Pushed through the conservative GR-marginalization budget ($\sigma_{\rm GR} = 1.0$ in quadrature with $\sigma(\fnl)=0.7$), the same 16th-percentile null-space draw maps to $2.2\sigma \times (0.7 / \sqrt{0.7^2 + 1.0^2}) \approx 1.25\sigma$, which is near the ${\sim}1.3$--$1.4\sigma$ all-combined endpoint of \S\ref{sec:spherex}; the headline $2.6$--$2.75\sigma$ quotes the two noise-weighting endpoints at the central $r$, not percentiles of this distribution. 

\par An injection/recovery test using 200 Monte Carlo realizations confirms that a local-template estimator applied to a bounce-shaped signal recovers $r_{\rm measured} = 0.90 \pm 0.01$---consistent with the CMB Fisher (signal-only) overlap ($r = 0.876$) but above the noise-weighted central value ($r = 0.84$; Sec.~\ref{sec:template}), because the injection-recovery test uses isotropic Gaussian noise (effectively CMB-like weighting) and a fixed reference coefficient set rather than sampling over the full null space. Each realization draws a Gaussian realization of the bounce-shaped bispectrum signal scaled to $\fnl = -35/16$, adds isotropic Gaussian noise with the published SPHEREx photometric-$z$ power spectra~\cite{Heinrich:2023} as the diagonal noise covariance, and applies a KSW-type optimal linear estimator~\cite{Komatsu:2005} against the local template on tiled flat-sky patches covering the full sky. 

\par No galactic mask is applied in these realizations; the test is therefore slightly optimistic in the sense that realistic partial-sky operation (Galactic mask $f_{\rm sky} \approx 0.7$) would increase the noise variance by $1/f_{\rm sky}$, i.e., the noise standard deviation increases by a factor $1/\sqrt{0.7} \approx 1.19$, a ${\sim}\,19\%$ degradation in $\sigma(\fnl)$. This $1/\sqrt{f_{\rm sky}}$ statement is a CMB-estimator heuristic appropriate only to this two-dimensional flat-sky test; it does not transfer to a three-dimensional galaxy-bispectrum analysis with photometric redshifts, where masking enters through the survey window and mode coupling, and this scaling is not used in any quantitative forecast. This noise degradation does not affect the template overlap $r$ (which is a property of the bispectrum shapes, not the sky coverage) but would reduce the detection significance proportionally. The injection-recovery approach here is a Fisher-space test of amplitude recovery using a two-dimensional flat-sky CMB-style estimator --- not the three-dimensional galaxy-bispectrum estimator a SPHEREx analysis would employ --- and not a full simulation pipeline; a complete validation with realistic SPHEREx mocks, sky masking, and photometric-$z$ scatter would be required before claiming a data-analysis result.

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{fig1_shape_function.png}
\caption{Matter-bounce bispectrum shape function $\BNL(k_1, k, k)$ (dimensionless, Eq.~(2)) as a function of the dimensionless squeeze ratio $k_1/k$, showing convergence to the corrected squeezed-limit value $-35/16$ (Appendix~\ref{app:convention}); the plotted curve is the Cai \etal\ shape normalized to its corrected squeezed amplitude (exactly one-half the printed $-35/8$). Red circle: squeezed benchmark. Orange square: equilateral. Green triangle: folded.}
\label{fig:shape}
\end{figure}

\begin{table}[h]
\centering
\begin{tabular}{lcc}
\toprule
Configuration & $\BNL$ (this work) & $\BNL$ (corrected) \\
\midrule
Squeezed ($k_1 \to 0$) & $-2.1875$ & $-35/16$ \\
Equilateral ($k_1 = k_2 = k_3$) & $-1.992$ & $-255/128$ \\
Folded ($k_1{=}2k_2{=}2k_3$)$^{\rm a}$ & $-1.125$ & $-9/8$ \\
\bottomrule
\end{tabular}
\caption{Confirmation of the matter-bounce shape function at three benchmark momentum configurations, quoted at the \emph{corrected} amplitude $\fnl^{\rm local}=-35/16$ (Appendix~\ref{app:convention}): the values are exactly one-half the amplitudes published in Cai \etal~\cite{Cai:2009fn} ($-35/8$, $-255/64$, $-9/4$), whose transcribed printed polynomial carries a $-(99/128)\sum_i k_i^3$ discrepancy relative to the exact vertex sum (Appendix~\ref{app:convention}), and agree with the general-$c_s$ result of Li \etal~\cite{LiQuintin:2017} at $c_s=1$. $^{\rm a}$The folded row sits on the degenerate boundary $k_1 = k_2 + k_3$ and is evaluated as the limit of the sequence $k_1 = 2k$, $k_2 = k_3 = k$.}
\label{tab:benchmarks}
\end{table}

\subsection{UV-Completion Independence (Conditional on Faithful Cubic-Order Transfer)}

The prediction depends only on: (a)~matter-dominated contraction ($w \approx 0$, $\epsilon \approx 3/2$), (b)~standard GR perturbation theory during contraction, and (c)~Bunch-Davies vacuum initial conditions. The prediction is therefore \emph{UV-completion-independent within the Wilson-Ewing class}: any UV completion that produces a nonsingular matter-dominated contraction satisfying (a)--(c) and faithfully transfers the cubic-order bispectrum across the bounce reproduces the same $-35/16$ value. The qualifier ``UV-completion-independent'' is used rather than the broader ``mechanism-independent'' phrasing: faithful third-order (cubic) transmission of the bispectrum through the bounce is itself an assumption (assumption (d), Sec.~\ref{sec:assumptions}) verified only at linear order~\cite{WilsonEwing:2012}, and the specific bounce-transition dynamics govern whether (d) holds. The bounce enters only through (i)~providing a nonsingular transition and (ii)~transferring the contraction-phase cubic correlator into the expanding phase; failures of (ii) at third order would re-introduce mechanism dependence at the order being tested.

\subsection{Assumptions}\label{sec:assumptions}

The $\fnl = -35/16$ prediction rests on six assumptions: (a)~exact matter domination during contraction ($w = 0$, $\epsilon = 3/2$); (b)~standard GR perturbation theory during contraction (no higher-order corrections from the bounce UV completion); (c)~Bunch-Davies vacuum initial conditions; (d)~faithful transmission of the bispectrum through the bounce at third order in perturbation theory; (e)~the CMB-observable modes originate from the contracting phase, not from a prolonged post-bounce inflationary epoch; and (f)~negligible fermion energy density during the contracting phase and bounce transition (so the Hehl-Datta--Mercuri four-fermion contact term $\langle\bar\psi\gamma^5\gamma^a\psi\rangle^2$ does not activate torsion or reactivate the Barbero-Immirzi parameter in the scalar cubic action; the prediction is therefore exact within the scalar-only Einstein-Cartan-Holst class of models, and conditional on fermion contributions during contraction being suppressed). Assumption~(f) is the closure of the ECH-decoupling caveat noted by Hehl-Datta-Mercuri: a scalar-only model satisfies it trivially, while bounce models with significant fermion sectors during contraction would require an explicit bound on $\langle\bar\psi\gamma^5\gamma^a\psi\rangle^2$ before $\fnl = -35/16$ can be quoted in that broader class. A rigorous order-of-magnitude bound on the threshold $\rho_{\rm fermion}/\rho_{\rm scalar}$ requires evaluating the four-fermion contact term $\langle\bar\psi\gamma^5\gamma^a\psi\rangle^2$ inside the contracted Einstein-Cartan-Holst cubic action and is not undertaken here. We therefore treat assumption (f) as an externally imposed constraint: the prediction $\fnl = -35/16$ applies to the scalar-only sector, and application to models with substantial fermion populations during contraction would require a per-model bound on this operator before the same numerical value can be quoted. Assumption (e) is satisfied in the Wilson-Ewing model (Sec.~\ref{sec:benchmark}), where the bounce connects directly to radiation domination with at most a brief inflationary transient ($N \ll 55$). Models that invoke prolonged post-bounce inflation ($N_{\rm tot} \gg 60$, as required by certain dark-energy mechanisms in modified-gravity bounce cosmologies; e.g., Zhu~\&~Cai~\cite{Zhu:2026echoes}) would push the bounce-imprinted modes far beyond the observable horizon, erasing the $\fnl$ signal and replacing it with the standard slow-roll value $\fnl \approx 0.015$. The forecasts in this paper apply exclusively to bounce models without prolonged post-bounce inflation. The viable Wilson-Ewing model uses $w = -0.003$, not exactly zero. The correction from exact matter domination depends on how the bispectrum integral scales with $\epsilon$ near $\epsilon = 3/2$. The matter-contraction Hankel index $\nu$ is finite at $\epsilon = 3/2$ ($\nu = 3/2 + \mathcal{O}(\epsilon - 3/2)$, Wilson-Ewing dust contraction); the sensitivity of cubic-order quantities to $\epsilon$ near this point is driven not by a divergent Hankel index but by the explicit $A_T \propto 1/\epsilon^3$ prefactor channel and by the $|\eta|^{-\nu}$ mode-function amplitude growth. Explicit cubic-action prefactors give a correction of ${\sim}\,0.6\%$, but the mode-function growth rate also changes with $\epsilon$, potentially amplifying the correction to ${\sim}\,1$--$8\%$. The $\epsilon$-correction enters through two channels: it modifies both the numerical value of $\fnl$ (multiplicative correction to $-35/16$) and the bispectrum shape (departure from the exact local template). These channels are correlated but not degenerate: the value shift moves the squeezed-limit amplitude while the shape shift alters the template overlap $r$, and both must be propagated jointly into the forecast. At the Planck best-fit spectral tilt, $\fnl \in [-2.175,\;-2.01]$ (the $0.6$--$8\%$ $\epsilon$-correction applied to the corrected central $-35/16$). Both bounds are well within $\sigma(\fnl) \approx 0.7$. Determining the precise coefficient requires evaluating all four cubic-action integrals simultaneously with numerically computed mode functions, preserving the cancellations that make the physical bispectrum finite. Assumption (d) has been verified at linear order~\cite{WilsonEwing:2012}. At cubic order, a semi-analytic order-of-magnitude estimate based on the superhorizon approximation for mode functions near the LQC bounce suggests that the bounce contribution to $\fnl$ is suppressed by $(k\,\eta_{\rm bounce})^2 \sim 10^{-4}$ for modes of observational interest, giving a correction $\delta\fnl \sim 10^{-3}$ (negligible if the superhorizon scaling holds; this is a scaling estimate, not a derived bound). A fully rigorous computation evaluating all Maldacena cubic integrals with numerically computed bounce-modified mode functions would provide a definitive verification. \emph{Physical support for assumption~(d).} The observationally relevant modes are deep superhorizon at the bounce ($k\,\eta_{\rm bounce}\sim 10^{-2}$, so gradient corrections are $O((k\,\eta_{\rm bounce})^2)\sim 10^{-4}$). For the adiabatic single-clock curvature perturbation, $\zeta$ is conserved on superhorizon scales \emph{to all orders in perturbation theory} (Lyth-Malik-Sasaki; Wands-Malik-Lyth-Liddle; Maldacena/$\delta N$ separate-universe), the single load-bearing hypothesis being $\dot\zeta\to 0$ (no active scalar growing mode). If the \emph{linear} superhorizon $\zeta$ is conserved through the bounce (verified at linear order in Wilson-Ewing~\cite{WilsonEwing:2012}), the same separate-universe argument extends to every equal-time correlator, so the full bispectrum is conserved with it up to the same $O((k\,\eta_{\rm bounce})^2)\sim 10^{-4}$ gradient correction --- upgrading the $10^{-3}$ estimate from a scaling guess to the leading gradient correction of a conservation theorem. This conservation is \emph{not} automatic for a generic bounce: Quintin, Sherkatghanad, Cai \& Brandenberger~\cite{Quintin:2015} show that a single NEC-violating scalar in GR at $c_s=1$ that suppresses $r$ \emph{by growing $\zeta$ during the bounce} instead \emph{enhances} $\fnl$ (their Eq.~44, Conjecture~1), so suppressing $r$ and keeping $\fnl$ small cannot be done simultaneously in that class. The Wilson-Ewing $\Lambda$CDM/LQC bounce, however, \emph{escapes this no-go by construction}: its $r_t\approx 10^{-4}$ is produced by LQC quantum-geometry tensor suppression and the small CDM sound speed $c_s\ll 1$ --- both flagged explicitly as escape routes by Quintin \etal~\cite{Quintin:2015} (their Conclusions: a noncanonical $c_s\ll 1$ vacuum removes the need to enhance $\zeta$, and their analysis ``does not immediately apply'' to LQC bounces where the NEC violation comes from the gravitational sector) --- so $\zeta$ need not be amplified and $\dot\zeta\to 0$ is the natural adiabatic behavior, consistent with the linear conservation verified in Wilson-Ewing. Assumption~(d) is therefore supported not merely by a dimensional scaling estimate but by nonlinear superhorizon $\zeta$-conservation within the LQC/low-$c_s$ escape sub-class. \emph{Cubic-order closure via single-clock structure.} The one hypothesis of the all-orders conservation theorem is adiabaticity ($\dot\zeta\to0$, no transient entropy mode). For the Wilson-Ewing $\Lambda$CDM/LQC bounce this is guaranteed by degree-of-freedom counting: effective LQC modifies the background through holonomy (and inverse-triad) corrections but adds \emph{no new scalar degree of freedom} --- in the dressed-metric/hybrid quantization used by Wilson-Ewing~\cite{WilsonEwing:2012}, and even in the deformed-algebra scheme~\cite{Cailleteau:2011kr}, the physical scalar sector remains single-clock (one matter clock, perturbations propagating on a quantum-dressed effective metric). A single-clock system has \emph{no isocurvature/entropy mode by field content}, so the only source of $\dot\zeta$ on superhorizon scales is non-adiabatic \emph{gradient} pressure, which is $O((k\eta_{\rm bounce})^2)$ by the gradient expansion at every perturbative order --- cubic included. Hence the transmitted bispectrum equals the contraction-phase bispectrum up to a bounded $O((k\eta_{\rm bounce})^2)\sim10^{-4}$ correction ($\delta\fnl\lesssim10^{-3}$, negligible against $\sigma(\fnl)\approx0.7$): transmission $=1\pm O((k\eta_{\rm bounce})^2)$ follows from single-clock nonlinear adiabaticity \emph{conditional on the dressed-metric quantization} (where $c_s^2=1$ and the gradient expansion is controlled), and requires no numerical cubic bounce evolution. The only quantization-dependent input is the sign of the subleading gradient coefficient (Lorentzian $c_s^2=1$ in the dressed-metric scheme adopted here) --- a discrete, citable model choice, not an open computation. In the alternative deformed-algebra scheme the effective sound speed passes through a transient $c_s^2=1-2\rho/\rho_c<0$ signature-change window precisely where the gradient expansion is least controlled; there the $(k\eta_{\rm bounce})^2$-suppression of the effect on deep-superhorizon modes is expected but not established, so the closure below is stated as \emph{conditional on the dressed-metric quantization}. Assumption~(d) is therefore \emph{closed to a bounded $\lesssim10^{-3}$ systematic in $\delta\fnl$ conditional on the dressed-metric LQC quantization} adopted here; in the deformed-algebra scheme the same bound is plausible but not derived through the signature-change window. Assumption~(d) remains the weakest link of the present derivation in the sense that it carries the largest model-dependence: it is verified at linear order~\cite{WilsonEwing:2012} and closed at cubic order to a bounded $\lesssim10^{-3}$ systematic via single-clock nonlinear adiabaticity (the sole quantization-dependent input, the subleading gradient coefficient, being a citable model choice rather than an open computation), so every ``robust across the bounce class'' statement in this paper is conditional on the single-clock structure of the Wilson-Ewing/LQC quantization; a direct numerical evaluation of the $O((k\eta_{\rm bounce})^2)$ coefficient in the dressed-metric scheme would sharpen the bound but is not required to establish it. The historical factor-of-two discrepancy in the literature --- Li \etal~\cite{LiQuintin:2017} obtain $\fnl = -35/16 = -2.1875$ when evaluated at $c_s = 1$ (their Eq.~5.1, $\fnl^{\rm local}=-165/16+65/(8c_s^2)$), against Cai \etal's~\cite{Cai:2009fn} published $\fnl = -35/8 = -4.375$ (their Eqs.~38--39) --- \emph{is resolved here in favour of $-35/16$}, which is the correct value we adopt as central. We re-summed Cai \etal's own four cubic-action vertex contributions (field redefinition, $\zeta\dot\zeta^2$, $\dot\zeta\,\partial\zeta\,\partial\chi$, $\zeta(\partial_i\partial_j\chi)^2$) at $\epsilon=3/2$ and took the squeezed limit $k_1\ll k_2=k_3$; the exact sum gives a clean $-35/16$ (with an $O(k_1^2/k^2)$ correction and no cancellation-sensitive ambiguity), and equals both Cai's own $\epsilon$-order-grouped intermediates and Li \etal's independent general-$c_s$ formula at $c_s=1$. Both papers use the \emph{identical} local-template normalization $\fnl = 10\,A/(3\sum k_i^3)$ (Cai Eq.~21 $=$ Li Eq.~4.20) with $\Phi = \tfrac{3}{5}\zeta$ (Cai Eq.~20), and the \emph{same} squeezed limit, so this is a discrepancy in the printed polynomial collapse, not a convention difference. One identified discrepancy between Cai \etal's transcribed final polynomial (their Eq.~37) and the exact sum of their own vertices is a $-(99/128)\sum_i k_i^3$ local-shaped term, absent from every intermediate expression in the same paper; it drives the transcribed printed polynomial's squeezed reduction from the correct $-35/16$ to $-305/64$ (Cai \etal's separately-published $-35/8$ is not reproduced by the transcribed printed coefficients and is retained only as an erroneous literature value; Appendix~\ref{app:convention}). We therefore \emph{adopt} the corrected value $\fnl = -35/16$ as the central input --- benchmark-configuration matching at three independent momentum triangles (Table~\ref{tab:benchmarks}, at the corrected amplitudes) and null-space stability ($r_{\cos} = 0.985 \pm 0.007$ across $10{,}000$ coefficient samples) confirm we reproduce the Cai/Li shape function --- while noting that the erroneous $-35/8$ (which would double every significance) is retained only as a literature-reference upper bookkeeping value in Appendix~\ref{app:convention}, not as a forecast input.

\subsection{The Viable Model}

The Wilson-Ewing $\Lambda$CDM quasi-dust model~\cite{WilsonEwing:2012} provides a complete observational package: $n_s = 0.964$ (from $w = -0.003$, one free parameter tuned to the Planck observed $n_s = 0.9649 \pm 0.0042$; the spectral index formula $n_s = 1 + 12w$ follows from the growing-mode solution in quasi-dust contraction~\cite{WilsonEwing:2012}, so $n_s$ is a fit to the data rather than a prediction), a tensor-to-scalar ratio $r_t \approx 10^{-4}$ (from LQC quantum-geometry tensor suppression; for the several $r$-like symbols used throughout see the notation conventions fixed at the head of Sec.~\ref{sec:intro}), and $\fnl = -35/16$ (tightly determined in the cubic sector, with residual $1$--$8\%$ $\epsilon$-correction uncertainty plus an additional $\sim 13\%$ amplitude scatter from the underdetermined polynomial coefficients $c_1$--$c_6$ ($r = 0.85 \pm 0.13$ across the $10{,}000$-sample null-space scan, Sec.~\ref{sec:benchmark}); conditional on assumptions (a)--(f) in Sec.~\ref{sec:assumptions}). We are not aware of observational tensions with this model within current uncertainties.

% ============================================================
\section{Observable Mapping to Large-Scale Structure}
\label{sec:lss}

\subsection{Scale-Dependent Bias}

Primordial local non-Gaussianity induces a scale-dependent correction to galaxy bias~\cite{Dalal:2007cu,Slosar:2008}:
\begin{equation}
\Delta b(k,z) = \frac{2\,\fnl\,(b_1 - 1)\,\delta_c}{\mathcal{M}(k,z)}\,,
\label{eq:sdb}
\end{equation}
with the Poisson--Newtonian transfer kernel
\begin{equation}
\mathcal{M}(k,z) = \frac{2\,k^2\,T(k)\,D(z)}{3\,\Omega_m\,H_0^2}\,,
\label{eq:Mkz}
\end{equation}
where $T(k)$ is the matter transfer function (normalized to $T(k)\to 1$ as $k\to 0$), $D(z)$ is the linear growth factor (normalized to $D(0)=1$), wavenumbers $k$ are comoving and quoted in $h\,\mathrm{Mpc}^{-1}$ throughout (we work in units $c=1$ and likewise express $H_0$ in $h\,\mathrm{Mpc}^{-1}$, so that $\mathcal{M}$ is dimensionless and $\Delta b$ carries no residual units), $\delta_c \approx 1.686$ is the spherical-collapse threshold, and $b_1$ is the linear Eulerian galaxy bias. Since $\Delta b \propto 1/\mathcal{M}$ and $\mathcal{M} \propto k^2$ on ultra-large scales (where $T(k)\to 1$), the signal grows as $\Delta b \propto 1/k^2$ as $k\to 0$~\cite{Dalal:2007cu,Slosar:2008}; this $1/k^2$ enhancement on the largest scales is what makes scale-dependent bias the most sensitive single channel for local-type $\fnl$. All downstream Fisher weightings, plots, and forecasts that invoke the SDB kernel in this paper use Eqs.~(\ref{eq:sdb})--(\ref{eq:Mkz}) as the canonical definition.

\subsection{Template Projection and Amplitude Recovery}
\label{sec:template}

The matter-bounce bispectrum is \emph{not} purely local: $\BNL$ varies from $-2.1875$ in the squeezed limit to $-1.125$ in the folded limit (a 49\% fractional variation, $|\Delta\BNL|/|\BNL^{\rm squeeze}|$; the fractional variation and template overlap $r$ are shape ratios, unchanged by the corrected overall amplitude), while the local template has constant $\BNL = \fnl$ for all configurations. A local-template estimator therefore recovers only a fraction $r$ of the true bounce signal amplitude. \emph{Notation for the overlap factor (fixed throughout).} We reserve the bare symbol $r$ for the Fisher/noise-weighted amplitude recovery factor (headline value $r=0.84\pm0.02$, Eq.~\ref{eq:r_noise}); the flat-weight (unweighted) shape overlap is always written $r_{\cos}$; the survey-optimal in-house-Fisher recovery used only as a validation cross-check is always written $r_{\rm eff}\approx0.99$; and the basis-measure stress-band distribution is the labelled $r=0.85\pm0.13$. These are distinct quantities and only the noise-weighted $r=0.84$ propagates into any significance:
\begin{equation}
\fnl^{\rm measured} = r \times \fnl^{\rm bounce}\,, \qquad
\sigma(\fnl^{\rm bounce}) = \sigma(\fnl^{\rm local})/r\,,
\label{eq:projection}
\end{equation}
\emph{Only the noise-weighted central value $r = 0.84 \pm 0.02$ (Eq.~\ref{eq:r_noise}) enters $\sigma_{\rm eff}$ and every headline significance in this paper; the broader null-space distribution $r = 0.85 \pm 0.13$ (range $0.55$--$1.14$; Sec.~\ref{sec:benchmark}) is a basis-measure stress band that is never propagated into $\sigma_{\rm eff} = \sigma(\fnl^{\rm local})/r$, so no $r > 1$ sample can shrink the effective uncertainty below the local-template baseline.} Here the amplitude recovery factor $r = \langle \BNL^{\rm bounce} \rangle_w / \BNL^{\rm squeeze}$ is the Fisher-weighted average of the bounce shape function normalized to the squeezed-limit value $\BNL^{\rm squeeze} = -35/16$ (the corrected central amplitude; $r$ is a shape ratio and is identical whether normalized to $-35/16$ or the erroneous $-35/8$); since both numerator and denominator are negative, $r$ is positive definite and is bounded above near unity for physical bispectrum shapes dominated by the squeezed limit. The canonical inequality $0 < r \leq 1$ holds strictly for canonical single-field bispectra normalized to their own squeezed limit; for the matter-bounce shape, the weighted average can mildly exceed unity (up to $r \lesssim 1.14$ in our $10{,}000$-sample null-space scan; see Sec.~\ref{sec:benchmark}) for null-space coefficient sets that produce slightly enhanced $|\BNL|$ at intermediate triangle configurations relative to the strict squeezed-limit value $-35/16$, while leaving the three benchmark configurations exact.\footnote{The constraint $r \leq 1$ assumes the bispectrum amplitude is monotonically maximized in the squeezed limit, which holds for canonical single-field local non-Gaussianity. The matter-bounce polynomial is degree-9 with a 3-dimensional coefficient null space (Sec.~\ref{sec:benchmark}); some null-space directions enhance $|\BNL|$ at folded or intermediate triangles relative to the squeezed-limit benchmark $\BNL^{\rm squeeze}=-35/16$, producing $r > 1$ under Fisher weighting that upweights those configurations. Such samples remain physical (they reproduce all three corrected benchmarks of Table~\ref{tab:benchmarks} --- one-half the Cai \textit{et~al.}~\cite{Cai:2009fn} printed values --- exactly and have shape cosine $r_{\cos}>0.97$ relative to the local template); the apparent excess of $r$ above unity reflects only that the squeezed-limit value is not the global maximum of $|\BNL|$ for these coefficient choices, not a violation of any physical condition. We retain the full null-space distribution $r = 0.85 \pm 0.13$ (range $0.55$--$1.14$) without truncation; restricting to $r \leq 1$ would amount to imposing an artificial single-field-like monotonicity that the matter-bounce shape does not satisfy. The headline noise-weighted central value $r = 0.84 \pm 0.02$ of Eq.~\ref{eq:r_noise} is well below unity and is unaffected by this reconciliation.} Equation~(\ref{eq:projection}) is exact in the Fisher limit where the noise covariance is diagonal in bispectrum space and the estimator is optimal for the local template. In practice, orthogonal-shape noise (equilateral, folded, and other bispectrum configurations not captured by the local template) can contribute additional variance when the true signal is not purely local; this ``projection noise'' is suppressed by the shape cosine $r_{\cos}$ (computed in the unweighted shape metric of Sec.~\ref{sec:benchmark}, not in the survey Fisher metric of the estimator: the suppression is a heuristic shape-similarity indicator, not an estimator-mismatch variance bound under SPHEREx weighting): using the mean $r_{\cos} \approx 0.985$ from the $10{,}000$-sample scan, $1 - r_{\cos}^2 \approx 0.03$; using only the conservative lower bound $r_{\cos} > 0.97$, $1 - r_{\cos}^2 < 0.06$. Either bound confirms the projection noise is subdominant to the other systematics in our budget.

\par \emph{Standard-basis template decomposition (non-local tails).} The shape
cosine $r_{\cos}$ above compares the bounce bispectrum only to the local template;
a natural question is whether the residual $1-r_{\cos}^2$ carries recoverable
equilateral or orthogonal signal that a local-only estimator misses. We therefore
project the physical bounce bispectrum $B_{\rm bounce}(k_1,k_2,k_3)=\BNL(k)\,
S_{\rm local}(k)$ onto the standard separable template basis --- local,
equilateral, and orthogonal~\cite{Senatore:2010} --- on the identical
$23{,}098$-triangle grid. Under uniform, CMB-Fisher ($w\propto k^2$), and LSS
($w\propto k$) weighting the bounce--local cosine is $|r_{\cos}|=0.985$--$0.986$ in
all cases (reproducing the headline $0.985$), while the joint projection onto
$\mathrm{span}\{{\rm local},{\rm equil},{\rm ortho}\}$ raises the recovered
Fisher-norm fraction by at most $0.004$ above the local-only value, i.e.\ a joint
multi-template estimator would recover only $\delta r\lesssim 0.002$ more amplitude
than the local-only estimator ($<0.3\%$ of the headline $r=0.84$, well within the
$\pm0.02$ uncertainty). The apparently large bounce--orthogonal cosine
($0.75$--$0.94$) is not independent non-local content: the orthogonal template is
itself defined as $-3\,S_{\rm local}+\dots$ and is strongly collinear with the
local template, so the joint fit --- which removes this collinearity --- confirms
the genuinely non-local (equilateral $+$ orthogonal-relative-to-local) content of
the residual is negligible. Two scope points apply: (i) it is the
\emph{physical} bispectrum $B_{\rm bounce}=\BNL\,S_{\rm local}$ (carrying the local
$1/k^3$ envelope, and thus $\sim97\%$ local) that is projected, not the bare
amplitude ratio $\BNL$; and (ii) this is a geometry-only shape-overlap bound
(matching the $r_{\cos}$ metric) --- the full three-dimensional
estimator-mismatch variance under the true SPHEREx multi-tracer bispectrum Fisher
covariance is not computed here, as it requires the Heinrich \etal~\cite{Heinrich:2023}
noise covariance rather than the shape metric. Within that scope, the non-local
tails at intermediate and folded configurations are geometrically bounded and do
not bias the local-template recast (reproduction:
\texttt{c11\_nonlocal\_template\_projection.py},
\texttt{c11\_nonlocal\_template\_projection.json}; Data and Code Availability).

Using the physics-derived polynomial, we computed $r$ under 10 physically motivated weighting schemes (uniform, $w = 1$; CMB Fisher, $w \propto k^2$; LSS scale-dependent-bias, $w \propto 1/k^2$; SPHEREx-like and MegaMapper-like survey-noise variants; and five region-masked variants, where $w$ is the Fisher weight entering the weighted average $r = \langle \BNL \rangle_w / \BNL^{\rm squeeze}$ defined below Eq.~\ref{eq:projection}), scanning over squeezed cutoffs ($x_{3,\rm min}$ from 0.001 to 0.2, where $x_3 \equiv k_3/k_1$ is the squeezed-limit ratio with $x_3 \to 0$ corresponding to the squeezed limit $k_3 \ll k_1 \approx k_2$). \emph{Index-labeling note:} the overlap scan parametrizes triangles with $k_1$ held fixed as a hard reference scale and $k_3$ taken as the long (squeezed) mode (so $x_3 = k_3/k_1 \to 0$ is the squeezed limit, with $k_3 \le k_2 \le k_1$ in the scan grid); this interchanges the index roles relative to the benchmark convention of Sec.~\ref{sec:benchmark}, where $k_1$ denotes the long mode and $k_2 \approx k_3$ the hard modes. The relabeling is purely a parametrization choice --- $\BNL$ is permutation-symmetric in $(k_1,k_2,k_3)$, so the squeezed-cutoff result below is unchanged by which index carries the long mode. The result is robust:
\begin{equation}
r = 0.84 \pm 0.02\,,
\label{eq:r_noise}
\end{equation}
with the range $r \in [0.829,\;0.876]$ spanning all physically motivated weighting schemes (the three noise-weighted values $0.829$ [scale-dependent-bias, $1/k^2$], $0.830$ [SPHEREx-like], $0.835$ [flat/uniform], together with the signal-only CMB-Fisher value $0.876$ --- four values total; the per-realization spread from the $\ell$-space Fisher-overlap artifact (Data and Code Availability) is wider, $[0.856, 0.895]$, but is dominated by Monte-Carlo noise of the $\ell$-space Fisher integrand rather than weighting-scheme variation). The $\pm 0.02$ of Eq.~(\ref{eq:r_noise}) characterizes the spread about the central $0.84$ dominated by the noise-weighted schemes; the signal-only CMB-Fisher endpoint $0.876$ lies just outside that band and is therefore always quoted separately as the optimistic endpoint rather than absorbed into the noise-weighted uncertainty. The signal-only (CMB Fisher, $k^2$-weighted) overlap gives $r = 0.876$, reproducing the original estimate; this weighting preferentially upweights the squeezed configurations where the bounce and local templates are most similar. Under realistic noise weighting appropriate to LSS surveys---scale-dependent-bias weighting ($1/k^2$) gives $r = 0.829$, SPHEREx-like weighting gives $r = 0.830$, and flat (uniform) weighting gives $r = 0.835$---the overlap drops to $r \approx 0.83$, because relative to the signal-only CMB-Fisher weighting --- which preferentially upweights the squeezed configurations where the bounce and local templates coincide --- the LSS noise-weighting shifts relative weight onto the intermediate and folded configurations where their integrated mismatch is largest, so the overlap drops below the signal-only CMB-Fisher endpoint; this $r_{\rm LSS} < r_{\rm CMB}$ ordering emerges from the full Fisher-weighted shape integration (\texttt{null\_space\_analysis.py}, Data and Code Availability). The corresponding $\sigma(\fnl)$ degradation factors are $1.14\times$ (CMB Fisher), $1.20\times$ (SPHEREx-like), and $1.21\times$ (LSS/SDB). The squeezed-limit cutoff is completely insensitive: varying $x_{3,\rm min}$ from 0.001 to 0.200 changes $r$ by $< 0.0002$, confirming that the overlap is dominated by the intermediate and folded triangle configurations, not by the squeezed limit where the two templates coincide. (This cutoff test is not in tension with the ${\sim}0.01$ shift under the log-weighted squeezed-enhanced grid of Sec.~\ref{sec:benchmark}: the cutoff test removes the most extreme squeezed triangles at fixed sampling measure --- configurations where the two templates agree and which therefore carry almost no mismatch weight --- whereas the grid-reweighting test changes the sampling density across \emph{all} configurations, including the intermediate and folded regions that dominate the mismatch; the two procedures probe different sensitivities and their different magnitudes are expected.) Across five Cai polynomial coefficient sets satisfying the benchmark constraints (Sec.~\ref{sec:benchmark}), the coefficient uncertainty contributes $r \in [0.867,\;0.888]$ at CMB Fisher weighting---a spread of $\pm 0.010$, subdominant to the noise-weighting variation. The mismatch is intrinsic to the shape---it is dominated by the folded triangle configuration ($\BNL = -1.125$ vs.\ $-2.1875$ at the squeezed limit, at the corrected central amplitude)---and cannot be removed by survey design or estimator optimization if one uses a local-template estimator; a shape-matched estimator optimized for the bounce bispectrum could recover $r \to 1$ at the cost of losing the local-template interpretability that connects to standard $\fnl$ constraints. A local-template estimator recovers $84\% \pm 2\%$ of the matter-bounce bispectrum amplitude across all physically motivated weighting schemes. We cross-checked the overlap at three independent levels; the $\ell$-space Fisher result has limited commensurability with the 3D LSS bispectrum and should be read as a consistency check rather than an independent validation: (i)~$\ell$-space Fisher overlap using fiducial $C_\ell$ from CAMB with a Planck noise model ($r = 0.878 \pm 0.012$, stable across $\ell_{\rm ref} = 50$--$950$); (ii)~Monte Carlo injection recovery with 200~realizations using a KSW-type estimator, SPHEREx Gaussian noise covariance, and full-sky geometry ($r_{\rm meas} = 0.90 \pm 0.01$; see Sec.~\ref{sec:benchmark} for details); (iii)~a literature search confirming no prior quantification of this overlap exists for the matter-bounce bispectrum (2009--2024). The full bispectrum-shape coefficient map and the per-configuration overlap values for all six monomial coefficient sets satisfying the Cai et al.\ benchmarks are archived as \texttt{phase3\_bispectrum\_shape\_overlap.json} (released with the paper's code; see Data and Code Availability), enabling independent reproduction of the $r = 0.84 \pm 0.02$ noise-weighted central value.

For the SPHEREx bispectrum forecast ($\sigma(\fnl^{\rm local}) = 0.7$) at the corrected central $\fnl = -35/16$, the template-corrected detection significance is ${\sim}\,2.6$--$2.75\sigma$ before GR and $b_\phi$ systematics (the range reflecting the noise-weighted overlap $r = 0.84 \pm 0.02$ and the $\epsilon$-correction uncertainty), reduced from the naive $3.13\sigma$ ($|{-}35/16|/0.7$). Under the CMB Fisher (signal-only) weighting the optimistic significance is $2.74\sigma$ ($r = 0.876$); under the more realistic LSS/SPHEREx-like noise weighting it is $2.63\sigma$ ($r = 0.84$). Including the full systematic budget (GR marginalization, $b_\phi$ uncertainty, photo-$z$ degradation), the realistic range is ${\sim}\,1.3$--$2.75\sigma$ (Sec.~\ref{sec:systematics}).

\subsection{Galaxy Bispectrum}

The galaxy bispectrum provides an independent measurement channel that accesses information at shorter wavelengths, reducing the dependence on ultra-large-scale modes~\cite{Heinrich:2023}. This makes bispectrum-based constraints more robust to large-scale systematics than power-spectrum-based scale-dependent bias alone.

% ============================================================
\section{SPHEREx Forecast}
\label{sec:spherex}

SPHEREx is an all-sky spectrophotometric survey ($0.75$--$5\,\mu$m) with spectral resolution $R \approx 40$--$130$ and approximately 450 million galaxies. DBI inflation is intentionally not propagated through the local-template SPHEREx forecast in this section: it has vanishing squeezed-limit local amplitude and an equilateral shape, so a local-template estimator does not discriminate against it; the relevant bispectrum-shape comparison against DBI is deferred to \S\ref{sec:bayesian} (where the multifield-tuned competitor envelope is drawn) and the DBI scoping discussion in \S\ref{sec:bayesian} (the parenthetical at the end of the joint $(\fnl, n_{\fnl})$ subsection). A dedicated multi-tracer bispectrum analysis~\cite{Heinrich:2023}, building on the canonical SPHEREx multi-tracer forecast lineage of Dor\'e \etal~\cite{Dore:2014} (the foundational SPHEREx galaxy-survey forecast paper; M\"unchmeyer \etal~\cite{Munchmeyer:2019} is the CMB kinetic-Sunyaev-Zel'dovich tomography companion forecast for the same parameter target), forecasts $\sigma(\fnl^{\rm local}) = 0.7$ from the bispectrum alone, with $\sigma(\fnl^{\rm local}) = 0.5$ when combined with the power spectrum. The Heinrich et al.\ forecast specifically constrains the \emph{local-shape} $\fnl$ at the SPHEREx-selected emission-line galaxy redshift distribution ($z \approx 0.5$--$2$, sample-variance-limited at the lowest $k$). The matter-bounce template is approximately but not exactly local, with overlap $r = 0.84 \pm 0.02$ (Sec.~\ref{sec:template}); we propagate this template mismatch into the detection significance separately. The Heinrich \etal~Fisher forecast is constructed at the local-template fiducial $\fnl = 0$; applying the resulting $\sigma(\fnl)\approx 0.7$ at the bounce-fiducial $\fnl = -2.1875$ relies on the leading-order linearization that the Fisher matrix is approximately invariant under fiducial shifts of order the parameter uncertainty (a standard but non-trivial Fisher-forecast assumption). As an order-of-magnitude check: the leading non-Gaussian correction to the bispectrum covariance from a non-zero fiducial $\fnl$ enters at second order in $\fnl$ through the connected six-point function. The dimensionless fractional covariance correction at fixed $k$ then scales as
\begin{equation}
\frac{\delta C}{C_{\rm Gauss}} \;\sim\; \frac{\fnl^2\,\Delta_\zeta^2(k)}{N_{\rm modes}(k)},
\label{eq:fiducial_shift}
\end{equation}
where $\Delta_\zeta^2(k) \equiv k^3 P_\zeta(k)/(2\pi^2) \approx 2.1\times 10^{-9}$ is the dimensionless curvature power spectrum and $N_{\rm modes}(k) \sim V_{\rm survey}\,k^2\,\delta k/(2\pi^2)$ is the dimensionless mode count in a thin $k$-shell. At SPHEREx scales $k \sim 0.01$--$0.1\,h\,\text{Mpc}^{-1}$ with $\fnl = -2.1875$, $\fnl^2\,\Delta_\zeta^2 \sim 1\times 10^{-8}$, so $\delta C/C \ll 10^{-3}$ even before dividing by the mode count. The propagated fractional shift in $\sigma(\fnl)$ follows from $\delta\sigma/\sigma \sim \tfrac{1}{2}\,\delta C/C$ and is therefore $\lesssim 5\times 10^{-4}$, well below the percent level. Eq.~(\ref{eq:fiducial_shift}) is a heuristic primordial-field scaling check in the curvature perturbation $\zeta$ and is \emph{not} a covariance-level derivation in the galaxy field: it does not include linear-bias, quadratic-bias, shot-noise, or the six-point galaxy-bispectrum covariance terms that enter the full multi-tracer covariance of Ref.~\cite{Heinrich:2023}, and the quoted $\lesssim 5\times 10^{-4}$ figure should be read as a heuristic dimensional check rather than a derived bound. A full re-derivation of the Heinrich Fisher matrix at the bounce fiducial converts this scaling check into a derived bound; we have now carried out an independent instance of that re-derivation, described in the next paragraph. The multi-tracer cosmic-variance cancellation invoked here originates with the multi-tracer power-spectrum technique of Seljak~\cite{Seljak2009} and McDonald \& Seljak~\cite{McDonaldSeljak2009}; the bispectrum-multi-tracer extension followed by Karagiannis \etal~\cite{Karagiannis2018} is what underwrites the Heinrich et al.\ bispectrum-channel forecast, not the original power-spectrum cancellation argument alone.

\emph{Independent bispectrum Fisher (validation of the recast).}\label{para:reconcile} \emph{$r$ versus $r_{\rm eff}$ --- one reconciliation, referenced throughout.} These two numbers measure \emph{different} quantities and are not in tension: $r = 0.84$ is the flat-weight (shape-overlap) template cosine adopted as the deliberately conservative recast factor of Eq.~(\ref{eq:projection}) --- it weights all triangle configurations by the naive shape inner product; $r_{\rm eff} = \sigma_{\rm local}/\sigma_{\rm bounce} \approx 0.99$ is the \emph{survey-optimal} amplitude-recovery factor of an actual multi-tracer bispectrum estimator, whose covariance is squeezed-dominated (the $\fnl$ weight piles up at low $k$, where the Gaussian covariance is smallest and where the bounce and local templates coincide by construction). Because the survey weighting concentrates on exactly the configurations where the two templates agree, the estimator recovers essentially all the amplitude ($r_{\rm eff} \approx 0.99$) even though a flat-weighted cosine over all triangles is only $0.84$. The independent Fisher therefore \emph{confirms} that $r = 0.84$ is conservative --- it under-states the amplitude a real estimator recovers --- and the paper deliberately headlines the conservative $r = 0.84$, using $r_{\rm eff} \approx 0.99$ only as a validation. Every other mention of these two numbers points here. To remove the single-source dependence, we have built an independent, from-scratch tree-level galaxy-bispectrum Fisher forecast (\path{c13_independent_bounce_fisher.py}; committed output \path{c13_independent_bounce_fisher.json}) that imports no Heinrich number. The full survey specification is exactly the committed SPHEREx public-products table (Dor\'e \etal~\cite{Dore:2014}: 5 photo-$z$ samples with the tabulated per-sample number densities $\bar n_i(z)$ and linear biases $b_i(z)$, 6 redshift bins over $z = 0$--$1.6$, $f_{\rm sky} = 0.75$) that Heinrich \etal\ adopt --- no rescaled or hand-tuned survey inputs. The physics inputs are the Planck~2018 matter power spectrum and transfer function from CAMB, the standard SPT tree-level ($F_2$-kernel) matter bispectrum, the scale-dependent-bias $\Delta b = 2\fnl\delta_c(b_1-1)/M(k)$ and primordial-transfer terms, over Heinrich's $k_{\max} = 0.2(1+z)\,h\,{\rm Mpc}^{-1}$ per bin. The estimator covariance is the leading-order Gaussian multi-tracer bispectrum covariance~\cite{Scoccimarro:1998,Sefusatti:2006}, i.e.\ the diagonal-triangle Wick contraction $\mathrm{Cov}[B_{abc},B_{a'b'c'}] = s_{123}\,V_f^{-1}\,k_{123}^{-1}\,P^{\rm tot}_{aa'}P^{\rm tot}_{bb'}P^{\rm tot}_{cc'}$ (with the multi-tracer total power $P^{\rm tot}_{ij} = P_{ij} + \bar n_i^{-1}\delta_{ij}$ carrying both cosmic-variance and shot-noise, $s_{123}$ the triangle symmetry factor, and the sum over tracer permutations), with $b_2/b_{s^2}$ held at their fiducial values (an honest limitation stated below), not a bespoke or unstated covariance. The full 5-sample multi-tracer Fisher reproduces the Heinrich local-template baseline, $\sigma(\fnl^{\rm local}) = 0.63$ (bias-fixed) to $0.69$ (bias-marginalized), within $2$--$11\%$ of their $0.7$ (validation ratio $0.89$--$0.98$, well inside Fisher-forecast tolerance). Evaluated directly at the bounce $\BNL$ template it gives $\sigma(\fnl^{\rm bounce}) = 0.63$--$0.69$, i.e.\ an independent recovery factor $r_{\rm eff} = \sigma_{\rm local}/\sigma_{\rm bounce} \approx 0.99$ and an unmarginalized detection significance $|{-}35/16|/\sigma(\fnl^{\rm bounce}) \approx 3.2$--$3.5\sigma$ (real-space monopole, the conservative floor; the redshift-space value below is $\approx 4.9$--$5.2\sigma$); both are stable across triangle-grid resolution. That $r_{\rm eff} \approx 0.99$ exceeds the paper's noise-weighted $r = 0.84$ because the actual SPHEREx bispectrum-estimator covariance is squeezed-dominated (the $\fnl$ signal is enhanced at low $k$ and the Gaussian covariance is smallest there), and in the squeezed limit the bounce and local templates coincide by construction; a primordial-transfer-only channel gives the same $r_{\rm eff} \approx 0.99$, confirming this is a weighting effect, not a dilution artifact. The paper's $r = 0.84$ is therefore a \emph{conservative} shape-overlap under folded/intermediate-weighted metrics, and the recast headline is validated, not overstated. We have since extended this independent Fisher to the full \emph{redshift-space} tree-level bispectrum (\path{c14_rsd_multipole_fisher.py}; committed output \path{c14_rsd_multipole_fisher.json}), dressing every leg with the linear Kaiser factor $Z_1(k,\mu) = b + f\mu^2$ (Kaiser~\cite{Kaiser:1987}) and the second-order redshift-space kernel $Z_2$ (Scoccimarro, Couchman \& Frieman~\cite{Scoccimarro:1999}; Sefusatti~\cite{Sefusatti:2006}) with the growth rate $f(z) = f\sigma_8/\sigma_8$ from the same CAMB cosmology, and integrating the Fisher integrand over the full line-of-sight orientation $(\mu_1,\phi)$ (so the $\ell = 0,2,4$ multipole content is included exactly, with no truncation). The redshift-space forecast \emph{tightens} the local-template baseline to $\sigma(\fnl^{\rm local}) = 0.42$ (bias-fixed) to $0.45$ (bias-marginalized), a $\sim\!35\%$ improvement over the real-space monopole in the one-directional sense expected from adding velocity information --- the full real-space-to-redshift-space gain, which is larger than the narrower $\sim\!18\%$ monopole-to-multipole gain quoted for an already-redshift-space analysis because the linear Kaiser monopole enhancement adds on top of the multipole anisotropy. Evaluated at the bounce template it gives $\sigma(\fnl^{\rm bounce}) = 0.42$--$0.45$, i.e.\ the same independent recovery factor $r_{\rm eff} = \sigma_{\rm local}/\sigma_{\rm bounce} \approx 0.99$ persists in redshift space (the Kaiser weighting does not move the $\fnl$ signal off the squeezed configurations where the two templates coincide), and an unmarginalized detection significance $|{-}35/16|/\sigma(\fnl^{\rm bounce}) \approx 4.9$--$5.2\sigma$. As an internal cross-check the $f \to 0$ limit of this redshift-space pipeline reproduces the real-space multi-tracer Fisher above to six significant figures. The remaining honest limitations of the independent Fisher are: it uses the leading-order Gaussian covariance with the diagonal-triangle Wick term, is tree-level with a linear $k_{\rm max}$ and does not marginalize $b_2$/$b_{s^2}$ (held at fiducial in both the real- and redshift-space runs, so the bias-marginalized $\sigma$ is a leading-order bound on the bias-nuisance axis), and models redshift-space distortions at tree-level Kaiser order without fingers-of-God damping (a small correction at the linear $k_{\max}$ used: because FoG damping \emph{suppresses} small-scale power it \emph{degrades} the information content, so including it would \emph{weaken} the absolute redshift-space $\sigma(\fnl^{\rm bounce})$ and the reported redshift-space gain --- omitting it is therefore \emph{optimistic}, not conservative, for the absolute significance; the effect is small at the linear $k_{\max}$ used and, being applied identically to $\sigma_{\rm local}$ and $\sigma_{\rm bounce}$, largely cancels in the recovery ratio $r_{\rm eff}$, which is the only quantity that enters a headline number); the \emph{ratio} $r_{\rm eff}$ is far more robust than either absolute $\sigma$ because these conservative offsets largely cancel in the ratio (both $\sigma_{\rm local}$ and $\sigma_{\rm bounce}$ are computed with the identical covariance, $k_{\max}$, and bias treatment, so a common multiplicative mis-estimate divides out), and $r_{\rm eff}$ is exactly the quantity the recast's $r$ asserts. We do not attach a formal uncertainty band to $r_{\rm eff}\approx0.99$: it is a single-configuration ratio under the stated tree-level Gaussian-covariance approximation, and a full error propagation would require marginalizing $b_2/b_{s^2}$ and the non-Gaussian covariance terms held fixed here; because $r_{\rm eff}$ is used only to \emph{corroborate} that the conservative headline $r=0.84$ under-states the recoverable amplitude (never to tighten a significance), this unquantified robustness does not enter any headline number. A single-effective-tracer collapse of the same pipeline gives $\sigma(\fnl^{\rm local}) \approx 15.6$, retained only as a labeled conservative bound (its offset from $0.7$ is the expected, sourced loss of dropping multi-tracer cosmic-variance cancellation, consistent with Heinrich's own few-tracer degradation). Crucially, this $3.2$--$3.5\sigma$ is the unmarginalized signal-plus-shot-noise number: the paper's GR-projection-degeneracy bracket ($\rho \approx 0.95$; the marginalized $\sim\!0.8$--$1.3\sigma$ lower edge of \S\ref{sec:systematics}) still applies on top and is retained exactly as before.

We adopt the Heinrich et al.\ $\sigma(\fnl) = 0.7$ as the baseline SPHEREx sensitivity for the headline forecast: the quoted detection significances are derived from that published $\sigma(\fnl) = 0.7$, degraded by the template mismatch, $\epsilon$-correction, and systematic factors quantified in subsequent sections, which makes the \emph{headline} a sensitivity recast rather than an independent forecast. We do, however, construct an independent in-house multi-tracer bispectrum Fisher (\path{c13_independent_bounce_fisher.py}, next paragraph) as a validation of that recast; it reproduces the Heinrich baseline to $2$--$11\%$ and confirms the bounce-template recovery, so the recast rests on a checked rather than an unverified rescaling. Three caveats apply. First, the Heinrich et al.\ forecast marginalizes over galaxy bias parameters $b_1$ and $b_2$ but treats the PNG bias parameter $b_\phi$ with a fixed universality relation; if $b_\phi$ is instead marginalized as a free parameter per tracer bin, the effective $\sigma(\fnl)$ could widen by $\mathcal{O}(20$--$50\%)$ (Sec.~\ref{sec:systematics}). Second, the forecast assumes a purely local bispectrum template. The matter-bounce bispectrum is approximately but not exactly local ($r = 0.84 \pm 0.02$ under noise weighting; Sec.~\ref{sec:template}); the mismatch means a local estimator applied to a bounce signal recovers only a fraction of the amplitude, which we account for via the template projection in Eq.~(\ref{eq:projection}), but potential additional losses from the non-local tails of the bounce shape in the bispectrum estimator covariance are not modeled. Third, $\sigma(\fnl) = 0.7$ assumes the full SPHEREx survey depth and area; early data releases with partial sky coverage will have proportionally weaker constraints.

For our target signal $\fnl = -35/16 = -2.1875$, the template-corrected detection significance (Eq.~\ref{eq:projection}) ranges from $2.74\sigma$ (optimistic: bispectrum only, CMB Fisher weighting $r = 0.876$, no GR or $b_\phi$ degradation) through $2.63\sigma$ (noise-weighted $r = 0.84$; Eq.~\ref{eq:r_noise}) to $1.5\sigma$ (the conservative floor reported here is defined as: $\sigma_{\rm GR} = 1.0$ added in quadrature to the baseline $\sigma(\fnl)=0.7$, without the additional widened $b_\phi$ prior; explicitly, $2.1875 \times 0.84 / \sqrt{0.7^2 + 1.0^2} \approx 1.50\sigma$; using the slightly lower noise-weighted endpoint $r = 0.829$ gives $\approx 1.49\sigma \approx 1.5\sigma$, so the floor is robust to the noise-weighting choice across the $r = 0.829$--$0.84$ noise-weighted range). Adding the widened $b_\phi$ prior of \S\ref{sec:systematics} (which moves the combined-sample baseline $\sigma(\fnl)$ from $0.7$ to $0.9$--$1.0$; the $\sigma(\fnl)=0.7$ Heinrich \etal\ baseline is the full multi-tracer combined-sample forecast, not a per-redshift-bin uncertainty) brings the all-combined endpoint to ${\sim}\,1.3$--$1.4\sigma$; this is the honest cumulative-systematics endpoint and is reported here as such, distinct from the $1.5\sigma$ GR-only floor of the headline range. The realistic range after the combined systematic budget is ${\sim}\,1.3$--$2.75\sigma$, with the lower endpoint reflecting the all-combined cumulative budget. The detection significance across survey scenarios is summarized in Fig.~\ref{fig:surveys}. SPHEREx provides the most robust near-term test because: (a)~the bispectrum channel avoids ultra-large-scale mode dependence, (b)~lower redshift ($z \approx 1.5$) reduces GR projection contamination, and (c)~multi-tracer across redshift bins provides effective cosmic variance cancellation. Anomaly-detected QSO candidates and unusual emission-line galaxies---identified by autoencoder spectral analysis on DESI~DR1 and SDSS~DR18 (Baron \& Poznanski~\cite{Baron2017}; Liang \etal~\cite{Liang2023} methodology)---offer an independent route to multi-tracer diversification; a preliminary Fisher forecast on DESI--SDSS cross-matched anomaly tracers projects a ${\sim}\,10$--$20\%$ improvement in $\sigma(\fnl)$ over the standard multi-tracer baseline (the exact gain depends on the anomaly subsample's number density, redshift distribution, and bias parameters, which are not yet fully characterized; the quoted gain is an upper bound pending the shot-noise-corrected Fisher analysis described in the shot-noise caveat below); this would extend to the SPHEREx bispectrum channel once anomaly-selected subsamples are propagated to the $z \approx 1.5$ population.

\textit{Shot-noise caveat.} Our Fisher forecast adopts the Heinrich et al.\ $\sigma(\fnl) = 0.7$ as a baseline, which is computed for the full SPHEREx emission-line galaxy sample ($\bar{n} \sim 10^{-3}\,h^3\,\mathrm{Mpc}^{-3}$). In this regime the signal is largely cosmic-variance limited and shot noise contributes at the $\lesssim 5\%$ level to $\sigma(\fnl)$---subdominant to the systematic budget quantified in Sec.~\ref{sec:systematics}. However, for anomaly-selected tracers ($\bar{n} \sim 10^{-5}\,h^3\,\mathrm{Mpc}^{-3}$), shot noise is more significant: the naive Poisson amplitude scaling gives $\sigma(\fnl)$ inflated by $\sqrt{11} \approx 3.3\times$ relative to the shot-noise-free Fisher limit at $\bar{n} \sim 10^{-5}$ (scaling as $\sigma(\fnl)_{\rm shot}/\sigma(\fnl)_{\rm CV} \sim \sqrt{1 + 1/(\bar{n}P_0)}$ where $P_0 \sim 10^4\,h^{-3}\,\mathrm{Mpc}^3$; for $\bar{n} \sim 10^{-5}$, $1/(\bar{n}P_0) \sim 10$), while the bispectrum-estimator effective degradation at the squeezed-limit modes that dominate $\fnl$ sensitivity is the more relevant $\sim 15$--$30\%$ at $z \sim 1$--$2$, because the squeezed limit preferentially downweights the highest-$k$ modes where shot noise is most severe, and would need to be included in any definitive forecast based on anomaly-selected subsamples. For the 10\% catastrophic photo-$z$ outlier fraction mentioned above, the 5\% degradation in the bispectrum $\sigma(\fnl)$ follows from a first-order Fisher correction: catastrophic outliers scatter modes across the $1/k^2$ bias kernel at rate $f_{\rm cat} = 0.1$, diluting the effective PNG signal by $\sim f_{\rm cat}^2/(1 + f_{\rm cat})^2 \approx 0.008$, well below the 5\% level; the dominant effect is a net smearing of the cross-bin Fisher matrix at the $\sim 5\%$ level consistent with published photo-$z$ degradation estimates~\cite{Giannantonio:2012}. The headline $1.3$--$2.75\sigma$ significance range refers to the full SPHEREx sample and does not rely on anomaly-selected tracers; the ${\sim}\,10$--$20\%$ improvement from anomaly tracers quoted above should be interpreted as an upper bound until a shot-noise-corrected Fisher matrix is computed for the anomaly subsample.

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{fig2_survey_comparison.png}
\caption{Detection significance for the corrected central $\fnl = -35/16$ across survey configurations. SPHEREx bars (left to right): naive uncorrected $3.13\sigma$ (reference only); template-corrected optimistic $2.6$--$2.75\sigma$ ($|\fnl|\,r/\sigma$, noise-weighted $r=0.84$); realistic post-systematic-budget envelope $1.3$--$2.75\sigma$; and all-combined conservative endpoint $1.3$--$1.4\sigma$. MegaMapper bars: ideal $3.7$--$3.85\sigma$; $1.5$--$3.5\sigma$ design-uncertainty envelope; conservative; single-tracer. All values are exactly one-half of what the erroneous $-35/8$ input would give (details in Sec.~\ref{sec:systematics}).}
\label{fig:surveys}
\end{figure}

% ============================================================
\section{MegaMapper Forecast}
\label{sec:megamapper}

MegaMapper is a proposed (not yet funded) Stage-V spectroscopic facility targeting $\sim 10$ million Lyman-break galaxies at $z = 2$--$5$ with multi-tracer capability~\cite{Schlegel:2022}. As of 2026, MegaMapper has no finalized instrument design, no confirmed site, and no approved funding; the forecasts below should be understood as illustrative of what a Stage-V spectroscopic survey \emph{could} achieve, not as commitments from a specific instrument. \emph{We label this section decisively as an uncalibrated projection}: the SPHEREx-derived GR-marginalization and $b_\phi$ systematic budget is transferred to MegaMapper as a placeholder, but it is \emph{not} calibrated to MegaMapper's higher-redshift ($z=2$--$5$) regime, where relativistic projection effects (Doppler, gravitational-redshift, lensing-magnification bias) grow steeply and become dominant rather than subdominant corrections~\cite{Jolicoeur:2025}. The post-systematic MegaMapper ranges below should therefore be read as design-uncertainty envelopes, not as calibrated forecasts; the ideal-case $\sigma(\fnl)\approx0.5$ is the only externally-anchored number, and the systematic degradation applied to it is a scoping proxy pending a dedicated high-$z$ GR-projection kernel. Published forecasts give $\sigma(\fnl) \approx 0.5$ under ideal conditions. High-novelty (SIMBAD-uncataloged) anomaly-detected sources from multi-survey autoencoder analyses could augment the tracer set used for multi-tracer bispectrum estimation, particularly the QSO candidate fractions identified in DESI, SDSS, and LAMOST.

The significance for a detection at the corrected central $\fnl = -2.1875$ ranges from $3.7$--$3.85\sigma$ at the published ideal $\sigma(\fnl) = 0.5$ with template-mismatch correction only ($r = 0.84$--$0.88$; without template correction the naive significance is $4.38\sigma$), to $1.3$--$2.75\sigma$ after the same GR marginalization and $b_\phi$ uncertainty budget applied to SPHEREx above (for illustration only; this budget is not independently calibrated to MegaMapper's higher-redshift $z=2$--$5$ sensitivities, where GR projection effects are expected to be substantially larger~\cite{Jolicoeur:2025}). At an intermediate $\sigma(\fnl) = 0.7$ (allowing for partial systematic degradation), the template-corrected significance is ${\sim}\,2.6\sigma$ optimistic, ${\sim}\,1.6\sigma$ conservative (combining the noise-weighted template overlap $r = 0.84$, $\sigma(\fnl) = 0.7$, and a $30\%$ $b_\phi$ prior widening that moves the combined-sample baseline $\sigma(\fnl)$ to $\approx\!0.9$: $2.1875 \times 0.84 / \sqrt{0.7^2+0.9^2} \approx 1.6\sigma$; with $\sigma_{\rm GR} = 1.0$ and no $b_\phi$ widening the floor is ${\sim}\,1.5\sigma$; this is a separate illustrative stress test using Table~\ref{tab:systematics} combination rules, not independently calibrated to MegaMapper's higher-$z$ systematics). The abstract quotes a wide $1.5$--$3.5\sigma$ range spanning the full envelope from the conservative systematic scenario to the midpoint between the ideal and degraded cases; this range reflects design uncertainty in the instrument concept (survey area, spectral resolution, target selection, and number density) at least as much as measurement uncertainty, and should not be interpreted as a well-characterized error bar. MegaMapper's forecast is more sensitive than SPHEREx's to: (a)~relativistic projection effects, which create substantial GR-induced bias at $z > 2$~\cite{Jolicoeur:2025}; (b)~PNG bias parameter $b_\phi$ uncertainty, which can degrade constraints if uncalibrated~\cite{Barreira:2022}; and (c)~multi-tracer implementation quality.

% ============================================================
\section{Inflation Mimicry and Bayesian Comparison}
\label{sec:bayesian}

\subsection{Can Inflation Reproduce the Signal?}

Standard single-field slow-roll inflation predicts $\fnl = (5/12)(1-n_s) \approx +0.015$~\cite{Maldacena:2002vr} as the gauge-frame value of the consistency relation; in conformal Fermi coordinates the physical observable is parametrically smaller~\cite{Pajer:2013,TanakaUrakawa:2011}. Either way the bounce-vs-inflation contrast remains qualitatively $|\fnl^{\rm bounce}| \gg |\fnl^{\rm inf}|$ and opposite in sign; we cite the gauge-frame ratio $|\fnl^{\rm bounce}|/|\fnl^{\rm inf}| = 2.1875/0.015 \approx 146$ as a benchmark, recognising that the physical contrast is even more favourable to the bounce. Non-canonical single-field models (DBI, etc.) produce equilateral-shape $\fnl$, not local. Table~\ref{tab:frames} consolidates the expected local $\fnl$ in the two frames so the gauge-frame (survey-observable) and physical-frame (theoretical-discriminator) values are cleanly separated.
\begin{table}[h]
\centering
\caption{\label{tab:frames}Expected squeezed-limit local $\fnl$ for the quasi-dust matter bounce and single-field slow-roll inflation, separated by observer frame. The \emph{gauge frame} carries the on-sky quantity the SPHEREx/BOSS local-template estimators measure --- this is the row relevant to the forecast. The \emph{physical (conformal-Fermi) frame}~\cite{Pajer:2013,TanakaUrakawa:2011} carries the frame-invariant theoretical discriminator, in which the single-field consistency relation drives $\fnl^{\rm inf}\to0$. The bounce value is frame-robust (its $-35/16$ is a contracting-phase cubic-action prediction, not a squeezed consistency-relation artifact); the inflation value is the one that shifts between frames. In both frames $|\fnl^{\rm bounce}|\gg|\fnl^{\rm inf}|$ with opposite sign, so the discriminating power is frame-independent.}
\begin{ruledtabular}
\begin{tabular}{lccc}
frame & $\fnl^{\rm bounce}$ & $\fnl^{\rm inf}$ & $|\fnl^{\rm bounce}|/|\fnl^{\rm inf}|$ \\ \hline
gauge (survey observable) & $-2.1875$ & $+0.015$ & ${\approx}\,146$ \\
physical (conformal-Fermi) & $-2.1875$ & $\to 0^{+}$ & $\gg 146$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

Non-attractor single-field inflation naturally gives $\fnl = +5/2$ (wrong sign)~\cite{Cai:2018non}. Reaching $-2.1875$ requires engineering the attractor-to-slow-roll transition. The standard quadratic curvaton gives minimum $\fnl \approx -1.25$ (insufficient). Self-interacting curvatons or curved field-space models can reach $-2.1875$ but require $\geq 2$ tuned parameters. Note on prior choice: the natural physical prior for the curvaton class is $|\fnl| \lesssim$ a few (and sign-symmetric about zero), so a competitor prior $[-5, +5]$ is the more physically motivated curvaton baseline than the broad $[-15, +15]$ multifield range used in the headline Bayes factor; under the curvaton-natural $[-5, +5]$ prior the headline Bayes factor at the recommended $\sigma_{\rm theory} = 1.0$ bounce prior is $\mathrm{BF} \approx 4$ (Sec.~\ref{sec:bayesian}, Table~\ref{tab:bayes}), still favoring the bounce but at a smaller margin.

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{fig5_inflation_comparison.png}
\caption{$\fnl$ landscape: matter bounce vs.\ inflationary alternatives. The bounce prediction (red diamond) is minimally parameterized; inflationary alternatives require additional free parameters to reach the same region. SPHEREx $1\sigma$ error bar shown in blue.}
\label{fig:inflation}
\end{figure}

\subsection{The Kinematic vs.\ Parametric Asymmetry}

The bounce predicts $\fnl \approx -35/16$ \emph{kinematically}, with the value tightly determined by the contraction dynamics (residual $\epsilon$-correction uncertainty of $1$--$8\%$; Sec.~\ref{sec:assumptions}). Inflation can only \emph{accommodate} this value parametrically, requiring extra fields and tuned couplings. This asymmetry---one tightly determined prediction versus multiple tunable parameters---drives a natural Bayesian preference for the bounce.

\subsection{Quantitative Bayesian Comparison}

We performed model comparison using three independent Monte Carlo ensembles of $10^5$ realizations each ($3\times 10^5$ aggregate) across three frameworks (analytic closed-form, mock power spectrum generation with fitting, and parameterized GR-contamination marginalization). The three ensembles use \emph{framework-specific priors} (delta-vs-flat $[-15,+15]$ for the analytic closed-form; lognormal $\sigma$-base for the mock-validation; $\sigma_{\rm GR}$ Gaussian for the GR-contamination marginalization) and are not three frames of a single ensemble — they are three cross-checks of the analytic Bayes-factor formula. The four-corner Bayes-factor prior grid reported in this section (curvaton-natural $[-5,+5]$ vs.\ broad multifield $[-15,+15]$ competitor priors $\times$ delta vs.\ $\sigma_{\rm theory}=1.0$ Gaussian bounce priors) is computed analytically via the closed-form Bayes-factor integration (the analytic formula given later in this section); the Monte Carlo ensembles span only a subset of these corners (the delta-vs-flat $[-15,+15]$ corner and the GR-marginalization variation), and the curvaton-natural $[-5,+5]$ column plus the $\sigma_{\rm theory}=1.0$ Gaussian row are derived analytically from the same closed-form integration without a dedicated Monte Carlo scan. The realization count is the convergence-stability sample size, chosen to drive the Monte Carlo estimate of the Bayes factor below the analytic shot noise; it is not a tightening of the underlying $\sigma(\fnl) = 0.7$ Heinrich \etal~2024 forecast, which is the input to every realization. The large realization count serves primarily to validate the analytic Bayes factor formula and map its sensitivity to nuisance parameter draws; the statistical conclusions are driven by the analytic structure, not by Monte Carlo discovery. The closed-form values quoted in this section and in Tables~\ref{tab:bayes} and~\ref{tab:gr} are deterministic integrals carrying no Monte Carlo uncertainty; the released recompute script additionally reports realization-marginalized mean/median/geometric-mean Bayes factors over $2\times 10^5$ draws per $\sigma_{\rm eff}$ as a convergence and stability check (e.g., for the delta-prior/narrow-competitor cell at $\sigma_{\rm eff}=0.7$, closed-form $7.0$ vs.\ realization-marginalized median $6.6$), with realization marginalization always lowering the central value; the spreads quoted in this paper come from prior and scenario variation, not from Monte Carlo noise. Each realization draws a mock $\fnl^{\rm obs}$ from a Gaussian centered on the corrected central prediction $-35/16$ with $\sigma$ drawn from the forecast uncertainty distribution, then computes the Bayes factor analytically for the bounce model (a delta-function prior at $-35/16$) against each inflationary competitor (a spread prior over the competitor's natural $\fnl$ range). The broad-competitor Bayes factors below are, by Eq.~(\ref{eq:bf_approx}), determined by the prior width $W$ and $\sigma_{\rm eff}$ and are insensitive to the exact detection location as long as it lies well inside the competitor prior (both $-35/8$ and $-35/16$ do), so halving the central value from the erroneous $-35/8$ to the corrected $-35/16$ leaves the broad-prior Bayes factors essentially unchanged; only the narrow $[-5,+5]$ CDF-tail terms shift, and remain negligible (the lower tail $\Phi((-5+35/16)/\sigma_{\rm eff})$ is $\lesssim 3\times10^{-5}$). The analytic Bayes factor for a point prediction versus a uniform prior $[\fnl^{\rm min}, \fnl^{\rm max}]$ is:
\begin{equation}
B = \frac{(\fnl^{\rm max} - \fnl^{\rm min}) \times \mathcal{L}(\fnl^{\rm obs} \mid \fnl = -35/16)}{\int_{\fnl^{\rm min}}^{\fnl^{\rm max}} \mathcal{L}(\fnl^{\rm obs} \mid \fnl)\, d\fnl}\,.
\label{eq:bf}
\end{equation}

\paragraph{Closed-form derivation of the Bayes factor.}
For a Gaussian likelihood $\mathcal{L}(\fnl^{\rm obs} \mid \fnl) = \mathcal{N}(\fnl^{\rm obs};\,\fnl,\,\sigma_{\rm eff}^2)$ with $\fnl^{\rm obs} = -35/16$ (mock detection at the corrected bounce prediction) and a uniform competitor prior on $W \equiv \fnl^{\rm max} - \fnl^{\rm min}$, the denominator of Eq.~(\ref{eq:bf}) evaluates to an error-function difference:
\begin{align}
B &= \frac{W \cdot \mathcal{N}(-35/16;\,-35/16,\,\sigma_{\rm eff}^2)}{\int_{\fnl^{\rm min}}^{\fnl^{\rm max}} \mathcal{N}(-35/16;\,\fnl,\,\sigma_{\rm eff}^2)\,d\fnl} \nonumber \\[4pt]
&= \frac{W\,/\,(\sqrt{2\pi}\,\sigma_{\rm eff})}{\Phi\!\left(\frac{\fnl^{\rm max} + 35/16}{\sigma_{\rm eff}}\right) - \Phi\!\left(\frac{\fnl^{\rm min} + 35/16}{\sigma_{\rm eff}}\right)}\,,
\label{eq:bf_exact}
\end{align}
where $\Phi(z) \equiv \frac{1}{2}[1 + \mathrm{erf}(z/\sqrt{2})]$ is the standard normal CDF. This is the \emph{exact} closed-form result; all tabled Bayes factors are computed from Eq.~(\ref{eq:bf_exact}) without approximation.

In the large-$W$ limit where the competitor prior is broad relative to $\sigma_{\rm eff}$ and the bounce prediction $-35/16$ is well inside the prior interval, both CDF arguments are large in magnitude and the denominator approaches unity; the Bayes factor reduces to the Gaussian-peak approximation:
\begin{equation}
B \approx \frac{W}{\sqrt{2\pi}\,\sigma_{\rm eff}} \quad (W \gg \sigma_{\rm eff})\,.
\label{eq:bf_approx}
\end{equation}

\paragraph{Numerical self-consistency check.}
For the four tabled values at $\sigma_{\rm eff} = 0.7$ (the $r \to 1$ no-rebooking endpoint):
\begin{itemize}
\item Delta prior, broad $[-15,+15]$ ($W = 30$, $\sigma_{\rm eff} = 0.7$): exact $B = 17.10$; approx $30/(\sqrt{2\pi}\times 0.7) = 17.07$ — error $0.18\%$ (sub-percent; both CDF tails are $< 10^{-7}$).
\item Delta prior, $\sigma_{\rm eff} = 0.833$ ($r = 0.84$ rebooked): exact $B = 14.36$; approx $30/(\sqrt{2\pi}\times 0.833) = 14.34$ — error $0.14\%$.
\item Gaussian bounce prior $\sigma_{\rm theory} = 1.0$, broad $[-15,+15]$, $\sigma_{\rm eff} = 0.7$: exact $B = 9.80$; approx $= 17.10/\mathcal{C}$ where $\mathcal{C}$ is the prior-convolution correction (not the simple $W/(\sqrt{2\pi}\sigma_{\rm eff})$ formula, which applies only to the delta-prior case). Equation~(\ref{eq:bf_approx}) thus applies to the \emph{delta-prior} corner only; the Gaussian-bounce-prior corners require the full Eq.~(\ref{eq:bf_exact}) with the bounce likelihood replaced by the prior-convolved marginal.
\item \textbf{Gaussian-bounce prior} ($\sigma_{\rm theory} = 1.0$), narrow $[-5,+5]$ competitor ($W = 10$, $\sigma_{\rm eff} = 0.7$): exact $B \approx 3.9$ from Eq.~(\ref{eq:bf_exact}) with the bounce likelihood replaced by the prior-convolved marginal (Gaussian-bounce-prior result; the closed-form CDF integration over the $\sigma_{\rm theory}=1.0$ Gaussian is required here; at the corrected center $-35/16$ the narrow-competitor tail is negligible, so this value is prior-width-dominated). \emph{Note:} Eq.~(\ref{eq:bf_approx}) is the \textbf{delta-prior} large-$W$ approximation ($B \approx W/(\sqrt{2\pi}\sigma_{\rm eff})$) and is \emph{not applicable} to the Gaussian-bounce-prior case. As a self-consistency cross-check only: applying the delta-prior formula Eq.~(\ref{eq:bf_approx}) to this narrow-competitor cell gives $10/(\sqrt{2\pi}\times 0.7) = 5.69$, and with the corrected central value $-35/16$ the competitor-prior CDF denominator is $\Phi((5+35/16)/0.7)-\Phi((-5+35/16)/0.7)=\Phi(10.3)-\Phi(-4.02)\approx 1-3\times10^{-5}\approx 1.0$, so unlike the erroneous $-35/8$ center (which left a finite $\Phi(-0.893)\approx 0.186$ lower tail) the tail is now negligible and the exact narrow \emph{delta-prior} $B \approx 5.69$; the Gaussian-bounce reduction to $\approx 3.9$ is dominated by the prior-width broadening. The narrow-prior $[-5,+5]$ column therefore uses Eq.~(\ref{eq:bf_exact}) exclusively for both the delta-prior row ($B \approx 5.7$, exact CDF) and the Gaussian-bounce-prior row ($B \approx 3.9$, exact prior-convolved CDF); Eq.~(\ref{eq:bf_approx}) is a valid cross-check for the narrow delta row only because the tail vanishes at the corrected center. All tabled narrow-prior values are exact CDF evaluations under their respective prior specifications.
\end{itemize}

To summarize the equation-to-prior assignment unambiguously: Eq.~(\ref{eq:bf_exact}) (the closed-form CDF integration) is the primary formula for \emph{all} tabled entries. Eq.~(\ref{eq:bf_approx}) ($B \approx W/\sqrt{2\pi}\sigma_{\rm eff}$) is a large-$W$ approximation applicable to the delta-prior row; for the broad $[-15,+15]$ competitor the $0.18\%$ sub-percent approximation error confirms the large-$W$ condition is met, and for the narrow $[-5,+5]$ competitor at $W=10$, $\sigma_{\rm eff}=0.7$ and the corrected central value $-35/16$, the competitor-prior CDF tails are negligible ($\Phi(-4.02)\approx 3\times10^{-5}$) so Eq.~(\ref{eq:bf_approx}) gives $B \approx 5.69$ in agreement with the exact CDF result $B \approx 5.7$ (unlike the erroneous $-35/8$ center, where a finite lower tail raised the exact value to $\approx 7.0$). Eq.~(\ref{eq:bf_exact}) remains \emph{required} for every Gaussian-bounce-prior entry: the Gaussian-bounce-prior narrow-competitor result $B \approx 3.9$ comes from Eq.~(\ref{eq:bf_exact}) with the bounce likelihood replaced by the prior-convolved marginal, and applying the delta-prior Eq.~(\ref{eq:bf_approx}) to that cell is a category error that inflates the result (giving the spurious $5.69$). Readers comparing rows across Table~\ref{tab:bayes} should therefore note that the delta-prior row and the Gaussian-bounce-prior row differ in both prior specification \emph{and} required equation.

The realizations marginalize over the uncertainty in survey performance parameters (multi-tracer efficiency, $b_\phi$, GR systematic level). Specifically, each realization draws: $\sigma(\fnl)$ uniformly from $[0.5, 1.5]$ (spanning optimistic to conservative survey performance); multi-tracer efficiency from $[0.5, 1.0]$; PNG bias parameter $b_\phi$ uncertainty as a Gaussian with 20\% scatter (this is an optimistic assumption; current theoretical knowledge of $b_\phi$ is limited, and relaxing this prior would degrade constraints, particularly for the SDB channel); and GR systematic shift from $\mathcal{N}(0, \sigma_{\rm GR})$ with $\sigma_{\rm GR}$ uniform in $[0, 1.0]$.

The delta-function prior on the corrected central value $\fnl = -35/16$ gives the \emph{maximum possible} Bayes factor for a point prediction; broadening the bounce prior to a finite-width Gaussian $\sigma_{\rm theory}$ \emph{reduces} the Bayes factor monotonically, because the integration over the prior dilutes the likelihood concentrated at $-35/16$. Any theoretical uncertainty in the $\fnl = -35/16$ value---from the $\mathcal{O}(\epsilon)$ correction ($0.6$--$8\%$; Sec.~\ref{sec:assumptions}), the nearly order-of-magnitude range in $\kappa_\epsilon$ (Sec.~\ref{sec:currentdata}), or the residual cubic-order adiabaticity check on bounce transmission---would broaden the effective bounce prior and therefore reduce the Bayes factor. We recommend the $\sigma_{\rm theory} = 1.0$ case as the most physically motivated baseline: it covers the full $\epsilon$-correction range comfortably (the $0.6$--$8\%$ shift is $\lesssim 0.18$ in $\fnl$ at the corrected central value, well inside $1\sigma$). To quantify the sensitivity, we report a single self-consistent set of Bayes factors versus the tuned multifield competitor with prior $[-15,+15]$ at fixed baseline GR ($\sigma_{\rm GR}=0.5$):
\begin{itemize}
\item \textbf{Delta prior} (point at the corrected central $-35/16$): median Bayes factor $\sim 17$ (theoretical maximum; by Eq.~(\ref{eq:bf_approx}) the broad-competitor value depends only on $W$ and $\sigma_{\rm eff}$, so it is unchanged from the erroneous $-35/8$ center).
\item \textbf{Gaussian prior, $\sigma_{\rm theory} = 0.5$} (encompassing the central $\epsilon$-correction window [$0.6$--$8\%$ systematic shift in $\fnl$, $\lesssim 0.18$ at the corrected central value] within $1\sigma$; positions $\fnl \in [-2.688, -1.688]$ at $1\sigma$ around the central value $\fnl = -2.1875$): $\sim 14$ (scipy.stats.norm: BF$=13.91$ at broad $[-15,+15]$; broad-competitor value is center-independent).
\item \textbf{Gaussian prior, $\sigma_{\rm theory} = 1.0$} (recommended baseline: covers the full $\epsilon$-correction range within $1\sigma$ at the corrected central $-35/16$): $\sim 10$ (BF$=9.80$, broad $[-15,+15]$ prior; center-independent).
\item \textbf{Gaussian prior, $\sigma_{\rm theory} = 2.0$} (a deliberately wide prior quoted only as a robustness check on the prior-width dependence, not as a physically motivated width): $\sim 6$ (scipy.stats.norm: BF$=5.65$ broad; center-independent).
\end{itemize}
{\sloppy
The recommended-to-theoretical-maximum range $\sim 10$--$17$ --- quoted in the abstract as the $r \to 1$ bookkeeping endpoint, with the abstract headline reading $\mathrm{BF} \approx 9$--$14$ under the noise-weighted $r \approx 0.84$ bounce-amplitude bookkeeping (see the template-mismatch bookkeeping paragraph below) --- therefore brackets the recommended baseline ($\sigma_{\rm theory}=1.0$, lower bound) and the delta-prior maximum (upper bound) at the broad multifield competitor prior $[-15,+15]$; under this convention, broader theoretical priors map to the lower end of the range and the delta prior maps to the upper end, making the relation between prior width and Bayes factor unambiguous: \emph{wider bounce prior $\Rightarrow$ smaller Bayes factor}. In addition to the discrete grid, a continuous marginalization over the prior-width hyperparameter $\sigma_{\rm theory} \sim \mathcal{U}[0.5, 2.0]$ (uniform hyperprior spanning the reported grid, same closed-form conventions) gives $\mathrm{BF} = 8.8$ at the broad $[-15,+15]$ competitor and $\mathrm{BF} \approx 3.5$ at the narrow $[-5,+5]$ competitor --- between the $\sigma_{\rm theory} = 0.5$ and $2.0$ endpoints and close to the recommended $\sigma_{\rm theory} = 1.0$ values ($9.80$ broad, ${\approx}3.9$ narrow); the marginal value sits slightly below the recommended-prior value because the uniform hyperprior weights the wide-prior end equally. The bounce-over-multifield ranking ($\mathrm{BF} > 1$) holds across the entire hyperprior support, so it is stable under continuous prior-width variation and is not an artifact of the discrete grid (three-point grid converged to $< 0.03$ in log-evidence; computation released as \texttt{c9l\_sigma\_theory\_continuous\_marginalization.py}, see Data and Code Availability).

}

\par For abstract-envelope readability, the four-corner Bayes-factor grid is
summarized in one place below. Columns: narrow $[-5,+5]$ vs.\ broad
$[-15,+15]$ multifield competitor priors. Rows: delta-prior vs.\
$\sigma_{\rm theory}=1.0$ Gaussian bounce priors. Together they bracket
the abstract's $r \to 1$ endpoint envelope $\mathrm{BF}\!\sim\!10$--$17$ (the
abstract headline reads $\mathrm{BF} \approx 9$--$14$ under the noise-weighted
$r \approx 0.84$ bounce-amplitude bookkeeping).
\begin{table*}[t]
\centering
\begin{tabular}{l|cc}
\hline
& multifield prior $[-5,+5]$ & multifield prior $[-15,+15]$ \\ \hline
delta prior at $-35/16$ & $\mathrm{BF}\!\sim\!5.7$ & $\mathrm{BF}\!\sim\!17$ \\
$\sigma_{\rm theory}\!=\!1.0$ Gaussian & $\mathrm{BF}\!\sim\!4$ & $\mathrm{BF}\!\sim\!10$ \\
\hline
\end{tabular}
\end{table*}
\noindent\emph{Reading:} the abstract's $r \to 1$ endpoint envelope $\mathrm{BF}\!\sim\!10$--$17$ is the $\sigma_{\rm theory}\!=\!1.0$ broad-multifield column ($\mathrm{BF}\!\sim\!10$) up to the delta-row broad-multifield column ($\mathrm{BF}\!\sim\!17$); the curvaton-natural narrow-competitor column gives the lower-envelope $\mathrm{BF}\!\sim\!4$ ($\sigma_{\rm theory}\!=\!1.0$) and $\mathrm{BF}\!\sim\!5.7$ (delta). The broad-competitor entries are center-independent (Eq.~\ref{eq:bf_approx}), so halving the central value to the corrected $-35/16$ leaves them unchanged; only the narrow delta corner shifts ($7.0\to 5.7$) because its CDF lower tail vanishes at the corrected center.



\par A reader who only reads this subsection can therefore reproduce the abstract's $r \to 1$ endpoint envelope $\mathrm{BF} \sim 10$--$17$ (rebooked to the headline $\mathrm{BF} \approx 9$--$14$ under the noise-weighted $r \approx 0.84$ bookkeeping) from the upper-right column (broad $[-15,+15]$ multifield competitor prior, spanning the recommended $\sigma_{\rm theory}=1.0$ baseline up to the delta-prior theoretical maximum) without integrating across the surrounding paragraphs; the curvaton-natural $[-5,+5]$ column (left) is the smaller-envelope sensitivity check reported in \S\ref{sec:bayesian}. The Bayes factors reported in Table~\ref{tab:bayes} should be interpreted as upper bounds given the current theoretical uncertainty in the bounce prediction, not as robust model-selection evidence.

\paragraph{Worked example: reproducing the abstract $\mathrm{BF} \approx 9$.}
Starting from the recommended $\sigma_{\rm theory} = 1.0$ Gaussian bounce prior and the broad multifield competitor prior $[-15,+15]$ (Eq.~\ref{eq:bf}), the abstract's headline $\mathrm{BF} \approx 9$ follows in three steps. (1)~Set the bounce likelihood width to $\sigma_{\rm eff} = \sigma(\fnl^{\rm local})/r = 0.7/0.84 \approx 0.833$ (noise-weighted $r = 0.84$ rebooking). (2)~Evaluate the analytic Bayes-factor formula with Gaussian bounce prior $\mathcal{N}(-35/16,\,\sigma_{\rm theory}^2=1.0)$, uniform competitor prior on $[-15,+15]$, and mock detection $\hat{f}_{\rm NL} = -35/16$ (the broad-competitor Bayes factor is center-independent, so this reproduces the same value as at the erroneous $-35/8$ center): $\mathrm{BF} = \mathcal{L}(\hat{f}_{\rm NL}|\text{bounce prior}) / \mathcal{L}(\hat{f}_{\rm NL}|\text{multifield prior})$. Using \texttt{scipy.stats.norm} with $\sigma_{\rm eff}=0.833$ reproduces $\mathrm{BF} \approx 9.2$ (the abstract rounds to $\approx 9$). (3)~At the $r \to 1$ no-rebooking endpoint ($\sigma_{\rm eff}=0.7$) the same formula gives $\mathrm{BF} \approx 9.8$ ($\approx 10$ in the abstract's $r \to 1$ Table~\ref{tab:bayes} entry); the modest difference ($9.2$ vs $9.8$) reflects the $\sigma_{\rm eff}$ inflation from $0.7$ to $0.833$ under the $r = 0.84$ rebooking. Both values reflect the competitor-prior sensitivity emphasized in Sec.~\ref{sec:bayesian}: because $\mathrm{BF} \propto W$ in the broad-competitor regime (Eq.~\ref{eq:bf_approx}), widening $[-15,+15]$ to $[-20,+20]$ ($W: 30 \to 40$) raises the Bayes factor by the factor ${\approx}4/3$ (e.g.\ $9.8 \to 13.1$ at the $r \to 1$ endpoint), while the curvaton-natural $[-5,+5]$ competitor reduces the headline to $\mathrm{BF} \approx 4$.

For a detection at the corrected central $\fnl = -35/16 = -2.1875$ by SPHEREx ($\sigma = 0.7$), the prior-sensitivity-resolved Bayes factors are (Table~\ref{tab:bayes}). The PRIMARY reported headline is the recommended $\sigma_{\rm theory} = 1.0$ Gaussian bounce prior (BF~$\sim 10$ vs.\ tuned multifield, broad $[-15,+15]$ column), which is the most physically motivated baseline; the delta-prior row is shown only as the theoretical-maximum upper bound and is not the recommended headline.

\begin{table*}[t]
\centering
\begin{tabular}{lcc}
\toprule
Bounce prior choice & BF vs.\ tuned multifield & BF vs.\ SSFSR \\
 & $[-15,+15]$, $r\!\to\!1$ endpoint & \\
 & ($r{=}0.84$ headline in parens) & \\
\midrule
\textbf{Gaussian, $\sigma_{\rm theory}=1.0$ (recommended headline)} & $\sim \mathbf{10}$ \mbox{\footnotesize($r{=}0.84$: $\mathbf{9.2}$)} & $\gg 1$ \\
Gaussian, $\sigma_{\rm theory}=0.5$ & $\sim 14$ & $\gg 1$ \\
Gaussian, $\sigma_{\rm theory}=2.0$ & $\sim 6$ & $\gg 1$ \\
\midrule
Delta at $\fnl=-35/16$, narrow $[-5,+5]$ multifield (GR-variation only) & 3.8--5.7$^a$ & $\sim 5$--$1.4\times10^2$$^c$ \\
Delta at $\fnl=-35/16$, broad $[-15,+15]$ multifield (GR fixed) & $\sim 17^b$ \mbox{\footnotesize($r{=}0.84$: $14.4$)} & $\sim 1.4\times10^2$$^c$ \\
\midrule
\multicolumn{3}{p{0.93\textwidth}}{\footnotesize \emph{All BF entries are the $r\!\to\!1$ (no-template-mismatch, $\sigma_{\rm eff}=0.7$) endpoint.} The promoted abstract headline $\mathrm{BF}\approx 9$--$14$ applies the noise-weighted $r=0.84$ rebooking $\sigma_{\rm eff}=\sigma(\fnl^{\rm local})/r=0.83$ to these entries (Eq.~\ref{eq:projection}): the recommended row~1 maps $9.8\!\to\!9.2$ and the delta/broad row maps $17\!\to\!14.4$, bracketing the $9$--$14$ headline. The $0.55$--$1.14$ null-space $r$ band (Table~\ref{tab:systematics}) is a distributional stress band and never enters $\sigma_{\rm eff}$.} \\
\bottomrule
\end{tabular}
\caption{Primary Bayes-factor sensitivity to the bounce prior, evaluated for a mock SPHEREx detection at the corrected central $\fnl=-35/16=-2.1875$ ($\sigma=0.7$) with the GR-contamination amplitude fixed (no GR marginalization), except row~4, which reports the GR-marginalization spread. The broad-competitor Bayes factors are center-independent (Eq.~\ref{eq:bf_approx}) and hence identical to those at the erroneous $-35/8$ center; only the narrow $[-5,+5]$ delta corner shifts ($7.0\to 5.7$) as its CDF lower tail vanishes at the corrected center. ``BF vs.\ SSFSR'' evaluates standard single-field slow-roll as a point hypothesis at its parameter-free prediction $\fnl^{\rm inf}\approx 0.015$ (effectively $\fnl=0$ at survey precision); the tabulated entries are exact closed-form Eq.~(\ref{eq:bf}) evaluations under the stated assumptions, but because their magnitudes are strongly prior-width and point-hypothesis dependent, they should be \emph{interpreted} only at the order-of-magnitude level. The $\sigma_{\rm theory}=1.0$ Gaussian row is the recommended physically motivated headline (BF~$\sim 10$ at broad multifield $[-15,+15]$, BF~$\sim 4$ at narrow $[-5,+5]$): it covers the full $\epsilon$-correction range (${\lesssim}\,0.18$ in $\fnl$) within $1\sigma$ at the corrected central $-35/16$ (see Sec.~\ref{sec:bayesian}). The delta-prior row is reported only as the theoretical-maximum upper bound; any finite theoretical uncertainty in the $\fnl=-35/16$ value monotonically reduces the Bayes factor (Sec.~\ref{sec:bayesian}). $^a$The 3.8--5.7 spread on this row reflects the GR-marginalization variation across the scenarios of Table~\ref{tab:gr} at fixed \emph{narrow} multifield competitor prior $[-5,+5]$ (halved-tail values at the corrected $-35/16$ center); this is the same competitor prior that produces the narrow ``BF vs.\ Tuned'' column of Table~\ref{tab:gr}, and the two tables are numerically identical by construction (both from the closed-form Eq.~(\ref{eq:bf}) recompute, \texttt{c9g\_bf\_table\_recompute.py}). The recommended $\sigma_{\rm theory}=1.0$ headline of BF~$\sim 10$ is approximately constant under the same GR variation. $^b$The $\sim\!17$ row reports the delta-bounce-prior Bayes factor at the broad-multifield competitor prior $[-15,+15]$ with the GR-contamination amplitude fixed (no GR marginalization; $\sigma_{\rm eff}=0.7$); GR marginalization at $\sigma_{\rm GR}=0.5$ reduces it to ${\sim}\,14$. This is the delta-prior endpoint of the competitor-prior sensitivity scan in Sec.~\ref{sec:bayesian}. The $r \to 1$ endpoint envelope $\sim\!10$--$17$ quoted in the abstract brackets the recommended-prior lower bound (BF~$\sim\!10$, row 1) up to the delta-prior maximum at broad multifield (BF~$\sim\!17$, row 5), with broader bounce priors giving \emph{smaller} Bayes factors; the abstract headline $\mathrm{BF} \approx 9$--$14$ applies the noise-weighted $r \approx 0.84$ bounce-amplitude bookkeeping to these endpoints (see the template-mismatch bookkeeping paragraph). $^c$The ``BF vs.\ SSFSR'' entries are the closed-form point-vs-point ratio $\exp[(35/16)^2/(2\sigma_{\rm eff}^2)]$ at the corrected $-35/16$ center: $1.4\times10^2$ at $\sigma_{\rm eff}=0.7$ (no GR) down to ${\sim}\,5$ at $\sigma_{\rm eff}=1.22$ ($\sigma_{\rm GR}=1.0$), matching the SSFSR column of Table~\ref{tab:gr}. These are two-to-eight orders of magnitude below the superseded $-35/8$ values and are quoted at the order-of-magnitude level only, but remain $\gg 1$; the discrimination against the parameter-free single-field prediction is qualitatively unchanged. }
\label{tab:bayes}
\end{table*}

Varying the multifield competitor prior width gives Bayes factors from ${\sim}5.7$ (narrow $[-5,+5]$) to 17 (broad $[-15,+15]$) at the delta bounce prior, illustrating the strong prior sensitivity in the competitor direction. Under the recommended $\sigma_{\rm theory}=1.0$ Gaussian bounce prior, the corresponding range is BF~$\sim 4$--$10$ across the same multifield-competitor-prior variation (scipy.stats.norm recompute: ${\approx}3.9$ narrow, 9.80 broad), again with broader competitor priors giving larger Bayes factors. The recommended headline (BF~$\sim 10$ at $\sigma_{\rm theory}=1.0$, broad multifield $[-15,+15]$) and the delta-prior maximum (BF~$\sim 17$ at the same multifield competitor) bracket the $r \to 1$ endpoint envelope $\sim 10$--$17$ (abstract headline $\approx 9$--$14$ after the noise-weighted $r \approx 0.84$ rebooking); the headline number we promote is the lower bound (BF~$\sim 10$ at $r \to 1$, $\approx 9$ rebooked), not the delta-prior maximum. The sense of the prior dependence is fixed: the delta prior is the maximum, every finite-width broadening reduces the Bayes factor.

\paragraph{Template-mismatch bookkeeping of the Bayes factors.} The mock detection and the $\sigma = 0.7$ likelihood width above place both the detection and the bounce prediction at the corrected central $\fnl = -35/16$ with the survey's local-template uncertainty applied directly --- the $r \to 1$ (no-template-mismatch) bookkeeping endpoint. The rebooking to noise-weighted $r \approx 0.84$ proceeds by replacing the effective uncertainty with $\sigma_{\rm eff} = \sigma(\fnl^{\rm local})/r \approx 0.7/0.84 \approx 0.83$ in Eq.~(\ref{eq:bf}), or equivalently evaluating the likelihood at the noise-weighted prediction $r \times \fnl^{\rm bounce} \approx -1.838$ rather than at $\fnl^{\rm bounce} = -35/16$; both rescalings yield the same modest shift in the Bayes-factor four-corner grid reported below. Under the Eq.~(\ref{eq:projection}) bookkeeping in bounce-amplitude space, $\sigma_{\rm eff} = \sigma(\fnl^{\rm local})/r \approx 0.83$ at the noise-weighted $r = 0.84$, and the closed-form Eq.~(\ref{eq:bf}) four-corner grid reduces modestly: $17.1 \to 14.4$ (delta, broad), $9.8 \to 9.2$ ($\sigma_{\rm theory}=1.0$, broad), $5.7 \to 4.8$ (delta, narrow), $3.9 \to 3.9$ ($\sigma_{\rm theory}=1.0$, narrow); the $r \to 1$ envelope $\mathrm{BF}\,{\sim}\,10$--$17$ correspondingly reads ${\sim}\,9$--$14$ in strict bounce-amplitude bookkeeping --- the bookkeeping the abstract now adopts for its headline, with the $r \to 1$ values retained in Table~\ref{tab:bayes}. The alternative fully measured-space bookkeeping (prediction and detection at $r\,\fnl^{\rm bounce} \approx -1.84$ with $\sigma = 0.7$ and unrescaled competitor priors, since the multifield competitor is itself local-shaped) gives a similar modest reduction at the narrow delta corner; the broad-competitor cells are unchanged. No qualitative conclusion changes under either consistent bookkeeping; the rescaling computation is released with the paper's code (see Data and Code Availability).

\paragraph{Bayes-factor closure against the QSFI continuum.} The multifield competitor priors above represent the curvaton class. The Bayes-factor discrimination against quasi-single-field inflation (QSFI)~\cite{Chen:2009zp} is parameter-dependent through the heavy-field mass-to-Hubble ratio $\mu/H$, which sets the squeezed-limit scaling dimension $\Delta = 3/2 - \sqrt{9/4 - \mu^2/H^2}$. Across $\mu/H \in [0,\,3/2]$ the QSFI bispectrum interpolates between the local-template scaling ($\Delta = 0$ at $\mu/H = 0$, the massless-isocurvaton / curvaton-like limit, where the shape-to-local ratio $(k_3/k_1)^{\Delta}$ is flat) and an intermediate shape whose squeezed amplitude is suppressed by $(k_3/k_1)^{3/2}$ relative to local ($\Delta = 3/2$ at $\mu/H = 3/2$). At the QSFI degenerate endpoint $\mu/H \to 0$ the QSFI shape reproduces the local-template scaling that the matter bounce also approximately matches ($r_{\cos} > 0.97$, Sec.~\ref{sec:template}); near this endpoint shape-based discrimination weakens substantially, and the residual Bayes factor is set by the amplitude prior-predictive distributions and nuisance modeling rather than by shape mismatch --- shape degeneracy alone does not force $\mathrm{BF}\to 1$. BF-based \emph{shape} discrimination of the bounce from QSFI therefore requires $\mu/H$ appreciably above $0$, where the squeezed-limit suppression relative to the local template is resolved. The true bounce-vs-QSFI discrimination is therefore parameter-dependent in $(\mu/H, n_{\fnl})$ space rather than at a single Bayes-factor value, and the abstract envelope ($\mathrm{BF} \approx 9$--$14$ headline; ${\sim}\,10$--$17$ at the $r \to 1$ endpoint) should be read as bracketing the curvaton-class discrimination only, not as a single QSFI-class number.
% [Correction note relocated to changelog: an earlier version placed the local-degenerate endpoint
%  at mu/H=3/2; corrected per Chen-Wang squeezed scaling. Closed OAI-E1 R36conf.]

% ============================================================
\section{Systematics and Robustness}
\label{sec:systematics}

\emph{Scope of the systematic budget (stated up front).} Every systematic contribution in this section, and the consolidated budget of Table~\ref{tab:systematics}, is combined by addition in quadrature ($\sigma_{\rm eff} = \sqrt{\sigma_{\rm base}^2 + \sum_i \sigma_i^2}$). This is a transparent scoping \emph{heuristic}, not a joint multi-tracer marginalized Fisher: correlations between nuisances (notably the $b_1$--$b_\phi$--$\fnl$ large-scale degeneracy) can tighten or loosen the combined budget depending on the sign of the covariance, so the realistic ${\sim}\,1.3$--$2.75\sigma$ envelope is a sensitivity recast rather than a self-consistent joint-covariance forecast. For the strongest of these degeneracies a joint Fisher has now been computed (\texttt{c8\_fnl\_running\_fisher.json}; Planck~2018 cosmology, CAMB~1.6.6; SPHEREx public-product inputs, Dor\'e+2014-validated; \S\ref{sec:lss}). In the SDB power-spectrum channel the $\fnl$-only constraint $\sigma(\fnl)=1.53$ ($1.43\sigma$ detection at the corrected central $\fnl=-35/16$, biases fixed) degrades to $\sigma_{\rm marg}(\fnl)=3.08$ once the running is marginalized (a $2.0\times$ degradation, anti-correlation $\rho=-0.87$, detection $0.71\sigma$), and to $7.06$ once the per-sample linear biases are additionally co-marginalized (a $4.6\times$ degradation, $\rho=-0.97$, detection $0.31\sigma$). This resolves the open sign in the caveat above: the $b_1$--$\fnl$--$n_{\fnl}$ correlations \emph{loosen} rather than tighten the constraint, with the running $n_{\fnl}$ the dominant degradation direction --- so the additive-quadrature budget is refined, not contradicted, by an explicit joint covariance in the channel where this degeneracy is largest. This SDB degradation does not propagate directly to the bispectrum-only headline of \S\ref{sec:spherex}, which accesses additional triangle configurations that partially break the $\fnl$--$n_{\fnl}$ degeneracy. \emph{Joint-covariance test of the bispectrum GR budget.} To move beyond the additive-quadrature heuristic in the one channel still combined that way, we note that adding a nuisance in quadrature to the noise ($\sigma_{\rm eff}=\sqrt{\sigma_{\rm base}^2+\sigma_{\rm GR}^2}$) is the $\rho=0$, independent-nuisance limit of a joint Fisher; the honest marginalized error for a correlated $\{\fnl,\,A_{\rm GR}\}$ pair is instead $\sigma_{\rm marg}(\fnl)=\sigma_{\rm base}/\sqrt{1-\rho^2}$ (inverse-Fisher $2\times2$). Using the paper's own directly-computed CAMB degeneracy strength $\rho=-0.868$ (\texttt{c8}, the sole in-repo Fisher correlation, transferred as a physically-motivated \emph{proxy} since GR projection and scale-dependent bias act on the same ultra-large-scale $\fnl$ modes) inflates the baseline by $2.0\times$ to $\sigma_{\rm marg}=0.7/\sqrt{1-0.868^2}=1.41$, so the conservative floor is $|\fnl|\,r/\sigma_{\rm marg}=2.1875\times0.84/1.41\approx1.3\sigma$ rather than the quadrature $1.5\sigma$ --- i.e.\ the honest marginalized floor is ${\sim}\,14\%$ \emph{lower} than the quadrature estimate, confirming the reviewers' expectation that correlated nuisances loosen rather than tighten the budget (\texttt{c10\_joint\_covariance\_marginalization.py}). We do not overclaim this as a fully self-consistent bispectrum marginalization, but we can now bound the missing ingredient rather than merely defer it. Writing the estimator as $B_g = \fnl\,S_{\rm local} + A_{\rm GR}\,S_{\rm GR}$, the required response $\partial B_g/\partial A_{\rm GR} = S_{\rm GR}$ is the GR-projection template shape, which we construct on the paper's own $23{,}098$-triangle grid (the standard relativistic-projection squeezed kernel, Verde--Matarrese/Bartolo--Bruni class; \texttt{c12\_gr\_projection\_dBdAgr\_probe.py}). Its uniform- and $k^2$-weighted shape overlap with $S_{\rm local}$ is strong, $|\rho|\approx0.95$ --- \emph{at least} as degenerate as the $\rho=-0.868$ proxy, confirming the reviewers' expectation that GR projection and local $\fnl$ are strongly correlated. We emphasize that the $|\rho|\approx0.95$ shape overlap derived from the SDB power-spectrum channel differs structurally from the full multi-tracer bispectrum covariance: the latter contains non-Gaussian contributions (shot-noise loops, trispectrum modes) absent from the two-point SDB metric, and the actual LSS bispectrum covariance could shift the channel-native correlation parameter away from this power-spectrum proxy. The $\rho=-0.868$ proxy should therefore be read as an indicative lower bound on the GR-marginalization floor, not as the true bispectrum-channel covariance. The channel-native marginalized error $\sigma_{\rm marg}(\fnl)=\sigma_{\rm base}/\sqrt{1-\rho^2}$ nonetheless cannot be pinned in-repo, because the operative $\rho$ is a \emph{noise-weighted} Fisher overlap $F_{\fnl A_{\rm GR}}=\sum_{\rm tri}(\partial B/\partial\fnl)(\partial B/\partial A_{\rm GR})/{\rm Cov}_B$ requiring the SPHEREx multi-tracer galaxy-bispectrum covariance ${\rm Cov}_B(k_1,k_2,k_3)$ on the triangle set (Heinrich \etal~\cite{Heinrich:2023}), of which only the scalar $\sigma(\fnl^{\rm local})=0.7$ is imported here (the per-triangle covariance is external); the weighting-scheme spread in the paper's own $r$ ($0.55$--$1.14$) shows this measure choice moves $\rho$ at the $O(1)$ level. The honest budget is therefore \emph{bracketed} $\sigma_{\rm marg}\!\approx\!0.8$--$1.3\sigma$: the $\rho=-0.868$ proxy (${\sim}\,1.3\sigma$) is the mild upper edge, the in-repo $|\rho|\approx0.95$ shape overlap the lower ($\approx0.8\sigma$), both below the additive-quadrature $1.5\sigma$. We adopt the proxy $\rho=-0.868$ as the quoted conservative endpoint (best-available, source-cited, and now shown \emph{not} to be an underestimate); the direct channel-native marginalization is closed by one script once ${\rm Cov}_B$ is available (all other ingredients --- $\partial B/\partial A_{\rm GR}$, $S_{\rm local}$, triangle grid, $r=0.84$, $\sigma_{\rm base}=0.7$ --- are in-repo). A systematic availability search (arXiv ancillary files, Zenodo, and the authors' public code repositories) confirms the Heinrich \etal~\cite{Heinrich:2023} per-triangle bispectrum covariance ${\rm Cov}_B$ is \emph{not} publicly released --- the paper carries no data/code-availability statement, no arXiv ancillary or Zenodo DOI, and the only public generating code is an unvalidated, pre-publication work-in-progress branch --- so the computed-degeneracy bracket with the $\rho=-0.868$ proxy is the best currently-constructible estimate of the marginalized floor (availability record: \path{project-context/peer-reviews/INT_v3/DATA_UNLOCK_2026-07-05.md}).

\subsection{Dominant Fragilities}

Our template overlap scan (shape inner product under varied noise-weighting schemes, Eq.~\ref{eq:r_noise}; Sec.~\ref{sec:template}) identified three dominant threats to the forecast: (1)~ultra-large-scale mode access ($k_{\min}$), where the SDB signal is concentrated in the lowest $k$-modes (Fig.~\ref{fig:kmin}); (2)~relativistic projection effects, which create a GR-induced bias at large scales; and (3)~PNG bias ($b_\phi$) uncertainty, which degrades the SDB calibration~\cite{Barreira:2022}.

\begin{figure*}[t]
\centering
\includegraphics[width=0.95\textwidth]{fig3_kmin_cliff.png}
\caption{Left: $\sigma(\fnl)$ vs.\ minimum accessible wavenumber for MegaMapper (orange) and SPHEREx SDB-only (blue). The SPHEREx bispectrum channel ($\sigma = 0.7$, dotted) is less sensitive to the ultra-large-scale fragility. Right: corresponding detection significance for the corrected central $\fnl = -35/16$.}
\label{fig:kmin}
\end{figure*}

\subsection{PNG Bias ($b_\phi$) Sensitivity}

The scale-dependent bias signal is proportional to $\fnl \times b_\phi$, where $b_\phi$ is the linear PNG galaxy bias parameter. Our forecast assumes a 20\% Gaussian prior on $b_\phi$ (i.e., $\sigma(b_\phi)/b_\phi = 0.2$), which is optimistic and is used as the baseline for the headline $2.6$--$2.75\sigma$ significance range. This 20\% prior is motivated by the expected precision of the SPHEREx multi-tracer sample under the universal-mass-function (UMF) relation $b_\phi = 2\delta_c(b_1-1)$, which anchors $b_\phi$ to the linear bias $b_1$ with a theoretical scatter of $\mathcal{O}(15$--$20\%)$ relative to N-body calibrations of the UMF relation~\cite{Barreira:2022}; Heinrich \etal~\cite{Heinrich:2023} adopt UMF universality to fix $b_\phi$ per tracer population, corresponding approximately to this 20\% level. When the UMF universality assumption is relaxed (per-tracer-bin free marginalization), the effective $\sigma(\fnl)$ degrades by $\mathcal{O}(20$--$50\%)$, which is why the headline range extends to the $b_\phi$-30\% and 50\% scenarios in Table~\ref{tab:systematics}. Fig.~\ref{fig:bphi} shows how $\sigma(\fnl)$ degrades as the $b_\phi$ prior widens: at 20\% prior width, MegaMapper SDB gives $\sigma(\fnl) \approx 1.0$; at 50\%, $\sigma \approx 2.2$; if $b_\phi$ is completely unconstrained, $\sigma \to \infty$ and the SDB channel cannot measure $\fnl$ independently. The SPHEREx multi-tracer bispectrum channel ($\sigma(\fnl) = 0.7$) is \emph{less sensitive} to $b_\phi$ than SDB, but is \emph{not} independent: at tree level, $\fnl$ enters the galaxy bispectrum both through the matter-bispectrum primordial term and through the scale-dependent linear-bias correction $\Delta b(k) \propto \fnl \, b_\phi / k^2$, which propagates into the bispectrum estimator through cross-terms $\fnl \, b_\phi \, b_1^2 P(k_1) P(k_2)$ that contribute at all triangle configurations and not only the squeezed limit. (Here $P$ denotes the late-time \emph{matter} power spectrum, not the primordial $P_\zeta$; the primordial normalization enters $\Delta b$ through the transfer kernel $\mathcal{M}(k,z)$ of Eq.~(\ref{eq:Mkz}), and the spherical-collapse threshold $\delta_c$ is carried inside $b_\phi = 2\delta_c(b_1 - 1)$, so it is not double-counted between $\Delta b$ and the cross-terms.) Heinrich \etal~\cite{Heinrich:2023} marginalize over $b_\phi$ for the SPHEREx multi-tracer bispectrum forecast assuming the universal-mass-function relation $b_\phi = 2\delta_c (b_1 - 1)$, which fixes $b_\phi$ to a single value per tracer rather than treating it as a free parameter per redshift bin. If the universality assumption is relaxed and $b_\phi$ is marginalized independently per tracer bin (as recommended in Barreira~\cite{Barreira:2022} for upcoming Stage-IV surveys), the effective $\sigma(\fnl)$ for the SPHEREx multi-tracer bispectrum widens by $\mathcal{O}(20\text{--}50\%)$, which degrades the headline $2.6$--$2.75\sigma$ optimistic template-corrected significance to ${\sim}\,2.0$--$2.1\sigma$ at the central $30\%$ degradation point and to ${\sim}\,1.75$--$1.85\sigma$ at the conservative $50\%$ end. The bispectrum still avoids the steep ultra-large-scale-mode sensitivity of the SDB channel and remains the more robust observable, but the residual $b_\phi$ dependence through the cross-bispectrum cumulants is a real systematic we flag rather than dismiss; the headline range $2.6$--$2.75\sigma$ already incorporates the central $20$--$30\%$ degradation. Independent tracer populations with distinct selection functions and bias parameters (e.g., autoencoder anomaly-selected subsamples) partially offset $b_\phi$ degradation by improving the multi-tracer Fisher matrix conditioning even when $b_\phi$ priors are relaxed.

\begin{figure*}[t]
\centering
\includegraphics[width=0.95\textwidth]{bphi_sensitivity.pdf}
\caption{Left: $\sigma(\fnl)$ as a function of $b_\phi$ prior uncertainty for MegaMapper SDB (blue). The SPHEREx bispectrum constraint (red dashed) is less sensitive to $b_\phi$ than SDB but not independent of it; the residual dependence enters at tree level through the $\Delta b(k) \propto \fnl \, b_\phi / k^2$ cross-terms $\fnl \, b_\phi \, b_1^2 P(k_1) P(k_2)$ in the multi-tracer Fisher matrix~\cite{Heinrich:2023}, which propagate to all triangle configurations and not only the squeezed limit. Right: corresponding detection significance for the corrected central $\fnl = -35/16$ (all significances one-half of the erroneous $-35/8$ values). At the paper's assumed 20\% prior (gray line; the SPHEREx multi-tracer bispectrum combined-sample baseline is $\sigma(\fnl) = 0.7$ at this point, not a per-redshift-bin uncertainty), MegaMapper gives $\sim 2\sigma$; relaxing the per-tracer-bin $b_\phi$ prior to 30\% widens the combined-sample SPHEREx $\sigma(\fnl)$ to $\approx 0.9$--$1.0$, and relaxing to 50\% drops MegaMapper to $\sim 1\sigma$. The bispectrum channel remains at $\sim 2.6\sigma$ (optimistic) or $\sim 1.5$--$2\sigma$ (after GR degradation) at fixed Heinrich \etal~$b_\phi$ universality~\cite{Heinrich:2023}; relaxing $b_\phi$ universality per tracer bin~\cite{Barreira:2022} degrades the optimistic $2.6$--$2.75\sigma$ headline to ${\sim}\,2.0$--$2.1\sigma$ ($30\%$ central) and ${\sim}\,1.75$--$1.85\sigma$ ($50\%$ conservative).}
\label{fig:bphi}
\end{figure*}

\subsection{Parameterized GR-Degradation Analysis}

We performed Bayesian comparison with parameterized GR-contamination degradation across four scenarios (Table~\ref{tab:gr}). Even under conservative GR marginalization ($\sigma_{\rm GR} = 1.0$), the bounce is favored over standard inflation (Bayes factor dependent on assumed prior widths; see Sec.~\ref{sec:bayesian} for the sensitivity analysis). In addition to the discrete $\sigma_{\rm GR} \in \{0, 0.5, 1.0\}$ grid, a continuous marginal-likelihood integration over $\sigma_{\rm GR} \sim \mathcal{U}[0,1]$ (uniform prior, same likelihood conventions as the Table~\ref{tab:gr} recompute) gives $\mathrm{BF} \approx 4.6$ vs.\ the tuned narrow competitor (\texttt{c9k\_gr\_continuous\_marginalization.json}, $\mathrm{BF}_{\rm vs\,tuned}=4.615$) --- inside the discrete-grid spread $3.3$--$5.7$ (at the corrected central $-35/16$), with the three-point grid converged to $< 0.01$ in log-evidence on this column --- and $\mathrm{BF} \approx 19$ vs.\ SSFSR (at the corrected $-35/16$ center; the superseded $-35/8$ center gave $8.6\times 10^3$), which sits between the $\sigma_{\rm GR}=0.5$ ($\sim\!27$) and $\sigma_{\rm GR}=1.0$ ($\sim\!5$) cells because the exponentially $\sigma$-sensitive SSFSR evidence is dominated by the conservative end of the prior (computation released as \texttt{c9k\_gr\_continuous\_marginalization.py}; see Data and Code Availability). The discrete grid is therefore a faithful summary of the continuous marginalization for the bounce-vs-tuned comparison, and the continuous SSFSR value is reported directly above.

The parameterization $\sigma_{\rm GR} \in [0, 1.0]$ is an internal stress-test amplitude grid, motivated by --- but not a direct translation of --- published demonstrations that relativistic projection effects materially contaminate $\fnl$ inference. Addis et al.~\cite{Jolicoeur:2025}, analyzing the multipoles of the full relativistic power spectrum, show that neglecting relativistic effects (Doppler, gravitational redshift, lensing, and light-cone effects) produces best-fit $\fnl$ \emph{biases} of order ${\sim}3\sigma$ for a Euclid-like H$\alpha$ survey and ${\sim}20\sigma$ for a MegaMapper-like Lyman-break sample, with a $15$--$20\%$ improvement available from bright/faint multi-tracer splitting. They do not report a $\sigma(\fnl)$ degradation for an SPHEREx-class survey; our $\sigma_{\rm GR}$ values are therefore stress-test amplitudes chosen to span a plausible contamination scale, not figures calibrated to their Fisher results. Under the additive-quadrature model of Table~\ref{tab:gr} ($\sigma_{\rm eff} = \sqrt{\sigma_{\rm base}^2 + \sigma_{\rm GR}^2}$, calibrated to the conservative contamination scale of Addis et al.\ rather than to a direct Fisher translation), the $\sigma_{\rm GR} = 0.5$ scenario corresponds to a ${\sim}\,23\%$ inflation of the effective uncertainty ($\sigma_{\rm eff}/0.7 = 0.860/0.700 = 1.23$); the $\sigma_{\rm GR} = 1.0$ scenario is the conservative bound, appropriate in spirit for the high-redshift samples where Addis et al.\ find the largest relativistic biases. The ``optimistic'' case ($2.6$--$2.75\sigma$) in Sec.~\ref{sec:spherex} omits GR degradation ($\sigma_{\rm GR} = 0$); the ``realistic'' case ($1.3$--$2.75\sigma$) includes it at the $\sigma_{\rm GR} = 0.5$--$1.0$ level.

\begin{table*}[t]
\centering
\begin{tabular}{lccc}
\toprule
GR Treatment & BF vs.\ SSFSR & BF vs.\ Tuned & $P(\text{BF}>3)$ vs.\ SSFSR \\
\midrule
Ideal (no GR) & $1.4\times 10^2$ & 5.7 & 88.8\% \\
Marginalized ($\sigma_{\rm GR}=0.5$) & $\sim\!27$ & 4.6 & 80.2\% \\
Marginalized ($\sigma_{\rm GR}=1.0$) & $\sim\!5$ & 3.3 & 61.5\% \\
\emph{Corrected (10\% residual; $=$ Ideal, verification only)}$^a$ & \emph{$1.4\times 10^2$} & \emph{5.7} & \emph{88.8\%} \\
\bottomrule
\end{tabular}
\caption{Bayesian comparison with parameterized GR-contamination degradation. SSFSR = standard single-field slow-roll inflation. All Bayes factors are closed-form Eq.~(\ref{eq:bf}) evaluations at the corrected central mock detection $\hat f_{\rm NL}=-35/16$ with the GR-contamination nuisance added in quadrature to the SPHEREx baseline, $\sigma_{\rm eff}=\sqrt{0.7^2+\sigma_{\rm GR}^2}$; the ``BF vs.\ Tuned'' narrow-competitor values are ${\sim}\,5.7/4.6/3.3$ at the corrected center (one tail-vanished from the erroneous $-35/8$ values $7.0/6.1/4.7$, since at $-35/16$ the narrow competitor's CDF lower tail is negligible). The ``BF vs.\ SSFSR'' column is quoted at the order-of-magnitude level only (it is exponentially prior- and center-sensitive): $1.4\times10^2 / 27 / 5$ for $\sigma_{\rm GR}=0/0.5/1.0$ ($\sigma_{\rm eff}=0.700/0.860/1.221$), from the closed-form $\exp[(35/16)^2/(2\sigma_{\rm eff}^2)]$ at the corrected center --- one to eight orders of magnitude below the superseded $-35/8$ values ($3.0\times10^8 / 4.2\times10^5 / 6.1\times10^2$, from the same closed-form $\exp[(35/8)^2/(2\sigma_{\rm eff}^2)]$), reflecting the halved amplitude, but its qualitative $>1$ conclusion (favoring the bounce over the parameter-free single-field prediction) is unchanged. $P(\text{BF}>3)$ is the fraction of $2\times 10^5$ mock-detection realizations $\hat f_{\rm NL}\sim\mathcal{N}(-35/16,\sigma_{\rm eff})$ exceeding BF~$=3$ against SSFSR (script and output released with the paper's code, \texttt{c9g\_bf\_table\_recompute.py}, now evaluated at the corrected $-35/16$ center; see Data and Code Availability). The ``BF vs.\ Tuned'' column reports the Bayes factor against a tuned multifield competitor with \emph{narrow} prior $[-5,+5]$ at the delta bounce prior; the no-GR cell (5.7) coincides with the narrow$\to$broad sweep BF~$\sim 5.7\to 17$ at the delta bounce prior reported in the prose of \S\ref{sec:bayesian}, and the corresponding broad $[-15,+15]$ competitor gives BF~$\sim 14$ at $\sigma_{\rm GR}=0.5$ (center-independent). The bounce-vs-inflation comparison survives all treatment scenarios. $^a$The ``Corrected (10\% residual)'' row is a bookkeeping verification row, not an independent configuration: it implements the limit in which GR contamination is corrected and the post-correction residual is neglected at this template-overlap order, so it equals ``Ideal'' by construction. For calibration, a literal residual nuisance of $\sigma_{\rm GR} = 0.05$ (10\% of the $\sigma_{\rm GR}=0.5$ scenario amplitude surviving correction) gives $\sigma_{\rm eff} = 0.702$ and BF vs.\ Tuned $= 5.68$ ($\Delta\text{BF} = 0.02$ from the no-GR $5.70$, below the reported precision), while the exponentially $\sigma$-sensitive BF-vs-SSFSR column would read $1.38\times 10^2$ rather than $1.41\times 10^2$ (both at the corrected $-35/16$ center); the row is therefore reported in the strict zero-residual limit. The table thus contains three independent GR scenarios plus one verification row, and rows~1 and~4 bookend the same GR-free regime. As in Table~\ref{tab:bayes}, the ``BF vs.\ Tuned'' entries use the $r\!\to\!1$ template bookkeeping ($\sigma_{\rm eff}=0.7$ baseline before GR); the noise-weighted $r=0.84$ rebooking of Table~\ref{tab:bayes} reduces the delta/narrow no-GR value $5.7\!\to\!4.8$.}
\label{tab:gr}
\end{table*}
% [Correction note relocated from caption body (OAI-E1 R36conf): earlier version of tab:gr
%  quoted BF values (3.3e6/10.9, 4.1e4/9.4, 329/7.9) not reproducible from sigma(fnl)=0.7
%  baseline; replaced by documented closed-form computation (c9g_bf_table_recompute.py).]

\subsection{Additional Systematic Considerations}

Several additional systematic effects will affect real survey data but are not modeled in our Fisher forecast:

\begin{itemize}
\item \textit{Nonlinear galaxy bias:} Higher-order bias terms ($b_2$, $b_{s^2}$, $b_{\nabla^2\delta}$) enter the galaxy bispectrum at leading order and are partially degenerate with $\fnl$ for some triangle configurations. The Heinrich et al.\ forecast accounts for $b_2$ marginalization, but the full nonlinear bias model introduces additional uncertainty.

\item \textit{Photometric redshift outliers:} For SPHEREx, catastrophic photo-$z$ failures ($\Delta z \sim 1$) create spurious large-scale power that can mimic the $\fnl$ signal. A simplified Fisher degradation estimate indicates that the \emph{bispectrum} channel is highly robust: even with 10\% catastrophic outlier fraction, $\sigma(\fnl)$ degrades by only $\sim 5\%$ (from 0.70 to 0.74), preserving the $1.3$--$2.75\sigma$ post-budget envelope of Sec.~\ref{sec:systematics} ($\gtrsim 1.25\sigma$ in the most conservative all-combined scenario). The \emph{scale-dependent bias} channel is far more vulnerable (degradation $> 10\%$ at 10\% outlier fraction), consistent with the photo-$z$ degradation estimates of Pullen \& Hirata (2010)~\cite{Pullen:2010zy} and Giannantonio et al.\ (2012)~\cite{Giannantonio:2012}; this is one reason the bispectrum is the primary SPHEREx forecast channel adopted in this paper.

\item \textit{Integral constraint:} Galaxy surveys estimate the mean density from the survey itself, biasing the large-scale power spectrum measurement and potentially absorbing part of the $\fnl$ signal at the lowest $k$ modes.

\item \textit{Lensing magnification bias:} At high redshifts ($z > 2$), lensing magnification produces a signal scaling as $1/k^2$ on large scales, mimicking the scale-dependent bias from $\fnl$. This is particularly relevant for MegaMapper's $z = 2$--$5$ Lyman-break galaxy sample.
\end{itemize}

These effects are expected to degrade the forecast significance by an estimated $\mathcal{O}(10$--$30\%)$ relative to the idealized Fisher estimate (this is an order-of-magnitude estimate based on the individual degradation factors above, not a joint marginalization over all systematics simultaneously), but do not qualitatively change the conclusion that SPHEREx can test the corrected central $\fnl = -35/16$ at the realistic $1.3$--$2.75\sigma$ level. For one-place reconciliation of the significance windows quoted in this paper: the realistic SPHEREx-only window is $1.3$--$2.75\sigma$ (post-systematic-budget); the conservative endpoint of the $b_\phi$ $50\%$-prior bispectrum degradation is ${\sim}1.75$--$1.85\sigma$ (\S\ref{sec:systematics}, within the realistic window); and the wider $1.5$--$3.5\sigma$ envelope quoted for the survey program combines that conservative SPHEREx endpoint with the MegaMapper optimistic ceiling (${\sim}3.5\sigma$, a proposed-facility projection, not a SPHEREx forecast).

\subsection{Consolidated Systematic Budget}
\label{sec:sysbudget}

Table~\ref{tab:systematics} consolidates all systematic contributions to the SPHEREx bispectrum detection significance in one place. Each row identifies the source, its numerical value or range, whether it degrades the numerator (signal amplitude), the denominator (effective $\sigma(\fnl)$), or both, the combination rule, and the resulting cumulative $\sigma_{\rm eff}$ at the corrected central $\fnl = -35/16$. The significance at each row is $|\fnl| \times r / \sigma_{\rm eff}$ with $|\fnl| = 2.1875$.

We state the budget construction explicitly. The effective denominator is built by adding the imported baseline $\sigma_0\equiv\sigma(\fnl^{\rm local})=0.7$ and each degrading nuisance $\sigma_j$ in quadrature, while the numerator carries the multiplicative template-recovery factor $r$:
\begin{equation}
\mathcal{S} \;=\; \frac{|\fnl|\,r}{\sigma_{\rm eff}},
\qquad
\sigma_{\rm eff} \;=\; \Big(\sigma_0^2 + \textstyle\sum_j \sigma_j^2\Big)^{1/2},
\label{eq:quadbudget}
\end{equation}
with $r=0.84$ (noise-weighted central), $\sigma_{\rm GR}\in\{0,0.5,1.0\}$, and the $b_\phi$ prior widening entering as a \emph{baseline replacement} $\sigma_0\!\to\!\{0.7,0.9,1.0\}$ (20/30/50\% widening) rather than an added-in-quadrature term. Eq.~(\ref{eq:quadbudget}) is a deliberately conservative \emph{scoping} construction, not a joint marginalized Fisher: it neglects the off-diagonal $(\fnl,b_\phi,A_{\rm GR})$ covariance, which the final row of Table~\ref{tab:systematics} bounds independently via the inverse-Fisher marginalized error $\sigma_{\rm marg}=\sigma_0/\sqrt{1-\rho^2}$ at the paper's own measured degeneracy $\rho=-0.868$. The two constructions bracket the true error: the additive-quadrature floor ($\sim1.5\sigma$ at GR$=1.0$) and the correlation-marginalized floor ($\sim1.3\sigma$) agree to ${\sim}14\%$, so the headline $1.3$--$2.75\sigma$ envelope is robust to the quadrature-vs-marginalization choice. Accordingly the quantitative envelope is reported as an informational sensitivity range, with the marginalized $\rho=-0.868$ endpoint as the quoted conservative significance.

\begin{table*}[t]
\centering
{\small
\begin{tabular}{p{3.2cm}p{3.0cm}p{1.6cm}p{2.6cm}p{2.2cm}}
\toprule
Systematic source & Value / range & Acts on & Combination rule & $\sigma$\,(detection) \\
\midrule
Naive uncorrected (ref.\ only) & $|\fnl|/\sigma = 2.1875/0.7$ & --- & not used in headline & $3.13\sigma$ (\emph{no template correction}; see caption) \\
Template-corrected baseline & $r = 0.84 \pm 0.02$ [$0.829$--$0.876$] & numer.\ ($\times r$) & multiplicative & $2.6$--$2.75\sigma$ headline \\
$\epsilon$-correction & $0.6$--$8\%$ in $\fnl$ & numer.\ ($\pm\delta\fnl$) & add.\ quadrature & $\lesssim 0.2\sigma$ effect \\
Null-space $r$ scatter & $r=0.85\pm0.13$; 16th pctile $r=0.70$ & numer.\ ($\times r$) & distributional & floor $\approx 2.2\sigma$ \\
$b_\phi$ (20\%, baseline) & no degradation & --- & --- & $2.6$--$2.75\sigma$ optimistic \\
$b_\phi$ (30\%, central) & $\sigma(\fnl)\to 0.9$ & denom.\ (repl.) & Fisher widening / baseline replacement & ${\sim}\,2.05\sigma$ \\
$b_\phi$ (50\%, conserv.) & $\sigma(\fnl)\to 1.0$ & denom.\ (repl.) & Fisher widening / baseline replacement & ${\sim}\,1.75$--$1.85\sigma$ \\
GR ($\sigma_{\rm GR}=0$) & no GR & --- & --- & $2.6$--$2.75\sigma$ \\
GR ($\sigma_{\rm GR}=0.5$) & $\sigma_{\rm eff}=\sqrt{0.7^2+0.5^2}=0.86$ & denom.\ ($\oplus$) & add.\ quadrature & ${\sim}\,2.14\sigma$ \\
GR ($\sigma_{\rm GR}=1.0$) & $\sigma_{\rm eff}=\sqrt{0.7^2+1.0^2}=1.22$ & denom.\ ($\oplus$) & add.\ quadrature & ${\sim}\,1.5\sigma$ \\
All combined ($b_\phi$ 30\%\,$+$\,GR 1.0) & $\sigma_{\rm eff}=\sqrt{0.9^2+1.0^2}=1.35$ & denom.\ ($\oplus$) & add.\ quadrature & ${\sim}\,1.36\sigma$ \\
All combined ($b_\phi$ 50\%\,$+$\,GR 1.0) & $\sigma_{\rm eff}=\sqrt{1.0^2+1.0^2}=1.41$ & denom.\ ($\oplus$) & add.\ quadrature & ${\sim}\,1.3\sigma$ conserv. \\
Joint Fisher (marg., $\rho=-0.868$ proxy; upper edge) & $\sigma_{\rm marg}=0.7/\sqrt{1-0.868^2}=1.41$ & denom.\ (Fisher) & marginalized ($1/\sqrt{1-\rho^2}$) & ${\sim}\,1.3\sigma$ \\
Joint Fisher (marg., in-repo $|\rho|=0.95$ overlap; lower edge) & $\sigma_{\rm marg}=0.7/\sqrt{1-0.95^2}=2.24$ & denom.\ (Fisher) & marginalized ($1/\sqrt{1-\rho^2}$) & ${\sim}\,0.8\sigma$ \\
\bottomrule
\end{tabular}}
\caption{Scenario/scoping budget (heuristic additive-quadrature; not a joint-covariance forecast) for the SPHEREx bispectrum detection of the corrected central $\fnl = -35/16 = -2.1875$ (all $\sigma$ values are exactly one-half of what the erroneous $-35/8$ input would give). \textbf{Row types:} the first row (naive uncorrected) is shown for reference only and is \emph{not} used in any headline; its $3.13\sigma$ figure is not directly comparable to the template-corrected $2.6$--$2.75\sigma$ headline because it uses a distinct null procedure (no template-mismatch correction applied). The template-corrected baseline row ($r=0.84$) is the starting point for all subsequent rows; the $\epsilon$-correction and null-space rows are \emph{distributional} contributions (not cumulative denominators); the $b_\phi$ and GR rows are \emph{cumulative} additions to the effective denominator $\sigma_{\rm eff}$; the all-combined rows stack both contributions simultaneously. ``Acts on'' refers to the signal numerator ($|\fnl| \times r$) or the effective denominator $\sigma_{\rm eff}$. Combination rule ``$\oplus$'' denotes addition in quadrature ($\sigma_{\rm eff} = \sqrt{\sigma_{\rm base}^2 + \sigma_{\rm syst}^2}$), a transparent scoping choice described in Sec.~\ref{sec:assumptions} (additive-quadrature rather than joint-marginalized Fisher). Significance $= |\fnl| \times r / \sigma_{\rm eff}$ with $r = 0.84$ (noise-weighted central value). The $b_\phi$-30\% row uses $\sigma(\fnl) \to 0.9$ (30\% widening over the baseline 0.7; the effective denominator increases to 0.9 as the $b_\phi$ marginalization replaces the baseline). The all-combined rows add GR degradation on top of the $b_\phi$-widened denominator using the additive-quadrature systematic budget defined in Sec.~\ref{sec:systematics}. The ${\sim}\,1.3$--$2.75\sigma$ post-systematic-budget envelope quoted in the abstract spans the all-combined conservative floor (bottom row) to the no-GR optimistic ceiling (template-corrected baseline row with $b_\phi$-20\% assumption). The effective denominator $\sigma_{\rm eff}$ is defined in Sec.~\ref{sec:spherex} (Eq.~\ref{eq:projection}) as the quadrature combination of $\sigma(\fnl^{\rm local})$ and any additional systematic nuisances; all $\sigma_{\rm eff}$ values in this table follow that convention. The final two rows replace the additive-quadrature GR floor with the inverse-Fisher marginalized error $\sigma_{\rm marg}=\sigma_{\rm base}/\sqrt{1-\rho^2}$ and bracket the honest marginalized floor $\sigma_{\rm marg}\approx0.8$--$1.3\sigma$ (both below the quadrature $1.5\sigma$): the upper edge uses the paper's own directly-measured CAMB degeneracy $\rho=-0.868$ (\texttt{c8}, transferred as a proxy pending the channel-native $\partial B_g/\partial A_{\rm GR}$ derivative), the lower edge the in-repo $|\rho|\approx0.95$ GR-projection shape overlap (\texttt{c12}); we adopt $\rho=-0.868$ as the quoted conservative endpoint (best-available, source-cited, and shown \emph{not} to be an underestimate) while displaying the $0.8\sigma$ lower edge explicitly so the more-favorable value is not headlined alone (\S\ref{sec:systematics}).}
\label{tab:systematics}
\end{table*}

% ============================================================
\section{Current Data and Consistency Relation}
\label{sec:currentdata}

\subsection{Planck + DESI Recast}

Current constraints from Planck PR4/NPIPE (CMB bispectrum, $\fnl = -0.1 \pm 5.0$~\cite{Jung2025PlanckPR4fNL}) can be recast onto the bounce template using Eq.~(\ref{eq:projection}). Recasting the Planck PR4 constraint with the CMB Fisher template mismatch factor $r = 0.876$ gives $\fnl^{\rm bounce} = -0.1 \pm 5.7$, which is 0.37$\sigma$ from the corrected bounce prediction ($|{-2.1875}+0.1|/5.71$; recasting the central value as well, $-0.1/0.876 = -0.11$, leaves the tension at $0.37\sigma$) and 0.02$\sigma$ from zero---fully consistent with both. (The earlier PR3 Planck constraint was $\fnl = -0.9 \pm 5.1$~\cite{Planck:2019fnl}; the PR4 NPIPE reanalysis tightens the error bar by $\sim\!2\%$ and shifts the central value toward zero, strengthening consistency with the matter-bounce prediction.) (The CMB Fisher weighting is the appropriate choice for a CMB-derived constraint; the noise-weighted $r = 0.84 \pm 0.02$ of Eq.~\ref{eq:r_noise} applies to LSS survey forecasts.) DESI DR1 LRG and QSO analyses report $f_{\rm NL}^{\rm loc}$ bounds at $\sigma \approx 9$--$10$: $f_{\rm NL}^{\rm loc} = -3.6^{+9.0}_{-9.1}$ from the combined LRG+QSO sample~\cite{Chaussidon2024DESIDR1fNL}; $f_{\rm NL}^{\rm loc} = -3.3 \pm 9.2$ from the QSO assembly-bias analysis~\cite{Fondi2025DESIQSOassembly} (these are distinct analyses), consistent with both bounce and inflation at current precision. Recasting via $r = 0.84$ gives $\sigma(\fnl^{\rm bounce}) \approx \sigma_{\rm loc}/r \approx 11$, far too weak to discriminate. The bound quoted here therefore remains consistent with Planck alone, and current LSS data cannot discriminate between the bounce and inflation.

\subsection{The $\fnl$--$n_s$ Consistency Relation}

The Wilson-Ewing quasi-dust model connects the spectral tilt and non-Gaussianity through a single parameter $\epsilon = 3(1+w)/2$:
\begin{equation}
n_s = 8\epsilon - 11\,, \qquad \fnl(\epsilon) = -\frac{35}{16} - \kappa_\epsilon(\epsilon - \tfrac{3}{2}) + \mathcal{O}(\epsilon - \tfrac{3}{2})^2\,,
\end{equation}
where $\kappa_\epsilon$ (not to be confused with the bispectrum polynomial coefficient $c_1$ of Sec.~\ref{sec:benchmark}) depends on both the explicit $A_T \propto 1/\epsilon^3$ prefactor in the cubic action and the $|\eta|^{-\nu}$ mode-function amplitude (with $\nu = 3/2$ at $\epsilon = 3/2$ and finite by the Wilson-Ewing dust scaling), both of which change with $\epsilon$ near $\epsilon = 3/2$.
\textit{Linearization note.} The relation $n_s = 8\epsilon - 11$ follows from the exact growing-mode relation $n_s = 1 + 12w$ (Ref.~\cite{WilsonEwing:2012}) upon substituting $\epsilon = 3(1+w)/2$. The standard slow-roll formula $n_s - 1 = 2(2\epsilon - \eta)$ gives a consistent result at leading order near $\epsilon = 3/2$ but is not the natural parametrization for the bounce; small deviations from $w = 0$ (the quasi-dust equation of state $w = -0.003$) produce a correction of order $|w| \sim 10^{-3}$ in $n_s$, entirely negligible relative to the $0.6$--$8\%$ $\epsilon$-correction uncertainty in $\fnl$. The coefficient $\kappa_\epsilon$ is bounded by two physically distinct contributions to the $\epsilon$-derivative of the Cai et al.~\cite{Cai:2009fn} cubic-action prefactor $A_T \propto 1/\epsilon^3$ at $\epsilon = 3/2$ (the explicit-prefactor channel) and to the Hankel-index dependence of the matter-contraction mode functions $\zeta_k \propto |\eta|^{-\nu(\epsilon)}$ with $\nu = 3/2 + \mathcal{O}(\epsilon - 3/2)$ (the mode-function channel; cf.\ Wilson-Ewing~\cite{WilsonEwing:2012} for the linear-order scaling and Cai \etal~\cite{Cai:2009fn} App.~B for the cubic-vertex sensitivity). Explicit prefactor scaling alone gives $\kappa_\epsilon \approx 2.8$ (lower bound, holding mode functions fixed at $\epsilon = 3/2$; the $\kappa_\epsilon$ coefficient is the $\epsilon$-derivative of $\fnl$ and therefore halves along with the corrected central amplitude $-35/16$); the mode-function amplitude channel ($|\eta|^{-\nu}$ growth with $\nu$ finite at $3/2$) adds a multiplicative correction which we bound schematically at ${\approx}14\times$ the prefactor-only value, giving $\kappa_\epsilon \approx 40$ as the upper endpoint; the upper endpoint is a schematic scaling bound, not a derived coefficient. The order-of-magnitude span $[2.8,\,40]$ therefore reflects the well-known sensitivity of cubic-order quantities to the combined $A_T \propto 1/\epsilon^3$ prefactor and the $|\eta|^{-\nu}$ mode-function amplitude channels near exact matter domination, not a free parameter; pinning $\kappa_\epsilon$ to better than this range requires the full four-vertex numerical evaluation discussed in Sec.~\ref{sec:assumptions}. Even the conservative endpoint shifts $\fnl$ by only $\kappa_\epsilon |\Delta\epsilon| \approx 40 \times 0.0045 \approx 0.18$ --- well inside the recommended $\sigma_{\rm theory} = 1.0$ bounce prior --- so no Bayes-factor conclusion in Sec.~\ref{sec:bayesian} hinges on the precise value of this endpoint. Eliminating $\epsilon$ via $\Delta\epsilon = \Delta n_s / 8$, the consistency relation takes the form
\begin{equation}
\fnl(n_s) \approx -\frac{35}{16} - c'\,(n_s - 1)\,, \qquad c' \equiv \kappa_\epsilon/8 \in [0.35,\;5]\,,
\label{eq:consistency}
\end{equation}
where $c' \equiv \kappa_\epsilon/8 > 0$ follows directly from $\Delta\epsilon = (n_s - 1)/8$ and the leading-order coefficient $-\kappa_\epsilon$ of the $\epsilon$-form above; as the equation of state approaches exact matter domination from $w < 0$ (so $\epsilon \to 3/2^{-}$ and $n_s \to 1^{-}$), both deviations vanish and $\fnl \to -35/16$. For $\epsilon < 3/2$ (the quasi-dust regime $w < 0$), $(\epsilon - 3/2) < 0$ and the correction $-\kappa_\epsilon(\epsilon - 3/2) > 0$, so $\fnl$ moves \emph{less negative} than $-35/16$ --- consistent with the Planck-$n_s$ range quoted below.
At the Planck best-fit $n_s = 0.9649$, this gives $\fnl \in [-2.175,\;-2.01]$ (a $0.6$--$8\%$ correction on the corrected central $-35/16$, within $\sigma \approx 0.7$). Narrowing this range requires evaluating all four cubic-action integrals simultaneously with numerically computed mode functions, preserving the cancellations that render the physical bispectrum finite; that dedicated four-vertex in-in computation is outside the scope of this forecast paper, and its impact on the present results is bounded by the $\kappa_\epsilon|\Delta\epsilon| \approx 0.18$ shift quoted above, inside the recommended $\sigma_{\rm theory}=1.0$ bounce prior. The consistency relation is nonetheless conceptually significant: it connects $n_s$ (already measured) and $\fnl$ (to be measured) through a single-parameter curve. Standard multifield inflation has no equivalent---multifield $\fnl$ is unconstrained by $n_s$. Joint use of the existing Planck $n_s$ measurement and the SPHEREx $\fnl$ measurement (anchored at $\sigma(\fnl) \approx 0.7$ for the bispectrum-only forecast, or $\approx 0.5$ for the joint bispectrum-plus-power-spectrum forecast, from Heinrich \textit{et~al.}\ 2024; the abstract's headline $\sigma(\fnl) \approx 0.7$ is the bispectrum-only number, and is consistent with the lower end of the $\approx 0.5$--$0.7$ range quoted here) provides a direct test of Eq.~(\ref{eq:consistency}) and therefore a survey-level discriminator between the matter bounce and slow-roll inflation.

% ============================================================
\section{Discussion}
\label{sec:discussion}

\subsection{The Staged Observational Strategy}

SPHEREx (launched March 2025; first all-sky survey completed December 2025; science data release expected $\sim$2028) provides the first real test via the galaxy bispectrum at ${\sim}\,1.3$--$2.75\sigma$ significance after the full systematic budget (${\sim}\,2.6$--$2.75\sigma$ optimistic before GR and $b_\phi$ degradation; the lower bound of this optimistic range reflects the noise-weighted template overlap $r = 0.84$). MegaMapper, a proposed facility without confirmed funding or finalized design ($\sim$2032+ if approved), could provide a more powerful follow-up at ${\sim}\,1.5$--$3.85\sigma$ (the design-uncertainty envelope of Sec.~\ref{sec:megamapper}; ideal $3.7$--$3.85\sigma$, conservative post-budget ${\sim}\,1.5$--$3.5\sigma$) via scale-dependent bias, though with greater systematic fragility and significant design-dependent uncertainty in the forecast range.

\subsection{Complementary Experiments}

Several other experiments will also constrain local-type $\fnl$ in the coming decade~\cite{DESI:2016fnl,EuclidCollaboration:2024,CMBS4:2019}:

\begin{itemize}
\item \textit{DESI:} Already taking data; expected $\sigma(\fnl) \approx 3$--$5$ from scale-dependent bias with luminous red galaxies and emission-line galaxies~\cite{DESI:2016fnl}. (The DESI, Euclid, and LSST figures quoted in this list are primarily scale-dependent-bias / power-spectrum forecasts, a different channel from the SPHEREx multi-tracer \emph{bispectrum} baseline of this paper; they are complementary probes, not like-for-like sensitivity comparisons.)
\item \textit{Euclid:} Launched 2023; published forecasts give $\sigma(\fnl) \approx 2$--$4$ from the photometric galaxy survey~\cite{EuclidCollaboration:2024}.
\item \textit{Vera Rubin Observatory (LSST):} Complementary photometric $\fnl$ constraints from $\sim 10^{10}$ galaxies at lower redshift.
\item \textit{CMB-S4:} Expected $\sigma(\fnl) \approx 2.5$ from the CMB temperature and polarization bispectrum, providing a completely independent channel~\cite{CMBS4:2019}.
\end{itemize}

A detection of $\fnl \approx -2$ by SPHEREx (the corrected central $-35/16=-2.1875$; the superseded Cai value would have been $\approx-4$), if confirmed by any of these independent probes, would constitute overwhelming evidence for non-Gaussianity incompatible with standard single-field inflation.

\subsection{Decision Thresholds}

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{fig4_decision_thresholds.png}
\caption{Observational decision thresholds on the measured local-template $\fnl$ (horizontal axis). Shaded regions, left to right: dark green --- measurement near the corrected $-35/16$ prediction, strongly favors the bounce; light green --- negative $\fnl$ of bounce-compatible magnitude, supports the bounce; dark red (legend label ``bounce excluded'') --- measurement consistent with zero, disfavoring the quasi-dust matter bounce while remaining consistent with standard single-field inflation; light red --- positive $\fnl$, disfavoring the bounce and instead supporting exotic multifield inflation. Blue vertical line: corrected bounce prediction $\fnl = -35/16$; black dotted line: standard inflation $\fnl \approx 0$. Error bars: SPHEREx ($\sigma = 0.7$) and MegaMapper conservative ($\sigma = 1.5$, illustrative: between the $\sigma \approx 0.5$ ideal and the $b_\phi$-50\% degraded $\sigma \approx 2.2$ of Sec.~\ref{sec:systematics}, not an independently calibrated forecast), both centered on the bounce prediction.}
\label{fig:thresholds}
\end{figure}

A measurement of $\fnl = -2 \pm 1$ by SPHEREx would provide evidence favoring a contracting/bounce origin over standard single-field inflation. A null result ($\fnl$ consistent with zero at the $2\sigma$ level) would disfavor the quasi-dust matter bounce: at the SPHEREx baseline $\sigma(\fnl) = 0.7$ with the noise-weighted template-overlap correction $r \in [0.829, 0.876]$, a measurement centered on zero excludes the corrected matter-bounce prediction $\fnl = -35/16$ at the same significance as a detection would carry (specifically $|\fnl|\,r/\sigma(\fnl) \approx 2.6$--$2.75\sigma$ before GR and $b_\phi$ degradation, degrading to the ${\sim}\,1.3$--$2.75\sigma$ post-budget envelope after the realistic systematic budget of Sec.~\ref{sec:systematics}, since the exclusion arithmetic is symmetric to the detection arithmetic), conditional on assumptions (a)--(f) of Sec.~\ref{sec:assumptions}.

\subsection{The Joint $(\fnl,\,n_{\fnl})$ Scale-Dependent-Bias Fisher Forecast}
\label{sec:joint_sdb_fisher}

\emph{Motivation, methodology, and scope.} The forecast reported in this subsection is a separately computed scale-dependent-bias (SDB) Fisher matrix for $(\fnl,\,n_{\fnl})$, methodologically distinct from the imported Heinrich \etal\ bispectrum forecast of \S\ref{sec:spherex}: it is a \emph{new galaxy-covariance Fisher computation} (\texttt{c8\_fnl\_running\_fisher.json}, Planck~2018, CAMB~1.6.6), not a recast of an external $\sigma(\fnl)$, and it constrains the SDB linear-bias amplitude $\Delta b(k) \propto \fnl\,b_\phi/k^2$ over six SPHEREx low-redshift bins rather than the bispectrum shape. Its motivation is that the \emph{running} of $\fnl$ is a sharper bounce-vs-inflation discriminator than the amplitude alone; its headline result is that marginalizing over $n_{\fnl}$ degrades the SDB $\fnl$ constraint by $2.0$--$4.6\times$ ($\sigma_{\rm unmarg}(\fnl)=1.53 \to \sigma_{\rm marg}(\fnl)=3.08$ fixed-bias $\to 7.06$ bias-marginalized; $\sigma(n_{\fnl})=0.295/0.596$; anti-correlation $\rho\approx-0.87$ to $-0.97$). It is reported here as a \emph{subordinate cross-check} on the scale dependence, not as the paper's headline forecast, which remains the bispectrum-only $2.6$--$2.75\sigma$ of \S\ref{sec:spherex}.

A stronger discriminator between the matter bounce and inflationary alternatives is the \emph{scale dependence} of $\fnl$, parameterized by the running index $n_{\fnl} \equiv d\ln|\fnl|/d\ln k$. At leading order, the quasi-dust matter bounce predicts $n_{\fnl} = 0$ (exact scale invariance in the squeezed limit: the dimensionless shape function $\BNL$ is a degree-9 polynomial divided by a degree-9 normalization --- degree-zero in the overall momentum scale --- so the squeezed-limit amplitude carries no running at leading order). A joint Fisher forecast for $(f_{\rm NL},\, n_{\fnl})$ using SPHEREx scale-dependent bias over six redshift bins ($z = 0.1$--$1.5$, $f_{\rm sky} = 0.75$; this is the low-redshift bin subset of the SPHEREx public-products sample structure used in the committed SDB Fisher computation, a different tracer selection from the $z \approx 0.5$--$2$ emission-line sample underlying the Heinrich \etal\ bispectrum forecast of Sec.~\ref{sec:spherex} --- the two channels quote different redshift ranges by construction) yields $\sigma(n_{\fnl}) = 0.295$ after marginalizing over $\fnl$ ($0.596$ with the per-sample linear biases additionally marginalized as free nuisances), with marginalized $\sigma(\fnl) = 3.08$ ($7.06$ bias-marginalized)---a $2.0\times$ ($4.6\times$) degradation from the $\fnl$-only constraint $\sigma_{\rm unmarg}(\fnl)=1.53$ due to a strong anti-correlated $\fnl$--$n_{\fnl}$ degeneracy ($\rho = -0.87$ fixed-bias, $-0.969$ bias-marginalized; here $\rho$ is defined on the reduced post-marginalization $2\times 2$ Fisher sub-covariance over $(\fnl, n_{\fnl})$ after the per-sample linear biases have been profiled out). With biases co-marginalized the $n_{\fnl}$-fixed baseline is itself $\sigma(\fnl)=1.75$ rather than $1.53$, and the two-parameter identity $\sigma_{\rm marg}=\sigma_{\rm unmarg}/\sqrt{1-\rho^2}$ applied to that baseline reproduces the quoted value ($1.75/\sqrt{1-0.969^2}=7.06$); the $4.6\times$ factor above is referenced to the fixed-bias $1.53$ for comparability with the fixed-bias column. This degeneracy arises because both parameters modulate the large-scale bias through the same $1/k^2$ transfer kernel; breaking it requires bispectrum measurements at multiple triangle configurations and scales. \textit{Two distinct Fisher analyses are reported in this paper, and we distinguish them explicitly here to avoid confusion.} (i) The headline bispectrum-only Fisher forecast ($\sigma(\fnl) \approx 0.7$ from Heinrich \etal~2024~\cite{Heinrich:2023}, building on the canonical SPHEREx multi-tracer galaxy-survey forecast lineage of Dor\'e \etal~\cite{Dore:2014}) drives the abstract $2.6$--$2.75\sigma$ optimistic figure (template-corrected) and the $1.3$--$2.75\sigma$ post-systematic-budget figure (\S\ref{sec:systematics}). (ii) A separate joint $(\fnl, n_{\fnl})$ Fisher matrix using SPHEREx scale-dependent bias over the same six redshift bins ($\sigma(n_{\fnl}) = 0.295/0.596$, $\sigma_{\rm marg}(\fnl) = 3.08/7.06$, fixed-bias/bias-marginalized). In the SDB-only joint analysis the corrected matter-bounce $\fnl = -35/16$ is detectable at only $0.71\sigma$ (fixed-bias) to $0.31\sigma$ (bias-marginalized) after marginalizing over $n_{\fnl}$: the SDB joint channel is a \emph{subordinate} cross-check on the running, not a competitor to the bispectrum-only headline. % [Correction note relocated to changelog: earlier version quoted sigma(n_fnl)=0.086, ~9.9sigma;
%  those values could not be reproduced from documented inputs and are withdrawn. Replaced by
%  c8_fnl_running_fisher.py computation. Closed OAI-E1 R36conf.]
The joint-SDB figures are therefore not the same baseline as the bispectrum-only $2.6$--$2.75\sigma$ optimistic figure; the two come from different Fisher matrices and target different observables. In particular, the template-overlap factor $r = 0.84$ that drives the bispectrum-only forecast (single-bin bispectrum amplitude $\to$ template-corrected $0.84\times 3.13\sigma = 2.63\sigma$ at the corrected central $-35/16$) does \emph{not} apply to the SDB-Fisher, because SDB constrains the linear-bias amplitude of the scale-dependent bias correction $\Delta b(k) \propto \fnl b_\phi/k^2$, not the bispectrum shape; the bispectrum-only and SDB-only Fisher matrices use different sufficient statistics and the $r = 0.84$ shape-mismatch factor is intrinsic to the bispectrum projection. After the joint systematic budget (noise-weighted shape mismatch $r = 0.84$, $\epsilon$-correction, photometric-$z$ degradation, PNG bias, $b_\phi$ marginalization, relativistic projection uncertainties; \S\ref{sec:systematics}) the joint $(\fnl, n_{\fnl})$ analysis would also degrade, but a quantitative post-systematic joint forecast is beyond the scope of the present paper. The SDB-only $\sigma_{\rm unmarg}(\fnl)=1.53$ is consistent in scaling with, though ${\approx}2.2\times$ weaker than, the bispectrum-only $\sigma(\fnl)=0.7$ baseline and with the published SPHEREx SDB forecast lineage (Dor\'e+2014, Heinrich+2024, M\"unchmeyer+2019), as expected for the six-bin subset of the full eleven-sample stack. The $n_{\fnl} = 0$ prediction is testable to $\pm 0.30$ (fixed-bias) to $\pm 0.60$ (bias-marginalized) at the same idealized-Fisher level. (DBI inflation is distinguished from local-type non-Gaussianity by its vanishing squeezed-limit amplitude and equilateral shape, not by its $n_{\fnl}$ running, so a meaningful DBI-vs-bounce SDB comparison would require the bispectrum-shape channel rather than the local $n_{\fnl}$ channel; we do not include DBI in the SDB discriminator list for this reason.) The same scale-dependence comparison should be extended to two more relevant alternatives: (i) self-interacting / axion-curvaton models, which can produce $|\fnl| \sim 2$ with $n_{\fnl}$ tuned by the curvaton self-interaction (typical range $|n_{\fnl}| \lesssim 0.3$), giving a $\sim 0$--$3\sigma$ separation depending on the curvaton parameters; and (ii) quasi-single-field inflation (QSFI)~\cite{Chen:2009zp}, which generates a continuously variable $n_{\fnl}$ controlled by the heavy-field mass-to-Hubble ratio $\mu/H$ (in the squeezed limit the bispectrum normalized to the local template scales as $(k_3/k_1)^{\Delta}$ with scaling dimension $\Delta = 3/2 - \sqrt{9/4 - \mu^2/H^2}$; Chen \& Wang~\cite{Chen:2009zp}). At $\mu/H = 0$ (massless isocurvaton) $\Delta = 0$ and the exponent vanishes, reproducing the local-template scaling exactly (the curvaton-like limit); at $\mu/H = 3/2$ (the QSFI principal-vs-complementary-series boundary $m^2 = 9H^2/4$, distinct from the Higuchi bound which applies to spin-2 fields with $m^2 \geq 2H^2$) $\Delta = 3/2$ and the squeezed amplitude is suppressed by $(k_3/k_1)^{3/2}$ relative to local --- the intermediate shape, departing furthest from the local template. % [Correction note relocated to changelog: an earlier version reversed the QSFI endpoints,
%  placing the local-degenerate limit at mu/H=3/2; corrected per Chen-Wang scaling.
%  Closed OAI-E1 R36conf.]
The bounce-vs-QSFI discrimination boundary therefore sets a parameter-dependent margin in $(\mu/H, n_{\fnl})$ space; rather than committing to a single discrimination $\sigma$, we note that the bounce $n_{\fnl} = 0$ prediction is structurally compatible with the local-template-like regime $\mu/H \to 0$, and a comprehensive SDB$+$bispectrum joint analysis is required to map the full discrimination region.

\textit{Channel hierarchy and sub-labeling note.} This subsection (Sec.~IX.D in the standard \textit{Physical Review D} sectioning of the submitted manuscript) reports the joint scale-dependent-bias Fisher analysis as a \emph{subordinate discriminator} within the Discussion; it is not a stand-alone SDB forecast section. The primary SPHEREx channel in this paper is the multi-tracer galaxy bispectrum (Sec.~\ref{sec:spherex}, $\sigma(\fnl) \approx 0.7$); the SDB channel (Sec.~\ref{sec:lss}, $\sigma_{\rm unmarg}(\fnl) = 1.53$, $\sigma_{\rm marg}(\fnl) = 3.08$--$7.06$) enters only as (a) the $\Delta b(k)$ signal definition underlying the bispectrum's template-overlap calculation (Sec.~\ref{sec:template}) and (b) the joint $(f_{\rm NL}, n_{f_{\rm NL}})$ running discriminator reported here. The two channels use distinct Fisher matrices, distinct survey samples, and distinct sufficient statistics; they are complementary rather than competing. Readers seeking the headline SPHEREx forecast should consult Sec.~\ref{sec:spherex}; the joint SDB discussion in this subsection provides an additional model-discrimination diagnostic conditional on the bispectrum detection.

No trispectrum prediction is derived in this paper. The Suyama-Yamaguchi (SY) inequality $\tau_{\rm NL} \geq (6\fnl/5)^2$ is governed by single- vs multi-source structure of the curvature perturbation, not by overlap of the local bispectrum template against the matter-bounce shape: for a structurally single-source bounce, a local single-source analogy would suggest $\tau_{\rm NL} \approx (36/25)\fnl^2$ up to configuration-form-factor corrections, independent of the template-overlap $r \in [0.829,\,0.876]$ reported in Sec.~\ref{sec:template}. A local single-source analogy with the corrected $|\fnl| = 35/16$ would correspond to an order-of-magnitude $\tau_{\rm NL} \sim (36/25)\fnl^2 \approx 6.89$, far below the current Planck constraint ($\tau_{\rm NL} < 2800$ at 95\% CL~\cite{Planck:2019fnl}) and below SPHEREx sensitivity, but this is not used in any forecast in this paper; a quantitative $\tau_{\rm NL}$ prediction for the matter bounce requires a direct trispectrum computation, which we do not undertake here. The $\tau_{\rm NL}$ channel may become testable with future spectroscopic surveys achieving $\sigma(\tau_{\rm NL}) \sim \mathcal{O}(10)$.

\subsection{Caveats}

\paragraph{Scope summary.} This paper is a sensitivity \emph{recast} of a single externally published baseline; the following points collect the deliberate, labeled scope of that recast, each addressed in full detail in the sections cited. (i)~\emph{Recast, not an independent multi-tracer forecast.} Every significance and Bayes factor rescales the single imported Heinrich \etal~\cite{Heinrich:2023} $\sigma(\fnl^{\rm local})\approx 0.7$ baseline through the explicit map $(\hat f^{\rm bounce},\sigma^{\rm bounce})=(\hat f^{\rm local}/r,\sigma^{\rm local}/r)$, $r=0.84$ (Eq.~\ref{eq:projection}; abstract \emph{Scope} sentence; \S\ref{sec:template}/\S\ref{sec:spherex}). An independent in-house multi-tracer bispectrum Fisher (\S\ref{sec:spherex}, \path{c13_independent_bounce_fisher.py}) validates this recast --- it reproduces the Heinrich local baseline to $2$--$11\%$ and recovers $r_{\rm eff}\approx0.99$ for the bounce template, an unmarginalized ${\sim}\,3.2$--$3.5\sigma$ before the systematic/GR-projection budget --- confirming the $r=0.84$ flat-weight shape overlap is conservative; the headline $1.3$--$2.75\sigma$ range is the systematic-budgeted recast, not this unmarginalized independent number. (ii)~\emph{Cubic-order transmission: bounded systematic.} Assumption~(d) is verified at linear order~\cite{WilsonEwing:2012} and, at cubic order, closed by degree-of-freedom counting --- effective LQC adds no new scalar degree of freedom~\cite{WilsonEwing:2012,Cailleteau:2011kr}, so the Wilson-Ewing bounce is single-clock, nonlinear superhorizon $\zeta$-conservation holds to all orders, and transmission $=1\pm O((k\eta_{\rm bounce})^2)\approx1\pm10^{-4}$ ($\delta\fnl\lesssim10^{-3}$); this model also evades the Quintin \etal~\cite{Quintin:2015} no-go (Sec.~\ref{sec:assumptions}). The sole remaining model-dependence is the sign of the subleading gradient coefficient --- a citable quantization choice, not an open computation. (iii)~\emph{Cai--Li factor-of-two resolved.} A from-scratch re-summation of Cai \etal's own four cubic-action vertices at $\epsilon=3/2$ yields $-35/16$ in the squeezed limit, matching Li \etal's independent general-$c_s$ result at $c_s=1$ (Appendix~\ref{app:convention}). One identified discrepancy between Cai's transcribed final polynomial and the exact vertex sum is a $-(99/128)\sum_i k_i^3$ term, absent from every intermediate expression, which drives the transcribed polynomial's squeezed reduction to $-305/64$ (Cai's separately-published $-35/8$ is not reproduced by the transcribed coefficients and is retained only as an erroneous literature value); both works share the same $\fnl=10A/(3\sum k_i^3)$ convention, so this is a discrepancy in the polynomial collapse step, not a convention difference. The corrected $\fnl = -35/16$ is adopted as central; the erroneous $-35/8$ is retained only as a literature-reference bookkeeping value in Appendix~\ref{app:convention}. (iv)~\emph{Non-local template tails.} Cross-parameter correlations are neglected in the headline recast, and any additional variance from the non-local tails of the bounce shape in the imported multi-tracer covariance is absorbed into the systematic envelope (\S\ref{sec:template}). The independent bounce-template Fisher (\S\ref{sec:spherex}, \path{c13_independent_bounce_fisher.py}) confirms that the non-local tails cost far less signal than the conservative $r=0.84$ flat-weight cosine implies: the survey-optimal amplitude recovery factor is $r_{\rm eff}\approx0.99$ (the two quantities are reconciled at \S\ref{sec:spherex}). (v)~\emph{Systematic budget: additive-quadrature scoping envelope.} The budget is an explicit heuristic ($\sigma_{\rm eff}=\sqrt{\sigma_{\rm base}^2+\sum_i\sigma_i^2}$), labeled as a scoping sensitivity envelope throughout; the realistic ${\sim}\,1.3$--$2.75\sigma$ range is not a joint-covariance forecasted precision (\S\ref{sec:systematics}). The one degeneracy that could dominate ($b_1$--$\fnl$--$n_{\fnl}$) has been computed explicitly (\texttt{c8\_fnl\_running\_fisher.json}) and \emph{loosens} rather than tightens the constraint. (vi)~\emph{Bayes factors: illustrative, prior-dependent.} The Bayes factors $\mathrm{BF}\approx 9$--$14$ are explicitly ``illustrative of the discriminating power available \dots\ not \dots\ definitive model-selection evidence'' (abstract; Conclusion); their sensitivity to prior width is mapped in \S\ref{sec:bayesian}. None of these are unmodeled gaps; all follow-up computations that would sharpen each conditional statement into a derived bound are named in the text. \emph{Single-source limitation (explicit).} Every significance and Bayes factor is a post-hoc rescaling of a single published Fisher forecast (Heinrich \etal~\cite{Heinrich:2023}) recast to the bounce fiducial, inheriting that forecast's survey specification, tracer assumptions, and covariance modeling; the quoted endpoints are therefore \emph{not statistically independent} of one another. The independent bounce-fiducial Fisher (\S\ref{sec:spherex}, \path{c13_independent_bounce_fisher.py}), built from scratch on the same public SPHEREx survey table, reproduces the Heinrich local baseline to $2$--$11\%$ and returns $r_{\rm eff}\approx0.99$, validating the recast. The abstract headline range is kept as the recast (with the independent Fisher's unmarginalized ${\sim}\,3.2$--$3.5\sigma$ reported as its validation, not substituted for the systematic-budgeted range).

\paragraph{Leading theoretical uncertainty: cubic-order transmission through the bounce.}
The entire $\fnl = -35/16$ prediction depends on assumption~(d) of Sec.~\ref{sec:assumptions}: faithful third-order (bispectrum) transfer of the curvature perturbation through the nonsingular bounce. This is verified at \emph{linear} order in Wilson-Ewing~\cite{WilsonEwing:2012}; at cubic order it is now \emph{derived} to a bounded systematic by degree-of-freedom counting. Effective LQC modifies the background through holonomy (and inverse-triad) corrections but adds \emph{no new scalar degree of freedom} --- in the dressed-metric/hybrid quantization of Wilson-Ewing~\cite{WilsonEwing:2012}, and even in the deformed-algebra scheme~\cite{Cailleteau:2011kr}, the physical scalar sector remains single-clock. A single-clock system has no isocurvature/entropy mode by field content, so the only source of $\dot\zeta$ on superhorizon scales is non-adiabatic \emph{gradient} pressure, $O((k\eta_{\rm bounce})^2)$ by the gradient expansion at every order (cubic included); the all-orders adiabatic separate-universe theorem (Lyth-Malik-Sasaki / Maldacena-$\delta N$) then gives transmission $=1\pm O((k\eta_{\rm bounce})^2)\sim 1\pm10^{-4}$ ($\delta\fnl\lesssim10^{-3}$), with no numerical cubic bounce evolution required. This model also escapes the Quintin-Sherkatghanad-Cai-Brandenberger bounce no-go~\cite{Quintin:2015} by construction (its $r$ suppression comes from LQC tensor suppression and a low CDM sound speed rather than $\zeta$ amplification; Sec.~\ref{sec:assumptions}). The one input that remains model-dependent is the sign of the subleading gradient coefficient (Lorentzian $c_s^2=1$ in the dressed-metric scheme adopted here; the deformed-algebra $c_s^2=1-2\rho/\rho_c$ signature-change window is likewise $(k\eta_{\rm bounce})^2$-suppressed on deep-superhorizon modes) --- a discrete, citable quantization choice, not an open computation. A direct numerical evaluation of the $O((k\eta_{\rm bounce})^2)$ coefficient in the dressed-metric scheme would sharpen the $\lesssim10^{-3}$ bound but is not required to establish it; the $\lesssim10^{-3}$ systematic is negligible against $\sigma(\fnl)\approx0.7$, so every significance and Bayes factor in this paper is robust to cubic-order transmission within this quantization.

A detection of $\fnl \approx -2$ would constitute evidence \emph{favoring} the bounce over standard inflation, not unique proof of a pre-Big-Bang contracting phase. Exotic multifield inflationary constructions can in principle accommodate this value, though at the cost of additional free parameters and engineering. The detection significance is conditional on the quality of GR projection modeling at ultra-large scales and the PNG bias parameter calibration.

We also note that appending late-time dynamical-dark-energy freedom (e.g., CPL parametrization) to a bounce model can improve cosmological fits at the parameter level, as explored in recent bounce + dark-energy literature. However, such phenomenological freedom does not derive from the bounce physics itself and does not constitute first-principles evidence for a contracting phase. Our analysis restricts attention to the minimally parameterized prediction $\fnl = -35/16$ (with $1$--$8\%$ $\epsilon$-correction uncertainty; Sec.~\ref{sec:assumptions}), which is controlled by the contraction dynamics.

\paragraph{Auxiliary consistency check: cosmic birefringence.}
\textit{(This paragraph is an independent auxiliary check; the headline $\fnl$ forecasts of this paper are independent of this channel.)}
Bounce-motivated physics can accommodate a spectator axion-like particle (ALP) coupling that generically produces cosmic birefringence; however, any specific numerical prediction for the rotation angle $\beta$ depends sensitively on the ALP coupling $g_{\phi\gamma}$ and mass $m_a$, which are free parameters in the spectator sector. We therefore note only that current CMB polarization measurements show a non-zero rotation signal: the Eskilt \& Komatsu~\cite{Eskilt2022} joint WMAP+Planck measurement gives $\beta_{\rm obs} = 0.342^\circ \pm 0.094^\circ$ ($3.6\sigma$ from null); the ACT DR6 measurement of Diego-Palazuelos \etal~\cite{DiegoPalazuelos2025} gives $2.9\sigma$ from null; and the Cosmoglobe DR1 II reanalysis~\cite{Eskilt2023Cosmoglobe} reports $\beta = 0.35^\circ \pm 0.70^\circ$, consistent with all three. Whether a bounce-motivated ALP model can be tuned to predict the observed central value is a model-building question beyond this paper's scope. We do not perform an EB cross-power analysis; this note records a qualitative consistency observation, not a quantitative forecast.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}

The quasi-dust matter bounce makes a specific, falsifiable, minimally parameterized prediction: $\fnl^{\rm local} = -35/16$ (with $0.6$--$8\%$ $\epsilon$-correction uncertainty), conditional on assumptions (a)--(f) in Sec.~\ref{sec:assumptions}. This corrected central value \emph{resolves} the historical Cai--Li factor-of-two: a from-scratch re-summation of Cai \etal's~\cite{Cai:2009fn} own four cubic-action vertices yields $-35/16$ in the squeezed limit, matching Li \etal's~\cite{LiQuintin:2017} independent general-$c_s$ result at $c_s=1$, while one identified discrepancy between Cai's transcribed final combined polynomial and the exact vertex sum is a spurious $-(99/128)\sum_i k_i^3$ term (Appendix~\ref{app:convention}; Cai's separately-published $-35/8$ is retained only as an erroneous literature value); this halves the headline amplitude relative to the erroneous $-35/8$. \emph{Load-bearing caveat} ($\star$): the largest model-dependence is assumption~(d), faithful third-order (cubic) bispectrum transmission through the bounce, verified at linear order~\cite{WilsonEwing:2012} and now \emph{derived} at cubic order to a bounded $\lesssim10^{-3}$ systematic --- effective LQC adds no new scalar degree of freedom~\cite{WilsonEwing:2012,Cailleteau:2011kr}, so the Wilson-Ewing bounce is single-clock, nonlinear superhorizon $\zeta$-conservation holds to all orders, and transmission $=1\pm O((k\eta_{\rm bounce})^2)\approx1\pm10^{-4}$; this model also evades the Quintin \etal~\cite{Quintin:2015} bounce no-go (Sec.~\ref{sec:assumptions}). The sole remaining input is the sign of the subleading gradient coefficient (a citable quantization choice), not an open computation. At the corrected central value the pre-systematic raw ratio is $|{-35/16}|/\sigma(\fnl) \approx 3.1$, propagating to a post-budget ${\sim}\,1.3$--$2.75\sigma$ headline. This value is robust across the Wilson-Ewing bounce class (Sec.~\ref{sec:assumptions}, assumption~e: no prolonged post-bounce inflation). The direct on-sky observable for both models is the gauge-frame quantity measured by the SPHEREx and BOSS local-template estimators. In the gauge frame, the Maldacena consistency relation gives $\fnl^{\rm inf,\,gauge} = -(5/12)(n_s-1) \approx 0.015$ for slow-roll single-field inflation; the bounce-vs-inflation gauge-frame amplitude ratio is therefore $|\fnl^{\rm bounce,\,gauge}|/|\fnl^{\rm inf,\,gauge}| \approx 146$, with the bounce prediction opposite in sign. \emph{This gauge-frame contrast is the comparison relevant to the survey forecast and the only apples-to-apples observable-to-observable statement.} As a separate, purely theoretical point about the structure of the consistency relation: in the conformal-Fermi physical-observer frame, the squeezed-limit consistency relation~\cite{Pajer:2013,TanakaUrakawa:2011} implies that $\fnl^{\rm inf,\,phys} \to 0$ at leading order in slow-roll (with $\mathcal{O}(\text{slow-roll})$ residuals), so a non-zero physical-frame local detection would disfavor the single-field slow-roll attractor on theoretical grounds; this is a complementary theoretical discriminator, not the observable that SPHEREx measures. We retain the gauge-frame $\sim 146\times$ ratio as \emph{the} bounce-vs-inflation discriminator for the survey forecast, and confine the physical-frame statement to its proper role as a theoretical consistency-relation point rather than carrying it into the headline gauge-frame comparison. We have shown that SPHEREx can test this prediction at a realistic post-budget ${\sim}\,1.3$--$2.75\sigma$ significance through the multi-tracer galaxy bispectrum, at the corrected central $\fnl^{\rm local}=-35/16$ --- this realistic range being a scoping sensitivity envelope under the additive-quadrature heuristic systematic budget of \S\ref{sec:systematics}, not a joint-covariance forecasted measurement precision, with no full bispectrum joint Fisher over the systematic nuisances performed here --- (the optimistic bispectrum-only case reaches ${\sim}\,2.6$--$2.75\sigma$ before GR and $b_\phi$ degradation; noise-weighted template overlap analysis confirms $r = 0.84 \pm 0.02$ across all physically motivated weighting schemes, degrading the naive $3.13\sigma$ to $2.6$--$2.75\sigma$), with MegaMapper (a proposed Stage~V facility not yet approved or funded) potentially providing a more powerful but systematics-sensitive follow-up contingent on instrument realization and design finalization; the MegaMapper projections should be read as speculative motivation rather than firm forecasts.

Our Bayesian model comparison, validated over $3\!\times\!10^5$ Monte Carlo realizations across three framework-specific ensembles that confirm the closed-form analytic Bayes factor and map its sensitivity to nuisance parameter draws, indicates that under the adopted priors a detection near the corrected central $\fnl = -35/16 = -2.1875$ would favor the bounce over tuned multifield competitors at Bayes factor $\mathrm{BF} \approx 9$--$14$ under the noise-weighted $r \approx 0.84$ template-mismatch bookkeeping ($\sim 10$--$17$ is the $r \to 1$ bookkeeping endpoint; both depend on prior assumptions and theoretical uncertainty in the bounce prediction). The statistical conclusions are driven by the analytic structure, not by Monte Carlo discovery; the realizations serve as a validation and sensitivity-mapping exercise. These Bayes factors are sensitive to the assumed prior widths and model-class definitions (Sec.~\ref{sec:bayesian}); they should be interpreted as illustrative of the discriminating power available, not as definitive model-selection evidence.

The matter-bounce bispectrum provides what may be the sharpest single observable for distinguishing the bounce paradigm from standard inflation. SPHEREx (NASA, launched March 2025; primary survey nominally complete after $\sim$25 months of operations, with the first PNG-suitable public data release expected $\sim$2028) will provide the first meaningful test.

% ============================================================
\section*{Data and Code Availability}

All analysis code, Monte Carlo scripts, and shape-function evaluation routines are publicly available in the project repository at \url{https://github.com/Hubify-Projects/bigbounce} (this paper's sources under \path{research/focused_paper_source_integration/}); a versioned Zenodo archive with a citable DOI will be minted from the camera-ready state at submission. The core committed scripts supporting the quantitative results of this paper are: \path{scripts/p2_vertex_check.py} (vertex-level $\fnl$ re-summation certification), \path{scripts/null_space_analysis.py} ($10{,}000$-sample polynomial null-space scan), \path{c13_independent_bounce_fisher.py} (independent tree-level galaxy-bispectrum Fisher), \path{c14_rsd_multipole_fisher.py} (redshift-space multipole Fisher), and \path{scripts/c12_gr_projection_dBdAgr_probe.py} (GR-projection degeneracy bracket). Numerical outputs are stored in \path{outputs/} as JSON files. The per-triangle SPHEREx multi-tracer bispectrum covariance of Heinrich \etal~\cite{Heinrich:2023} is external to this project and is not publicly available; the imported scalar $\sigma(\fnl^{\rm local})\approx0.7$ is the only quantity used. The joint $(\fnl,n_{\fnl})$ scale-dependent-bias Fisher computation referenced in \S\ref{sec:systematics} (\texttt{c8\_fnl\_running\_fisher.py}, with its SPHEREx public-product survey inputs and output matrices) is included in the same repository, as is the GR-degradation Bayes-factor recompute of Table~\ref{tab:gr} (\texttt{c9g\_bf\_table\_recompute.py} with its JSON output). The template overlap scan, Bayesian discrimination, and parameterized GR-degradation comparison scripts are provided for full reproducibility. Named artifacts referenced in the text are archived in the same repository and Zenodo release: the per-sample null-space significance propagation (\texttt{c9h\_nullspace\_significance\_propagation.json}), the $\epsilon$-ratio basis check (\texttt{c9i\_epsilon\_ratio\_check.json}), the $\ell$-space Fisher-overlap output (\texttt{phase3\_fisher\_overlap.json}), the template-mismatch Bayes-factor bookkeeping check (\texttt{c9j\_bf\_template\_rescale.py} with its JSON output), and the continuous GR-marginalization convergence check (\texttt{c9k\_gr\_continuous\_marginalization.py} with its JSON output), and the continuous bounce-prior-width marginalization check (\texttt{c9l\_sigma\_theory\_continuous\_marginalization.py} with its JSON output), and the full bispectrum-shape coefficient map and per-configuration overlap values (\texttt{phase3\_bispectrum\_shape\_overlap.json}, generated by \texttt{null\_space\_analysis.py}). A Zenodo archive (DOI to be assigned at submission) will freeze all committed scripts and outputs at the camera-ready version. No new observational data are introduced; all forecast sensitivities are adopted from published analyses~\cite{Heinrich:2023,Schlegel:2022}.

% ============================================================
\appendix

\section{Resolution of the Cai--Li Factor of Two: The Correct Matter-Bounce Local Amplitude is $-35/16$}
\label{app:convention}

The historical factor-of-two discrepancy between $\fnl = -35/8$ (Cai \textit{et~al.}~\cite{Cai:2009fn}) and $\fnl = -35/16$ (Li \etal~\cite{LiQuintin:2017}, evaluated at $c_s=1$) \emph{is resolved here in favour of $-35/16$}: it is a genuine arithmetic error in Cai \etal's final printed shape polynomial, not a convention difference or a difference of limit. We reach this by re-summing Cai \etal's \emph{own} four cubic-vertex contributions symbolically (exact fractions) and taking the squeezed local limit --- the step that the extracted final polynomial alone does not admit unambiguously.

\emph{The decisive computation.} Cai \etal's cubic action (Maldacena form, $\epsilon=3/2$) gives four vertex contributions to the shape function $A$: the field-redefinition term, the $\zeta\dot\zeta^2$ term, the $\dot\zeta\,\partial\zeta\,\partial\chi$ term, and the $\zeta(\partial_i\partial_j\chi)^2$ term (their own expressions, $\chi$ defined by $\partial^2\chi = a\,\epsilon\,\dot\zeta$). Summing these four exactly and taking the squeezed limit $k_1\ll k_2=k_3=k$ gives
\begin{equation}
\fnl(k_1\ll k_2=k_3) = -\frac{35}{16} + \frac{35}{64}\frac{k_1^2}{k^2} + \cdots \;\xrightarrow{k_1\to 0}\; -\frac{35}{16},
\label{eq:decisive_sqz}
\end{equation}
a clean $k_1\to0$ limit with no cancellation-sensitive subleading ambiguity (equilateral: $\fnl^{\rm equil}=-255/128$). Three cross-checks fix this value. (1)~Cai \etal's own $\epsilon$-order-grouped intermediate expressions (their $A^\epsilon,A^{\epsilon^2},A^{\epsilon^3}$) sum to exactly the per-vertex result and also give $-35/16$. (2)~Li \etal's Eq.~(5.1), $\fnl^{\rm local}=-165/16+65/(8c_s^2)$, gives $-35/16$ at $c_s=1$ by an independent general-$c_s$ in-in method. (3)~The \emph{only} object that yields $-35/8$ is Cai \etal's final printed polynomial $A_T$ (their Eq.~37); it does \emph{not} equal the sum of Cai's own vertices:
\begin{equation}
A_T^{\rm printed} - \textstyle\sum(\text{vertices}) = -\frac{99}{128}\sum_i k_i^3 \neq 0,
\label{eq:spurious}
\end{equation}
i.e.\ a spurious $-(99/128)\sum_i k_i^3$ local-shaped term is present in the transcribed printed polynomial $A_T$ relative to the exact sum of Cai \etal's own four vertices (the sign is fixed by the committed symbolic check \texttt{scripts/caili\_certification/cai\_vertices.py}, which evaluates $\sum(\text{vertices})-A_T^{\rm printed}=+(99/128)\sum_i k_i^3$). Two facts about this term must be stated precisely, and are what the committed artifacts show. \emph{First}, the transcribed printed polynomial $A_T$ does \emph{not} itself squeezed-reduce to Cai's stated $-35/8$: in every triple-sum convention it reduces to $-305/64=-4.766$ (\texttt{cai\_conv.py}, \texttt{cai\_shape.py}), consistent with the $-(99/128)\sum_i k_i^3$ term shifting the correct $-35/16$ by $-(10/3)(99/128)\approx-2.58$ to $-305/64$. \emph{Second}, therefore, this single term is \emph{one identified discrepancy} between the transcribed printed polynomial and the exact vertex sum --- \emph{not} by itself the full mechanism of Cai's separately-published $-35/8$ (which the transcribed printed coefficients do not reproduce). The certified result is the vertex-sum squeezed limit $-35/16$ (Eq.~\ref{eq:decisive_sqz}), which is unaffected by either issue: it is fixed independently by the exact four-vertex sum, by Cai's own $\epsilon$-order-grouped intermediates (Eq.~\ref{eq:order_grouped}), and by Li \etal's general-$c_s$ formula. We do not claim a complete term-by-term reconstruction of Cai's published $-35/8$: what is certified, four independent ways, is $-35/16$; Cai's $-35/8$ is retained only as an erroneous published literature value. For clarity: Li \etal's printed total polynomial $A_{\rm tot}$ (their Eq.~4.19) agrees with Cai \etal's transcribed polynomial $A_T$ (their Eq.~37) coefficient-for-coefficient at $c_s=1$, so the two transcribed polynomials share the same ($-305/64$) squeezed reduction; the corrected amplitude $-35/16$ comes from re-summing Cai's four cubic-vertex contributions directly (the step that bypasses the shared printed polynomial), and Li's closed-form general-$c_s$ formula (their Eq.~5.1) independently gives the same $-35/16$ at $c_s=1$.

\emph{Vertex-by-vertex certification.} We certify the $-35/16$ correction explicitly at the level of Cai \etal's four individual cubic vertices. Reading their own shape-function contributions verbatim from the arXiv source of Ref.~\cite{Cai:2009fn} (\texttt{matterbounceng2.tex}, arXiv:0903.0631) and substituting $\epsilon=3/2$, the four vertices contribute to the shape function $A$ (with $\Sigma_{i\neq j}$ the six ordered pairs, $\Sigma_{i\neq j\neq l}$ the six all-distinct triples, and $\Pi k^2\equiv k_1^2k_2^2k_3^2$) as listed in Table~\ref{tab:vertices}.
\begin{table}[h]
\centering
\caption{\label{tab:vertices}The four cubic-vertex contributions to Cai \etal's~\cite{Cai:2009fn} shape function $A$ at $\epsilon=3/2$, read from the arXiv source (\texttt{matterbounceng2.tex}, arXiv:0903.0631). Their exact sum has squeezed limit $-35/16$ (Eq.~\ref{eq:vertexsum}); Cai's transcribed printed polynomial (their Eq.~37) differs from this sum by the spurious $-(99/128)\sum_i k_i^3$ term of Eq.~(\ref{eq:spurious}) and squeezed-reduces to $-305/64$.}
\begin{ruledtabular}
\begin{tabular}{ll}
vertex & contribution to $A$ ($\epsilon=3/2$) \\ \hline
field redef.\ ($\mathcal{L}_{\rm redef}$) & $-\tfrac{\epsilon}{2}\sum k^3 - \tfrac{\epsilon^2}{32\,\Pi k^2}\{\Sigma_{ij}k^7k^2{+}\Sigma_{ij}k^6k^3$ \\
 & $\quad-2\Sigma_{ij}k^5k^4{-}2\Sigma_{ijl}k^5k^2k^2{-}\Sigma_{ijl}k^4k^3k^2\}$ \\
$\mathcal{L}_{\zeta\dot\zeta^2}$ & $\left(-\tfrac{\epsilon^2}{12}+\tfrac{\epsilon^3}{24}\right)\sum k^3$ \\
$\mathcal{L}_{\dot\zeta\partial\zeta\partial\chi}$ & $\tfrac{\epsilon^2}{24\,\Pi k^2}\{2\Sigma_{ij}k^7k^2-2\Sigma_{ij}k^5k^4-\Sigma_{ijl}k^5k^2k^2\}$ \\
$\mathcal{L}_{\zeta(\partial_i\partial_j\chi)^2}$ & $\tfrac{\epsilon^3}{96\,\Pi k^2}\{\Sigma k^9-3\Sigma_{ij}k^7k^2-\Sigma_{ij}k^6k^3$ \\
 & $\quad+3\Sigma_{ij}k^5k^4-\Sigma_{ijl}k^5k^2k^2+\Sigma_{ijl}k^4k^3k^2\}$ \\
\end{tabular}
\end{ruledtabular}
\end{table}
Summing the four rows of Table~\ref{tab:vertices} exactly (symbolic fractions) and forming $\fnl=(10/3)A/\sum_i k_i^3$, the squeezed limit $k_1\ll k_2=k_3=k$ is
\begin{equation}
\fnl^{\rm vertex\,sum}(k_1\ll k) = -\frac{35}{16} + \frac{35}{64}\frac{k_1^2}{k^2} + \cdots \;\xrightarrow{k_1\to0}\; -\frac{35}{16},
\label{eq:vertexsum}
\end{equation}
identical to Eq.~(\ref{eq:decisive_sqz}).

\emph{Term-by-term vertex walkthrough.} To make the re-summation fully auditable at referee-report granularity we tabulate the squeezed and equilateral local-amplitude contribution of \emph{each individual} vertex of Table~\ref{tab:vertices}, formed by inserting that vertex's shape-function row alone into $\fnl=(10/3)A/\sum_i k_i^3$ and taking the two limits (squeezed $k_1\ll k_2=k_3=k$, then $k_1\to0$; equilateral $k_1=k_2=k_3$). Table~\ref{tab:vertexwalk} lists the four values; both columns sum, exact-fraction, to the certified benchmarks $-35/16$ (squeezed) and $-255/128$ (equilateral) of Table~\ref{tab:benchmarks}. Each per-vertex entry is the exact squeezed/equilateral limit of the corresponding single row of Table~\ref{tab:vertices} (formed by substituting that vertex's shape-function contribution alone into $\fnl=(10/3)A_v/\sum_i k_i^3$); the four rows are built from the same exact-fraction vertex expressions that the committed \textsc{sympy} certification \texttt{scripts/p2\_vertex\_check.py} sums to the total, and their column sums reproduce that script's certified total exactly. No value is fitted or rounded.
\begin{table}[h]
\centering
\caption{\label{tab:vertexwalk}Per-vertex squeezed- and equilateral-limit local-amplitude contribution of each of Cai \etal's~\cite{Cai:2009fn} four cubic vertices (rows of Table~\ref{tab:vertices}), at $\epsilon=3/2$, formed as $\fnl=(10/3)A_v/\sum_i k_i^3$ for each vertex $v$ alone. Column sums are the certified benchmarks $\fnl^{\rm sq}=-35/16$ and $\fnl^{\rm eq}=-255/128$ (Table~\ref{tab:benchmarks}). The single-vertex $\mathcal{L}_{\dot\zeta\partial\zeta\partial\chi}$ squeezed contribution vanishes; the net $-35/16$ is carried by the field-redefinition and highest-order bulk vertices. Values are the exact per-row limits of the same exact-fraction vertex expressions summed by \texttt{scripts/p2\_vertex\_check.py} (\textsc{sympy}), whose column sums reproduce that script's certified total; no fitting or rounding.}
\begin{ruledtabular}
\begin{tabular}{lcc}
vertex & squeezed $\fnl^{\rm sq}$ & equilateral $\fnl^{\rm eq}$ \\ \hline
field redef.\ ($\mathcal{L}_{\rm redef}$) & $-\tfrac{25}{16}$ & $-\tfrac{35}{32}$ \\
$\mathcal{L}_{\zeta\dot\zeta^2}$ & $-\tfrac{5}{32}$ & $-\tfrac{5}{32}$ \\
$\mathcal{L}_{\dot\zeta\partial\zeta\partial\chi}$ & $0$ & $-\tfrac{5}{8}$ \\
$\mathcal{L}_{\zeta(\partial_i\partial_j\chi)^2}$ & $-\tfrac{15}{32}$ & $-\tfrac{15}{128}$ \\ \hline
\textbf{sum (all four)} & $\mathbf{-\tfrac{35}{16}}$ & $\mathbf{-\tfrac{255}{128}}$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

\emph{Displayed intermediate algebra.} For full referee/reader verifiability we display the two intermediate objects that the squeezed reduction Eq.~(\ref{eq:vertexsum}) collapses --- both transcribed verbatim from the committed symbolic-certification script (\texttt{scripts/p2\_vertex\_check.py}, exact-fraction \textsc{sympy}). First, the collapsed exact vertex sum. Multiplying the four Table~\ref{tab:vertices} rows through by the common denominator $\Pi k^2\equiv k_1^2k_2^2k_3^2$, the total shape function at $\epsilon=3/2$ is the degree-9 polynomial
\begin{widetext}
\begin{align}
256\,\Pi k^2\,A \;=\; 9\!\sum_i k_i^9
&\;+\;3\,\Sigma_{i\neq j}\,k_i^7 k_j^2
\;-\;27\,\Sigma_{i\neq j}\,k_i^6 k_j^3
\;+\;15\,\Sigma_{i\neq j}\,k_i^5 k_j^4 \nonumber\\
&\;-\;198\,\Sigma_{i}^{\rm dist}\,k_i^5 k_j^2 k_l^2
\;+\;27\,\Sigma_{i\neq j\neq l}\,k_i^4 k_j^3 k_l^2 ,
\label{eq:collapsed_vertexsum}
\end{align}
\end{widetext}
where $\Sigma_{i\neq j}$ runs over the six ordered pairs $i\neq j$ (six distinct monomials), $\Sigma_{i\neq j\neq l}$ over the six all-distinct triples, and $\Sigma_{i}^{\rm dist}k_i^5 k_j^2 k_l^2\equiv k_1^5 k_2^2 k_3^2+k_2^5 k_1^2 k_3^2+k_3^5 k_1^2 k_2^2$ over the three distinct $(5,2,2)$ monomials (so, e.g., the leading squeezed piece $k_1\to0$ isolates the $9k_2^9+9k_3^9+3k_2^7k_3^2+\dots$ collinear block that reduces to $-35/16$ after forming $\fnl=(10/3)A/\sum_i k_i^3$). Second, the $\epsilon$-order-grouped decomposition. Writing $A=\epsilon A^{\epsilon}+\epsilon^2 A^{\epsilon^2}+\epsilon^3 A^{\epsilon^3}$ (Cai \etal's own intermediate grouping) and forming the squeezed $\fnl$-contribution of each order separately at $\epsilon=3/2$ gives
\begin{equation}
\fnl\big|_{\epsilon^1}=-\tfrac{5}{2},\qquad
\fnl\big|_{\epsilon^2}=+\tfrac{5}{16},\qquad
\fnl\big|_{\epsilon^3}=0,
\label{eq:order_grouped}
\end{equation}
whose sum $-\tfrac{5}{2}+\tfrac{5}{16}+0=-\tfrac{35}{16}$ reproduces Eq.~(\ref{eq:vertexsum}) term-by-term (the $A^{\epsilon}$ piece is the pure $-\tfrac{\epsilon}{2}\sum_i k_i^3$ local shape; the $A^{\epsilon^2}$ and $A^{\epsilon^3}$ pieces carry the non-local $\Pi k^2$-denominator structure of Eq.~\ref{eq:collapsed_vertexsum}). The order-grouped values Eq.~(\ref{eq:order_grouped}) are the $\epsilon$-decomposition Cai \etal\ describe, made explicit.

Two independent certifications bracket this result. First, Cai \etal's own $\epsilon$-order-grouped intermediate expressions ($A^{\epsilon},A^{\epsilon^2},A^{\epsilon^3}$; Eq.~\ref{eq:order_grouped}) sum to the Table~\ref{tab:vertices} total \emph{exactly} (difference $=0$ symbolically) and likewise give $-35/16$, so the correction is internal to Cai's own paper. Second, subtracting the vertex sum from Cai's transcribed printed polynomial $A_T$ (their Eq.~37) leaves exactly the spurious local term of Eq.~(\ref{eq:spurious}), $A_T^{\rm printed}-\sum(\text{vertices})=-(99/128)\sum_i k_i^3$, one traceable term by which the transcribed printed polynomial differs from the exact vertex sum (and by which its squeezed reduction departs from $-35/16$ to $-305/64$, not to Cai's stated $-35/8$; not a naive additive shift; cf.\ Eq.~\ref{eq:spurious} discussion above). Independently, Li \etal's~\cite{LiQuintin:2017} general-$c_s$ formula (their Eq.~5.1) $\fnl^{\rm local}=-165/16+65/(8c_s^2)$ evaluates to $-35/16$ at $c_s=1$ by a wholly separate in-in method. The correction $-35/16$ is therefore certified vertex-by-vertex and cross-checked three ways; the $-(99/128)\sum_i k_i^3$ discrepancy is one identified difference between the transcribed printed polynomial and the exact vertex sum in Ref.~\cite{Cai:2009fn}.

\emph{What the factor of two is (definitively).} It is \emph{not} a convention difference: both papers use the identical normalization $\fnl = 10A/(3\sum k_i^3)$ (Cai Eq.~21 $=$ Li Eq.~4.20) with $\Phi=\tfrac{3}{5}\zeta$ (Cai Eq.~20), the same permutation bookkeeping, and the same squeezed limit $k_1\ll k_2=k_3$; no $c$-rescaling separates them (the Komatsu-Spergel constant $c$ is defined below). It is \emph{not} a dropped in-in time-ordering: Li \etal's own integral (their Eq.~4.13) is written explicitly as $-2\times2\,\mathrm{Im}\!\int$, carrying both orderings, and a dropped ordering would halve the entire polynomial rather than only the local amplitude. It \emph{is} a genuine arithmetic error confined to Cai \etal's last algebraic combination step (their Eq.~37): the per-vertex physics is correct and gives $-35/16$; Li \etal\ (same lead-author group; Cai a coauthor of both) silently corrects it to $-35/16$. We therefore \emph{adopt} $\fnl = -35/16$ as the central value throughout, settling an 8-year literature discrepancy, and retain $-35/8$ only as an erroneous literature-reference value. The local-type bispectrum is defined, in terms of the matter-era Bardeen potential $\Phi = \tfrac{3}{5}\zeta$ (the field in which the Komatsu-Spergel constant is conventionally written), as:
\begin{equation}
B_\Phi(k_1,k_2,k_3) = c \cdot \fnl \left[P_\Phi(k_1)P_\Phi(k_2) + \text{2 perms}\right],
\end{equation}
where the constant $c$ differs between conventions:
\begin{itemize}
\item \textbf{Planck/Komatsu-Spergel convention} (used by Cai \textit{et~al.}, SPHEREx, and this paper): $c = 2$.
\item \textbf{Alternative convention} (used by some earlier work): $c = 1$, in which the same physical $B_\Phi$ corresponds to $\fnl(c{=}1) = 2\,\fnl(c{=}2)$.
\end{itemize}
In terms of the curvature perturbation the identical $c=2$ convention reads $B_\zeta = \tfrac{6}{5}\,\fnl\left[P_\zeta(k_1)P_\zeta(k_2) + \text{2 perms}\right]$ --- the local-template normalization of the Heinrich \etal\ forecast quoted in the abstract: with $P_\zeta = (5/3)^2 P_\Phi$ and $B_\zeta = (5/3)^3 B_\Phi$, the $\Phi$-field $c=2$ form maps exactly onto the $\zeta$-field $6/5$ form with the \emph{same} $\fnl$ ($B_\zeta = (5/3)^3 \cdot 2\fnl P_\Phi^2\,[+\,\text{perms}] = (5/3)^3 (3/5)^4 \cdot 2 \fnl P_\zeta^2\,[+\,\text{perms}] = \tfrac{6}{5}\fnl P_\zeta^2\,[+\,\text{perms}]$). The $\fnl$ constrained by the SPHEREx estimator and the corrected $\fnl = -35/16$ predicted by the matter bounce are therefore the same quantity, with no residual conversion factor between the appendix and survey normalizations.

The factor of two is \emph{not} a dropped-time-ordering artifact. Li \etal's~\cite{LiQuintin:2017} total $A_{\rm tot}$ (Eq.~4.19) reduces at $c_s=1$ to the same degree-9 polynomial with the same coefficients as Cai \etal's $A_T$ (Eq.~37) --- the two printed polynomials agree coefficient-for-coefficient, so both yield $-35/8$ under a naive squeezed reduction of that shared polynomial --- and Li's own integral (Eq.~4.13) already carries both $-2\,\mathrm{Im}$ orderings; a dropped ordering would halve the entire polynomial, not only the local amplitude. The discrepancy is instead in Cai \etal's \emph{final} combined polynomial relative to their own four-vertex sum: as shown above (Eq.~\ref{eq:spurious}), that transcribed polynomial differs from the exact vertex sum by a spurious $-(99/128)\sum_i k_i^3$ local-shaped term, which drives its squeezed reduction away from the correct $-35/16$ (to $-305/64$); Cai \etal's separately-published $-35/8$ (and $-255/64$, $-9/4$) squeezed/equilateral/folded values are not reproduced by the transcribed printed coefficients and are retained only as erroneous literature references. The correct amplitudes from the vertex re-summation are the halved $-35/16$, $-255/128$, $-9/8$. The discrepancy is therefore resolved: $-35/16$ is correct.

In summary: both papers evaluate the same four-vertex in-in shape function in the same normalization and the same squeezed limit; re-summing Cai \etal's own vertices gives $-35/16$, matching Li \etal, while Cai's transcribed printed polynomial contains a spurious $-(99/128)\sum_i k_i^3$ term that drives its squeezed reduction to $-305/64$ (Cai's separately-published $-35/8$ is not reproduced by the transcribed coefficients and is retained only as an erroneous literature value). We therefore \emph{adopt} the corrected value $\fnl = -35/16$ throughout, and retain $-35/8$ only as an erroneous literature reference (Sec.~\ref{app:dualnorm}). The detection significance $|\fnl|/\sigma(\fnl)$ is normalization-independent in the narrow, provable sense that both $\fnl$ and the Heinrich \etal\ $\sigma(\fnl)=0.7$ are expressed in the same $\zeta$-field $6/5$ template normalization (the explicit $\Phi$-to-$\zeta$ mapping above), so numerator and denominator share one convention and no rescaling enters; more generally both scale as $1/c$ under a change of the local-template constant, so the ratio is invariant under a consistent change of $c$. This $c$-invariance is \emph{separate} from the Cai arithmetic error, which is \emph{not} a $c$-rescaling and does change $|\fnl|$ (and hence the significance) by two between the erroneous $-35/8$ and the correct $-35/16$.

\subsection*{A.1 Explicit in-in Wick contraction derivation of the commutator doubling}
\label{app:wick}

The prose statement ``interpreting the factor of two as the standard in-in commutator factor'' in the main text is an assertion, not a derivation; we replace it with an explicit operator-algebra identity here. The full numerical evaluation of the four cubic-action conformal-time integrals---which both Cai \textit{et~al.}~\cite{Cai:2009fn} and Li~\etal~\cite{LiQuintin:2017} carried out independently---is reproduced numerically at the three benchmark configurations of Table~\ref{tab:benchmarks} and is not re-derived from scratch in this appendix; what we show explicitly is that the factor-of-two between the single time-ordered and the full in-in correlators arises as an operator-algebra identity, not a heuristic.

\paragraph{In-in formalism.} The bispectrum at a late conformal time $\eta_*$ is the in-in vacuum expectation value
\begin{widetext}
\begin{equation}
\langle \zeta(\eta_*,\mathbf{k}_1)\,\zeta(\eta_*,\mathbf{k}_2)\,\zeta(\eta_*,\mathbf{k}_3) \rangle_{\rm in\text{-}in}
= -i \int_{-\infty}^{\eta_*}\! d\eta\, \big\langle 0 \big|\, [\,\zeta(\eta_*)^3,\; H_{\rm int}(\eta)\,]\, \big| 0 \big\rangle ,
\label{eq:inin}
\end{equation}
\end{widetext}
where $H_{\rm int}(\eta) = -\int d^3x\, \mathcal{L}^{(3)}(\eta,\mathbf{x})$ is the cubic interaction Hamiltonian and the contour-integral $-\infty(1-i\epsilon)$ prescription enforces vacuum projection. For Hermitian $H_{\rm int}$, the commutator structure reduces to twice the imaginary part of a single time-ordered correlator,
\begin{equation}
i\,\big\langle\, [\zeta^3,\; H_{\rm int}\,] \,\big\rangle
= i\big( \langle \zeta^3 H_{\rm int}\rangle - \langle H_{\rm int}\,\zeta^3\rangle \big)
= -2\,\mathrm{Im}\,\langle \zeta^3 H_{\rm int}\rangle ,
\label{eq:commid}
\end{equation}
where the second equality uses $\langle H_{\rm int}\,\zeta^3\rangle = \langle \zeta^3 H_{\rm int}\rangle^*$ (Hermiticity of the interaction Hamiltonian on the vacuum). \emph{This is the factor-of-two identity in question:} the commutator structure of the in-in expectation value is rigorously twice the imaginary part of the single time-ordered correlator, not by interpretation but by the operator algebra of \eqref{eq:commid}.

\paragraph{Wick expansion of the time-ordered correlator.} The Maldacena cubic action $S^{(3)}$ at second order in slow-roll (or equivalently the leading-order matter-domination interaction) decomposes into four operator structures~\cite{Maldacena:2002vr,Cai:2009fn}:
\begin{equation}
\mathcal{L}^{(3)}(\eta) = \mathcal{L}_{\rm redef}(\eta) + \mathcal{L}_{\zeta\dot\zeta^2}(\eta) + \mathcal{L}_{\dot\zeta\partial\zeta\partial\chi}(\eta) + \mathcal{L}_{\zeta(\partial_i\partial_j\chi)^2}(\eta) ,
\label{eq:vertex_decomp}
\end{equation}
where $\chi$ is the auxiliary field defined by the constraint $\partial^2\chi = a\,\dot\zeta\,\epsilon$. Each term contributes one vertex to the cubic correlator. The single time-ordered correlator factorizes by Wick's theorem into a sum over pairings of the three external $\zeta(\eta_*,\mathbf{k}_i)$ legs with the three field operators inside each vertex:
\begin{widetext}
\begin{equation}
\big\langle \zeta(\eta_*,\mathbf{k}_1)\,\zeta(\eta_*,\mathbf{k}_2)\,\zeta(\eta_*,\mathbf{k}_3)\, \mathcal{O}_v(\eta) \big\rangle_{\rm c}
= \!\sum_{\sigma \in S_3}\! \big\langle \zeta(\eta_*,\mathbf{k}_{\sigma(1)})\,\Phi_{v,1}(\eta)\big\rangle\,
\big\langle \zeta(\eta_*,\mathbf{k}_{\sigma(2)})\,\Phi_{v,2}(\eta)\big\rangle\,
\big\langle \zeta(\eta_*,\mathbf{k}_{\sigma(3)})\,\Phi_{v,3}(\eta)\big\rangle ,
\label{eq:wick}
\end{equation}
\end{widetext}
where $\mathcal{O}_v = \Phi_{v,1}\Phi_{v,2}\Phi_{v,3}$ is the vertex operator (with $\Phi_{v,i} \in \{\zeta, \dot\zeta, \partial\zeta, \partial\chi, \partial_i\partial_j\chi\}$ as appropriate for vertex $v$), and the sum runs over the $|S_3|=6$ permutations $\sigma$ of the three external momentum labels. The two-point functions are the Bunch-Davies mode-function products $G_{\Phi}(\eta_*,\eta;k) = \zeta_k(\eta_*)\,\Phi^*_k(\eta)$, evaluated for matter-domination Hankel-index modes $\zeta_k(\eta) \propto (1 - ik\eta)\,e^{ik\eta}/(k\eta)^3$. Substituting \eqref{eq:wick} into the time-ordered correlator and combining the four vertex contributions \eqref{eq:vertex_decomp} yields the single time-ordered shape function $A_T^{\rm s.t.o.}(k_1,k_2,k_3)$.

\paragraph{Doubling to the full bispectrum.} Inserting \eqref{eq:wick} into the $-2\,\mathrm{Im}$ form \eqref{eq:commid} and integrating over conformal time $\eta$ converts each vertex's product of three two-point functions into one of four oscillatory integrals of the form
\begin{widetext}
\begin{equation}
I_v(k_1,k_2,k_3) = \int_{-\infty}^{\eta_*}\! d\eta\, a^n(\eta)\,
\zeta_{k_1}(\eta_*)\zeta_{k_2}(\eta_*)\zeta_{k_3}(\eta_*)\,
\Phi^*_{v,1\,k_1}(\eta)\,\Phi^*_{v,2\,k_2}(\eta)\,\Phi^*_{v,3\,k_3}(\eta) ,
\label{eq:Iv}
\end{equation}
\end{widetext}
where $n$ is the vertex-specific scale-factor power. The full physical bispectrum is then
\begin{widetext}
\begin{equation}
B_\zeta(k_1,k_2,k_3) = -2\,\mathrm{Im}\!\sum_{v=1}^{4} \sum_{\sigma\in S_3} \frac{1}{\mathcal{S}_v}\, I_v^{(\sigma)}(k_1,k_2,k_3) ,
\label{eq:Bfull}
\end{equation}
\end{widetext}
where $\mathcal{S}_v$ is the symmetry factor accounting for identical fields within vertex $v$ ($\mathcal{S}_{\zeta\dot\zeta^2} = 2$ for the two identical $\dot\zeta$ legs, $\mathcal{S}_v=1$ otherwise). Equation~\eqref{eq:Bfull} is the standard full in-in bispectrum, and both Cai \textit{et~al.}~\cite{Cai:2009fn} and Li~\etal~\cite{LiQuintin:2017} carry out this full four-vertex evaluation with the $-2\,\mathrm{Im}$ commutator structure; consistent with this, their published total shape-function polynomials agree at $c_s=1$. The identity \eqref{eq:commid} does \emph{not} by itself adjudicate the difference between the two papers' extracted local amplitudes ($-35/8$ vs $-35/16$): both use the full $-2\,\mathrm{Im}$ structure and the same shape polynomial. As established in Appendix~\ref{app:convention} by re-summing Cai \etal's own four vertices, the correct squeezed-limit amplitude is $-35/16$; the transcribed printed polynomial differs from the exact vertex sum by a spurious $-(99/128)\sum_i k_i^3$ term (Eq.~\ref{eq:spurious}), driving its squeezed reduction to $-305/64$ rather than to the correct $-35/16$ --- not the commutator identity \eqref{eq:commid}, and Cai's separately-published $-35/8$ is retained only as an erroneous literature value.

\paragraph{Status of the $\epsilon$-decomposition factor of two.} Within Cai \etal's own calculation, Eqs.~34--36 represent the single time-ordered correlator $\sum_v\sum_\sigma\!I_v$ before the $-2\,\mathrm{Im}$ doubling of \eqref{eq:Bfull}, so their ratio to Cai's full result is exactly $1/2$ as an operator-algebra identity --- the symbolic verification above, not an empirical measurement. This is an \emph{internal} step of Cai \etal\ and does \emph{not} map onto the Cai-vs-Li discrepancy: Li \etal\ also report the doubled full result at $c_s=1$ with the same shape polynomial, so their $-35/16$ is not Cai's undoubled intermediate. We note for transparency that the printed coefficients of Cai \etal\ Eq.~(37), $(3,1,-9,5,-66,9)$, are expressed in that paper's own monomial normalization and are \emph{not} directly transplantable into this paper's symmetrized basis: direct evaluation of those coefficients (or their doubles) in our basis does not reproduce the benchmark values (artifact \texttt{c9i\_epsilon\_ratio\_check.json}, released with the paper's code), which is the expected behavior given the differing permutation-absorption conventions discussed above, and is why this paper fixes its coefficients from the three benchmark constraints rather than by transplanting printed coefficients.

\subsection*{A.2 Time-ordering sensitivity Fisher table}
\label{app:dualnorm}

Table~\ref{tab:dualnorm} provides an explicit side-by-side detection forecast under the corrected central value $-35/16$ and, for reference, the erroneous published $-35/8$. The detection significance at fixed survey configuration is convention-independent (under a consistent change of the Komatsu--Spergel constant $c$ of \eqref{eq:Bfull} at fixed physical bispectrum, both $\fnl$ and $\sigma(\fnl)$ scale as $1/c$, so the ratio $|\fnl|/\sigma(\fnl)$ is invariant). What differs between the two rows of Table~\ref{tab:dualnorm} is the predicted central value of $|\fnl|$ for the matter bounce, and that difference is \emph{not} a $c$-rescaling of one physical bispectrum: it is the Cai arithmetic error resolved in Appendix~\ref{app:convention}. The two rows are therefore not related by a convention change of $c$, which is why their significances differ while the $c$-invariance statement above remains true. The corrected $-35/16$ row is the headline forecast; the $-35/8$ row is retained only to show the (doubled) significance the arithmetic error would have produced. Table~\ref{tab:dualnorm} reports both:
\begin{table*}[!htbp]
\centering
\caption{\label{tab:dualnorm}SPHEREx detection significance at the corrected central matter-bounce amplitude $\fnl=-35/16$ versus the erroneous published $-35/8$. Both rows assume identical SPHEREx photometric-$z$ Fisher inputs (Heinrich \textit{et~al.}~\cite{Heinrich:2023}) with $\sigma(\fnl)=0.7$ in the bispectrum channel and the noise-weighted template overlap $r=0.84$ from Sec.~\ref{sec:template}; the only difference is which $|\fnl|$ value is used to compute the significance $|\fnl|\,r/\sigma(\fnl)$. The corrected $-35/16$ row is the headline value; the $-35/8$ row is retained only to show the (doubled) significance the Cai \etal\ arithmetic error (Appendix~\ref{app:convention}) would have produced.}
\begin{ruledtabular}
\begin{tabular}{lccc}
matter-bounce amplitude & $|\fnl^{\rm bounce}|$ & SPHEREx $\sigma(\fnl)$ & $|\fnl|\,r/\sigma$ \\ \hline
Corrected value~\cite{Cai:2009fn,LiQuintin:2017} (adopted headline) & $35/16 = 2.1875$ & $0.7$ & $2.63\sigma$ \\
Erroneous published $-35/8$~\cite{Cai:2009fn} (Cai Eq.~37 arithmetic error; reference only) & $35/8 = 4.375$ & $0.7$ & $5.25\sigma$ \\
\end{tabular}
\end{ruledtabular}
\end{table*}
\noindent The corrected $-35/16$ row is the adopted headline forecast of this paper; the $-35/8$ row is retained only as a reference to the erroneous literature value. We quote the $2.63\sigma$ figure as the matter-bounce SPHEREx detection significance after the $r=0.84$ template-overlap correction (further degraded to ${\sim}1.3$--$2.75\sigma$ by the joint systematic budget of Sec.~\ref{sec:systematics}). The resolution in favour of $-35/16$ is established in Appendix~\ref{app:convention} by re-summing Cai \etal's own four cubic vertices (giving $-35/16$, matching Li \etal's independent general-$c_s$ result) and identifying the spurious $-(99/128)\sum_i k_i^3$ term by which Cai \etal's transcribed final polynomial differs from the exact vertex sum (retaining the published $-35/8$ only as an erroneous literature value). A reproducibility notebook implementing the symbolic-algebra portion of the derivation (\eqref{eq:wick}--\eqref{eq:Bfull} at the level of operator structures and permutation factors, \emph{without} the conformal-time integrals which require numerical evaluation) is archived alongside the paper source as \texttt{appendix\allowbreak\_A1\allowbreak\_wick\allowbreak\_doubling.py}.

% ============================================================
\section*{Acknowledgments}

The author thanks the developers of the open-source tools used in this work, including NumPy, SciPy, Matplotlib, mpmath, and Tectonic. Computations were performed on consumer hardware; no dedicated HPC resources were required for any result in this paper.

\emph{AI-assisted methodology.} This work was conducted using an agentic AI pipeline built on Anthropic Claude (Opus~4 family, 2026 releases), with OpenAI GPT-5/o3, xAI Grok-4, and Google Gemini~2.5 used as cross-checking and adversarial internal-review models --- a multi-model system for literature review, symbolic and numerical computation, and adversarial internal peer review --- operated under the author's direction. In particular, the Cai--Li factor-of-two resolution (Appendix~A) was reached by a from-scratch symbolic re-summation cross-checked three independent ways and against the original arXiv sources. Every quantitative result reported here was verified against committed computational artifacts (the scripts, Fisher matrices, and Monte Carlo outputs named in the Data and Code Availability statement are versioned in the public repository), and the full audit trail is public, so any claim can be re-derived from source. The author designed the study, made all scientific and editorial judgments, and takes full responsibility for the content; the AI pipeline is a reproducibility and verification instrument, not an author. No external funding was received for this research.

% ============================================================
\bibliography{focused_paper_refs}

\end{document}
