# Reproducibility Materials — MOVED

**This directory is deprecated.** The full reproducibility bundle has moved to:

```
../reproducibility/
```

Or on GitHub:
https://github.com/Hubify-Projects/bigbounce/tree/main/reproducibility

The old files (`camb_modifications.diff`, `cobaya_config.yaml`, `params_bestfit.ini`)
have been removed. The cosmological model uses stock CAMB with N_eff as a free
parameter — no custom CAMB modifications are required.

See the new `reproducibility/README.md` for quick-start instructions.
