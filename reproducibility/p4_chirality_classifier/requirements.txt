# Paper 4 chirality classifier reproducibility
#
# Tested on H200 pod with the v2 model checkpoint
# (val_acc = 0.937, 3-class CW/CCW/NOT_SPIRAL).

torch>=2.1,<3.0
torchvision>=0.16,<0.20
timm>=0.9,<1.1
datasets>=2.14,<3.0           # for streaming the GZ DESI catalog
pandas>=2.0,<3.0
pyarrow>=14.0
numpy>=1.24,<2.0
huggingface_hub>=0.20         # for pulling the weights via curl/snapshot
