\documentclass[aps,prd,twocolumn,superscriptaddress,showpacs,preprintnumbers,nofootinbib,longbibliography,floatfix]{revtex4-2}

\usepackage{amsmath,amssymb,bm}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{booktabs}
\hypersetup{
  colorlinks=true,
  linkcolor=blue!60!black,
  citecolor=blue!60!black,
  urlcolor=blue!60!black,
  pdfnewwindow=true,
}

\newcommand{\repoBase}{https://github.com/Hubify-Projects/bigbounce/blob/main}
\newcommand{\artifact}[1]{%
  \href{\repoBase/#1}{\nolinkurl{#1}}%
}

\newcommand{\paperVersion}{v0.1.67-2026-06-11}
% v0.1.67-2026-06-11 (R34conf internal-confirmation closure wave; truth audit at
%   project-context/peer-reviews/R34conf_P5_TRUTH_AUDIT.md.
%   (1) R34-P5-28 PATTERN-051 REGRESSION (highest priority): abstract
%   "three-algorithm DESIVAST robustness ... |Delta f_CW| <~ 0.002 at
%   all three independent void definitions (largest |Delta|=0.0019,
%   V2-REVOLVER)" was inconsistent with the v0.1.66 NM-B GALZONE
%   paragraph showing V2-REVOLVER catalog-native |Delta|=0.0037.
%   FIXED: abstract now states "|Delta f_CW| <= 0.004 across all five
%   Bonferroni-5 void definitions: the three sphere-PIS contrasts
%   give |Delta| <= 0.002 (largest 0.0019, V2-REVOLVER) and the two
%   catalog-native GALZONE contrasts give |Delta| <= 0.0037
%   (V2-REVOLVER catalog-native; |z_Delta|=1.25, p=0.21); all five
%   nominal nulls below the Bonferroni-5 |z|=2.58 threshold."
%   Cell counts cite outputs/30_ext4_galzone_complement_contrasts.json.
%   (2) R34-P5-14/22 (h vs h^-1 Mpc unit conversion): REBUT. REDERIVED:
%   distance D in real units; D[h^-1 Mpc] = D[Mpc]/[h^-1 Mpc] =
%   h*D/[1 Mpc] = h * D[Mpc]. So multiplying the Mpc value by h
%   gives h^-1 Mpc. Sanity: chi(z=0.2) ~ 838 Mpc at Planck18 h=0.6766,
%   * h = 567 h^-1 Mpc ~ 570 h^-1 Mpc, matching the printed value.
%   Source convention is correct; auditor's "divide by h" claim is
%   inverted. No edit; added a one-line derivation footnote at the
%   conversion site to forestall future re-raises.
%   (3) R34-P5-12 Clopper-Pearson notation: tex source already uses
%   $0.05^{1/6}$ with proper braces (l.1839); the auditor saw a PDF-
%   extractor degraded rendering. Added prose qualifier "the one-sided
%   Clopper-Pearson bound 1 - alpha^{1/n} for 0 successes in n trials
%   at level alpha" for clarity.
%   (4) R34-P5-15 4x2 contingency tables: new Appendix B added —
%   tab:contingency_classCWCCW (CW/CCW per V-Web class derived from
%   abstract n_class * f_CW) and tab:contingency_classProgram (class
%   x bright/dark derived from per-class bright fractions). Cell
%   counts allow independent recomputation of chi^2=3.55 (4x2 CW/CCW
%   x class) and chi^2=4932 / Cramer's V=0.078 (class x program).
%   (5) R34-P5-24 sigma_from_half scaling: l.279 "scale as sqrt(n)"
%   already correct in this version; the auditor's "scale as n" claim
%   was based on a stale PDF extract. Verified clean — no edit.
%   (6) R34-P5-04 (16.4 -> 16.36 in abstract): updated. (7) R34-P5-05
%   ("classifier-monopole systematic" qualifier in abstract): added.
%   (8) R34-P5-25 ("clean null" wording): replaced with "consistent
%   with null" where it appeared as a rhetorical claim (preserved in
%   table contexts where it is the established statistical label).
%   (9) R34-P5-26 (void bin 1.64pp vs 1sigma=2.42pp): added the 1-sigma
%   half-width clarification alongside the 2-sigma floor in abstract.
%   (10) R34-P5-29 (Bonferroni threshold disambiguation in abstract):
%   density-quintile sentence now states "below the Bonferroni-5
%   threshold |sigma|~3.09 (alpha=0.01 per quintile)" distinct from
%   the |sigma|~2.58 DESIVAST family threshold.
%   Headline science unchanged; three-algorithm DESIVAST null stands.)
% v0.1.66-2026-06-11 (EXT4 external-round closure wave; truth audit at
%   project-context/peer-reviews/EXT4_P5_TRUTH_AUDIT.md. NM-B (the round's
%   one VERIFIED-substantive finding): the Bonferroni-5 primary family
%   mixed unlike estimands — the two catalog-native GALZONE rows (V2-
%   REVOLVER, V2-VIDE) tabulated only one-sample void f_CW while the
%   declared primary estimand is the two-sample Delta-f_CW contrast.
%   Closed by computing the GALZONE non-void complement (rows that fail
%   the catalog-native void cut OUT=0 ^ ZONE>=0 ^ VOID0>=0) from the
%   committed DESIVAST GALZONE+ZONEVOID join used by artifact 22, and
%   tabulating true two-sample Delta-f_CW / SE / z / p / 95% CI for
%   rows 4-5 of tab:analysis_tree using the same sign convention and
%   binomial-SE formulae as tab:desivast_three_algo. V2-REVOLVER:
%   n_void=104,912 / n_nonvoid=40,877, Delta=-0.0037, SE=0.0029,
%   z=-1.25, p=0.21, 95% CI [-0.0094,+0.0021]. V2-VIDE: n_void=74,111
%   / n_nonvoid=71,678, Delta=+0.0019, SE=0.0026, z=+0.72, p=0.47,
%   95% CI [-0.0033,+0.0070]. Both nominal nulls below the Bonferroni-5
%   threshold |z|=2.58; family-level conclusion unchanged. Artifact:
%   outputs/30_ext4_galzone_complement_contrasts.json. EXT3 items NM3
%   ("BGS-randoms-weighted low-z stress test" rename) and Nm5
%   (Conclusions DESIVAST-first ordering) verified IN-PLACE at v0.1.65
%   under updated line numbers (the EXT3 audit grepped stale lines).
%   No truth-audit edits required for those items.)
% v0.1.65-2026-06-11 (EXT3 external-round closure wave; truth audit at
%   project-context/peer-reviews/EXT3_P5_TRUTH_AUDIT.md. EF4: multiplicity
%   paragraph rewritten to lead with the declared primary estimand
%   Delta-f_CW and its two-sample statistics (|z_Delta|<=1.12, p>=0.26
%   for all three DESIVAST algorithms) before reporting Bonferroni-5; one-
%   sample void fractions now secondary. EF3: ASTRA EDR row in Table II
%   relabeled "supporting diagnostic consistency check (EDR overlap-size
%   caveat)". EF2: descriptive vs inferential language clarified in V-Web
%   per-cell range paragraph and Phase-2 robustness summary. EF1: per-cell
%   label-shuffle null relabeled "Per-cell" (was "Pre-cell"). EF6: RSD-
%   robustness Monte Carlo conclusion narrowed to "Delta f_CW is stable"
%   (void membership counts move by ~34%; only the estimand is stable).
%   EF7: Table VI (DESIVAST canonical) caption notes k=20 KDTree
%   equivalence to k-unbounded rerun at 0.18% level. EF8: declared primary
%   estimand statistics (Delta, SE, z, p, 95% CI) now inline at Table VI.
%   Artifact 29 committed:
%   29_ext3_desivast_footprint_retabulation.json.)
% v0.1.64-2026-06-11 (R31conf micro-restamp. EF5 P0 closed: Table II
%   analysis-tree row "T-Web concurrent-lit & void-class overlap" relabeled
%   "volume-fraction comparison" — §IX.C explicitly states no per-galaxy
%   cross-match against the concurrent T-Web literature is attempted; the
%   comparison is purely on volume fractions, so the old label implied a
%   per-object overlap test that does not exist. R31conf verification:
%   project-context/peer-reviews/R31conf_VERIFICATION.md.)
% v0.1.63-2026-06-10 (EXT2 external-round closure wave; truth audit at
%   project-context/peer-reviews/EXT2_P5_TRUTH_AUDIT.md. EF1: abstract sample
%   ledger rewritten to separate the two parent populations explicitly —
%   DESIVAST primary 56,981 k=20 VoidFinder void spirals from the 678,945
%   z<=0.24 matched-spiral parent (n_lz), vs V-Web secondary 783,820 unique
%   env-labeled spirals (791,635 minus 7,815) — the prior one-breath ledger
%   presented 56,981 against the V-Web parent, a false scope impression.
%   EF4: "incorporates the matched-sample monopole uncertainty by
%   construction" (statistically incorrect — conditioning on the observed CW
%   count FIXES the monopole) -> conditional-on-observed-CW-count framing,
%   with monopole uncertainty propagated separately in the analytic
%   sigma_from_half comparisons. EDR-overlap V-Web concurrence demoted from
%   "strong robustness result" to supporting diagnostic consistency check
%   (25,186-spiral, ~175 deg^2 overlap; strong per-galaxy label
%   disagreement disclosed).)
% v0.1.52-2026-06-09 (R22prov truth-audit closure wave; work order in
%   project-context/peer-reviews/R22prov_P5_TRUTH_AUDIT.md; recompute artifacts
%   outputs/17_v0151_closure_recomputes.json + 18_v0151_stratified_and_density.json):
%   META-E1 headline parent relabeled 791,635 -> 812,793 env-labeled rows
%   (783,820 unique; sec VIII.F mechanism corrected to duplicate survey/program
%   coadd rows, NOT a "relaxed confidence filter"); OAI-E6/E2/m17 bright/dark
%   per-class split recomputed on the declared superset parent (filament
%   bright 394,181 sigma=-2.98 / dark 13,759 sigma=+1.61, |z|=2.1 — the prior
%   416,701/21,203 + 3.4sigma figures came from an unfiltered nearest-label
%   join and are withdrawn); sec XI rewritten with measured per-split values
%   (bright-dark Delta=0.81pp, z=-2.0); OAI-M1 sigma_pred(filament) -3.16 ->
%   -3.32; OAI-E1/T4 Phase-2 sweep recomputed on the declared parent (ranges
%   1.7-4.1pp dominated by the small void bin; old 0.066-0.220pp table was on
%   the unfiltered 5.57M-row join, withdrawn); OAI-E3 Tempel concordance
%   recomputed like-for-like on the 96,753-spiral declared-parent overlap
%   (filament pair 0.29pp; old 110,586/0.026pp withdrawn); META-M6 omnibus
%   chi2 added (canonical p=0.315, z-shell p=0.990); stale P4 -0.12sigma
%   MASTER citation replaced with v1.0.166 re-anchored headline; OAI-M7 KDTree
%   k=20 guard (exact rerun: n_void 56,981->57,081, +0.18%, conclusions
%   invariant); Grok-M3 grid convergence N in {128,384}; META-M2 stratified
%   shuffles; META-M3 parent-tracer density proxy; plus T5-T8 text bundles
%   (p<1e-300, 0.26pp, caption signs, T-Web/V-Web harmonization, sigma
%   non-comparability notes, Jeffreys naming, artifact-path macros, etc.).
% v0.1.50-2026-06-09 (fire-21 P5-META-E1+E2 closure via COMPUTE, not text):
%   new §sec:zshell_robustness — z-shell selection-corrected T-Web rebuild
%   (21 shells, per-shell footprint-only means, canonical grid/R_s/λ_th
%   held fixed). Populations migrate massively (void ×10, wall ×23,
%   cluster −54%) but the environmental-independence null HOLDS and
%   TIGHTENS: cross-class f_CW range 1.98pp → 0.05pp, all classes
%   |σ_mono| ≤ 0.39. Artifact: outputs/16_cosmic_web_zshell_corrected.json.
\newcommand{\paperTimestamp}{June 2026}
% v0.1.62-2026-06-10 (R29 post-EXT1 internal-round closure wave; truth audit at
%   project-context/peer-reviews/R29_P5_TRUTH_AUDIT.md):
%   Wald-shift CORRECTION: M1-vs-M0 env-coefficient shifts were overstated as
%     "<=0.01 SE"; recomputed from 27_ext1_logistic_program_control.json the
%     true per-class shifts are void 0.006 / wall 0.10 / cluster 0.11 sigma-hat
%     — bound restated as <=0.12 SE with the per-class values + artifact shown;
%   Two void-volume-fraction denominators disambiguated: V-Web tidal void class
%     ~24.4% of in-footprint cell volume vs ~0.1% of cells inside a DESIVAST
%     VoidFinder sphere (the old prose implied a single ~0.1% fraction);
%   885-vs-297 occupied-pixels reconciled: fig caption NSIDE=32 (median 4
%     voids/pix) vs body NSIDE=16 stratification (median 14) now cross-noted
%     at both sites;
%   "concentrated entirely"/"confirmed by" causal language softened to
%     "dominated by"/"consistent with" for the BGS-bright tracer-program
%     decomposition of the catalog-wide monopole offset;
%   Cramér's V=0.078 contextualized (small effect; chi2 driven by n=811,609);
%   Rs resolved-cells header restated as Rs >~ cell size = 25.9 Mpc/h with the
%     {25,50} retention rule explicit;
%   residual "catalog-level" -> "catalog-wide monopole offset" sweep completed
%     (cluster-class -4.7sigma site now "catalog-wide-monopole-projected").)
% v0.1.61-2026-06-10 (EXT1 closure wave; work order in
%   project-context/peer-reviews/EXT1_P5_TRUTH_AUDIT.md):
%   F2: DESIVAST interior void counts updated to final ApJ 982,38 values
%     (1,461/420/295 preprint -> 1,489/389/297 final; 420->389 interior
%     V2-REVOLVER, 295->297 V2-VIDE, plus version ledger note);
%   F8: Cramér's V=0.078 and log10(p)≈-1069 added next to chi2=4932
%     in abstract and §VI.A.d;
%   F9+F20: DESIVAST BGS bright/dark program split computed and tabled
%     (tab:desivast_program_split; outputs/28_ext1_desivast_program_split.json);
%     void bright n=56477 σ=-1.56, dark n=469 σ=-1.80 both null;
%   F16: void-bin smallness typo fixed: "small cluster volume fraction of 1%"
%     -> "small void volume fraction (~0.1% of in-footprint cells)";
%   C1/F19/F21: "catalog-level -5sigma headline" -> "catalog-wide monopole
%     offset" at all 2 problematic instances (§VI.A.d and §VIII.maximal HEALPix);
%   F7/F22: analysis-tree table added (tab:analysis_tree) to §V.B enumerating
%     Bonferroni-5 primary family, Bonferroni-9 Phase-2 sweep, and 8
%     descriptive secondary paths;
%   F10/C2: Rs=10 rows in Table VI visually separated and labelled
%     "grid-unresolved, retained for completeness only"; max(resolved 6)=1.64σ
%     now explicitly the primary robustness bound;
%   F25: Logistic regression M0 (env only) and M1 (env + bright program)
%     added to §VI.A.d (joint env Wald p=0.43->0.52 after program control;
%     env coefficients shift ≤0.01SE; outputs/27_ext1_logistic_program_control.json);
%   F11: RSD language softened: "inherit no anisotropic RSD systematic" ->
%     "RSD-bounded at the level of this fixed-void-geometry membership
%     sensitivity test; full immunity would require void-catalog reconstruction";
%   F17: n=6 qualifier "in this six-object illustrative check" added at both
%     occurrences (abstract and §sec:desivast_xmatch);
%   F1: footprint-mask proxy section strengthened: explicit note that the
%     primary DESIVAST void/non-void null is not affected by the HEALPix
%     proxy limitation (both void and non-void are point-in-sphere inside
%     BGS coverage volume).
% --- Preamble changelog (full pre-v0.1.44 history archived to CHANGELOG_pre-v0.1.44.txt) ---
% v0.1.49-2026-06-09 (P5-E3 close + 2 new figures). P5-E3: added shared-provenance
%   explanation for the 0.0066" median cross-match separation (DESI TARGET_RA/DEC
%   and Paper IV coordinates both inherit Legacy Surveys Tractor astrometry, so
%   matched pairs differ only at coordinate-precision level; 1.0" acceptance is
%   therefore conservative; p99=0.30" tail = re-derived coordinates between
%   releases). New figures: fig_z_histogram (matched-sample redshift distribution,
%   §III.D) and fig_cw_vs_z (equivariant CW fraction vs z, §VI.B, consistent with
%   label-shuffle p=0.372 no-z-dependence result).
% v0.1.44-2026-06-03 (R-upgraded-round9 closure bundle, 7 do-now fixes from
%   direct-vendor Gemini-2.5-Pro + Grok-4 + GPT-4o + Perplexity triage on v0.1.43).
%   1. GRO-B1 — title retitled to lead with DESIVAST primary path
%      ("A DESIVAST Three-Algorithm Test on 56,981 Void Spirals with V-Web
%      Cross-Check Across 791,635 DR1 Matched Spirals").
%   2. GRO-B2 — "largest-sample null confirmation" / "strongest" wording softened
%      to "largest controlled-sample environmental-dependence test ... in DESI DR1
%      to date" with single "null is not positive evidence" hedge.
%   3. GEM-M1 + GPT-M3 + GRO-M1 3-way RSD reframing — §Limitations RSD lead now
%      opens with anisotropic tidal-tensor eigenvalue deformation as dominant
%      effect (Hahn/Hoffman/Cautun); scalar σ_v/(aH) is secondary.
%   4. PER-M1 — abstract DESIVAST scope tightened: "DESIVAST provides the void
%      catalog (three-algorithm VoidFinder/V2-REVOLVER/V2-VIDE); DR1 provides
%      the environmental anchor (V-Web tidal classification on 14.6M spectro)".
%   5. GEM-m1 — "any future model" scoped to "the bounce-chirality coupling
%      class described in §sec:intro" (intro + conclusions + §XI.B).
%   6. GEM-n1 — §XI.B retitled "Implications for bounce and inflation models".
%   7. GRO-n1 — 380-line preamble changelog stripped; archived to
%      pipelines/p5_desi_chirality/paper/CHANGELOG_pre-v0.1.44.txt.
% v0.1.43-2026-06-02 (R-upgraded-round8 maintenance — see archived changelog).
% v0.1.42-2026-06-02 (concurrent-literature DR1/EDR cross-checks consolidated).
% v0.1.41-2026-06-02 (toy-EFT appendix gauge/rotational caveat — GEM-B1 close).
% v0.1.40-2026-06-02 (R-upgraded-round3: 6 fixes — toy EFT caveat, RSD-immunity,
%   "strongest" → "largest-sample null", RSD-bound rephrase, bib trim, preprint
%   wording).
% v0.1.39-2026-06-02 (R-upgraded-postretro: title elevation, DESIVAST primary-
%   path subsection, RSD anisotropy caveat, BGS/dark tracer decomposition).
% --- Pre-v0.1.39 full history in pipelines/p5_desi_chirality/paper/CHANGELOG_pre-v0.1.44.txt ---

\begin{document}

\title{Environmental Dependence of Spiral Chirality: A DESIVAST Three-Algorithm Test on 56{,}981 Void Spirals with T-Web (Hahn 2007) Tidal-Tensor Cross-Check Across 791{,}635 DR1 Matched Spirals\protect\footnote{See \S\ref{sec:env_finder} for the implementation note on the T-Web/V-Web nomenclature: the tidal-tensor classifier used here implements the Hahn 2007 T-Web recipe ($\phi_k = -\delta_k/k^2$ then $T_{ij} = -k_i k_j \phi_k$), not the Hoffman 2012 V-Web (velocity-shear). Earlier preprint versions used ``V-Web'' in the title for backward-compatibility with the HF dataset slug and the GitHub release tag; the title is updated to ``T-Web'' for accuracy.}}

\author{Houston Golden}
\email{houston@hubify.com}
\affiliation{Independent Researcher, Los Angeles, California, USA}

\date{\paperTimestamp\ --- \paperVersion}

\begin{abstract}
We cross-match the 8{,}474{,}531-galaxy chirality catalog of Paper~IV
\cite{golden_chirality_2026} (companion work, not yet peer-reviewed)
with the DESI Data Release~1 redshift
catalog ($16.36 \times 10^6$ \texttt{ZWARN=0} input rows) to test whether
spiral galaxy handedness is statistically independent of large-scale
structure environment. \emph{Scope of the two DR1 inputs:} DESIVAST~\cite{DESIVAST2025} provides the
\emph{void catalog} (three-algorithm: VoidFinder sphere-growing,
V2-REVOLVER and V2-VIDE watershed); DESI DR1 redshifts provide the
\emph{environmental anchor} through which we run a V-Web tidal
classification on $14{,}622{,}283$ DR1 spectroscopic galaxies to
assign every matched spiral to a cosmic-web class. The primary path
of this paper is the DESIVAST-anchored void cross-check; the V-Web
classification is the supporting cross-check across the full matched
sample. The $1''$ matched catalog contains
$2{,}232{,}212$ unique galaxies; of these, $791{,}635$ carry an
unambiguous post-TTA (test-time-augmentation) equivariant CW or CCW label and form the
chirality-relevant subsample. We classify each matched spiral into one
of four cosmic-web classes \{void, wall, filament, cluster\} by running
a tidal-tensor cosmic-web classifier (Hahn~\textit{et~al.}~2007
\cite{Hahn2007}; Cautun~\textit{et~al.}~2014 \cite{Cautun2014})
\footnote{We use the tidal-tensor formulation
$T_{ij} = \partial^2\Phi/\partial x_i\partial x_j$ with $\Phi$ from
Poisson's equation on the smoothed overdensity field (the Hahn~2007
recipe, sometimes called the T-Web variant), classifying by the count
of eigenvalues exceeding $\lambda_{\rm th}$. The Hoffman~\textit{et~al.}~2012
\cite{Hoffman2012} velocity-shear V-Web requires a separate velocity
reconstruction not used here; for backward compatibility with prior
analyses we retain the ``V-Web'' label which is sometimes used loosely
for the family of tidal-field classifiers.} on the full
$14{,}622{,}283$-galaxy DESI DR1 spectro sample on a $256^3$ comoving
grid with a $25\,{\rm Mpc}/h$ Gaussian smoothing and eigenvalue
threshold $\lambda_{\rm th}\!=\!0$. Sample ledger: \emph{two distinct parent populations are used.}
(1)~\textbf{DESIVAST primary:} $56{,}981$ k\,=\,20 VoidFinder void spirals
drawn from $678{,}945$ z\,$\leq$\,$0.24$ matched spirals (the DESIVAST BGS
coverage range; $n_{\rm lz} = 678{,}945$).
(2)~\textbf{V-Web secondary:} $783{,}820$ unique chirality-relevant matched spirals
with an environment row ($791{,}635$ minus $7{,}815$ without), carried on
$812{,}793$ environment-labeled survey--program coadd rows.
The $56{,}981$ void spirals are drawn from the DESIVAST low-$z$ parent
($678{,}945$), not from the full V-Web parent ($812{,}793$).
\textbf{Headline result:} the CW
fraction shows no environment dependence beyond (i) the known
Paper~IV catalog-wide classifier-monopole systematic of $\approx\!0.26$\,pp
(percentage points; a correctable classifier systematic --- not a
statistical limit --- that dominates the V-Web filament/cluster
residuals at $n \gtrsim 4 \times 10^5$ and is subtracted explicitly
in the monopole-referenced tests) and (ii) the counting-statistics
floor of $\pm 4.8$\,pp
(the $2\sigma$ binomial half-width of the $n = 428$ V-Web void bin,
whose observed $1.64$\,pp offset is well inside the corresponding $1\sigma$
half-width of $\pm 2.42$\,pp; $-0.68\sigma_{\rm from\,half}$), within DESI DR1 at V-Web resolution.
Per-class CW fractions on the $812{,}793$
env-labeled spiral rows (covering $783{,}820$ of the $791{,}635$
unique chirality-relevant matched spirals, counted once per repeat
DR1 survey--program coadd row carried by the environment table;
$7{,}815$ matched spirals lack an environment row,
\S\ref{sec:desivast_p4_residual})
are, in order of decreasing $n$:
$0.4980$ (filament; $n\!=\!408{,}187$, $-2.61\sigma$),
$0.4963$ (cluster; $n\!=\!397{,}505$, $-4.66\sigma$),
$0.5034$ (wall; $n\!=\!6{,}673$, $+0.55\sigma$), and
$0.4836$ (void; $n\!=\!428$, $-0.68\sigma$ — survey-edge
artifact dominated at $z\!\lesssim\!0.24$, see DESIVAST-anchored
re-projection below). The quoted $\sigma_{\rm from\,half}$ values
scale as $\sqrt{n}$ at fixed fractional offset and are therefore not
mutually comparable across classes of different $n$. The range across
classes is
$1.98$\,percentage points, an omnibus $4\times2$ homogeneity test
across the four classes is null ($\chi^2 = 3.55$, 3~d.o.f.,
$p = 0.31$ on the row-level parent; $\chi^2 = 3.00$, $p = 0.39$ on
the $783{,}820$ unique-spiral subset, so the $2.7\%$ duplicate rows
do not drive the verdict), and the negative $\sigma$ values
in filament and cluster track the catalog-wide
$\Delta f_{\rm CW}\!=\!-0.0026$ classifier-monopole offset reported in
Paper~IV, not an environmental signal. A Phase~2 sensitivity sweep
across nine cells $\{R_s,\lambda_{\rm th}\} \in \{10, 25,
50\}\,{\rm Mpc}/h \times \{0.0, 0.1, 0.3\}$ confirms the result: the
per-cell cross-class range of CW fractions ($1.7$--$4.1$\,percentage
points) is dominated by the counting noise of the small void bin
($n = 363$--$853$ per cell), the per-cell label-shuffle look-elsewhere
$p$-values span $0.13$--$0.56$ (no cell below $0.05$), and the
headline sign-pattern
(filament and cluster at the catalog-mean offset, void and wall
uninformative at $|\sigma| \lesssim 3$) is invariant under the
smoothing scale and threshold choices (the $R_s = 10\,{\rm Mpc}/h$
cells sit below the $25.9\,{\rm Mpc}/h$ grid resolution and are
retained only as a degenerate near-unsmoothed limit, \S\ref{sec:phase2}). We also report null tests in
redshift (label-shuffle $p\!=\!0.372$), projected $k\!=\!5$ NN density
($|\sigma|_{\max}\!=\!3.94$ across density quintiles, pre-monopole-
subtraction; the corresponding monopole-subtracted residual is
$|\sigma_{\rm obs}-\sigma_{\rm pred}|\!=\!1.87$, below the
Bonferroni-5 quintile threshold $|\sigma|\!\approx\!3.09$
($\alpha\!=\!0.01$ per quintile, distinct from the
$|\sigma|\!\approx\!2.58$ DESIVAST-primary family threshold at $\alpha\!=\!0.05$)),
and sky-position
(HEALPix scans at NSIDE $\in \{16, 32, 64\}$ with label-shuffle nulls
$p\!=\!0.61/0.135/0.413$): none reach $3\sigma$ after look-elsewhere
correction. We interpret this as no evidence for environment-dependent
chirality beyond the catalog-monopole offset at current sensitivity;
the V-Web void class at $z\!\lesssim\!0.24$ is sample-size limited at
$n\!=\!428$ chirality-relevant spirals and dominated by survey-edge
artifacts (see \S\ref{sec:tweb_concurrent}), so the controlling void
constraint comes from the DESIVAST-anchored re-projection
($n\!=\!56{,}981$, $\Delta f_{\rm CW}\!=\!0.0007$) rather than the
V-Web void label.

\textbf{Robustness.} We strengthen the headline with the Tempel
\textit{et~al.}~2014~\cite{Tempel2014} friends-of-friends group
classifier as a supporting cross-survey consistency check
(different parent catalog, SDSS DR10, only $\sim\!12$k galaxies in
the filament-like bin, richness-selection rather than tidal-tensor,
approximate richness-to-tidal mapping; like-for-like filament-class
concordance on the common overlap sample
$0.29$\,pp, within counting statistics; supporting rather than load-bearing:
the primary robustness evidence is the on-DESI DESIVAST
cross-classifier and Phase 2 V-Web sensitivity analyses) and four DESIVAST-anchored
re-projections of the V-Web null on a $\sim\!130\times$ larger void
sample (methodologically correlated by construction because they
reuse the same matched-spiral subsample, but spanning the
VoidFinder sphere-growing vs.\ ZOBOV watershed algorithmic axes):
(i) re-running the chirality analysis with DESIVAST-defined voids
\emph{as the classifier} (rather than V-Web) on
$n_{\rm void}^{\rm DESIVAST} = 56{,}981$ matched spirals
($\sim\!130\times$ the V-Web void sample size, supplemented by an
$n=6$ per-galaxy classifier-disagreement check --- in this
six-object illustrative check, $0/6$ V-Web ``void''
spirals fall inside any of the $101{,}863$ DESIVAST VoidFinder holes
at $z \leq 0.24$; the $n=6$ sample is too small for a formal purity
constraint, but it illustrates the survey-shell systematic driving the
V-Web void class at low $z$) returns $f_{\rm CW}^{\rm void} = 0.4964$ vs
$f_{\rm CW}^{\rm non-void} = 0.4971$, $\Delta f_{\rm CW} \equiv
f_{\rm CW}^{\rm non\mbox{-}void} - f_{\rm CW}^{\rm void} = +0.0007$,
statistically indistinguishable; (ii)
three-algorithm DESIVAST robustness (VoidFinder + V2-REVOLVER + V2-VIDE)
returns $|\Delta f_{\rm CW}| \le 0.004$ across all five Bonferroni-5 void
definitions: the three sphere-PIS contrasts (VoidFinder, V2-REVOLVER,
V2-VIDE on the catalog point-in-sphere construction) give
$|\Delta f_{\rm CW}| \le 0.002$ (largest $|\Delta| = 0.0019$, V2-REVOLVER,
${\sim}1.2\sigma$ of the $n_{\rm void}$ counting floor
$1/(2\sqrt{n}) \approx 0.0016$), and the two catalog-native GALZONE
contrasts give $|\Delta f_{\rm CW}| \le 0.0037$ (V2-REVOLVER catalog-native
$\Delta = -0.0037$, $|z_\Delta| = 1.25$, $p = 0.21$ on $n_{\rm void} = 104{,}912$
vs.\ $n_{\rm non\mbox{-}void} = 40{,}877$; V2-VIDE catalog-native
$\Delta = +0.0019$, $|z_\Delta| = 0.72$, $p = 0.47$); all five nominal
nulls remain below the Bonferroni-5 threshold $|\sigma| = 2.58$
(\artifact{pipelines/p5\_desi\_chirality/outputs/30\_ext4\_galzone\_complement\_contrasts.json});
(iii) HEALPix sky-position stratification by maximal-void density per
pixel finds the $-5\sigma$ catalog-wide monopole offset concentrated entirely
in the ``0 maximal voids per pixel'' bin (sky regions outside DESIVAST
coverage), with pixels carrying $\geq 1$ maximal void returning
$\sigma \in [-2.04, -0.09]$; and (iv) the per-pixel Pearson correlation
between maximal-void density and chirality $\sigma$ at NSIDE $= 32$
across $n = 727$ valid pixels is $r = +0.006$ ($p = 0.88$), statistically
indistinguishable from zero. The signal tracks survey-mask geometry,
not environment density, consistent with the BGS-selection-function-
conditioned imaging-leg systematics tracked in Paper~IV; this is
consistent with a tracer-program decomposition in which the catalog-wide
monopole offset is dominated by the BGS-bright sample, with the
LRG/ELG/QSO-dark sample returning $\sigma = +1.25$ (filament class,
on the declared env-labeled parent: bright $n=394{,}181$ at
$\sigma=-2.98$ vs dark $n=13{,}759$ at $\sigma=+1.61$, opposite sign).
The joint two-sample
z-test on the bright-vs-dark $f_{\rm CW}$ difference is
$|z| \approx 2.1\sigma$ on the filament class (the largest dark
subsample available per class; computed at the env-labeled row level,
where a galaxy observed under both programs enters both splits, so
the splits are not disjoint and the $z$ is approximate —
\S\ref{sec:results_within_class_density}); the cluster class joint
$|z| \approx 0.5\sigma$ is sample-size-limited ($n_{\rm dark}^{\rm
cluster} = 4{,}234$) and does not independently confirm or refute the
selection-function-conditioned interpretation. At the whole-catalog
level the bright-vs-dark difference is $0.81$\,pp
($f_{\rm CW} = 0.4970$ vs $0.5051$, $|z| \approx 2.0\sigma$).
A contingency test (\S\ref{sec:results_vweb})
finds V-Web class and target program are \emph{not} independent
($\chi^{2}\!=\!4932$, $3$ d.o.f., $\log_{10}p\!\approx\!-1069$,
Cram\'er's $V\!=\!0.078$ --- a small effect by conventional standards,
with the $\chi^2$ driven by sample size $n\!=\!811{,}609$ rather than
effect magnitude --- max class-to-overall
bright-fraction deviation $1.5$\,pp), so the V-Web filament
bright-vs-dark sign-flip is best read as a residual structure
that the current data do not allow us to cleanly partition between a
pure BGS-selection-function-origin (propagated through a
V-Web-class-correlated target-program mix) and a residual
target-program-conditioned astrophysical signal. The headline
environment-independence statement of this paper is anchored on the
DESIVAST primary analysis below (\S\ref{sec:desivast_primary}),
which is constructed to be insensitive to this residual; the
${\sim}2\sigma$ filament sign-flip is flagged as a diagnostic to be
disentangled by future Rubin/LSST + DESI DR2 follow-up where the
cluster-restricted dark sample will be $\gtrsim 5\times$ larger.
\end{abstract}

\maketitle

\section{Introduction}\label{sec:intro}

Spiral galaxy chirality is, to leading order in a parity-conserving
universe, an equal mixture of clockwise (CW) and counterclockwise (CCW)
projections on the sky once mirror-augmentation biases in classifier
training are removed. Paper~IV~\cite{golden_chirality_2026} (a companion
work by the same author, currently in preparation and not yet peer
reviewed; the present manuscript treats its catalog and quoted monopole
offset as inputs whose uncertainty is propagated explicitly below)
establishes the global mixture in the post-test-time-augmentation
equivariant classifier as a CW fraction of $0.4974 \pm 0.000279$ ---
a $-0.26$\,pp monopole offset from exact parity that is statistically
significant in pure counting terms ($\approx\!9\sigma$) and is treated
throughout Paper~IV and this paper as a known classifier-monopole
systematic reference, not a parity violation --- while the Paper~IV
real-space \emph{dipole} is consistent with isotropy at $+0.41\sigma$
($p = 0.31$). That dipole null is a \emph{global} statement; it
does not constrain whether handedness is independent of
\emph{environment}.

The present paper is a focused, environment-conditional null test. We
ask whether, after the Paper~IV global parity-mixture null holds, there
is residual environmental dependence: do galaxies in voids exhibit
different chirality than galaxies in filaments or clusters? No
published bounce or inflation model currently predicts a specific
environment-conditional chirality signature at this scale; the present
null therefore supplies an empirical upper bound on any future model
in the bounce-chirality coupling class (Sec.~\ref{sec:p4}) that would
produce one at the $\gtrsim\!25\,\text{Mpc}/h$ smoothing scale of the
V-Web classification used here, and complements the Paper~IV
global-dipole bound at the catalog-monopole level.

We approach the test in three stages. First, we cross-match the
canonical chirality catalog with DESI Data Release~1
(Section~\ref{sec:data}). Second, we run a V-Web tidal-tensor cosmic-web
classifier (Section~\ref{sec:vweb}) on the parent DESI DR1 spectroscopic
catalog to assign each matched spiral to one of \{void, wall, filament,
cluster\}. Third, we test the chirality-by-environment dependence with
Jeffreys binomial credible intervals and label-shuffle permutation nulls
(Sections~\ref{sec:results}--\ref{sec:phase2}). The test is
bounce-model agnostic.

\section{Relation to Paper IV}\label{sec:p4}

Paper~IV~\cite{golden_chirality_2026} produces a flat catalog of
$8{,}474{,}531$ galaxies classified into \{CW, CCW, NS\} by an
equivariant ViT-Small classifier with $Z_2$ test-time augmentation.
The canonical labels live in the column \texttt{class\_eq} of the
HuggingFace catalog
(\href{https://huggingface.co/datasets/bamfai/galaxy-chirality-catalog}%
{bamfai/galaxy-chirality-catalog}). The catalog inherits sky positions from
DESI Legacy DR8 Tractor and is not redshift-resolved beyond Tractor
photo-$z$; this paper supplies spectroscopic redshifts and environment
context by joining to DESI Data Release~1.

Paper~IV's headline results bear on the present paper in three ways.
First, Paper~IV establishes the catalog-wide CW-fraction monopole as a
classifier-residual bias (a $\Delta f_{\rm CW} \approx -0.0026$ offset
from $0.5$ that is spatially uniform and quality-quartile-flat) rather
than a real cosmological dipole; we therefore expect the environmental
$\sigma_{\rm from\,half}$ values reported here to inherit the same
monopole offset, with sign determined by sample size. Second,
Paper~IV's current headline (v1.0.166) is a real-space full-sky dipole
null at $+0.43\sigma$ ($p\!=\!0.30$) from direct amplitude estimation,
reinforced by a weighted template fit that excludes a clean
Shamir-amplitude ($1.7\%$) dipole; an earlier harmonic-space
subsample-mask MASTER-deconvolved $\ell\!=\!1$ statistic was withdrawn
in Paper~IV v1.0.166 after a provenance audit traced its mask to a
synthetic footprint. Any environmental signal
here would have to coexist with the re-anchored real-space null. Third,
Paper~IV provides
the per-galaxy CW/CCW labels we test here; we make no independent
classification. The per-galaxy catalog labels and the monopole offset
consumed by this paper are unaffected by Paper~IV's harmonic-channel
revision.

\section{Data}\label{sec:data}

\subsection{Chirality catalog (Paper IV)}

We use the HuggingFace galaxy chirality catalog,
filtered to the chirality-relevant equivariant class $\in$
\{\texttt{CW}, \texttt{CCW}\}. Imaging-leg provenance is retained for
the per-leg systematics split in Section~\ref{sec:systematics}:
BASS+MzLS, DECaLS, DES.

\subsection{DESI Data Release 1}

We use the canonical \texttt{zall-pix-iron.fits} HEALPix-coadded
redshift catalog from DESI DR1 (specprod tag \texttt{iron}, the DR1
spectroscopic reduction;
\url{https://data.desi.lbl.gov/public/dr1/spectro/redux/iron/zcatalog/}),
restricted to \texttt{ZWARN==0},
\texttt{SPECTYPE} $\in$ \{GALAXY, QSO\}, and $0.01 \le z \le 4$.
These quality cuts leave $16{,}361{,}731$ rows — the ``DR1 input''
count quoted throughout this paper (and in the abstract as
\texttt{ZWARN=0} input rows) is this post-cut count, not the raw
\texttt{zall} row total. Further restricting to
\texttt{SPECTYPE==GALAXY} and the V-Web cosmic-web finder's tighter
window $0.01 \le z \le 2.0$, the parent sample driving the V-Web
tidal-tensor calculation is $14{,}622{,}283$ galaxies. These row counts are
\emph{derived in this work} by applying our cuts to the DR1 zall
catalog (not published DR1 constants); the fetch + filter driver
is derived in this work.

\subsection{Cross-match method}

We compute nearest-neighbour angular separations on the celestial sphere
using \textsc{astropy} \texttt{SkyCoord.match\_to\_catalog\_sky}. The
primary acceptance radius is $1.0''$ (DESI fiber positioning tolerance);
sensitivity is reported at $\{0.5, 1.0, 2.0, 3.0, 5.0\}''$. Duplicates
on the chirality side are resolved by nearest-separation winner; after
this dedup the chirality-relevant subsample carries one row per DESI
\texttt{TARGETID} ($791{,}635$ rows, $791{,}635$ unique
\texttt{TARGETID}s — no many-to-one matches survive the join).

\subsection{Matched catalog summary}\label{sec:matched_summary}

Table~\ref{tab:match_summary} summarizes the $1''$ matched catalog.
The matched-primary count is $2{,}349{,}908$ before dedup and
$2{,}232{,}212$ after. The chirality-relevant subsample is $791{,}635$
galaxies. Median separation is $0.0066''$ and the 99th-percentile
separation is $0.30''$, both well inside the $1.0''$ acceptance.
The 6.6-mas median is far below any independent-astrometry match
precision and reflects shared coordinate provenance rather than
astrometric match quality: the DESI \texttt{TARGET\_RA}/\texttt{TARGET\_DEC}
coordinates are inherited directly from the DESI Legacy Imaging Surveys
Tractor catalogs, and the Paper~IV chirality-catalog coordinates derive
from the same Legacy (DR8) Tractor astrometry, so matched pairs differ
only at the coordinate-precision/rounding level. The $1.0''$ acceptance
radius is therefore conservative for this catalog pair. The
$p_{99} = 0.30''$ tail reflects the small subset of objects whose
coordinates were re-derived between data releases.
Sensitivity to acceptance radius is mild: $\{0.5, 1.0, 2.0, 3.0,
5.0\}''$ produces $\{2.34, 2.35, 2.37, 2.39, 2.44\}\!\times\!10^6$
matched-primary rows, a $\le 4\%$ band.
Figure~\ref{fig:z_histogram} shows the redshift distribution of the
matched sample: it peaks at $z \approx 0.15$--$0.2$ (median $0.168$)
and falls off steeply above $z \approx 0.5$, with a sparse tail
extending to the maximum $z = 3.83$.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig_z_histogram.png}
\caption{Redshift distribution of the matched chirality $\times$
DESI~DR1 spiral sample ($1''$ acceptance, after dedup). The
distribution peaks at $z \approx 0.15$--$0.2$ (median $0.168$) and
falls off steeply above $z \approx 0.5$; a sparse tail extends to the
maximum $z = 3.83$ (Table~\ref{tab:match_summary}).}
\label{fig:z_histogram}
\end{figure}

\begin{table}[t]
\caption{Matched chirality $\times$ DESI DR1 catalog ($1''$
acceptance). Input rows are the post-cut DESI DR1 selection
(\texttt{ZWARN=0}, GALAXY/QSO, $0.01 \le z \le 4$).}
\label{tab:match_summary}
\begin{ruledtabular}
\begin{tabular}{lr}
Quantity & Value \\
\hline
DESI DR1 input rows (post-cut) & $16{,}361{,}731$ \\
Matched primary & $2{,}349{,}908$ \\
Matched primary after dedup & $2{,}232{,}212$ \\
Chirality-relevant & $791{,}635$ \\
\quad CW & $393{,}592$ \\
\quad CCW & $398{,}043$ \\
\quad \emph{NS} (Paper~IV \textsc{not\_spiral} class; excluded) & $1{,}440{,}577$ \\
SPECTYPE GALAXY & $2{,}215{,}032$ \\
SPECTYPE QSO & $17{,}180$ \\
Leg BASS+MzLS & $688{,}608$ \\
Leg DECaLS & $1{,}538{,}880$ \\
Leg DES & $4{,}724$ \\
$z$ median & $0.168$ \\
$z$ max & $3.83$ \\
$p_{50}$ separation & $0.0066''$ \\
$p_{99}$ separation & $0.30''$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

\section{V-Web cosmic-web classification}\label{sec:vweb}

\subsection{Algorithm}\label{sec:vweb_algo}

We compute environment labels via the V-Web tidal-tensor classifier
(Hahn~\textit{et~al.}~2007 \cite{Hahn2007};
Hoffman~\textit{et~al.}~2012 \cite{Hoffman2012};
Cautun~\textit{et~al.}~2014 \cite{Cautun2014}).
\emph{Nomenclature reminder} (see also the title footnote):
throughout this paper ``V-Web'' names \emph{our implementation} of the
Hahn~2007 tidal-tensor (T-Web) recipe; ``T-Web'' refers to the recipe
itself (as in the title's ``T-Web (Hahn 2007)'') and to external
implementations of it (e.g.\ the concurrent
DR1 analysis of Ref.~\cite{TWebDESI2026}).

\begin{enumerate}
\item Filter DESI DR1 \texttt{zall} to \texttt{ZWARN==0}, SPECTYPE
$=$ GALAXY, $0.01 \le z \le 2.0$ (yields $14{,}622{,}283$ galaxies).
\item Compute comoving distance $\chi(z)$ via Planck~2018
\cite{Planck2018cosmoparams}. Unit convention used throughout the
pipeline: \textsc{astropy} returns $\chi$ in Mpc and we multiply by
$h$ explicitly to work in $h^{-1}$\,Mpc (sanity value:
$\chi(z\!=\!0.2) = 570.4\,h^{-1}$\,Mpc), the same convention as the
DESIVAST hole-catalog $X/Y/Z$ columns used in
\S\ref{sec:desivast_primary}.\footnote{Dimensional derivation:
a distance $D$ expressed in $h^{-1}$\,Mpc has numerical value
$D/(h^{-1}\,{\rm Mpc}) = h\,D/(1\,{\rm Mpc}) = h\cdot D[{\rm Mpc}]$, so
multiplying the value in Mpc by $h$ yields the value in $h^{-1}$\,Mpc.
With Planck~2018 $h=0.6766$, $\chi(z\!=\!0.2)\!\approx\!843$\,Mpc gives
$0.6766 \times 843 = 570\,h^{-1}$\,Mpc, matching the sanity anchor.}
\item Map to Cartesian
$(X,Y,Z) = \chi (\cos\delta\cos\alpha, \cos\delta\sin\alpha,
\sin\delta)$.
\item Cloud-in-Cell deposit onto a $256^3$ comoving grid (full DR1
bounding box $6{,}634$\,Mpc$/h$ at $256^3 \to$ cell $25.9$\,Mpc$/h$).
The deposit operates on the row-level parent ($14{,}622{,}283$
survey--program coadd rows over $14{,}100{,}704$ unique
\texttt{TARGETID}s, \S\ref{sec:desivast_p4_residual}), so repeat
coadds ($521{,}579$ rows, $3.6\%$ of the parent) contribute one
deposit per row, mildly over-weighting multiply-observed targets. A
deduplicated (one-deposit-per-\texttt{TARGETID}) full field rebuild on
the identical grid geometry quantifies this: in-footprint class volume
fractions shift by at most $0.70$~pp (wall $-0.70$, filament $+0.68$,
cluster $+0.20$, void $-0.18$), $97.8\%$ of common-mask cells and
$97.9\%$ of matched-spiral assignments retain their class, and
per-class $f_{\rm CW}$ moves by at most $0.23$~pp (the small wall
class; filament $-0.005$~pp, cluster $+0.013$~pp), leaving every
environment--chirality conclusion unchanged (artifact
\artifact{pipelines/p5\_desi\_chirality/outputs/23\_unique\_parent\_rebuild.json}).
\item Build a survey-footprint mask by dilation of occupied cells
(\textsc{scipy} \texttt{binary\_dilation} with the default
face-connected $3\!\times\!3\!\times\!3$ cross structuring element,
$\lceil R_s/{\rm cell}\rceil + 1 = 2$ iterations at the canonical
configuration; the count is recomputed per Phase~2 sweep cell,
giving 3 at $R_s\!=\!50$): $2{,}417{,}697$ occupied $\to$ $3{,}150{,}086$ in-mask
($18.8\%$ of the cube); $\bar\rho_{\rm cell}\!=\!4.64$ galaxies/cell.
\item Convert counts to overdensity
$\delta = \rho / \bar\rho - 1$.
\item Gaussian-smooth $\delta$ in Fourier space with kernel $R_s$
(default $25$\,Mpc$/h$; Phase 2 sweep over $\{10, 25, 50\}$).
\item Solve Poisson in $k$-space: $\Phi(k) = -\delta_k / k^2$
(with $k\!=\!0$ mode zeroed).
\item Tidal tensor: $T_{ij}(k) = -k_i k_j \Phi(k)$ (Fourier sign
convention $\partial_i \partial_j \leftrightarrow (ik_i)(ik_j) =
-k_i k_j$, so with $\Phi(k) = -\delta_k/k^2$ this gives
$T_{ij}(k) = +k_i k_j \delta_k / k^2$; the committed implementation
\artifact{pipelines/p5\_desi\_chirality/env\_finder/01\_compute\_vweb.py} applies exactly this sign,
matching the title-footnote convention --- the reported volume
fractions, e.g.\ void $24.4\%$ / cluster $1.0\%$, are consistent with
the standard sign); inverse-FFT the six
unique components. Conventions: $k_i = 2\pi n_i / L$ with $L$ the box
side (\texttt{scipy} \texttt{fftfreq} scaled by $2\pi/{\rm cell}$),
so $k$ carries physical $h\,{\rm Mpc}^{-1}$ units; the CIC
mass-assignment window is \emph{not} deconvolved before the second
derivatives. Only the ordering and sign of the eigenvalues relative
to $\lambda_{\rm th}$ enter the classification, so $\lambda_{\rm th}$
is defined on this (window-convolved) normalization.
\item At each grid cell, diagonalize the symmetric $3\!\times\!3$
tensor to get eigenvalues
$\lambda_1 \ge \lambda_2 \ge \lambda_3$.
\item Classify by count of $\lambda > \lambda_{\rm th}$:
$0 \to$ void, $1 \to$ wall, $2 \to$ filament, $3 \to$ cluster
(Cautun \textit{et~al.}\ \cite{Cautun2014} geometric default
$\lambda_{\rm th}\!=\!0$).
\item NN-interpolate the per-cell label + smoothed log-density
($\log_{10}(1 + \delta_{\rm smooth})$, the quantity stored as the
per-galaxy density covariate and used for all within-class density
stratifications; quartile binning on it is identical to binning on
the linear density because the transform is monotone) to each
galaxy.
\end{enumerate}

\subsection{Phase 1 volume fractions}\label{sec:vweb_volfrac}

For the canonical configuration $(R_s\!=\!25\,{\rm Mpc}/h,
\lambda_{\rm th}\!=\!0, N_{\rm grid}\!=\!256^3)$ the in-footprint
volume fractions are
\{void $0.244$, wall $0.413$, filament $0.333$, cluster $0.010$\}(Fig.~\ref{fig:volfrac}).
The cluster volume fraction ($1.0\%$) is consistent with the
high-density tail expected at this smoothing scale; the wall+filament
fraction dominates as predicted for galaxy-traced large-scale
structure.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig_p5_volume_fractions_pie.png}
\caption{In-footprint V-Web volume fractions for the canonical
$(R_s\!=\!25\,{\rm Mpc}/h, \lambda_{\rm th}\!=\!0, N_{\rm grid}\!=\!256^3)$
run on $14{,}622{,}283$ DESI DR1 spectroscopic galaxies. The cluster
volume fraction ($1.0\%$) reflects the high-density tail; the
wall+filament fraction ($74.5\%$) dominates as expected for
galaxy-traced large-scale structure.}
\label{fig:volfrac}
\end{figure}

\section{Statistical methods}\label{sec:stats}

For each binned analysis we report observed CW fraction, the Jeffreys
$95\%$ binomial credible interval (Jeffreys prior
${\rm Beta}(1/2,1/2)$), and the signed deviation from $0.5$
$\sigma_{\rm from\,half} \equiv (n_{\rm CW} - 0.5 N)/(0.5\sqrt{N})$
(the standard one-sample binomial $z$-score against the null
$p = 0.5$; we keep the explicit name to distinguish it from the
monopole-referenced $\sigma_{\rm pred}$ below).
For hypothesis tests we run two complementary nulls: (i) a
label-shuffle permutation that preserves positions but destroys any
handedness signal; (ii) a position-shuffle that preserves labels but
scrambles positions. For the per-bin count statistics used in this
paper the two are the same permutation null (permuting either side of
the label--position assignment induces the identical null
distribution of bin counts), so all quoted look-elsewhere $p$-values
are from the label-shuffle implementation; the position-shuffle is
retained as a pipeline cross-check rather than separately tabulated. Both nulls draw $N_{\rm MC}\!=\!1000$
independent permutations from a deterministic-seeded NumPy
\texttt{default\_rng} (seed fixed in the pipeline config; the same
config-level seed initializes each scan family's generator, so
identical underlying random streams are shared across families ---
the distinct-stream re-draws of \S\ref{sec:results_healpix} confirm
the quoted $p$-values are stream-independent within the
$N_{\rm MC}\!=\!1000$ permutation standard error); per-bin
$\sigma_{\rm from\,half}$ is recomputed under each draw, and the
null distribution is constructed as the empirical CDF of the
per-realization statistic. Analysis drivers are available in the
companion data repository (defined in
Appendix~\ref{sec:data_code}). Multi-bin scans are
corrected for multiple testing with Bonferroni at $\alpha\!=\!0.01$.

We explicitly compare each $\sigma_{\rm from\,half}$ against the
Paper~IV-predicted classifier-monopole offset
\begin{equation}\label{eq:sigma_pred}
\sigma_{\rm pred} \;=\; \frac{\Delta f_{\rm CW}}{0.5 / \sqrt{N}}
\;=\; 2 \cdot \Delta f_{\rm CW} \cdot \sqrt{N},
\end{equation}
with $\Delta f_{\rm CW}\!=\!-0.0026$ (the monopole offset expressed as
a fractional deviation from $0.5$, i.e.\
$\Delta f_{\rm CW} \equiv \bar f_{\rm CW} - 0.5$) to flag whether a
deviation is attributable to the global bias rather than
environment-correlated chirality. Where a residual is computed against
a non-half reference $p_0$ (e.g.\ $f_{\rm CW}^{\rm P5} = 0.4972$ in
$\sigma_{\rm vs\,monopole}$), we retain the $0.5/\sqrt{N}$ denominator
for cross-table comparability; the exact $\sqrt{p_0(1-p_0)/N}$
denominator differs from it by a factor
$\sqrt{4 p_0 (1-p_0)} = 0.99998$ at $p_0 = 0.4972$, i.e.\
${<}0.01\%$ in $\sigma$, far below the quoted precision. The monopole reference itself carries finite counting
uncertainty, which we propagate explicitly: where $\sigma_{\rm pred}$
uses the Paper~IV catalog-wide $\Delta f_{\rm CW}$
($N\!=\!3.2\times10^6$, ${\rm se}\!\approx\!\sqrt{0.25/(3.2\times10^6)}
= 2.8\times10^{-4}$), the induced uncertainty on $\sigma_{\rm pred}$
for an $N\!\approx\!4\times10^5$ class is $2\,{\rm se}\,\sqrt{N}
\approx 0.36$; where the matched-sample monopole $f_{\rm CW}^{\rm P5}$
is the reference ($N\!=\!812{,}793$, ${\rm se}\!\approx\!5.5\times
10^{-4}$), it is $\approx 0.7$ for the same $N\!\approx\!4\times10^5$
class. Residuals of order $1$--$2\sigma$
(e.g.\ the Phase-2 maximum $1.87$) should be read with this band in
mind; the label-shuffle permutation null, which fixes the total CW
count at its observed value, is conditional on the observed matched-sample
CW count --- i.e.\ every permutation test in this
paper treats the global CW fraction as fixed at its observed value rather
than propagating its uncertainty; uncertainty in the Paper~IV classifier
monopole is propagated separately in the analytic $\sigma_{\rm from\,half}$ comparisons, which are displayed only
through those analytic values --- and no
conclusion in this paper depends
on a residual threshold finer than this band. Because
$\sigma_{\rm from\,half}$ grows as $\sqrt{N}$ at
fixed fractional offset, raw $\sigma$ values are not comparable across
bins of different $N$; only the monopole-subtracted residuals are.
The formal version of this comparison is the per-class one-sample
residual test against $f_{\rm CW}^{\rm P5}$ reported in
Table~\ref{tab:p4_monopole_residual} (all $|\sigma_{\rm vs\,monopole}|
< 1.15$) together with the omnibus $4\times2$ homogeneity test of
\S\ref{sec:results_vweb}; deviations beyond
$|\sigma_{\rm obs} - \sigma_{\rm pred}| > 3$ would be candidate
environmental signals.

\subsection{Look-elsewhere (LEE) correction}\label{sec:lee}

For multi-bin scans (HEALPix per-pixel deviations, redshift quintiles,
density quintiles), we report two complementary LEE corrections.

\emph{Parametric Bonferroni}: for $K$ independent bins controlled at
the two-sided family-wise level $\alpha$ (per-bin level $\alpha/K$),
the threshold on the maximum-absolute-$\sigma$ statistic is
\begin{equation}\label{eq:bonferroni}
|\sigma|^{\rm Bonf}_{\alpha,K} \;=\; \sqrt{2}\,
\mathrm{erfc}^{-1}\!\left(\frac{\alpha}{K}\right),
\end{equation}
which is conservative when the bin-level statistics are not
independent (e.g.\ HEALPix per-pixel deviations correlated by the
mask boundary). For $K\!=\!5$ density quintiles at $\alpha\!=\!0.01$,
Eq.~\eqref{eq:bonferroni} gives $|\sigma|^{\rm Bonf}_{0.01,5}\!\approx\!3.09$;
for $K\!=\!1054$ NSIDE-16 HEALPix pixels at $\alpha\!=\!0.05$,
$|\sigma|^{\rm Bonf}_{0.05,1054}\!\approx\!4.05$.

\emph{Empirical max-stat MC null}: for each scan we run $N_{\rm MC}$
label-shuffle permutations preserving sample size; for each
realization we record the maximum absolute $\sigma_{\rm from\,half}$
across the $K$ bins. The empirical $p$-value is
\begin{equation}\label{eq:empirical_p}
p_{\rm LEE} \;=\; \frac{1 + \#\{k : |\sigma|_{\max}^{(k)} \ge |\sigma|_{\max}^{\rm obs}\}}{1 + N_{\rm MC}},
\end{equation}
which preserves any sample-correlation structure that
Eq.~\eqref{eq:bonferroni} ignores. We adopt the empirical max-stat
null as primary and report the parametric Bonferroni threshold as a
cross-check; on every scan in this paper the two return the same
verdict (no scan crosses either the empirical max-stat $p < 0.05$
criterion or the parametric Bonferroni threshold at
$\alpha\!=\!0.01$). All $N_{\rm MC}\!=\!1{,}000$ permutation
$p$-values in this paper carry a Monte-Carlo standard error
${\rm se}_{\rm MC} = \sqrt{p(1-p)/N_{\rm MC}}$, i.e.\
${\approx}0.010$ at $p \approx 0.13$ and ${\approx}0.015$ at
$p \approx 0.5$; quoted second/third decimals should be read against
this resolution.

\subsection{Primary vs.\ secondary analysis paths (pre-registration
caveat)}\label{sec:primary_path}

The paper reports environment-stratified chirality results from
multiple environment classifiers (V-Web on the full DESI~DR1 spectro
sample, Tempel+2014~\cite{Tempel2014} FoF on the SDSS DR10 overlap,
DESIVAST~\cite{DESIVAST2025} VoidFinder+V2-REVOLVER+V2-VIDE on the
low-$z$ BGS sample, ASTRA~\cite{ASTRADESI2026} on the EDR rosettes,
and an external T-Web~\cite{TWebDESI2026} concurrent-literature
overlay) along with multiple stratifications of each
(redshift quartiles, density quartiles, tracer-program splits,
HEALPix sky-position scans). This is an inherently multi-classifier,
multi-stratification analysis and a single \emph{a priori} preregistered
analysis plan was not filed; the choice of which classifier to
report as ``primary'' is therefore made post-hoc, and we declare it
explicitly here to bound the garden-of-forking-paths concern.

\textbf{Primary analysis path.} We designate the
DESIVAST-anchored void cross-check
(Section~\ref{sec:desivast_primary}) as the
\emph{primary} environment-dependent chirality analysis in this paper:
it has the largest controlled sample ($n_{\rm void}^{\rm DESIVAST} =
56{,}981$), is built on a publicly released peer-reviewed
DR1 BGS void catalog (Rinc\'on \textit{et al.}\ 2025,
ApJ~982, 38~\cite{DESIVAST2025}) standardized across the
DESI collaboration, ships three independent void-finding algorithms
(VoidFinder, V2-REVOLVER, V2-VIDE) for built-in robustness, and
provides catalog-native per-galaxy zone memberships
(\texttt{GALZONE}/\texttt{ZONEVOID}) that the V-Web tidal-tensor
re-projection cannot match. The headline-result statement therefore
rests on the DESIVAST-anchored $|\Delta f_{\rm CW}| \lesssim 0.002$ null
across all three algorithms (Section~\ref{sec:desivast_primary},
Table~\ref{tab:desivast_three_algo}).

\textbf{Secondary diagnostic paths.} The V-Web cosmic-web run
(Section~\ref{sec:results_vweb}, Phase~2 sweep
Section~\ref{sec:phase2}), the Tempel FoF cross-validation
(Section~\ref{sec:tempel}), the ASTRA EDR per-object
cross-validation (Section~\ref{sec:astra_per_object}), the
T-Web concurrent-literature overlay
(Section~\ref{sec:tweb_concurrent}), and all redshift / density /
sky-position / tracer-program stratifications are reported as
\emph{secondary} diagnostic consistency checks. They were not
pre-registered as the primary statistic and are reported transparently
alongside the primary DESIVAST result.

\textbf{Multiplicity bookkeeping.} The DESIVAST primary path itself
exposes three independent void-finding algorithms (VoidFinder,
V2-REVOLVER, V2-VIDE) plus two catalog-native zone definitions, giving
five effective primary-class statistics. The declared primary estimand
is the void-vs-non-void contrast $\Delta f_{\rm CW}$, whose two-sample
statistics are tabulated for the three sphere-PIS algorithms in
Table~\ref{tab:desivast_three_algo} ($|z_\Delta| \leq 1.12$, all
two-sided $p_\Delta \geq 0.26$) and for the two GALZONE catalog-native
estimators in \S\ref{sec:desivast_catalog_native}
(V2-REVOLVER: $|z_\Delta| = 1.25$, $p_\Delta = 0.21$;
V2-VIDE: $|z_\Delta| = 0.72$, $p_\Delta = 0.47$). Treating the five
DESIVAST estimators as a Bonferroni-5 family at $\alpha = 0.05$, the
per-test threshold is $|z|^{\rm Bonf}_{0.05,5} \approx 2.58$; no
tabulated contrast across the five rows approaches it. The
family-level conclusion is a clean primary null on the declared
contrast estimand uniformly across all five rows. The secondary V-Web / Tempel / ASTRA
/ T-Web / per-stratification estimators carry their own multiplicity
budget but, per the primary/secondary separation above, do not enter
the headline-result family. We caution that any single secondary-path
finding (e.g.\ the bright/dark target-class sign-flip discussed in
Section~\ref{sec:results_vweb}) reaching $|\sigma| \sim 3$ in
isolation should be read as a per-stratification diagnostic, not as
an independent test of the headline null; the headline is anchored on
the DESIVAST primary path.

\textbf{Analysis-tree declaration.}
Table~\ref{tab:analysis_tree} enumerates the full analysis tree tested
in this paper, distinguishing the Bonferroni-5 primary family from the
nine secondary Bonferroni tests in the Phase~2 sensitivity sweep and
from descriptive stratifications that carry no additional look-elsewhere
correction. All tests outside the primary family are \emph{descriptive
consistency checks}; a finding at $|\sigma| \sim 2$--$3$ in a
secondary path should be interpreted against its own per-test threshold,
not against the primary Bonferroni-5 gate.

\begin{table*}[ht]
\caption{Analysis-tree declaration. The Bonferroni-5 primary family
covers the five DESIVAST estimators; the Phase~2 nine-cell secondary
family covers the $(R_s, \lambda_{\rm th})$ sweep; all other paths are
labelled descriptive. Thresholds from Eq.~\eqref{eq:bonferroni}.}
\label{tab:analysis_tree}
\begin{ruledtabular}
\begin{tabular}{llcl}
Path & Tests & $K$ & Status \\
\hline
\multicolumn{4}{l}{\textbf{Primary (Bonferroni-5; $\alpha=0.05$, threshold $|\sigma|=2.58$)}} \\
DESIVAST VoidFinder & void vs non-void $f_{\rm CW}$ & 1 & primary \\
DESIVAST V2-REVOLVER & void vs non-void $f_{\rm CW}$ & 1 & primary \\
DESIVAST V2-VIDE & void vs non-void $f_{\rm CW}$ & 1 & primary \\
V2-REVOLVER catalog-native & GALZONE void vs non-void $f_{\rm CW}$ & 1 & primary \\
V2-VIDE catalog-native & GALZONE void vs non-void $f_{\rm CW}$ & 1 & primary \\
\hline
\multicolumn{4}{l}{\textbf{Secondary sweep (Bonferroni-9; $\alpha=0.05$, threshold $|\sigma|=2.77$)}} \\
Phase 2 cell $(R_s=10,\lambda=0.0)$ & max-class residual & 1 & secondary \\
Phase 2 cell $(R_s=10,\lambda=0.1)$ & max-class residual & 1 & secondary \\
Phase 2 cell $(R_s=10,\lambda=0.3)$ & max-class residual & 1 & secondary \\
Phase 2 cell $(R_s=25,\lambda=0.0)$ & max-class residual & 1 & secondary \\
Phase 2 cell $(R_s=25,\lambda=0.1)$ & max-class residual & 1 & secondary \\
Phase 2 cell $(R_s=25,\lambda=0.3)$ & max-class residual & 1 & secondary \\
Phase 2 cell $(R_s=50,\lambda=0.0)$ & max-class residual & 1 & secondary \\
Phase 2 cell $(R_s=50,\lambda=0.1)$ & max-class residual & 1 & secondary \\
Phase 2 cell $(R_s=50,\lambda=0.3)$ & max-class residual & 1 & secondary \\
\hline
\multicolumn{4}{l}{\textbf{Descriptive (no additional look-elsewhere correction)}} \\
V-Web 4-class omnibus & $4\!\times\!2$ homogeneity $\chi^2$ & --- & descriptive \\
Redshift scan & label-shuffle $p$ vs $z$ & --- & descriptive \\
Density quintiles & bright-fraction residuals & --- & descriptive \\
HEALPix (NSIDE $=16,32,64$) & sky-position scan & --- & descriptive \\
Tracer-program split & bright vs dark $f_{\rm CW}$ & --- & descriptive \\
Tempel FoF overlap & filament concordance & --- & descriptive \\
ASTRA EDR per-object & supporting diagnostic consistency check (EDR overlap-size caveat) & --- & descriptive \\
T-Web concurrent-lit & volume-fraction comparison & --- & descriptive \\
\end{tabular}
\end{ruledtabular}
\end{table*}

\section{Results}\label{sec:results}

\subsection{Cosmic-web environment (headline)}\label{sec:results_vweb}

Table~\ref{tab:cw_vs_env} reports CW fraction by cosmic-web class on
the $812{,}793$ env-labeled spiral rows produced by joining the
$791{,}635$ chirality-relevant matched spirals to the canonical V-Web
environment table (the join carries $783{,}820$ unique env-matched
spirals, counted once per repeat DR1 survey--program coadd row;
\S\ref{sec:desivast_p4_residual} documents the reconciliation — the
superset and unique-spiral monopoles agree to four decimals, so the
headline is invariant under either parent). An omnibus $4\times2$
homogeneity test (CW/CCW $\times$ class) on the table counts returns
$\chi^2 = 3.55$ with 3~d.o.f.\ ($p = 0.31$): the four classes are
statistically consistent with a single common CW fraction. Because
the row-level parent repeats $2.7\%$ of \texttt{TARGETID}s across
program coadds (technically violating the independence assumption of
the contingency test), we recompute the same test on the
$783{,}820$-row unique-spiral subset (one row per \texttt{TARGETID}):
$\chi^2 = 3.00$, $p = 0.39$ ($\chi^2 = 2.92$, $p = 0.41$ further
excluding the $79$ \texttt{TARGETID}s with conflicting duplicate env
labels), so the duplicates have negligible impact on the null verdict
(\artifact{pipelines/p5\_desi\_chirality/outputs/21\_r23conf\_meta\_closures.json}). The
label-shuffle look-elsewhere $p$ on the max-class
$|\sigma_{\rm from\,half}|$ statistic is likewise null and insensitive
to stratification of the shuffle by imaging leg and DESI program
($p = 0.12$ free vs $0.12$ stratified;
\artifact{pipelines/p5\_desi\_chirality/outputs/18\_v0151\_stratified\_and\_density.json}).

\begin{table}[t]
\caption{CW fraction per cosmic-web environment, canonical V-Web
$(R_s\!=\!25\,{\rm Mpc}/h, \lambda_{\rm th}\!=\!0)$, on the
$812{,}793$-row env-labeled parent. $\sigma_{\rm from\,half}$ scales
as $\sqrt{n}$ at fixed offset and is not comparable across rows of
different $n$.}
\label{tab:cw_vs_env}
\begin{ruledtabular}
\begin{tabular}{lrrrr}
Env & $n$ & $n_{\rm CW}$ & $f_{\rm CW}$ & $\sigma_{\rm from\,half}$ \\
\hline
void & $428$ & $207$ & $0.4836$ & $-0.68$ \\
wall & $6{,}673$ & $3{,}359$ & $0.5034$ & $+0.55$ \\
filament & $408{,}187$ & $203{,}261$ & $0.4980$ & $-2.61$ \\
cluster & $397{,}505$ & $197{,}284$ & $0.4963$ & $-4.66$ \\
\hline
\textbf{range} & --- & --- & $\mathbf{0.0198}$ & --- \\
\end{tabular}
\end{ruledtabular}
\end{table}

Figure~\ref{fig:cw_by_env} visualizes the per-class CW fractions with
$95\%$ Jeffreys binomial credible intervals; all four classes bracket
the Paper~IV global $\bar f_{\rm CW}\!=\!0.4974$ horizontal reference,
and the void $95\%$ CI brackets parity $f_{\rm CW}\!=\!0.5$. The
range across the four classes is $1.98$\,percentage points, set by
the counting noise of the low-$n$ void bin ($0.484$) against the
catalog-monopole-dominated high-$n$ filament and cluster bins
($\sim 0.497$).

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig_p5_cw_by_env_bar.png}
\caption{CW fraction per cosmic-web class on the canonical V-Web run,
on the $n=812{,}793$ env-labeled spiral rows (covering $783{,}820$ of
the $791{,}635$ unique chirality-relevant matched spirals; $7{,}815$
lack an environment row, \S\ref{sec:results_vweb}). Bars show the
observed $f_{\rm CW}$ per class; black error bars are $95\%$ Jeffreys
binomial credible intervals, drawn on the row-level parent --- the
$2.7\%$ duplicate rows violate strict i.i.d., but the worst-case
design-effect inflation of the interval widths is
$\sqrt{812{,}793/783{,}820} = 1.018$, i.e.\ $\le\!1.9\%$, and the
homogeneity $\chi^2$ is recomputed on the unique-spiral subset in
\S\ref{sec:results_vweb}. The void bin ($n\!=\!428$) is dominated
by counting noise and brackets parity. The dashed horizontal line is
parity ($f_{\rm CW}\!=\!0.5$); the dotted red line is the Paper~IV
global $\bar f_{\rm CW}\!=\!0.4974$ classifier-monopole offset. All
four classes bracket the Paper~IV monopole, consistent with no
environmental dependence beyond the catalog-wide classifier bias.}
\label{fig:cw_by_env}
\end{figure}

The negative
$\sigma$ values in filament and cluster track the catalog-wide
classifier-monopole offset of Paper~IV: predicting
$\sigma_{\rm pred}$ from $\Delta f_{\rm CW}\!=\!-0.0026$ gives
$\sigma_{\rm pred}({\rm filament})\!\approx\!-3.32$ and
$\sigma_{\rm pred}({\rm cluster})\!\approx\!-3.28$, both within
order-unity of observation. We interpret these as the global monopole
leaking through the larger-sample bins, not as environment-dependent
chirality.

\textbf{Void-bin smallness.} The void bin has only $n\!=\!428$
galaxies. Two distinct void volume fractions appear in this paper and refer to different denominators: (i)~the V-Web tidal-classifier void class occupies ${\approx}24.4\%$ of in-footprint cell volume (\S\ref{sec:vweb_algo}, \artifact{pipelines/p5\_desi\_chirality/env\_finder/reports/01\_volume\_fractions.json}), while (ii)~only ${\approx}0.1\%$ of in-footprint grid cells fall inside a DESIVAST VoidFinder sphere (the point-in-sphere membership criterion on the $25.9\,{\rm Mpc}/h$ grid used for the sky-stratification analysis of \S\ref{sec:desivast_maximal_healpix}). The small V-Web void-bin count ($n\!=\!428$) arises because despite the V-Web void class covering $24.4\%$ of volume, the sparse $r \le 17.8$ DESI Legacy spiral selection combined with the $z \leq 0.24$ depth cut samples only a thin galaxy population within those cells. The $\sigma\!=\!-0.68$ on the void
class is statistical noise at this $N$; the $95\%$ Jeffreys credible
interval is $f_{\rm CW}^{\rm void} \in [0.435, 0.530]$, which brackets
parity.

\subsection{Redshift dependence}\label{sec:results_z}

The matched chirality-relevant subsample has redshift median $0.168$
and maximum $3.83$. The label-shuffle permutation null on the
max-absolute-deviation-from-half over $1{,}000$ shuffles returns
$p\!=\!0.372$.
A logistic regression of the CW indicator on $\{z, |\sin\delta|,
\cos\alpha, \text{confidence}\}$ gives a $z$-coefficient (per unit
redshift, in log-odds) of $0.0095 \pm 0.0229$ ($z$-score $0.41$,
$p = 0.68$) with an intercept of $0.0028 \pm 0.0097$ ($p = 0.77$) —
both consistent with zero, i.e.\ no
redshift dependence. A label-shuffle permutation stratified by
imaging leg and DESI program (the conservative null given the
per-leg/per-program residuals of \S\ref{sec:systematics}) leaves the
redshift-quintile look-elsewhere $p$ unchanged
($p = 0.81$ stratified vs $0.80$ free-shuffle).

\textit{Physical-covariate robustness.}---Extending the regression with
galaxy physical covariates from the Galaxy Zoo DESI morphology catalog
\cite{Walmsley:2023} (joined on \texttt{dr8\_id}, $100\%$ coverage of the
declared parent: Petrosian half-light radius, $r$-band apparent magnitude,
featured fraction, merger fraction, and --- on the $152{,}455$-galaxy
featured subsample where the decision tree reaches the question ---
edge-on fraction) leaves the environmental-independence verdict unchanged:
the joint 3-dof environment Wald test gives $p=0.46$ ($0.41$ without
covariates) on the canonical labels and $p=0.99$ on the z-shell-corrected
labels. The regression parent is the $783{,}741$ unique env-matched
spirals, one row per galaxy (the $783{,}820$ env-matched spirals of
\S\ref{sec:desivast_p4_residual} minus the $79$ \texttt{TARGETID}s
whose repeat coadd rows carry conflicting environment classes); the
covariate-extended models retain all $783{,}741$ ($100\%$
covariate-complete). The env-only Wald $p = 0.41$ is therefore not
expected to coincide with the Pearson omnibus $p = 0.31$ of
\S\ref{sec:results_vweb}: the omnibus $\chi^2$ is computed on the
$812{,}793$-row env-labeled superset, while the Wald test is a
different statistic on the smaller unique-galaxy parent — both are
null. Every individual environment coefficient is null after adjustment
(largest: void $-0.081\pm0.107$), with a maximum adjustment-induced
coefficient shift of $0.17\sigma$. No physical covariate is predictive of
chirality; the only marginally predictive regressor is classifier
confidence ($p\approx0.02$), a classifier property rather than an
environmental one. Artifact:
\path{pipelines/p5_desi_chirality/outputs/20_v0151_covariate_regression.json}.
Figure~\ref{fig:cw_vs_z} shows the binned equivariant CW fraction
across the matched sample's redshift range.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig_cw_vs_z.png}
\caption{Equivariant CW fraction versus redshift across the matched
DR1 chirality-relevant sample, with 95\% binomial confidence
intervals per bin. The low-$z$ bins that dominate the sample
(median $z = 0.168$) sit on the $0.5$ line; bins above
$z \approx 0.5$ contain few objects and have correspondingly wide
intervals. The binned values are consistent with no redshift
dependence, in agreement with the label-shuffle permutation test
($p = 0.372$) and the logistic regression in the text.}
\label{fig:cw_vs_z}
\end{figure}

\subsection{Projected density dependence}\label{sec:results_density}

The angular separation to the $k\!=\!5$ NN spiral on the sphere serves
as a projected-density proxy. Binned in density quintiles, the maximum
absolute deviation is $|\sigma|_{\max}\!=\!3.94$,
within the Paper~IV monopole prediction from Eq.~\eqref{eq:sigma_pred}:
at $N\!=\!158{,}327$ per quintile the predicted
$|\sigma_{\rm pred}|\!=\!2\!\cdot\!|{-0.0026}|\!\cdot\!\sqrt{158{,}327}\!\approx\!2.07$,
so the residual deviation beyond the monopole is
$|\sigma_{\rm obs}-\sigma_{\rm pred}|\!\approx\!1.87$, below the
Bonferroni-5 $|\sigma|^{\rm Bonf}_{0.01,5}\!=\!3.09$ threshold.
Figure~\ref{fig:cw_vs_density} shows the observed quintile values
side-by-side with the Paper~IV-monopole prediction; the bars track the
prediction within counting statistics in all five quintiles.

Because the $k\!=\!5$ NN proxy above is computed among the
chirality-relevant spirals themselves, it is mildly endogenous (the
tracer set is the test set). As a cross-check we recomputed the proxy
exogenously as the angular distance to the 5th-nearest galaxy among
\emph{all} $2{,}232{,}212$ deduplicated matched primaries (any
chirality class) and re-binned the spirals into quintiles of that
parent-tracer proxy: the maximum per-quintile deviation is
$|\sigma|_{\max} = 3.64$ with a maximum monopole-subtracted residual
of $|\sigma_{\rm obs}-\sigma_{\rm pred}| = 1.57$, again below the
Bonferroni-5 threshold — the density null is not an artifact of the
endogenous tracer choice
(\artifact{pipelines/p5\_desi\_chirality/outputs/18\_v0151\_stratified\_and\_density.json}).
A mask-edge buffer test gives the same answer: excluding the
$74{,}070$ spirals in HEALPix NSIDE-$64$ footprint-edge pixels
(${\sim}0.9^\circ$ angular buffer) and recomputing both the proxy and
the quintile edges on the retained sample leaves $91.0\%$ of retained
spirals in their original quintile and shifts no per-quintile
$f_{\rm CW}$ of Figure~\ref{fig:cw_vs_density} by more than
$0.12$\,pp — the quintile assignment is invariant to mask-edge
effects
(\artifact{pipelines/p5\_desi\_chirality/outputs/22\_r24conf\_local\_batch.json}).

\begin{table}[h]
\caption{\label{tab:density_quintile_residual}
Per-quintile $|\sigma_{\rm obs}-\sigma_{\rm pred}|$ residual table.
$\sigma_{\rm obs}$ is the binomial $\sigma$-from-half on the observed
CW fraction in each quintile; $\sigma_{\rm pred}=2\Delta f_{\rm CW}\sqrt{N}$
[Eq.~\eqref{eq:sigma_pred}]
at $\Delta f_{\rm CW}=-0.0026$ (Paper~IV-monopole prediction).
All five residuals lie below the Bonferroni-5 threshold
$|\sigma|^{\rm Bonf}_{0.01,5}=3.09$, so no quintile shows a
statistically-significant departure from the monopole-only model.
}
\begin{ruledtabular}
\begin{tabular}{lrrrr}
Quintile & $f_{\rm CW}$ & $\sigma_{\rm obs}$ & $\sigma_{\rm pred}$ & $|\sigma_{\rm obs}-\sigma_{\rm pred}|$ \\
\hline
1 (lowest)  & 0.4976 & $-1.94$ & $-2.07$ & 0.13 \\
2           & 0.4987 & $-1.06$ & $-2.07$ & 1.01 \\
3           & 0.4950 & $-3.94$ & $-2.07$ & 1.87 \\
4           & 0.4961 & $-3.08$ & $-2.07$ & 1.01 \\
5 (highest) & 0.4985 & $-1.16$ & $-2.07$ & 0.91 \\
\end{tabular}
\end{ruledtabular}
\end{table}

\begin{figure*}[t]
\centering
\includegraphics[width=0.95\textwidth]{fig_p5_cw_vs_density.png}
\caption{Density-quintile null with Paper IV monopole-prediction
overlay. \emph{Left:} CW fraction per projected-density quintile
($k\!=\!5$ NN proxy, $N\!=\!158{,}327$ per bin) with $95\%$ Jeffreys
binomial CIs; dashed parity $f_{\rm CW}\!=\!0.5$ and dotted
Paper~IV $\bar f_{\rm CW}\!=\!0.4974$ references. \emph{Right:}
observed $\sigma_{\rm from\,half}$ per quintile (bars) vs the
Paper~IV-monopole prediction $\sigma_{\rm pred}\!=\!2\Delta f_{\rm CW}\sqrt{N}$
(red diamonds) at $\Delta f_{\rm CW}\!=\!-0.0026$; dotted blue lines
mark the Bonferroni-5 thresholds at $\alpha\!=\!0.01$. The observed
signed $\sigma$ tracks the monopole prediction within counting
statistics; no quintile deviates from the prediction by more than
$\sim\!2\sigma$.}
\label{fig:cw_vs_density}
\end{figure*}

\subsection{Within-class density-stratified cluster + filament
follow-up}\label{sec:results_within_class_density}

The catalog-wide-monopole-projected cluster-class deviation of $-4.7\sigma$ at
$n_{\rm cluster} = 397{,}505$ (Section~\ref{sec:results_vweb}) is the
strongest single-class signal in the headline table. To check whether
this is a genuinely density-dependent effect within the cluster class
or a boundary-misclassification artifact at the low-density edge of
the V-Web cluster label, we stratify the cluster (and filament for
comparison) class into four equal-population density quartiles using
the V-Web per-galaxy density field.

\begin{table}[ht]
\caption{Within-class density-stratified $f_{\rm CW}$ for the V-Web
cluster and filament classes (matched-spiral $n_{\rm cluster} =
397{,}505$, $n_{\rm filament} = 408{,}187$). Quartiles binned by the
V-Web per-galaxy \emph{log}-density covariate of \S\ref{sec:vweb_algo}
step~12; $\bar\rho$ is the quartile mean of
$\log_{10}(1 + \delta_{\rm smooth})$ (Gaussian-smoothed overdensity
in units of the cube-mean cell density, NN-interpolated to each
galaxy position; dimensionless). Quartile membership is identical
under the linear density (monotone transform); definition verified by
exact recompute in
\artifact{pipelines/p5\_desi\_chirality/outputs/21\_r23conf\_meta\_closures.json}.
The $\sigma_{\rm from\,half}$ values scale as $\sqrt{n}$ at fixed
offset and are not comparable across rows of different $n$; the
inferential comparison is the monopole-referenced residual of
\S\ref{sec:stats}.}
\label{tab:within_class_density}
\begin{ruledtabular}
\begin{tabular}{lrrrr}
Class & Quartile & $\bar\rho$ & $n$ & $\sigma_{\rm from\ half}$ \\
\hline
Cluster  & Q1 & $1.55$ & $99{,}398$ & $-3.07$ \\
         & Q2 & $1.80$ & $99{,}369$ & $-3.42$ \\
         & Q3 & $2.01$ & $99{,}526$ & $-0.37$ \\
         & Q4 & $2.21$ & $99{,}212$ & $-2.46$ \\
Filament & Q1 & $0.90$ & $102{,}050$ & $-0.69$ \\
         & Q2 & $1.34$ & $102{,}065$ & $-1.97$ \\
         & Q3 & $1.58$ & $102{,}033$ & $-0.63$ \\
         & Q4 & $1.86$ & $102{,}039$ & $-1.92$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

The cluster signal is \emph{not} monotonically increasing in density.
The most-typical-cluster-density quartile Q3 ($\bar\rho = 2.01$,
$n = 99{,}526$) returns $\sigma = -0.37$, statistically null after
Bonferroni-4 correction. The strongest cluster sub-deviations are
Q1+Q2 at $\bar\rho \approx 1.55$--$1.80$ (the low-density edge of
the cluster class, where the V-Web $\lambda_{\rm th} = 0$ default
class boundary against filament is most uncertain), while the densest
quartile Q4 returns intermediate $\sigma = -2.46$. Treating the four
cluster quartiles as a simultaneous family, the monopole-subtracted
residuals $\sigma_{\rm obs} - \sigma_{\rm pred}$ at
$\sigma_{\rm pred} = 2\,(-0.0026)\sqrt{n} \approx -1.64$ are $-1.43$
(Q1), $-1.78$ (Q2), $+1.27$ (Q3), and $-0.82$ (Q4) --- all four below
the Bonferroni-4 $\alpha = 0.05$ threshold of $2.50$, so no quartile
shows a significant departure from the monopole-only model after
multiplicity correction. The catalog-wide-monopole-projected
$-4.7\sigma$ cluster signal is therefore not a clean density-dependent
chirality effect; it is concentrated at the cluster/filament class
boundary and is consistent with a boundary-misclassification leakage
from the much larger filament class (whose own quartile deviations
$|\sigma| < 2$ are uniformly small). The class boundary is verifiable
quantitatively from the table: cluster Q1 ($\bar\rho = 1.55$) is
\emph{less dense} than filament Q4 ($\bar\rho = 1.86$), so the
densest filament galaxies are in fact denser than the least-dense
cluster galaxies -- the two classes overlap in $\bar\rho$ at the
$\lambda_{\rm th} = 0$ boundary by construction. The filament class shows no
density trend; the four quartile $\sigma$ values range
$-0.63$--$-1.97$, all individually below the Bonferroni-4 $|\sigma| =
2.50$ threshold at $\alpha = 0.05$.

This within-class density-stratified follow-up reinforces the
headline environment-independence finding: the only V-Web class that
crosses naive $|\sigma| = 3$ at the class level (cluster, $-4.7\sigma$)
does not survive within-class density stratification as a clean
density-dependent effect.

\paragraph{Redshift-stratified cross-check.} A second decomposition
bins the cluster class into four redshift quartiles to test whether
the boundary-leakage interpretation depends on redshift. Marginalizing
over density, the cluster $\sigma_{\rm from\ half}$ per
$z$-quartile is $-2.33$ (Z1, $\bar z = 0.045$, $n = 99{,}377$),
$-1.73$ (Z2, $\bar z = 0.083$, $n = 99{,}376$), $-3.14$ (Z3,
$\bar z = 0.122$, $n = 99{,}376$), and $-2.12$ (Z4, $\bar z = 0.190$,
$n = 99{,}376$). All four $z$-quartile deviations sit in the
$-1.7$ to $-3.2\sigma$ band; one of the four (Z3, $-3.14$)
marginally exceeds the Bonferroni-4 $|\sigma| = 3.02$ threshold at
$\alpha = 0.01$, while the other three do not. The Paper~IV monopole
prediction at the common quartile population ($n \approx 99{,}376$)
is $\sigma_{\rm pred} = 2 \cdot (-0.0026) \cdot \sqrt{99{,}376}
= -1.64$, so the monopole-subtracted quartile residuals
$\sigma_{\rm obs} - \sigma_{\rm pred}$ are $-0.69$ (Z1), $-0.09$
(Z2), $-1.50$ (Z3), and $-0.48$ (Z4) — all null on the
monopole-subtracted basis used throughout, including the one
quartile that crosses the raw threshold. The
deviation is approximately uniform across redshift, consistent with
a stationary classifier-bias monopole (constant $f_{\rm CW}$ offset)
rather than a redshift-dependent astrophysical signature.
The 2D $z$-quartile $\times$ density-quartile decomposition
(sixteen-cell table available in the companion data repository) shows that the strongest
sub-cells -- $(Z_3, D_2)$ at $\sigma = -3.00$, $(Z_1, D_4)$ at
$\sigma = -2.70$, $(Z_4, D_1)$ at $\sigma = -2.67$ -- do not
preferentially align with any single redshift bin, ruling out an
evolutionary cluster-density-chirality coupling at the present
sample size.

\paragraph{Tracer-program stratification.} A fourth orthogonal
cross-check stratifies the full matched-spiral catalog by DESI
program (the broad selection-function bucket: \texttt{bright},
\texttt{dark}, \texttt{backup}, \texttt{other}.
Per-program $f_{\rm CW}$ and $\sigma_{\rm from\ half}$:
\texttt{bright} (BGS-dominated; $n = 775{,}760$, $\bar z = 0.145$)
$f_{\rm CW} = 0.4970$, $\sigma = -5.25$; \texttt{dark} (LRG, ELG, QSO;
$n = 14{,}782$, $\bar z = 0.255$) $f_{\rm CW} = 0.5051$,
$\sigma = +1.25$; \texttt{backup} ($n = 875$) $f_{\rm CW} = 0.5143$,
$\sigma = +0.85$; \texttt{other} ($n = 218$) $f_{\rm CW} = 0.4954$,
$\sigma = -0.14$. \textbf{The catalog-wide $-5\sigma$ monopole offset is
entirely driven by the \texttt{bright} program}; \texttt{dark} returns
$\sigma = +1.25$ (null in the opposite sign), \texttt{backup} returns
$\sigma = +0.85$ (null), \texttt{other} returns $\sigma = -0.14$
(null). A genuinely environment-dependent or astrophysical chirality
signal would propagate across BGS-bright and LRG/ELG/QSO-dark targets
at comparable strength; the bright-specific concentration is consistent
with the BGS-selection-function-conditioned imaging-leg systematics
that Paper~IV tracks in detail rather than a real environment-driven
effect, and reinforces the headline environment-independence finding.

\paragraph{Filament-class within-class decomposition.} Repeating the
density-quartile / $z$-quartile / tracer-program decompositions on
the second-largest V-Web class (filament, $n_{\rm filament} =
408{,}187$, the volume-dominant population in the matched-spiral
catalog) returns the same triple null pattern as cluster. Density-
quartile $\sigma_{\rm from\ half}$ already reported in
Table~\ref{tab:within_class_density} are all $|\sigma| < 2$ (range
$-0.63$ to $-1.97$). $z$-quartile-marginalized $\sigma$ values:
Z1 ($\bar z = 0.067$, $n = 102{,}047$) $\sigma = -1.37$;
Z2 ($\bar z = 0.139$) $-1.72$; Z3 ($\bar z = 0.205$) $-1.57$;
Z4 ($\bar z = 0.320$) $-0.55$. All four within $|\sigma| < 2$.

The filament-class tracer-program decomposition reproduces the cluster-
class \texttt{bright}-vs-\texttt{dark} sign-flip discussed above on an
independent V-Web class. On the declared env-labeled parent of
Table~\ref{tab:cw_vs_env} (per-program splits sum to the class totals
by construction): filament \texttt{bright} ($n = 394{,}181$,
$f_{\rm CW} = 0.4976$) $\sigma = -2.98$ vs filament \texttt{dark}
($n = 13{,}759$, $f_{\rm CW} = 0.5069$)
$\sigma = +1.61$ (\emph{opposite sign} again), a two-sample
$|z| \approx 2.1$. We flag one structural caveat on this $2.1\sigma$
statistic: it is computed at the \emph{row} level of the
$812{,}793$-row env-labeled parent, where a galaxy observed under
both programs contributes one row to \emph{each} program split with
the same chirality label, so the bright and dark splits are not
disjoint in unique \texttt{TARGETID}s. The overlap is material: the
per-class dark rows already exceed the unique dark population
(filament-dark $13{,}759$ $+$ cluster-dark $4{,}234$ $= 17{,}993$
rows vs $14{,}782$ unique dark spirals in the matched catalog,
\S\ref{sec:systematics}), and the committed artifacts do not carry a
per-class unique-\texttt{TARGETID} program split, so the row-level
two-sample $z$ — which assumes disjoint independent samples — should
be read as approximate. The overlap-free version of the test exists
only at the whole-catalog level, where the unique-galaxy
bright-vs-dark split gives $|z| \approx 2.0$
(\artifact{pipelines/p5\_desi\_chirality/outputs/17\_v0151\_closure\_recomputes.json});
the omnibus homogeneity tests
($p = 0.31$ canonical, $p = 0.99$ selection-corrected;
\S\ref{sec:results_vweb}) remain the primary statistics of this
paper. An earlier draft quoted filament
bright/dark $n$ of $416{,}701$/$21{,}203$ with a $3.4\sigma$
two-sample split; those values were computed on an unfiltered
nearest-label join that bypassed the matched-primary deduplication
(its bright$+$dark total exceeded the filament class total) and are
withdrawn in favor of the declared-parent recompute
(\artifact{pipelines/p5\_desi\_chirality/outputs/17\_v0151\_closure\_recomputes.json}).
A genuinely environment-dependent
filament-chirality signal would propagate consistently across both
target classes; the sign-flip recurrence across both the cluster and
filament classes (the two largest V-Web environments) supports the
reading that the V-Web class-level $f_{\rm CW}$ deviations are sourced
by the BGS-selection-function-conditioned imaging-leg systematics
Paper~IV tracks, not by environment-driven astrophysics.

\paragraph{V-Web class vs.\ target-program orthogonality.} The BGS-selection-function-origin interpretation
above relies on V-Web environmental classification being approximately
orthogonal to the \texttt{bright}/\texttt{dark} target split: if
V-Web class were strongly correlated with target program, then a
genuine target-class-dependent astrophysical chirality signal could
masquerade as an environment-class signal (or vice versa). We test
this directly on the matched-spiral catalog. The per-V-Web-class
\texttt{bright}/(\texttt{bright}+\texttt{dark}) ratio is
$\{0.981,\,0.962,\,0.966,\,0.989\}$ across
\{void, wall, filament, cluster\}, against the overall
matched-spiral ratio $0.978$. A two-way contingency test
(V-Web class $\times$ \texttt{bright}/\texttt{dark}, four-by-two
on the $n_{\rm bright+dark} = 811{,}609$ bright$+$dark subset of the
$812{,}793$-row env-labeled parent) gives
$\chi^{2} = 4932$ with three degrees of freedom and
$p \ll 10^{-300}$ ($\log_{10} p \approx -1069$; below double-precision
underflow) and Cram\'er's $V = \sqrt{\chi^2/n} = 0.078$ (small
effect despite the enormous sample) ---
V-Web class and target program are \emph{not} independent;
the cluster class is the most-bright (98.9\%) and the wall class is the
least-bright (96.2\%), with the void and filament classes between.
The maximum class-to-overall deviation in bright-fraction is $1.5$\,pp.
We therefore cannot assert
V-Web class orthogonality to the target-program split, and
the $|z|\!\approx\!2.1\sigma$ \texttt{bright}-vs-\texttt{dark} sign-flip
in the filament class
is best read as a residual structure that the current data do
not allow us to cleanly partition between
(a) a BGS-selection-function-only origin propagated through a
V-Web-class-correlated target-program distribution, and
(b) a residual class-and-target-program-conditioned astrophysical
signal at the $\sim\!2\sigma$ level on $n_{\rm dark}^{\rm filament}
= 13{,}759$.
Together with the catalog-level bright-vs-dark difference
($0.81$\,pp, $|z| \approx 2.0$; \S\ref{sec:systematics}), this is the
most notable residual structure in the paper after
the catalog-monopole subtraction; the DESIVAST-anchored primary
analysis (\S\ref{sec:desivast_primary}) is constructed to be
independent of this residual, because the DESIVAST void definition
restricts to the volume-limited $z \leq 0.24$ BGS sample where
target-program mixing is far more constrained. Contingency-test reproduction is a $\chi^{2}$ on the env-class
$\times$ tracer-program cross-tab derivable from the
\texttt{desi\_env\_vweb.parquet} + matched-spiral catalog join
documented in \S\ref{sec:vweb_algo}.

\textbf{Logistic regression controlling for target program.}
To formally assess whether V-Web environmental coefficients survive
program control, we fit two logistic-regression models on the
$n = 782{,}710$ bright$+$dark env-matched spirals
(filament = reference class):
$M_0$: $\Pr(\rm CW) \sim \beta_0 + \beta_{\rm env}$; and
$M_1$: $\Pr(\rm CW) \sim \beta_0 + \beta_{\rm env} + \beta_{\rm bright}$.
The joint Wald test on the three V-Web environment coefficients gives
$\chi^2 = 2.75$ ($p = 0.43$) in $M_0$ and $\chi^2 = 2.25$ ($p = 0.52$)
in $M_1$, both far from significance. The bright-program indicator
itself is marginally significant ($z = -1.97$, $p = 0.049$), consistent
with the known bright-program monopole. Crucially, the three V-Web
environment coefficients (void, wall, cluster vs filament) shift by
$\leq 0.12$ on their standard errors after program adjustment
in $M_1$ relative to $M_0$ (void $0.006\,\hat\sigma$, wall $0.10\,\hat\sigma$,
cluster $0.11\,\hat\sigma$; shifts computed as $|\beta_{M_1}-\beta_{M_0}|/\mathrm{SE}_{M_0}$
from \artifact{pipelines/p5\_desi\_chirality/outputs/27\_ext1\_logistic\_program\_control.json}),
confirming that the environmental-independence
null is not confounded by the V-Web$\times$program non-orthogonality.
Artifact:
\artifact{pipelines/p5\_desi\_chirality/outputs/27\_ext1\_logistic\_program\_control.json}.

\subsection{Sky-position regional coherence}\label{sec:results_healpix}

HEALPix per-pixel CW-deviation scans at NSIDE $\in \{16, 32, 64\}$,
compared against $1{,}000$-shuffle label-shuffle nulls:

\begin{table}[ht]
\caption{HEALPix per-pixel CW-deviation scan with label-shuffle nulls.
$n_{\rm pix}$ is the number of occupied (data-containing) pixels used
in each scan, not the full-sky pixel count.}
\label{tab:healpix}
\begin{ruledtabular}
\begin{tabular}{rrrrr}
NSIDE & $n_{\rm pix}$ & $|\sigma|^{\rm obs}_{\max}$ &
$|\sigma|^{\rm null,p99}_{\max}$ & $p$ \\
\hline
$16$ & $1{,}054$ & $3.32$ & $4.50$ & $0.607$ \\
$32$ & $3{,}303$ & $4.13$ & $4.78$ & $0.135$ \\
$64$ & $7{,}208$ & $3.92$ & $4.77$ & $0.413$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

No NSIDE returns $p<0.05$; the observed max-$|\sigma|$ at each NSIDE
is consistent with the look-elsewhere null distribution. Because the
free label-shuffle could in principle be anticonservative given the
per-leg/per-program residuals of \S\ref{sec:systematics}, we re-ran
all three scans with the shuffle \emph{stratified} by imaging leg and
DESI program: the stratified look-elsewhere $p$-values agree with
their matched free-shuffle re-draws within one Monte-Carlo standard
error ($p = 0.63 / 0.089 / 0.41$ stratified vs
$0.64 / 0.10 / 0.42$ free-shuffle re-draws at NSIDE $16/32/64$,
pairwise differences $\leq 0.011$ against
${\rm se}_{\rm MC} = \sqrt{p(1-p)/N_{\rm MC}} \approx 0.009$--$0.015$).
The re-draws use RNG streams distinct from the deterministic-seed
headline runs; the NSIDE-16/64 re-draws sit within one
${\rm se}_{\rm MC}$ of the headline $0.607/0.413$, while the NSIDE-32
re-draw ($0.10$) sits ${\sim}2.4\times$ the pairwise
${\rm se}_{\rm MC}$ below the headline $0.135$ --- a stream-to-stream
fluctuation larger than typical but immaterial to the verdict, since
every variant ($0.089$--$0.135$) is far from the $p<0.05$ criterion;
\artifact{pipelines/p5\_desi\_chirality/outputs/18\_v0151\_stratified\_and\_density.json}).
Figure~\ref{fig:healpix_skymap} visualizes the per-pixel signed
$\sigma_{\rm from\,half}$ at NSIDE=32 as a Mollweide projection; no
coherent large-scale structure is visible, matching the $p\!=\!0.135$
null verdict.

\begin{figure*}[t]
\centering
\includegraphics[width=0.85\textwidth]{fig_p5_healpix_skymap_nside32.png}
\caption{Per-pixel signed $\sigma_{\rm from\,half}$ for the
chirality-relevant matched-spiral subsample at NSIDE=32 (Mollweide
projection, equatorial coordinates). The observed
$|\sigma|^{\rm obs}_{\max}\!=\!4.13$ vs the label-shuffle null
$|\sigma|^{\rm null,\,p99}_{\max}\!=\!4.78$ gives a look-elsewhere
$p\!=\!0.135$; no NSIDE returns $p\!<\!0.05$. The map shows no coherent
large-scale structure beyond random pixel-level scatter; the high-$|\sigma|$
pixels are isolated rather than clustered, consistent with the
counting-statistics distribution expected from label-shuffle.}
\label{fig:healpix_skymap}
\end{figure*}

\section{Phase 2 sensitivity sweep}\label{sec:phase2}

To test the robustness of the Section~\ref{sec:results_vweb} headline
against V-Web hyperparameter choices, we run a Phase~2 sweep over nine
cells $R_s \in \{10, 25, 50\}\,{\rm Mpc}/h \times N_{\rm grid}\!=\!256^3
\times \lambda_{\rm th} \in \{0.0, 0.1, 0.3\}$. Each cell is a complete
V-Web rebuild on the $14{,}622{,}283$-galaxy DR1 spectro sample, joined
per cell to the declared $812{,}793$-row env-labeled spiral parent of
Table~\ref{tab:cw_vs_env} (the per-cell sweep parquets and configs are
in
\href{https://github.com/Hubify-Projects/bigbounce/tree/main/pipelines/p5_desi_chirality/data/desi_env/phase2_sweep}{\nolinkurl{pipelines/p5\_desi\_chirality/data/desi\_env/phase2\_sweep/}};
the per-cell statistics below are recomputed in
\artifact{pipelines/p5\_desi\_chirality/outputs/17\_v0151\_closure\_recomputes.json}).
An earlier draft of this table reported per-cell ranges of
$0.05$--$0.22$\,pp computed on an unfiltered nearest-label join
($5{,}571{,}152$ CW/CCW rows including non-primary and duplicate
matches, which inflates every class $n$ and shrinks the range
statistic); those values are withdrawn in favor of the declared-parent
recompute below.

\emph{Grid-resolution caveat on the $R_s = 10$ cells.} At $256^3$ the
grid cell is $25.9\,{\rm Mpc}/h$, so the $R_s = 10\,{\rm Mpc}/h$
Gaussian kernel is \emph{below the grid sampling scale}: the field
carries no modes at that scale and those three cells behave as a
near-unsmoothed pixelated-field limit rather than a physically
distinct smoothing choice. The grid-convergence test of
\S\ref{sec:zshell_robustness} validates only $R_s = 25\,{\rm Mpc}/h$
(at $N_{\rm grid} = 128^3$--$384^3$). We therefore retain the
$R_s = 10$ rows for completeness but exclude them from the physical
robustness claim; restricted to the six resolved cells
($R_s \in \{25, 50\}$), the maximum monopole-subtracted residual is
$1.64\sigma$ (instead of $1.87\sigma$, which occurs in an
under-resolved $R_s = 10$ cell) and the label-shuffle look-elsewhere
$p$-values span $0.13$--$0.48$.

\emph{Mask-dilation scaling across the sweep.} The footprint-dilation
iteration count is not frozen at the canonical value: each sweep cell
recomputes $\lceil R_s/{\rm cell}\rceil + 1$ from its own $R_s$ (2
iterations at $R_s \in \{10, 25\}$, 3 at $R_s = 50$), and a local
rebuild reproduces the published $R_s\!=\!50$ mask to within 4 of
$3{,}416{,}329$ in-mask cells (volume fractions agree to
$<\!10^{-6}$). The counterfactual $R_s\!=\!50$ rebuild that retains
the $R_s\!=\!25$-tuned 2-iteration mask shifts class volumes by
$\le 0.82$\,pp, retains $99.75\%$ of matched-spiral class
assignments, and moves per-class $f_{\rm CW}$ by at most $0.27$\,pp
(the $n\!=\!406$ void bin), leaving every sweep conclusion unchanged
(\artifact{pipelines/p5\_desi\_chirality/outputs/26\_r27conf\_ess\_recomputes.json}).

\begin{table*}[t]
\caption{Phase 2 sensitivity sweep on the declared env-labeled parent:
per-cell cross-class range of $f_{\rm CW}$ (pp), void-class population
(the bin whose counting noise dominates the range), maximum per-class
monopole-subtracted residual $|\sigma_{\rm obs}-\sigma_{\rm pred}|$
[Eq.~\eqref{eq:sigma_pred}], and label-shuffle look-elsewhere
$p$-value on the max-class $|\sigma_{\rm from\,half}|$ statistic
($N_{\rm MC}=1{,}000$). The range column is dominated in every cell
by the void-bin binomial error at these $n_{\rm void}$. Per-cell
void-bin deviations (top row to bottom):
$|\sigma_{\rm void}| = 0.47 / 1.07 / 0.93 / 0.68 / 1.08 / 0.86 /
0.95 / 1.35 / 0.87$, never exceeding $1.35$
(\artifact{pipelines/p5\_desi\_chirality/outputs/17\_v0151\_closure\_recomputes.json}).
The $p_{\rm LEE}$ values are corrected within each cell's own
$K\!=\!4$ class family only; they are not globally corrected across
the other scan families in this paper. A rerun with imaging-leg
$\times$ program \emph{stratified} shuffles leaves every cell's
$p_{\rm LEE}$ essentially unchanged (stratified $0.14$--$0.54$ vs.\
free $0.16$--$0.55$; $N_{\rm MC}=1{,}000$), and a global max-stat
correction across cells (one parent permutation propagated through
all nine cells per draw, preserving cross-cell correlations) gives
$p_{\rm global} = 0.36$ over all nine cells and $0.27$ over the six
resolved $R_s \in \{25,50\}$ cells
(\artifact{pipelines/p5\_desi\_chirality/outputs/24\_r24conf\_pod\_session.json}). The $R_s\!=\!10$ rows sit below the
$25.9\,{\rm Mpc}/h$ grid sampling scale and are retained only as a
degenerate near-unsmoothed limit, excluded from the robustness claim
(\S\ref{sec:phase2}).}
\label{tab:phase2}
\begin{ruledtabular}
\begin{tabular}{rrrrrr}
$R_s$ (Mpc$/h$) & $\lambda_{\rm th}$ & range (pp) & $n_{\rm void}$ &
max $|\sigma_{\rm obs}-\sigma_{\rm pred}|$ & $p_{\rm LEE}$ \\
\hline
\multicolumn{6}{l}{\textit{Grid-unresolved ($R_s < 25.9$\,Mpc$/h$ grid scale; retained for completeness only, excluded from robustness claim)}} \\
$10^\dagger$ & $0.0$ & $1.72$ & $363$ & $1.71$ & $0.56$ \\
$10^\dagger$ & $0.1$ & $2.71$ & $594$ & $1.87$ & $0.42$ \\
$10^\dagger$ & $0.3$ & $2.01$ & $837$ & $1.69$ & $0.38$ \\
\hline
\multicolumn{6}{l}{\textit{Resolved cells ($R_s \gtrsim$ cell size $= 25.9$\,Mpc$/h$; $R_s \in \{25, 50\}$ retained; robustness claim based on these six cells)}} \\
$25$ & $0.0$ & $1.97$ & $428$ & $1.38$ & $0.13$ \\
$25$ & $0.1$ & $2.48$ & $627$ & $1.35$ & $0.14$ \\
$25$ & $0.3$ & $1.83$ & $853$ & $1.22$ & $0.23$ \\
$50$ & $0.0$ & $3.69$ & $401$ & $1.39$ & $0.34$ \\
$50$ & $0.1$ & $4.12$ & $599$ & $1.52$ & $0.41$ \\
$50$ & $0.3$ & $2.81$ & $825$ & $1.64$ & $0.48$ \\
\hline
\textbf{max (all 9)} & --- & $\mathbf{4.12}$ & --- &
$\mathbf{1.87}^\dagger$ & --- \\
\textbf{max (resolved 6)} & --- & $\mathbf{4.12}$ & --- &
$\mathbf{1.64}$ & --- \\
\end{tabular}
\end{ruledtabular}
\end{table*}

The per-cell cross-class $f_{\rm CW}$ range spans $1.7$--$4.1$\,pp
across the nine cells and is dominated in every cell by the
counting noise of the small void bin ($n_{\rm void} = 363$--$853$;
$1\sigma$ binomial floor $1.7$--$2.6$\,pp): range is therefore not a
significance statistic at these class populations. The two
significance columns carry the result: the maximum per-class
monopole-subtracted residual never exceeds $1.87\sigma$ (far below the
Bonferroni-9 threshold $|\sigma|^{\rm Bonf}_{0.05,9} \approx 2.77$
from Eq.~\eqref{eq:bonferroni}),
and no cell's label-shuffle look-elsewhere $p$ falls below $0.13$.
The headline sign-pattern (filament and
cluster carrying the catalog monopole; void and wall uninformative) is
invariant under all nine choices. Figure~\ref{fig:phase2} visualizes
the per-cell ranges as a $(R_s, \lambda_{\rm th})$ heat-map.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig_p5_phase2_sensitivity_heatmap.png}
\caption{Phase 2 sensitivity heat-map: per-cell range of $f_{\rm CW}$
across the four environment classes \{void, wall, filament, cluster\}
in percentage points, on the declared env-labeled spiral parent. Each
cell corresponds to a complete V-Web
re-run on the $14{,}622{,}283$-galaxy DESI DR1 spectro sample at
$(R_s, \lambda_{\rm th})$. The maximum range across all nine cells is
$4.12$\,percentage points (at $R_s\!=\!50\,{\rm Mpc}/h,
\lambda_{\rm th}\!=\!0.1$), dominated by the void-bin counting noise
($n_{\rm void} \le 853$ in every cell); the per-class
monopole-subtracted residuals stay below $1.87\sigma$ in all cells
(Table~\ref{tab:phase2}). The headline environmental-independence
result is robust to V-Web hyperparameter choices over the range
probed.}
\label{fig:phase2}
\end{figure}

The largest single-cell $|\sigma_{\rm from\,half}|$ across the entire
sweep is $4.66$ (cluster at the canonical $R_s\!=\!25,
\lambda_{\rm th}\!=\!0$ cell, $n\!=\!397{,}505$). This is the
catalog-wide
$\Delta f_{\rm CW}\!=\!-0.0026$ monopole leaking through a large
sample bin and is \emph{predicted}, not measured:
$\sigma_{\rm pred} = 2 \cdot (-0.0026) \cdot \sqrt{N} \approx -3.3$
leaves a monopole-subtracted residual of $-1.38$. (An earlier draft
quoted $|\sigma| = 11.32$ on an $n\!=\!3{,}696{,}152$ filament cell;
that population belonged to the withdrawn unfiltered nearest-label
join, not to any declared chirality parent of this paper.) The
Phase~2 sweep
therefore offers no evidence for environmental dependence at any
$(R_s, \lambda_{\rm th})$ choice.

\subsection{Per-cell significance framework}\label{sec:phase2_significance}

The per-cell range
of $f_{\rm CW}$ values across the four V-Web classes is a
\emph{descriptive} statistic that bounds the deviation from
class-uniformity within each $(R_s, \lambda_{\rm th})$ choice. We
report it as the leading descriptive robustness summary (the empirical
max-statistic null below is the inferential statement) because it is
sensitive to the residual environmental signal of interest after the
catalog-monopole subtraction. To convert the per-cell range into a
significance statement we use the same two-tier
$\sigma_{\rm pred} = 2 \cdot \Delta f_{\rm CW} \cdot \sqrt{N}$
Paper~IV-monopole reference as the headline analysis
(Eq.~\eqref{eq:sigma_pred}, \S\ref{sec:stats}):

\begin{itemize}
\item \textbf{Counting-statistics floor.} For each $(R_s,
\lambda_{\rm th})$ cell, the per-class $1\sigma$ counting-statistics
uncertainty on $f_{\rm CW}$ at the four V-Web classes is
$1/(2\sqrt{n_{\rm class}})$. For the dominant filament + cluster
classes at $n \sim 4 \times 10^5$ this is
$\sim 0.08$\,percentage points; for the wall class at
$n \sim 2$--$17$k it is $\sim 0.4$--$1.1$\,pp; for the void class at
$n = 363$--$853$ it is
$1.7$--$2.6$\,pp. In every cell the extreme class defining the range
is the \emph{void} bin, and the per-cell range stays within
$1.01\times$ the void-class $2\sigma$ counting floor
$1/\sqrt{n_{\rm void}}$ (maximum
ratio $1.01$ at $R_s\!=\!50, \lambda_{\rm th}\!=\!0.1$); equivalently,
the void bin's own deviation from half satisfies
$|\sigma_{\rm void}| \le 1.35$ in all nine cells. No
$(R_s, \lambda_{\rm th})$ cell shows an
inter-class range that exceeds the counting noise of the class that
produces it by more than $1\%$. We use the void-class $2\sigma$ floor
as a descriptive yardstick locating which class's counting noise
dominates the spread, not as a calibrated bound on the four-class
range statistic (whose null distribution depends on all four bin
sizes); the calibrated per-cell control is the empirical
label-shuffle $p_{\rm LEE}$ column of Table~\ref{tab:phase2}.

\item \textbf{Paper-IV-monopole reference.} The Paper IV
monopole prediction $\sigma_{\rm pred}^{\rm class} = 2 \cdot 0.0026
\cdot \sqrt{n_{\rm class}}$ ranges from $0.10\sigma$ (void) through
$0.42\sigma$ (wall) to $3.27\sigma$ (cluster) and $3.32\sigma$
(filament) at the canonical $R_s\!=\!25, \lambda_{\rm th}\!=\!0$
class populations. Subtracting these per-class predictions from the
per-cell observed $\sigma_{\rm from\,half}$ values yields the
$\sigma_{\rm vs\,monopole}$ residuals reported in
Table~\ref{tab:p4_monopole_residual} of \S\ref{sec:desivast_p4_residual}
(canonical-cell version) on the catalog-monopole $f_{\rm CW}^{\rm P5}$
basis: $|\sigma_{\rm vs\,monopole}|$ at all four classes falls
below $1.15$. The per-cell version of the same statistic is the
max-$|\sigma_{\rm obs}-\sigma_{\rm pred}|$ column of
Table~\ref{tab:phase2}: across all 36 cell--class combinations the
monopole-subtracted residual never exceeds $1.87\sigma$ (wall at
$R_s\!=\!10, \lambda_{\rm th}\!=\!0.1$). The headline conclusion of
this section is therefore: across nine $(R_s, \lambda_{\rm th})$
cells, the per-cell inter-class $f_{\rm CW}$ range is consistent with
the counting noise of the void bin that produces it, and every
per-class
$\sigma_{\rm from\,half}$ value matches the Paper~IV-monopole
prediction within
$|\sigma_{\rm obs}-\sigma_{\rm pred}| \le 1.87$.

\item \textbf{Per-cell label-shuffle null.} For each cell, the
label-shuffle permutation null on the per-class $\sigma_{\rm from\,half}$
statistic (Eq.~\eqref{eq:empirical_p}, $N_{\rm MC}\!=\!1{,}000$,
holding the per-class $n_{\rm class}$ fixed and re-drawing CW/CCW
labels) returns max-class $p$-values in the range $p_{\rm LEE}\!=\!
0.13\text{--}0.56$ across the nine cells (Table~\ref{tab:phase2}),
with no cell crossing
$p\!<\!0.05$. The empirical max-stat null is therefore consistent
with the parametric reading: no $(R_s, \lambda_{\rm th})$ cell shows
an inter-class chirality signal above the counting-statistics
shot-noise null.
\end{itemize}

The combined framework converts the descriptive max-range statistic
into an explicit significance statement: zero of the nine sweep
cells produces a per-cell range exceeding the void-bin
counting-statistics floor that drives it, and zero produces a per-class
$|\sigma_{\rm obs}-\sigma_{\rm pred}|$ residual above the Bonferroni-9
($\alpha
= 0.05$) threshold $|\sigma|^{\rm Bonf}_{0.05,9}\!\approx\!2.77$. The
Phase~2 sensitivity sweep robustness statement is therefore not
purely descriptive: it is bounded by the counting-statistics floor
of the per-class $f_{\rm CW}$ estimator and by the
catalog-monopole-subtracted per-class significance; the empirical
max-statistic permutation null is the component that controls the
false-positive rate, while the range/floor comparison is descriptive.

\section{DESIVAST-anchored void cross-validation}\label{sec:desivast_primary}

This section reports the \emph{primary} environment-dependent
chirality analysis of the paper (per the primary/secondary declaration
of \S\ref{sec:primary_path}): the DESIVAST-anchored cross-validation
of the V-Web void-class result on a ${\sim}130\times$ larger,
peer-reviewed DR1 BGS void catalog spanning three independent
void-finding algorithms (VoidFinder + V2-REVOLVER + V2-VIDE).

The DESIVAST DR1 release (Rinc\'on \textit{et al.}\ 2025, ApJ~982,
38~\cite{DESIVAST2025}) is the publicly released, peer-reviewed
DR1 BGS void catalog at low $z$, using the sphere-growing VoidFinder
algorithm and the watershed ZOBOV/V2 algorithm (REVOLVER and VIDE
prunings) on the volume-limited DR1 Bright Galaxy Survey sample
restricted to $z \leq 0.24$: 1,489 interior voids with VoidFinder,
389 with V2-REVOLVER, and 297 with V2-VIDE (final published counts
from ApJ~982, 38, Table~1; an earlier preprint version cited
1,461/420/295, which were preliminary values). DESIVAST is a void
catalog rather than a full four-class cosmic-web classifier, so the
DESIVAST-anchored chirality null tests environmental dependence on
the cleanest available DR1 void definition.

\emph{RSD treatment for DESIVAST.} The DESIVAST primary
path is RSD-bounded (rather than strictly immune) at the level
relevant to this work --- individual in/out membership flips for
spirals near hole boundaries are not excluded, but the displacement
scale argument below bounds their rate: DESIVAST defines voids via the geometry of large
($R_{\rm eff}\!\gtrsim\!10$\,Mpc/$h$) underdensities in the
volume-limited BGS sample, and the published VoidFinder /
V2-REVOLVER / V2-VIDE catalogs do not require a
reconstructed-position rerun for the void-membership label of an
individual matched spiral: the per-galaxy DESIVAST void/non-void
classification is a single point-in-sphere (or watershed-cell)
test using the void's published effective radius and center, and
the typical Kaiser-plus-finger-of-god displacement $\sigma_v/(aH)
\lesssim 5$\,Mpc/$h$ at $z \lesssim 0.24$ is several times smaller
than the void effective radii. The within-void/non-void chirality
fractions are therefore RSD-bounded at the level of this
fixed-void-geometry membership sensitivity test; full immunity would
require void-catalog reconstruction under RSD, which is not performed
here, but the FoG Monte Carlo below bounds the fractional membership
shift to well below the statistical uncertainty at the present precision. An empirical bound on membership-flip sensitivity
comes from the membership-definition variant of
\S\ref{sec:desivast_maximal_healpix}: swapping the permissive
hole-union criterion for the strict maximal-sphere interior reassigns
$36{,}181$ of $57{,}081$ void spirals --- a membership perturbation
far larger than any plausible RSD-induced boundary-flip set at
$\sigma_v/(aH) \lesssim 5$\,Mpc/$h$ --- and shifts
$\Delta f_{\rm CW}$ by only $0.6$\,pp with the null verdict
unchanged. A direct FoG-scale Monte Carlo sharpens this bound
(holding the published redshift-space void centers and radii fixed,
i.e.\ a fixed-void-geometry membership sensitivity test rather than a
re-derivation of the void catalog under RSD):
perturbing each spiral's line-of-sight comoving distance by a
$\sigma\!=\!5$\,Mpc/$h$ Gaussian (200 realizations at the
$\sigma_v/(aH)$ displacement scale above; used in place of a
deterministic per-galaxy compression, which would require a group
catalog that is not part of the P5 data set) reassigns
$\sim\!4.4\times10^{4}$ hole-union memberships per realization (the
void count rises from $57{,}081$ to $76{,}490 \pm 161$ as boundary
spirals scatter inward), yet $\Delta f_{\rm CW}$ stays within
$[-0.34, +0.37]$\,pp (coherent $\pm 5$\,Mpc/$h$ shifts:
$-0.16$/$+0.03$\,pp) and every realization's void/non-void two-sample
$|z|$ stays below $2\sigma$ (maximum $1.93$;
\artifact{pipelines/p5\_desi\_chirality/outputs/26\_r27conf\_ess\_recomputes.json}).
The conclusion drawn from this Monte Carlo is deliberately narrow:
void \emph{membership} is not insensitive to the perturbation (the
hole-union count moves by ${\sim}34\%$); what is stable under this
fixed-void-geometry perturbation is the estimand $\Delta f_{\rm CW}$
itself. We note that
this RSD-robustness argument applies to the per-galaxy void-membership
test itself; the internal composition of the ``non-void'' control
sample (a mix of walls, filaments, and clusters) is still subject to
RSD-induced boundary shifts between those non-void subclasses, but
those internal shifts do not affect the binary void / non-void
chirality fraction comparison reported here. This is in contrast to the V-Web secondary
path (\S\ref{sec:vweb}, \S\ref{sec:phase2}; limitations in
\S\ref{sec:limitations}), where the tidal-tensor eigenvalue
field is computed from redshift-space galaxy positions on a
$R_s\!=\!25$\,Mpc/$h$ Gaussian smoothing and is therefore
RSD-bounded only at the scalar-displacement level. The primary
P5 environment-independence claim, anchored on the
DESIVAST $\Delta f_{\rm CW}\!\approx\!0.0007$ null at
$n\!=\!56{,}981$, does not depend on the V-Web RSD argument.

\subsection{DESIVAST per-galaxy cross-match (this work)}\label{sec:desivast_xmatch}

The DESIVAST
public release at
\url{https://data.desi.lbl.gov/public/dr1/vac/dr1/desivast/v1.0/}
provides the VoidFinder NGC/SGC FITS files
(\texttt{DESIVAST\_BGS\_VOLLIM\_VoidFinder\_\{NGC,SGC\}.fits},
$89{,}003 + 12{,}860 = 101{,}863$ interior hole spheres comprising the
$3{,}765$ maximal voids). We fetched both files and performed a direct
per-galaxy cross-match of the V-Web void-class matched-spiral
subsample. Restricting the matched-spiral catalog to $z \leq 0.24$
(the DESIVAST BGS limit) leaves only $n = 6$ V-Web void-class spirals
(the V-Web run uses the full $14.6$M DESI spectro sample to $z = 2$,
of which the matched-spiral subsample inherits a flat-but-low-z-rare
distribution). Converting each of the 6 spirals to flat-$\Lambda$CDM
comoving Cartesian coordinates ($H_0 = 67.66$ km/s/Mpc, $\Omega_m =
0.315$, units $h^{-1}$~Mpc consistent with the DESIVAST hole catalog)
and testing point-in-sphere membership against all $101{,}863$ holes
returns \textbf{$0/6$ V-Web ``void'' spirals inside any DESIVAST hole}
in this six-object illustrative check;
minimum spiral-to-nearest-hole separations span $28.7$--$158.1\,$Mpc/$h$.

This $0/6$ disagreement in this six-object draw is consistent with the survey-shell systematic
that drives the V-Web vs T-Web void-fraction discrepancy
(reported in \S\ref{sec:tweb_concurrent} below as a
$+8$--$18\,$pp V-Web excess in the void class):
the V-Web void class at low $z$ is dominated by
survey-edge density artifacts that the DESIVAST volume-limited BGS
sample correctly suppresses. The $n = 6$ sample size is too small for
a binomial significance constraint on the chirality null directly, but
the per-galaxy disagreement quantifies the V-Web void-class purity at
$z \leq 0.24$: $0\%$ concordance with DESIVAST-defined voids on this
draw. With $0$ of $6$, the one-sided $95\%$ binomial upper bound on
the true in-hole fraction is $1 - 0.05^{1/6} = 39\%$ (the standard
one-sided Clopper--Pearson bound $1 - \alpha^{1/n}$ for $0$ successes in
$n$ trials at confidence level $1-\alpha$), so this purity
statement is indicative rather than statistically established;
within that limit it suggests the V-Web ``void'' label at low $z$
should be read as ``not in a DESIVAST-defined cosmic-web density
minimum'' rather than ``confirmed void galaxy.''
This is a direct empirical small-sample illustration of the
$+8$--$18\,$pp V-Web-vs-T-Web void-fraction discrepancy
reported in \S\ref{sec:tweb_concurrent} below,
not a separate effect.

\subsection{DESIVAST-anchored void classifier (this work)}\label{sec:desivast_anchored_void}

Pushing the cross-match further, we redefine the low-$z$ void class on the
catalog-anchored DESIVAST basis rather than the V-Web tidal-tensor
basis, then recompute the chirality null on the much larger
properly-powered subsample.

Restricting the matched-spiral catalog to $z \leq 0.24$ leaves
$n_{\rm lz} = 678{,}945$ spirals (the DESIVAST BGS coverage range).
Converting each spiral to comoving Cartesian and performing a
point-in-sphere test against all $101{,}863$ DESIVAST VoidFinder
hole spheres (via a $k = 20$-nearest-neighbour
\texttt{scipy.spatial.KDTree} query on the hole centres; the maximum
hole radius, computed from the parsed VoidFinder hole catalog, is
$24.5\,$Mpc/$h$) returns
$n_{\rm void}^{\rm DESIVAST} = 56{,}981$ DESIVAST-defined void galaxies
($8.39\%$ of the low-$z$ matched sample) and $n_{\rm non-void} =
621{,}964$ outside any DESIVAST hole.
\emph{$k$-sufficiency guard:} because up to $249$ hole centres can lie
within one maximum-hole-radius of a single galaxy ($28\%$ of the
low-$z$ sample has more than $20$ such candidates), we re-ran the
membership test exactly ($k$-unbounded per-hole radius queries):
the exact rerun moves $100$ galaxies ($+0.18\%$ of the
$56{,}981$-galaxy void class) into the void class
($n_{\rm void} = 57{,}081$, $f_{\rm CW}^{\rm void} = 0.4965$,
$\sigma^{\rm void} = -1.69$; $\Delta f_{\rm CW}$ void-vs-non-void
$+0.0006$ instead of $+0.0007$) — every conclusion in this section is
invariant, and we retain the $k=20$ catalog statistics below for
continuity with the released artifacts
(\artifact{pipelines/p5\_desi\_chirality/outputs/17\_v0151\_closure\_recomputes.json},
which also serves as the committed membership driver). \textbf{The DESIVAST-anchored
void class is $\boldsymbol{\sim\!130\times}$ larger than the V-Web void
class} ($n = 428$ from Section~\ref{sec:results_vweb}), providing a
properly-powered chirality-in-voids test at the cleanest available
public DR1 void catalog.

\begin{table}[ht]
\caption{Chirality fraction in DESIVAST-anchored vs non-void classes
on the $z \leq 0.24$ matched-spiral subsample (point-in-sphere test
against $101{,}863$ DESIVAST VoidFinder hole spheres; the $k=20$
KDTree query yields conclusions identical to the exact $k$-unbounded
rerun at the $0.18\%$ membership level, \S\ref{sec:desivast_xmatch}).}
\label{tab:desivast_canonical}
\begin{ruledtabular}
\begin{tabular}{lrrrr}
Class & $n$ & $n_{\rm CW}$ & $f_{\rm CW}$ & $\sigma_{\rm from\ half}$ \\
\hline
DESIVAST void & $56{,}981$ & $28{,}286$ & $0.4964$ & $-1.71$ \\
Non-void      & $621{,}964$ & $309{,}173$ & $0.4971$ & $-4.59$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

The two classes return $f_{\rm CW}$ values differing by only
$0.0007$ (0.07 percentage points): the void-galaxy and non-void
chirality fractions are \emph{statistically indistinguishable}. The
declared primary estimand is this two-sample contrast, and its full
statistics from the tabulated counts are: $\Delta f_{\rm CW} \equiv
f_{\rm CW}^{\rm non\mbox{-}void} - f_{\rm CW}^{\rm void} = +0.00067$,
${\rm SE}(\Delta) = 0.00219$ (binomial, two-sample), $z_\Delta = +0.31$,
two-sided $p_\Delta = 0.76$, $95\%$ CI $[-0.0036, +0.0050]$ --- a clean
null on the void-vs-non-void contrast itself, independent of the
catalog-level monopole carried by the one-sample $\sigma_{\rm from\,half}$
column. The
stronger $\sigma$ on the non-void class is purely a sample-size
effect ($n_{\rm non-void} / n_{\rm void} \approx 10.9$); both classes
carry the same $-5\sigma$-class P4 monopole signature scaled to their
respective sample sizes. \textbf{When the void class is defined by
DESIVAST rather than V-Web, the chirality-in-voids constraint is a
result consistent with null at full DESIVAST sample size.} 

This DESIVAST-anchored re-analysis is, to our knowledge, the largest
matched-sample
environmental-dependence test of spiral chirality in DESI DR1 to date,
on the chirality-relevant subsample defined here
(a null is not positive evidence; we report it as a controlled-sample
non-detection of the environment-dependence hypothesis on this dataset):
the V-Web $-0.68\sigma$ void deviation on $n = 428$ was counting-
statistics-limited and contaminated by the survey-shell systematic,
and the catalog-anchored re-analysis at $n_{\rm void} = 56{,}981$
returns a result consistent with null with both classes consistent with the catalog-
level uniform classifier-bias monopole.

\textbf{DESIVAST independence from target-program residuals.}
A key concern is whether the DESIVAST null inherits the bright/dark
target-program non-orthogonality documented in
\S\ref{sec:results_within_class_density}. The DESIVAST void definition
restricts to the volume-limited $z \leq 0.24$ BGS sample, where
$98.8\%$ of the $z \leq 0.24$ spirals are in the bright program
($n_{\rm bright}/n_{\rm lz} = 56{,}477/56{,}981 = 99.1\%$ for
void-assigned spirals; $n_{\rm dark} = 469$).
Table~\ref{tab:desivast_program_split} shows the bright/dark
program split of the DESIVAST void and non-void classes explicitly.
Both bright and dark sub-samples return null
chirality fractions within their respective void classes
($|\sigma| \leq 1.80$ for all cells). For completeness, the
within-program void-vs-non-void contrasts are: dark, void
$f_{\rm CW}=0.4584$ ($n=469$) vs.\ non-void $0.5056$ ($n=5{,}845$), a
nominal ${\approx}2.0\sigma$ contrast before any multiplicity
correction, consistent with small-$n$ noise; bright, void
$0.4967$ ($n=56{,}477$) vs.\ non-void $0.4970$ ($n=615{,}078$),
${\approx}0.1\sigma$. Neither constitutes a sign of a
program-conditioned environment signal. The DESIVAST void result
is therefore \emph{not driven by target-program mixing}: with
$99.1\%$ of void-assigned spirals in the bright program, the bright
sub-sample dominates and itself returns a null at $\sigma = -1.56$,
while the dark sub-sample ($n = 469$, $\sigma = -1.80$) is consistent
with the same null. The DESIVAST non-void bright sub-sample
($n = 615{,}078$, $\sigma = -4.72$) carries the full catalog-level
monopole signature scaled to $n$, as expected.
Artifact:
\artifact{pipelines/p5\_desi\_chirality/outputs/28\_ext1\_desivast\_program\_split.json}.

\begin{table}[ht]
\caption{DESIVAST VoidFinder bright/dark target-program split of
the void and non-void classes on the $z \leq 0.24$ matched-spiral
subsample ($n_{\rm lz} = 678{,}945$). Both programs return null
chirality fractions within the void class, confirming DESIVAST
independence from target-program residuals.}
\label{tab:desivast_program_split}
\begin{ruledtabular}
\begin{tabular}{llrrrr}
Class & Program & $n$ & $n_{\rm CW}$ & $f_{\rm CW}$ & $\sigma$ \\
\hline
Void     & bright & $56{,}477$ & $28{,}053$ & $0.4967$ & $-1.56$ \\
Void     & dark   & $469$      & $215$      & $0.4584$ & $-1.80$ \\
Non-void & bright & $615{,}078$ & $305{,}688$ & $0.4970$ & $-4.72$ \\
Non-void & dark   & $5{,}845$  & $2{,}955$  & $0.5056$ & $+0.85$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

\subsection{Three-algorithm DESIVAST robustness cross-check}\label{sec:desivast_three_algo}

The DESIVAST DR1 release ships three independent void definitions: the
sphere-growing VoidFinder algorithm above, plus the watershed
algorithms V2-REVOLVER ($n_{\rm void}^{\rm catalog} = 1{,}992$ effective
voids, maximum effective radius $43.5\,$Mpc/$h$) and V2-VIDE
($n_{\rm void}^{\rm catalog} = 1{,}478$, max $55.9\,$Mpc/$h$). These are
whole-catalog totals of \emph{effective voids}; the $389$ (V2-REVOLVER)
and $297$ (V2-VIDE) interior void counts quoted in the section opening are the
\emph{interior} (non-edge) void subsets at the $z \le 0.24$ statement
— two different object selections from the same release, not a
contradiction. Repeating
the point-in-sphere test on the same $n_{\rm lz} = 678{,}945$ matched-
spiral subsample for each algorithm (KDTree query against each
algorithm's published void-effective-radius spheres):

\begin{table*}[ht]
\caption{Three-algorithm DESIVAST void vs non-void chirality on the
$z \leq 0.24$ matched-spiral subsample ($n_{\rm lz} = 678{,}945$).
Sign convention: $\Delta f_{\rm CW} \equiv f_{\rm CW}^{\rm non\mbox{-}void}
- f_{\rm CW}^{\rm void}$. The last four columns give the two-sample
binomial contrast statistics of the declared primary estimand
$\Delta f_{\rm CW}$ (standard error, $z$-score, two-sided $p$, and
$95\%$ confidence interval), computed from the tabulated class counts.}
\label{tab:desivast_three_algo}
\begin{ruledtabular}
\begin{tabular}{lrrrrrrrrr}
Algorithm & $n_{\rm void}$ & $f_{\rm CW}^{\rm void}$ &
$\sigma^{\rm void}$ & $f_{\rm CW}^{\rm non-void}$ &
$\sigma^{\rm non-void}$ & $\Delta f_{\rm CW}$ & ${\rm SE}(\Delta)$ &
$z_\Delta$ ($p_\Delta$) & $95\%$ CI \\
\hline
VoidFinder   & $56{,}981$  & $0.4964$ & $-1.71$ & $0.4971$ & $-4.59$ & $+0.0007$ & $0.0022$ & $+0.31$ ($0.76$) & $[-0.0036, +0.0050]$ \\
V2-REVOLVER  & $102{,}911$ & $0.4986$ & $-0.88$ & $0.4967$ & $-4.94$ & $-0.0019$ & $0.0017$ & $-1.12$ ($0.26$) & $[-0.0052, +0.0014]$ \\
V2-VIDE      & $81{,}354$  & $0.4971$ & $-1.67$ & $0.4970$ & $-4.59$ & $-0.0001$ & $0.0019$ & $-0.05$ ($0.96$) & $[-0.0038, +0.0036]$ \\
\end{tabular}
\end{ruledtabular}
\end{table*}

All three algorithms return $|\Delta f_{\rm CW}| \lesssim 0.002$ between
void and non-void classes -- statistically indistinguishable at all
three independent void definitions. V2-REVOLVER returns
$f_{\rm CW}^{\rm void} = 0.4986$ slightly above $f_{\rm CW}^{\rm
non-void} = 0.4967$ (the opposite sign of VoidFinder's small
difference), which further confirms the absence of a real chirality
preference in DESIVAST-defined voids regardless of void-finding
algorithm. The void-class $\sigma$ values span $-1.71$ to $-0.88$ --
none crosses $|\sigma| = 2$; the non-void $\sigma$ values cluster
tightly at $-4.59$ to $-4.94$ because the non-void class is the
volume-dominant population carrying the full catalog-level monopole
signature.

The three-algorithm robustness extends the P5 headline
environment-independence test across the void-finding-algorithm axis
available from current public DR1 cosmic-web catalogs.

\subsection{Catalog-native V2 membership cross-check}\label{sec:desivast_catalog_native}

The watershed
DESIVAST catalogs (V2-REVOLVER and V2-VIDE) ship with per-galaxy zone
assignments via the \texttt{GALZONE} HDU (mapping each DESI BGS target
to a \texttt{ZONE} index, with \texttt{OUT}, \texttt{EDGE}, and
\texttt{DEPTH} flags) plus the \texttt{ZONEVOID} HDU (mapping each
zone to a \texttt{VOID0} index, $\geq 0$ if the zone belongs to a
non-edge void). Adopting the catalog-native definition of void
membership ($\texttt{OUT} = 0 \wedge \texttt{VOID0} \geq 0 \wedge
\texttt{ZONE} \geq 0$) and joining on \texttt{TARGET} =
\texttt{desi\_targetid} returns: V2-REVOLVER $n_{\rm void} = 104{,}912$,
$f_{\rm CW}^{\rm void} = 0.4992$, $\sigma^{\rm void} = -0.52$; V2-VIDE
$n_{\rm void} = 74{,}111$, $f_{\rm CW}^{\rm void} = 0.4972$,
$\sigma^{\rm void} = -1.50$. An earlier draft reported
$n_{\rm void} = 86{,}276$ / $64{,}514$ with $\sigma = -0.24$ / $-1.06$;
those values reproduce exactly only under a zone-indexing defect in
the NGC$+$SGC join (the two galactic caps' \texttt{ZONEVOID} tables
were concatenated without offsetting their cap-local zone indices, so
SGC rows overwrote the NGC zone-to-void assignments). The corrected
per-cap join values above supersede them
(\artifact{pipelines/p5\_desi\_chirality/outputs/22\_r24conf\_local\_batch.json},
which records both the corrected join and the exact reproduction of
the earlier numbers under the defect).

Both corrected void $\sigma$ values remain null and are
\emph{smaller in magnitude} than the sphere-approximation analogues
(V2-REVOLVER catalog-native $-0.52$ vs sphere $-0.88$; V2-VIDE
catalog-native $-1.50$ vs sphere $-1.67$), confirming that the
catalog-native void definition is the cleaner statistic.
The declared primary estimand $\Delta f_{\rm CW} \equiv
f_{\rm CW}^{\rm non\mbox{-}void} - f_{\rm CW}^{\rm void}$
(sign convention of Table~\ref{tab:desivast_three_algo}) is now also
tabulated on the catalog-native axis, with the non-void complement
defined as the GALZONE-joined rows that fail the catalog-native void
cut (\texttt{OUT}$=0\,\wedge\,$\texttt{ZONE}$\geq 0\,\wedge\,$%
\texttt{VOID0}$\geq 0$). V2-REVOLVER: $n_{\rm void} = 104{,}912$
($f_{\rm CW}^{\rm void} = 0.4992$) vs $n_{\rm non\mbox{-}void} =
40{,}877$ ($f_{\rm CW}^{\rm non\mbox{-}void} = 0.4955$),
$\Delta f_{\rm CW} = -0.0037$, ${\rm SE}(\Delta) = 0.0029$,
$z_\Delta = -1.25$, two-sided $p_\Delta = 0.21$, $95\%$ CI
$[-0.0094, +0.0021]$. V2-VIDE: $n_{\rm void} = 74{,}111$
($0.4972$) vs $n_{\rm non\mbox{-}void} = 71{,}678$ ($0.4991$),
$\Delta f_{\rm CW} = +0.0019$, ${\rm SE}(\Delta) = 0.0026$,
$z_\Delta = +0.72$, two-sided $p_\Delta = 0.47$, $95\%$ CI
$[-0.0033, +0.0070]$. Both two-sample contrasts are nominal nulls
sitting well below the Bonferroni-5 threshold
$|z|^{\rm Bonf}_{0.05,5} \approx 2.58$, so the catalog-native rows
(rows 4--5 of Table~\ref{tab:analysis_tree}) carry the same
declared primary estimand as the three sphere-PIS rows
(\artifact{pipelines/p5\_desi\_chirality/outputs/30\_ext4\_galzone\_complement\_contrasts.json}). The
\texttt{GALZONE} \texttt{EDGE} and \texttt{DEPTH} quality flags do not
alter this conclusion: every catalog-native void member already
carries $\texttt{EDGE} = 0$ (adding the $\texttt{EDGE} = 0$
requirement is a strict no-op), and tightening to
$\texttt{DEPTH} \geq 1$ ($\geq 2$) shifts $f_{\rm CW}^{\rm void}$ by
at most $0.04$ ($0.08$)\,pp for either algorithm, with all variants
still null ($|\sigma| \leq 1.80$). The V2-REVOLVER
catalog-native $\sigma = -0.52$ at $n_{\rm void} > 10^{5}$ is the
cleanest single chirality-in-voids measurement in this paper.

\subsection{Maximal-void HEALPix sky-position stratification}\label{sec:desivast_maximal_healpix}

The DESIVAST release also ships the 3,765 \emph{maximal voids}
(NGC$=3{,}241$ + SGC$=524$) with explicit RA/Dec/$R_{\rm eff}$ metadata
(effective radii $10$--$32\,$Mpc/$h$). Note the catalog-layer
distinction: the per-galaxy void-membership tests of
\S\ref{sec:desivast_primary}--\S\ref{sec:desivast_three_algo} use the
$101{,}863$ hole-level spheres (maximum hole radius
$24.5\,$Mpc/$h$), \emph{not} the maximal-void effective spheres used
for the sky-density stratification here; the two layers have distinct
maximum radii and should not be conflated when reasoning about
near-boundary membership. The inside-any-hole criterion is also
permissive relative to the catalog's intended interior-void geometry
(overlapping holes are aggregated into voids by VoidFinder), so
hole-sphere membership can over-count edge/overlap regions. A direct
comparison against the stricter interior-void definition (membership
in a void's \emph{maximal} sphere only, the VoidFinder void-defining
sphere; an exact subset of the hole-union membership) gives the same
null verdict: any-hole $n_{\rm void} = 57{,}081$,
$\Delta f_{\rm CW} = +0.06$\,pp ($z = +0.28$) vs.\ maximal-sphere
$n_{\rm void} = 20{,}900$, $\Delta f_{\rm CW} = -0.54$\,pp
($z = -1.55$), quoted in the
Table~\ref{tab:desivast_three_algo} sign convention
$\Delta f_{\rm CW} \equiv f_{\rm CW}^{\rm non\mbox{-}void} -
f_{\rm CW}^{\rm void}$ (the committed artifact stores the
opposite-signed $f^{\rm void} - f^{\rm non\mbox{-}void}$ values), so
the permissiveness of the hole-union criterion does
not drive the void-class result
(\artifact{pipelines/p5\_desi\_chirality/outputs/24\_r24conf\_pod\_session.json}). Binning the maximal voids by
HEALPix \texttt{NSIDE} $= 16$ pixel (equal-area pixels of
$\approx 13.4\,{\rm deg}^2$) returns 297 occupied pixels with
median 14 maximal voids per occupied pixel (note: the 885 occupied pixels cited in the Fig.~\ref{fig:voids_vs_chirality} caption refer to the finer \texttt{NSIDE} $= 32$ pixelization of the same void set, which yields more pixels at a smaller median count of 4 voids/pixel). Stratifying the $z \leq
0.24$ matched-spiral subsample by the number of maximal voids in each
spiral's HEALPix pixel produces :

\begin{table}[ht]
\caption{Sky-position-stratified $f_{\rm CW}$ by maximal-void count per
HEALPix pixel ($z \leq 0.24$ matched-spiral subsample, $n_{\rm lz} =
678{,}945$, NSIDE $= 16$).}
\label{tab:maximal_void_healpix}
\begin{ruledtabular}
\begin{tabular}{lrrr}
Maximal voids per pixel & $n$ & $f_{\rm CW}$ & $\sigma_{\rm from\ half}$ \\
\hline
$0$  & $378{,}511$ & $0.4961$ & $-4.75$ \\
$1$--$2$ & $19{,}247$ & $0.4985$ & $-0.43$ \\
$3$--$5$ & $23{,}127$ & $0.4997$ & $-0.09$ \\
$6+$ & $258{,}060$ & $0.4980$ & $-2.04$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

The $\sigma = -4.75$ deviation is \emph{concentrated entirely in the
``0 maximal voids per pixel'' bin} -- the sky regions where DESIVAST
finds no maximal voids at all, which from inspection of the catalog
footprint corresponds to the survey-mask outside the BGS bright-side
NGC+SGC coverage region. We caution that ``$0$ maximal voids per
pixel'' is a catalog-derived \emph{proxy} for being outside DESIVAST
coverage, not a formal intersection with a published DESIVAST angular
mask: some in-coverage pixels can stochastically contain zero maximal
voids at NSIDE\,16 because of sampling and the $z \leq 0.24$ depth
limit, so a residual fraction of the $0$-void bin may lie inside
coverage. Critically, \emph{the primary DESIVAST void/non-void null
(Table~\ref{tab:desivast_canonical}) is not affected by this proxy
limitation}: both the void and non-void classes in that test are
defined by direct point-in-sphere membership against the $101{,}863$
hole spheres, which are entirely internal to the DESIVAST BGS coverage
volume; the non-void control sample thus contains galaxies at
$z \leq 0.24$ that are not inside any DESIVAST hole but are not
required to lie outside the survey mask. An explicit footprint-mask
re-tabulation has now been executed: defining the DESIVAST usable
footprint as the union of the $101{,}863$ hole-sphere angular discs
(NSIDE $= 64$, inclusive) intersected with the holes' comoving radial
span, all $57{,}081$ exact-membership void spirals fall inside the
footprint (a construction sanity check), and restricting the non-void
control to the same footprint ($n = 253{,}276$ of $621{,}864$) gives
$f_{\rm CW}^{\rm non\mbox{-}void} = 0.4983$ ($\sigma_{\rm from\,half} =
-1.73$) and a contrast $\Delta f_{\rm CW} = +0.0018$
(${\rm SE} = 0.0023$, $z_\Delta = +0.78$, two-sided $p = 0.43$,
$95\%$ CI $[-0.0027, +0.0064]$) --- consistent with null, confirming that
the proxy-based attribution was correct: the excluded
outside-footprint non-void population ($n = 368{,}588$,
$\sigma = -4.53$) carries the catalog-level monopole signature
(artifact
\artifact{pipelines/p5\_desi\_chirality/outputs/29\_ext3\_desivast\_footprint\_retabulation.json}).
The $[-2.04, -0.09]$ range of every $\geq\!1$-void bin independently bounds
the in-coverage behavior of the sky-scan proxy. Pixels with $\geq 1$ maximal void return
$\sigma$ values in the range $[-2.04,\,-0.09]$, with the highest-void-
density bin ($6+$ per pixel, $n = 258{,}060$) at $\sigma = -2.04$ --
below the Bonferroni-4 $|\sigma| = 2.50$ threshold. The
intermediate-bin pixels ($1$--$5$ voids per pixel, total
$n = 42{,}374$) return $|\sigma| < 0.5$ -- statistically null.

This is an additional catalog-anchored cross-check on the sky-position axis, complementary to the abstract's (i)--(iv) enumeration, showing that the catalog-wide
$-5\sigma$ monopole offset tracks survey-mask geometry rather than environment
density: the cleanest chirality $\sigma$ values in this paper are the
pixels with the \emph{most} maximal-void coverage, not the fewest. The
$-4.75\sigma$ signal in the no-void-coverage sky region is consistent
with the imaging-leg / selection-function systematics described in
Paper~IV applied to the matched-spiral subsample that falls outside
the DESIVAST clean coverage. Quantitatively, the Paper~IV monopole
prediction at $N = 378{,}511$ (the 0-voids/pix bin) is
$\sigma_{\rm pred} = 2\Delta f_{\rm CW}^{\rm P4}\sqrt{N} = -3.20$;
the observed $-4.75\sigma$ leaves a residual of $-1.55\sigma$ which
is consistent with an imaging-leg systematic at the
$\sim\!1\sigma$ level. At the $6+$ voids/pix bin
($N = 258{,}060$), the Paper~IV prediction is $-2.64\sigma$ and the
observed is $-2.04\sigma$, residual $+0.60\sigma$ (fully null).
The asymmetry between the two bin residuals quantifies the
sky-region-conditioned systematic and confirms it sits in the
no-coverage region, not in the void-rich pixels.

\subsection{Cross-survey P4-monopole-residual analysis}\label{sec:desivast_p4_residual}

The cleanest
formulation of the environment-independence headline is to recompute
the per-class chirality signal \emph{after subtracting} the P5
matched-spiral catalog monopole $f_{\rm CW}^{\rm P5} = 0.4972$
($-5.07\sigma$ on the $n = 812{,}793$ env-labeled spiral rows of
Table~\ref{tab:cw_vs_env}. \emph{Parent reconciliation:} the
$21{,}158$-row excess (2.7\%) over the $791{,}635$ unique
chirality-relevant matched spirals arises mechanically in the
environment join — the V-Web environment table inherits one row per
DESI \texttt{zall} survey--program coadd entry, so \texttt{TARGETID}s
observed under more than one program carry duplicate environment rows
($14{,}622{,}283$ rows over $14{,}100{,}704$ unique
\texttt{TARGETID}s), and the inner join counts those spirals once per
coadd row. The join covers $783{,}820$ unique env-matched spirals
($7{,}815$ spirals have no environment row and drop out); only $79$
duplicated \texttt{TARGETID}s carry conflicting class labels between
their coadd rows (boundary cells re-evaluated at slightly different
pipeline redshifts). An earlier draft attributed the excess to a
``relaxed env-label confidence filter''; the join-multiplicity
diagnosis above, recomputed in
\artifact{pipelines/p5\_desi\_chirality/outputs/17\_v0151\_closure\_recomputes.json},
replaces that description. The per-class
$n_{\rm CW}$ values on the 812{,}793-row parent sum to
$404{,}111$ giving $f_{\rm CW} = 0.49719$, matching the 791{,}635-spiral
unique-galaxy monopole $0.4972$ to 4 decimals, so the headline
conclusion is
invariant under either parent. The same monopole shows up as
$-5.00\sigma$ on the
791{,}635-spiral chirality-relevant sample; the two $\sigma$ values
are sample-size-scaled projections of the same underlying offset),
which is the propagation of the
$\sim\!9.5\sigma$ catalog-level monopole reported in
Paper~IV~\cite{golden_chirality_2026} into the DESI-spectro-confirmed
subsample. \emph{Arithmetic reconciliation:} the P4 monopole
$\Delta f_{\rm CW}^{\rm P4} = -0.0026$ projects to
$\sigma_{\rm pred}^{\rm P5} = 2 \cdot 0.0026 \cdot \sqrt{791{,}635}
\approx 4.6\sigma$ on the chirality-relevant subsample; the observed
$-5.00\sigma$ corresponds to $\Delta f_{\rm CW}^{\rm P5} \approx
-0.0028$, ${\sim}8\%$ larger than the P4 catalog-mean. This residual
$8\%$ enhancement is consistent with the spectroscopically-confirmed
subsample being more strongly weighted to the BGS-bright leg that
Paper~IV identifies as carrying the largest per-leg
selection-function residual, not an additional environmental signal.
To fix conventions explicitly: every $\sigma_{\rm pred}$ quoted in
this paper [Eq.~\eqref{eq:sigma_pred}] uses the \emph{Paper~IV
catalog} monopole $\Delta f_{\rm CW} = -0.0026$, while the
$\sigma_{\rm vs\,monopole}$ residuals of
Table~\ref{tab:p4_monopole_residual} subtract the \emph{P5
matched-sample} monopole $f_{\rm CW}^{\rm P5} = 0.4972$ (equivalent
to $\Delta f_{\rm CW}^{\rm P5} \approx -0.0028$). Replacing the $f_{\rm CW} - 0.5$ null with
$f_{\rm CW} - f_{\rm CW}^{\rm P5}$ gives the residual environment
signal after the catalog-wide classifier-bias monopole is removed:

\begin{table}[ht]
\caption{Per-V-Web-class $\sigma_{\rm vs\ monopole}$ residuals on the
matched-spiral subsample. All four classes fall within
$|\sigma_{\rm vs\ monopole}| < 1.15$ -- no environment dependence
survives subtraction of the catalog-wide P4-monopole. Unlike the raw
$\sigma_{\rm from\,half}$ of Table~\ref{tab:cw_vs_env} (which scales
as $\sqrt{n}$), these monopole-subtracted residuals \emph{are}
comparable across rows.}
\label{tab:p4_monopole_residual}
\begin{ruledtabular}
\begin{tabular}{lrrr}
Class & $n$ & $f_{\rm CW} - f_{\rm CW}^{\rm P5}$ &
$\sigma_{\rm vs\ monopole}$ \\
\hline
Void     & $428$     & $-0.0135$ & $-0.56$ \\
Wall     & $6{,}673$ & $+0.0062$ & $+1.01$ \\
Filament & $408{,}187$ & $+0.0008$ & $+0.99$ \\
Cluster  & $397{,}505$ & $-0.0009$ & $-1.11$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

\textbf{All four V-Web classes fall within
$|\sigma_{\rm vs\ monopole}| < 1.15$} after the P4-monopole is
subtracted -- i.e.\ \emph{no} V-Web class shows a residual
environment-dependent chirality signal once the catalog-wide
classifier-bias monopole is removed. The prior per-class
$\sigma_{\rm from\ half}$ values of $-2.61\sigma$ (filament) and
$-4.66\sigma$ (cluster) reported in the headline table were entirely
the P4-catalog-monopole signature propagated into the V-Web class
subsamples weighted by their respective sample sizes, not an
environmental signal. At the HEALPix NSIDE $=32$ per-pixel level on
the same env-labeled matched-spiral parent, the distribution of
$\sigma_{\rm vs\ monopole}$ across the $1{,}791$ valid pixels
(pixels carrying $\ge 200$ spirals, full redshift range) has
mean $+0.008$, std $1.050$, skewness $+0.074$, and excess kurtosis
$+0.092$. The unit standard deviation (within $5\%$; because each
pixel's $\sigma$ is normalized by its own counting error,
heterogeneous pixel populations alone would not inflate the variance
--- the mild excess over unity instead traces the $2.7\%$ duplicate
rows and small residual selection-function structure, as the
unique-\texttt{TARGETID} recompute below returning std $1.015$
confirms --- not evidence for non-shot-noise tails), near-zero
skewness, and near-zero excess kurtosis
are all consistent with a pure shot-noise residual
around the P4-monopole, with no sign of systematic
environment-conditioned chirality offset
(\artifact{pipelines/p5\_desi\_chirality/outputs/17\_v0151\_closure\_recomputes.json}).
Recomputed on the unique-\texttt{TARGETID} parent ($783{,}820$
spirals, deduplicated before the per-pixel pass), the distribution is
statistically unchanged: $1{,}769$ valid pixels with mean $+0.005$,
std $1.015$, skewness $+0.107$, and excess kurtosis $+0.068$, and the
maximal-void correlations reported below are likewise stable on the
deduplicated parent ($r = +0.007$, $r_w = +0.006$, $\rho = +0.021$;
all $p > 0.5$)
(\artifact{pipelines/p5\_desi\_chirality/outputs/22\_r24conf\_local\_batch.json}).

This is a direct single-test demonstration that
the V-Web class-level $\sigma$ values quoted in the headline table
are sample-size-weighted projections of the P4 catalog-wide chirality
monopole, not environmental signals.

\textbf{Quantitative null correlation.} Pushing this to the
per-pixel level: at NSIDE $= 32$ on the same matched-spiral subsample,
we measure the Pearson correlation between the per-pixel maximal-void
count and the per-pixel chirality $\sigma_{\rm from\ half}$ across all
$n_{\rm pix}^{\rm both} = 727$ HEALPix pixels containing both
$\geq 200$ matched spirals (required for stable $f_{\rm CW}$ estimation)
and $\geq 1$ DESIVAST maximal void. The result is
\[
r(N_{\rm voids/pix}, \sigma_{\rm pix}) = +0.006, \quad p = 0.88,
\]
indistinguishable from zero (Figure~\ref{fig:voids_vs_chirality}).
Because $\sigma_{\rm pix}$ is noisier at pixels near the $\geq 200$
spiral floor than at dense pixels (heteroscedasticity), we also report
an error-weighted version (weights $\propto N_{\rm pix}$,
emphasizing the well-measured pixels): $r_w = +0.004$ ($p = 0.91$,
$n_{\rm eff} \approx 683$), and a rank (Spearman) correlation
$\rho = +0.020$ ($p = 0.59$) — the no-correlation conclusion is
unchanged under either treatment
(\artifact{pipelines/p5\_desi\_chirality/outputs/21\_r23conf\_meta\_closures.json}).
A genuinely environment-dependent chirality signal would produce a
detectable monotonic correlation between void density and the
direction-of-chirality deviation; the observed near-zero Pearson is
a direct single-statistic confirmation in this paper that the
catalog-wide monopole offset is not environment-driven.
The result is robust to the NSIDE choice and spiral-count threshold:
across the $3 \times 3$ grid of NSIDE $\in \{16, 32, 64\}$ and
spiral-count cuts $\in \{100, 200, 500\}$, 7 of 9 cells admit a
well-sampled Pearson estimate (the remaining 2 cells, NSIDE $= 64$
with cuts $200$ and $500$, are sample-limited at $n_{\rm pix}^{\rm both}
< 3$ because the high-cut $\times$ fine-pixel combination filters
out most pixels carrying both $\geq 1$ maximal void and $\geq$ cut
matched spirals). The 7 computable cells all return $|r| < 0.11$ with
$p > 0.10$, none statistically significant; the headline NSIDE $=32$
cut $=200$ result $r=+0.006$ is consistent with this range. 
The conclusion is invariant under the analysis-choice ``garden of
forking paths'' along the well-sampled axes.

\begin{figure*}
\includegraphics[width=\textwidth]{fig_p5_voids_vs_chirality_skymap.png}
\caption{HEALPix \texttt{NSIDE} $= 32$ Mollweide projection
(equatorial coordinates). Top: count
of DESIVAST maximal voids per pixel (885 occupied pixels at NSIDE $= 32$, median 4 voids/pix; the body text uses NSIDE $= 16$ for the sky-stratification table, yielding 297 occupied pixels at a coarser resolution). Bottom: per-pixel chirality $\sigma_{\rm from\ half}$
on the $z \leq 0.24$ matched-spiral subsample restricted to pixels
with $\geq 200$ spirals (1{,}496 valid pixels, $\sigma$ range
$-3.45$ to $+3.48$). The Pearson correlation across the
$n_{\rm pix}^{\rm both} = 727$ pixels containing both voids and
$\geq 200$ spirals is $r = 0.006$ ($p = 0.88$), statistically
indistinguishable from zero.}
\label{fig:voids_vs_chirality}
\end{figure*}

\section{Additional cosmic-web cross-checks}\label{sec:additional_cw}

The following sections report secondary diagnostic cross-checks
that supplement, but do not anchor, the primary DESIVAST-anchored
chirality null established in
Section~\ref{sec:desivast_primary} (per the primary/secondary
declaration of \S\ref{sec:primary_path}).

\subsection{Redshift-shell selection-corrected classifier
robustness}\label{sec:zshell_robustness}

The V-Web classifier of \S\ref{sec:vweb_algo} (our implementation of
the Hahn~2007 T-Web recipe) computes the overdensity
$\delta = \rho/\bar\rho - 1$ against a \emph{single global} mean cell
density $\bar\rho_{\rm cell} = 4.64$ galaxies/cell over the full
$0.01 \le z \le 2$ volume. Because the DESI~DR1 selection function is
strongly redshift dependent, the measured per-shell mean density spans
$\bar n_{\rm shell} \approx 294 \to 0.46$ galaxies/cell (a factor
${\sim}640$) from the lowest to the highest redshift shell: under the
global-mean convention, distant shells are systematically driven toward
apparent underdensity and nearby shells toward apparent overdensity, so
the four-class environment labels partially trace the radial selection
function rather than true large-scale structure. We note the analogous
\emph{angular} limitation explicitly: no spectroscopic
tiling/completeness weighting (e.g.\ DESI random-catalog or FKP-like
per-cell weights) is applied when building $\delta$; angular geometry
is controlled here only through the footprint mask, the interior-buffer
excision, and the NSIDE$\,=\,$64 geometry cross-check. The
randoms-weighted rebuild has now been run
(\artifact{pipelines/p5\_desi\_chirality/outputs/25\_completeness\_weighted\_rebuild.json}:
$7.5\times 10^7$ DESI DR1 BGS\_BRIGHT clustering randoms,
$\delta_w = n_g/(\alpha\,n_r) - 1$ on randoms-supported cells, restricted
to the $0.01 < z < 0.50$ window where the BGS randoms trace the
selection --- $99.3\%$ of matched spirals --- and compared against an
identically windowed unweighted control). In this BGS-randoms-weighted low-$z$ stress test, the completeness weighting substantially reshapes the in-window environment field, as expected when the radial selection gradient is removed:
the in-window void volume fraction collapses from $17.6\%$ to $0.75\%$
(${\approx}23\times$), class volume fractions shift by up to $21$~pp,
and only $44\%$ of common-mask cells and $26.6\%$ of matched spirals
retain their class label. The headline observable is nonetheless
\emph{classification-robust}: every class is individually consistent
with parity in \emph{both} builds --- unweighted: void
$f_{\rm CW} = 0.475 \pm 0.023$ ($n=478$, $-1.1\sigma$), wall $0.500$,
filament $0.498$, cluster $0.496$; weighted: void $0.502 \pm 0.003$
($n = 21{,}392$ after the class repopulates, $+0.5\sigma$), wall
$0.496$, filament $0.498$, cluster $0.496$. The largest between-build
shift ($+2.7$~pp, void) reflects that $478 \to 21{,}392$ repopulation,
with both endpoints individually null; the populous classes move by
$\le 0.40$~pp. The no-environment-dependence conclusion therefore holds
under both the survey-selection-shaped environment definition and the
BGS-randoms-weighted low-$z$ stress-test definition. Scope caveat: this rebuild tests
selection-completeness sensitivity in the $z<0.5$ window where the BGS
randoms trace the selection and where $99.3\%$ of the matched-spiral
signal lives; residual radial-selection contamination of the V-Web
labels at $z>0.5$ is not probed here and is bounded separately by the
$z$-shell correction and geometry cross-checks of this section. A
cube-connected 3-iteration dilation variant shifts volumes by
$\le 3.1$~pp (driven by the void fraction decreasing
$24.4\% \to 21.2\%$ under the more aggressive dilation) with $99.6\%$
spiral-assignment agreement and per-class $f_{\rm CW}$ shifts
$\le 0.77$~pp (same artifact), closing the mask-dilation sensitivity
item as well.

To test whether the headline environmental-independence conclusion is
robust to this, we rebuilt the classification with a first-order radial
selection correction
(\artifact{pipelines/p5\_desi\_chirality/scripts/16\_cosmic\_web\_zshell\_corrected.py}): the
overdensity is computed in 21 thin redshift shells ($\Delta z = 0.05$
for $z<0.5$, $\Delta z = 0.1$ for $0.5 \le z < 1.5$, one merged
$1.5$--$1.7$ shell into which the $1.7 < z \le 2.0$ tail of
the parent window is also clamped, so the top shell nominally
covers $1.5 \le z \le 2.0$ --- the realized tail is in fact
\emph{empty}: the filtered parent's maximum redshift is
$z = 1.6979$ and it contains zero galaxies with $1.7 < z \le 2.0$,
so the drop-tail and separate-shell sensitivity variants coincide
exactly with the published build
(\artifact{pipelines/p5\_desi\_chirality/outputs/24\_r24conf\_pod\_session.json});
every shell $\ge 2.16\times10^5$
galaxies), with the
per-shell mean taken over occupied-footprint cells only, while the
grid, smoothing scale $R_s$, and eigenvalue threshold
$\lambda_{\rm th}$ are held fixed at their canonical values. An
interior-buffer variant additionally removes galaxies within $R_s$ of
the footprint boundary; under this flag, $837{,}169$ of the
$3{,}150{,}089$ in-mask grid cells ($26.6\%$) lie within $R_s$ of the
occupancy-footprint boundary
(\artifact{pipelines/p5\_desi\_chirality/outputs/22\_r24conf\_local\_batch.json}). The same interior-buffer excision applied to
the \emph{canonical} (non-shell-corrected) labels — addressing the
zero-padded FFT boundary condition of the canonical Poisson solve
directly — retains $782{,}015$ of the $783{,}820$ unique env-matched
spirals and shifts the per-class CW fractions by at most $0.11$\,pp
(void) and $\le 0.05$\,pp (all other classes), with the omnibus
$4\times2$ test still null ($\chi^2 = 2.93$, $p = 0.40$): the
canonical environment fractions are stable against boundary
excision
(\artifact{pipelines/p5\_desi\_chirality/outputs/21\_r23conf\_meta\_closures.json}).

As expected, the per-galaxy class populations migrate substantially
once the selection is corrected: among matched spirals, the void class
grows from $428$ to $4{,}353$ ($\sim\!10\times$), wall from $6{,}673$
to $154{,}541$ ($\sim\!23\times$), filament from $408{,}187$ to
$472{,}547$, and cluster shrinks from $397{,}505$ to $181{,}352$
($-54\%$). The chirality result, however, is \emph{strengthened}: the
cross-class equivariant CW-fraction range collapses from
$1.98$\,pp (canonical) to $0.05$\,pp (corrected), with every class
within $|\sigma| \le 0.39$ of the sample monopole
($f_{\rm CW}^{\rm eq}$: void $0.4971$, wall $0.4968$, filament
$0.4973$, cluster $0.4973$; $n = 812{,}793$ joined spirals). The
interior-buffer variant ($1{,}862$ spirals removed) is statistically
indistinguishable. Residual per-class deviations from $0.5$ track the
known Paper~IV global classifier monopole, exactly as in the
canonical analysis: the three large classes span $-2.3$ to
$-3.7\sigma$ (wall $-2.51$, filament $-3.73$, cluster $-2.33$), while
void at $n = 4{,}353$ sits at $-0.38\sigma$, consistent with its much
smaller monopole prediction
($\sigma_{\rm pred} = 2 \cdot 0.0026 \cdot \sqrt{4{,}353} = 0.34$). The environmental-independence null therefore holds --- and
tightens by a factor ${\sim}40$ in cross-class range --- under the
selection-corrected classifier, demonstrating that the canonical
result was not an artifact of selection-function leakage into the
environment labels. We read this run as a \emph{stress test} of the
canonical classification rather than an independent confirmation: the
per-shell mean subtraction whitens the density field radially and,
combined with the masked FFT solve, can itself suppress genuine
radial eigenvalue contrast, so part of the $1.98 \to 0.05$\,pp range
collapse may reflect over-correction of real structure rather than
removal of selection leakage alone. The diagnostic that matters for
this paper's null is directional: a selection artifact masquerading
as an environmental signal would have \emph{appeared} in the
canonical run and \emph{survived} or grown here; instead both runs
are null ($p = 0.31$ and $p = 0.99$), and the corrected class volume
fractions and spatial coherence remain cosmologically plausible (the
per-shell statistics below), so the null verdict is robust to the choice
of correction. The omnibus $4\times2$ homogeneity test sharpens
accordingly: $\chi^2 = 0.11$ with 3~d.o.f.\ ($p = 0.99$) on the
selection-corrected classes ($n_{\rm CW}$: void $2{,}164$, wall
$76{,}777$, filament $234{,}990$, cluster $90{,}180$ of the per-class
$n$ above), versus $\chi^2 = 3.55$ ($p = 0.31$) on
the canonical classes — both null. Full per-shell diagnostics and
the per-galaxy
corrected label catalog are released with the reproducibility
artifacts
(\artifact{pipelines/p5\_desi\_chirality/outputs/16\_cosmic\_web\_zshell\_corrected.json}).
The per-shell statistics (all 21 shells hold $2.16\times10^{5}$ to
$1.19\times10^{6}$ galaxies) quantify the correction at the cell
level: before correction the in-mask mean of the global-mean
overdensity drifts from $+62.3$ in the lowest shell ($z<0.05$) to
$-0.90$ in the highest ($z \geq 1.5$), while the shell-normalized
$\delta$ is zero-mean in every shell by construction, with cell-level
variance ranging $1.6$--$4.9$ across shells
(\artifact{pipelines/p5\_desi\_chirality/outputs/22\_r24conf\_local\_batch.json}).

\paragraph{Grid-resolution convergence.} The canonical pipeline fixes
$N_{\rm grid} = 256^3$; the Phase~2 sweep varies $R_s$ and
$\lambda_{\rm th}$ but not the grid. As a convergence check we re-ran
the full canonical V-Web build at $N_{\rm grid} = 128^3$ (cell
$51.8\,h^{-1}$\,Mpc) and $384^3$ (cell $17.3\,h^{-1}$\,Mpc), holding
$R_s = 25\,h^{-1}$\,Mpc and $\lambda_{\rm th} = 0$ fixed, and re-joined
the declared spiral parent. Class populations are stable (void
$297/428/434$, filament $415{,}722/408{,}187/407{,}780$ at
$128^3/256^3/384^3$), the cross-class $f_{\rm CW}$ range stays within
$2.2/2.0/2.9$\,pp (void-bin noise dominated, as in
Table~\ref{tab:phase2}), and every class tracks the catalog monopole
at all three resolutions — the environmental-independence null is
converged with respect to grid resolution
(\artifact{pipelines/p5\_desi\_chirality/outputs/17\_v0151\_closure\_recomputes.json}).
This convergence test holds $R_s = 25\,h^{-1}$\,Mpc; the
$R_s = 10\,{\rm Mpc}/h$ Phase~2 cells are below the $256^3$ grid
sampling scale, lie outside the tested convergence regime, and are
accordingly excluded from the robustness claim
(\S\ref{sec:phase2}).

\paragraph{Geometry-footprint shell-mean cross-check.} The z-shell
rebuild above takes each shell's mean density over the
occupancy-derived (dilated) footprint, which could in principle bias
sparse high-$z$ shell means relative to a survey-geometry
(random-catalog-equivalent) denominator. We re-ran the rebuild with
the per-shell mean taken instead over all cells inside a
redshift-independent HEALPix NSIDE-64 angular footprint intersected
with the shell's radial range, holding everything else fixed. Class
populations shift again (void $20{,}736$, wall $314{,}367$, filament
$407{,}144$, cluster $70{,}546$ matched spirals), but the chirality
null is unchanged: the cross-class $f_{\rm CW}$ range is
$0.23$\,pp and every class sits within $|\sigma_{\rm vs\,monopole}|
\le 1.04$ of the sample monopole — the environmental-independence
conclusion does not depend on how the shell-mean denominator is
defined
(\artifact{pipelines/p5\_desi\_chirality/outputs/19\_v0151\_zshell\_geofootprint\_check.json}).

\subsection{Tempel+2014 FoF cross-validation}\label{sec:tempel}

To check that the V-Web cosmic-web headline is not specific to the
tidal-tensor classifier, we cross-validate against an entirely
independent classifier: the friends-of-friends (FoF) group catalog of
Tempel~\textit{et~al.}~2014 \cite{Tempel2014} on SDSS DR10. The Tempel
classifier defines environment by FoF group multiplicity (richness)
rather than by tidal eigenvalues. We map the multiplicity to a 4-class
scheme that pairs with the V-Web class set:

\begin{itemize}
\item $\mathrm{multiplicity}\!=\!1$ $\to$ \emph{isolated} (paired with V-Web void)
\item $2 \le \mathrm{multiplicity} < 5$ $\to$ \emph{small\_group} (paired with V-Web wall)
\item $5 \le \mathrm{multiplicity} < 20$ $\to$ \emph{filament\_like} (paired with V-Web filament)
\item $\mathrm{multiplicity} \ge 20$ $\to$ \emph{cluster\_like} (paired with V-Web cluster)
\end{itemize}

The Tempel catalog covers $588{,}193$ SDSS DR10 galaxies at
$z \le 0.20$ over RA $\in [110^\circ, 262^\circ]$, Dec $\in [-4^\circ,
70^\circ]$. We perform a $1''$ sky-coord NN join between the Tempel
catalog and the $791{,}635$-spiral declared chirality-relevant parent;
the overlap is $96{,}753$ spirals (the SDSS DR10 footprint is a
subset of DESI Legacy DR8 and Tempel's $z \le 0.20$ cut is much tighter
than our $z \le 4$ DESI cut). An earlier draft quoted an overlap of
$110{,}586$; that join omitted the matched-primary deduplication
filter and is withdrawn
(driver:
\artifact{pipelines/p5\_desi\_chirality/env\_finder/03\_tempel\_cross\_validation.py}).

\begin{table}[t]
\caption{Tempel+2014 FoF environment cross-validation on the
$96{,}753$-spiral Tempel-overlap subsample (declared parent). The
$\sigma_{\rm from\,half}$ values scale as $\sqrt{n}$ at fixed offset
and are not comparable across rows of different $n$ (\S\ref{sec:stats}).}
\label{tab:tempel}
\begin{ruledtabular}
\begin{tabular}{lrrrr}
Tempel class & $n$ & $n_{\rm CW}$ & $f_{\rm CW}$ & $\sigma_{\rm from\,half}$ \\
\hline
isolated      & $51{,}631$ & $25{,}558$ & $0.4950$ & $-2.27$ \\
small\_group  & $27{,}740$ & $13{,}746$ & $0.4955$ & $-1.49$ \\
filament\_like & $12{,}360$ & $6{,}155$ & $0.4980$ & $-0.45$ \\
cluster\_like  & $5{,}022$  & $2{,}520$ & $0.5018$ & $+0.25$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

\textbf{Concordance metric (like-for-like).} For each Tempel-vs-V-Web
class pairing we
report $|f_{\rm CW}^{\rm Tempel} - f_{\rm CW}^{\rm V\mbox{-}Web}|$ as a
concordance distance in percentage points, with \emph{both} classifiers
evaluated on the common labeled subset of the $96{,}753$-spiral
overlap: the V-Web side carries an environment label for $95{,}247$
of the $96{,}753$ ($23 + 145 + 16{,}701 + 78{,}378$); the remaining
$1{,}506$ overlap spirals have no V-Web environment row (the
overlap's share of the $7{,}815$-spiral env-join dropout,
\S\ref{sec:desivast_p4_residual}) and are excluded from the V-Web
side by the left join in the driver. (An earlier
draft compared the Tempel overlap against the V-Web classes of the
\emph{full} matched sample — two disjoint-population estimates whose
agreement is not a classifier-concordance statement; that comparison,
including its headline $0.026$\,pp filament figure, is withdrawn.)
On the overlap, the V-Web classifier is strongly tilted toward the
cluster class (void/wall/filament/cluster $=$
$23/145/16{,}701/78{,}378$) by the survey-shell density systematic at
low $z$ discussed in \S\ref{sec:tweb_concurrent} and
\S\ref{sec:astra_per_object}, so only the filament and cluster
pairings carry meaningful $n$ on both sides:

\begin{itemize}
\item \textbf{filament\_like\_vs\_filament: $0.29$\,pp.} Tempel
filament\_like $f_{\rm CW} = 0.4980$ ($n = 12{,}360$) vs V-Web
filament-on-overlap $f_{\rm CW} = 0.5009$ ($n = 16{,}701$). The
difference is $|z| = 0.49$ on a pooled two-proportion $z$-test
(pooled and unpooled variance models are numerically
indistinguishable at $\hat p \approx 0.5$) — statistically
indistinguishable; at the overlap
populations the $1\sigma$ two-sample floor is ${\sim}0.6$\,pp
(supporting, not load-bearing — the primary cross-classifier
validation remains the on-DESI DESIVAST re-projection at
$n_{\rm void}^{\rm DESIVAST} = 56{,}981$; see Abstract Robustness).
\item cluster\_like\_vs\_cluster: $0.67$\,pp (Tempel $0.5018$ at
$n = 5{,}022$ vs V-Web-on-overlap $0.4950$ at $n = 78{,}378$),
${\sim}0.9\sigma$ two-sample — consistent. V-Web's
$\lambda_{\rm th}\!=\!0$ ``cluster'' is far more
permissive than Tempel's richness $\ge 20$ cut.
\item isolated\_vs\_void ($10.4$\,pp at $n_{\rm V\mbox{-}Web} = 23$)
and small\_group\_vs\_wall ($2.9$\,pp at $n_{\rm V\mbox{-}Web} = 145$)
are uninformative: the V-Web side has essentially no void/wall
population on the low-$z$ SDSS overlap, and the differences are well
inside the corresponding counting noise.
\end{itemize}

Figure~\ref{fig:tempel_overlay} visualizes the two classifiers
side-by-side with shared y-axis range; the filament/filament\_like
pair sits essentially on top of the Paper~IV $\bar f_{\rm CW}$
reference line (the $0.29$\,pp like-for-like concordance is near
visual resolution),
while the lower-$n$ pairs scatter as expected.

\begin{figure*}[t]
\centering
\includegraphics[width=0.95\textwidth]{fig_p5_vweb_vs_tempel_overlay.png}
\caption{V-Web (left; full-sample canonical run, shown as reference)
vs Tempel+2014 FoF (right; $96{,}753$-spiral overlap) cross-validation:
per-class CW fraction with $95\%$ Jeffreys binomial credible intervals,
shared y-axis [0.43, 0.53]. Dashed reference is parity
$f_{\rm CW}\!=\!0.5$; dotted-red reference is the Paper~IV global
$\bar f_{\rm CW}\!=\!0.4974$ classifier-monopole offset. The
quantitative concordance statistic is computed like-for-like on the
common overlap sample (\S\ref{sec:tempel}): Tempel
filament\_like $f_{\rm CW}\!=\!0.4980$ ($n\!=\!12{,}360$) vs V-Web
filament-on-overlap $f_{\rm CW}\!=\!0.5009$ ($n\!=\!16{,}701$),
a $0.29$\,pp (${\sim}0.5\sigma$) difference, statistically
indistinguishable (supporting, not load-bearing; the primary
cross-classifier
validation is the on-DESI DESIVAST re-projection in
\S\ref{sec:desivast_primary}). Lower-$n$ classes scatter by counting statistics and
classifier-definition mismatch (V-Web $\lambda_{\rm th}\!=\!0$ vs
Tempel discrete richness thresholds) but do not contradict the
environmental-independence headline.}
\label{fig:tempel_overlay}
\end{figure*}

\textbf{Verdict.} The Tempel cross-validation confirms the V-Web
result at the high-richness end (like-for-like filament concordance
$0.29$\,pp, ${\sim}0.5\sigma$);
low-richness disagreements scale with counting statistics and the
classifier-definition mismatch and do not undermine the
environmental-independence headline. The Tempel data also produces a
result consistent with null at the level of its own bins: the maximum
$|\sigma_{\rm from\,half}|$ across the four Tempel classes is
$2.27$ (Tempel isolated), below the Bonferroni-4
$|\sigma|^{\rm Bonf}_{0.05,4}=2.498$ threshold at $\alpha = 0.05$ and
well below the
$|\sigma|^{\rm Bonf}_{0.01,4}=3.02$ threshold at $\alpha = 0.01$.

\subsection{Concurrent-literature DR1/EDR cosmic-web
cross-validation}\label{sec:tweb_concurrent}

Independent of the V-Web (Hahn~\textit{et al.}~2007 / Cautun~\textit{et
al.}~2014) classifier deployed in \S\ref{sec:vweb} and the Tempel FoF
classifier cross-validated in \S\ref{sec:tempel}, a contemporaneous
DESI DR1 cosmic-web analysis was released in 2026
April~\cite{TWebDESI2026}, applying the tidal-tensor (T-Web) formalism
on a $256^{3}$ grid in an $800$~Mpc cube over the full DESI DR1
footprint, with the same four-class void / sheet / filament / knot
taxonomy used in our V-Web run.

The T-Web DR1 in-footprint volume fractions reported in
Ref.~\cite{TWebDESI2026} are tracer-dependent and are reported
separately for the BGS, LRG, and ELG samples on an $800$~Mpc cubic
sub-volume (not a single all-tracer fraction). Taking BGS as the
closest analogue to our spectro sample,
$\{f^{V}_{\rm void}, f^{V}_{\rm sheet}, f^{V}_{\rm filament}, f^{V}_{\rm knot}\}_{\rm BGS}
\approx \{0.16,\, 0.45,\, 0.37,\, 0.04\}$ (superscript $V$: volume
filling fractions, not to be confused with the chirality fractions
$f_{\rm CW}^{\rm class}$);
the LRG and ELG samples shift these by a few percentage points each,
spanning the combined ranges
$\{0.06\text{--}0.16,\, 0.45\text{--}0.48,\, 0.37\text{--}0.40,\,
0.04\text{--}0.06\}$
across the three tracer samples. Any direct
comparison to our DR1-all-spectro V-Web run is therefore approximate
because of the differing target selection, redshift coverage, and
sample volume (cubic $800$~Mpc box vs.\ thin survey shell); no
per-galaxy cross-match against Ref.~\cite{TWebDESI2026} is attempted
here, and the comparison is purely on volume fractions. Our V-Web
in-footprint volume fractions
are $\{0.244,\, 0.413,\, 0.333,\, 0.010\}$. The sheet (V-Web wall) and
filament classes agree to within ${\sim}5$\,percentage points across
both methodologies (allowing for the tracer- and volume-mismatch
caveat above), which is approximate concordance for two independent
classifiers run on overlapping but non-identical samples of the same
survey. The void and knot/cluster classes
deviate by larger margins: V-Web's void fraction is \emph{higher} than
T-Web's by $+8$--$18$\,pp (the edge-density artifact populates the
V-Web 0-eigenvalue void class), and V-Web's cluster fraction is
\emph{lower} than T-Web's knot fraction by $3$--$5$\,pp (the densest
cells lose to mask-boundary smoothing). Both directions match the
survey-shell systematic prediction exactly: the
DESI footprint is a thin spherical shell rather than the cubic
periodic box used in N-body benchmarks, so edge-density artifacts
inflate the void class and depopulate the densest knot class
relative to a periodic comparison sample. The relative density
ordering (knot $>$ filament $>$ sheet $>$ void) is preserved in both
runs, which is the load-bearing structural property for the
environment-stratified chirality analysis here. We therefore treat
Ref.~\cite{TWebDESI2026} as an independent contemporaneous DR1
cosmic-web analysis that is consistent with our V-Web run at the
survey-shell-systematic level. (Ref.~\cite{TWebDESI2026} is currently
in submission to MNRAS; we do not treat it as peer-reviewed external
validation but rather as a contemporaneous independent measurement.)

A second concurrent DESI cosmic-web paper~\cite{ASTRADESI2026} (Zapata-Zuluaga
\textit{et al.}\ 2026, a DESI-EDR-based probabilistic environment catalog) uses
the ASTRA (Algorithm for Stochastic Topological RAnking) probabilistic
classifier on the DESI Early Data Release ($175$~deg$^{2}$, 20
rosettes), running 100 realizations per tracer-zone pair to deliver
per-object void / sheet / filament / knot \emph{membership
probabilities} and classification entropies rather than a single
deterministic class assignment. ASTRA is methodologically
\emph{complementary} to V-Web (this paper) and T-Web
(Ref.~\cite{TWebDESI2026}): the latter two are deterministic
tidal-tensor classifiers, ASTRA is a probabilistic positional-statistic
classifier that quantifies per-object assignment uncertainty.
ASTRA is published only on EDR while our V-Web run is on DR1; the
$\sim\!175$~deg$^{2}$ EDR rosettes are contained within the DR1
footprint, so a per-galaxy cross-match by \texttt{TARGETID} is
available in the EDR-overlap subsample and is reported in
\S\ref{sec:astra_per_object}. The BGS-anchored volume-filling-fraction
calibration in Ref.~\cite{ASTRADESI2026} is consistent with the V-Web
sheet/filament fractions reported here within the survey-shell
systematic discussed above.

A complementary public DR1 product, DESIVAST~\cite{DESIVAST2025}
(Rinc\'on \textit{et al.}\ 2025, ApJ 982, 38), is specifically a
\emph{void catalog} (not a full cosmic-web classifier) and is the
input to the primary chirality-in-voids analysis reported in
\S\ref{sec:desivast_primary} above; see there for the per-catalog
usage, cross-match, and three-algorithm robustness analysis.

\section{ASTRA EDR per-object
cross-validation}\label{sec:astra_per_object}

The ASTRA-DESI EDR probabilistic environment catalog
\cite{ASTRADESI2026} (Zenodo 10.5281/zenodo.19358024) provides
per-galaxy void / sheet / filament / knot membership probabilities
$\{P_{\rm void}, P_{\rm sheet}, P_{\rm filament}, P_{\rm knot}\}$ on
the $\sim\!175$~deg$^{2}$ EDR rosettes ($N_{\rm ASTRA} = 648{,}428$
unique \texttt{TARGETID}s spanning BGS\_ANY, LRG, ELG, and QSO
tracers). We perform a per-galaxy \texttt{TARGETID} join against the
P5 matched-spiral deduplicated-primary subsample and against the V-Web
environment catalog of \S\ref{sec:vweb}, recovering
$N_{\rm overlap} = 25{,}186$ spirals that carry all three labels:
chirality classification (P4), ASTRA per-class membership probabilities
(this section), and V-Web deterministic class assignment
(\S\ref{sec:vweb}). This is an EDR-overlap per-galaxy cross-check of
the V-Web canonical classifier against the ASTRA published DESI
environmental catalog within this campaign and is the closest currently
available substitute for the full-DR1 environmental VAC discussed in
\S\ref{sec:limitations}. The cross-match pipeline and result summary are available in the companion data repository.

We compute the chirality-by-environment $f_{\rm CW}$ statistic on the
$25{,}186$-galaxy overlap under three classifiers run on the
\emph{same galaxies}: (i)~the ASTRA argmax classifier
(\texttt{env\_class} $=$ argmax over the four ASTRA probabilities);
(ii)~the ASTRA entropy-weighted classifier (each galaxy contributes
$P_{\rm class}$ fractional count and $P_{\rm class}\!\cdot\!\mathbb{1}_{\rm CW}$
fractional CW count to each class, with sub-class variance
$\textstyle\sum_i P_i^2 / 4$ under the Bernoulli-$0.5$ null; this
variance model assumes independence across objects and neglects the
within-object negative correlation among the four class weights,
which sum to one --- an approximation adequate for the per-class
$\sigma$ quoted here but one that slightly mis-states the joint
variance structure if classes are combined or compared; a $10^4$-draw
Monte Carlo using the actual per-object probabilities under the
independent Bernoulli-$0.5$ null reproduces the analytic per-class
standard deviations to within $1.2\%$ and the per-class $\sigma$
values to $<\!0.02$, while directly measuring the neglected
between-class coupling --- class-sum correlations up to $+0.42$ from
shared objects --- so the approximation is validated at the per-class
level used here,
\artifact{pipelines/p5\_desi\_chirality/outputs/22\_r24conf\_local\_batch.json}); and
(iii)~the V-Web deterministic classifier restricted to the same
overlap, for like-for-like comparison.

The headline statistics, filtered to classes with $n \geq 100$ to
suppress small-sample artifacts, are summarized in
Table~\ref{tab:astra_headline}. Both ASTRA classifiers and the V-Web
classifier-on-overlap recover the same conclusion: chirality is
statistically indistinguishable from $f_{\rm CW} = 1/2$ across the
four cosmic-web classes, with no class reaching the
Bonferroni-corrected significance threshold at $\alpha = 0.01,\,K = 4$
($|\sigma|_{\rm Bonf} = 3.02$).

\begin{table*}[!htb]
\caption{ASTRA EDR per-object cross-validation, headline statistics on
the $N_{\rm overlap} = 25{,}186$ EDR-overlap matched-spiral subsample.
Range and max-$|\sigma|$ filtered to classes with $n \geq 100$. The
entropy-weighted row lists effective (probability-weighted) $n_{\rm
eff}$ per class; per-class $n_{\rm CW}$ values are in the released
per-object artifact
(\artifact{pipelines/p5\_desi\_chirality/results/analysis\_astra\_per\_object/cw\_fraction\_by\_env\_astra.csv}).}
\label{tab:astra_headline}
\centering
\begin{ruledtabular}
\begin{tabular}{lccl}
Classifier & $f_{\rm CW}$ range (pp) & max $|\sigma|_{\rm vs\,1/2}$ &
per-class $n$ (void/wall/fil./cl.) \\
\hline
V-Web on same overlap         & $1.08$ & $2.68$ & $1$ / $2$ / $7{,}972$ / $17{,}211$ \\
ASTRA argmax                  & $2.08$ & $2.25$ & $2{,}985$ / $7{,}980$ / $8{,}864$ / $5{,}357$ \\
ASTRA entropy-weighted        & $1.17$ & $2.00$ & $3{,}338$ / $7{,}724$ / $8{,}433$ / $5{,}691$ \\
\end{tabular}
\end{ruledtabular}
\end{table*}

The per-galaxy classifier agreement between V-Web and ASTRA argmax is
\emph{poor} on this overlap subsample. ASTRA argmax distributes the
$25{,}186$ spirals as $11.9\%$~void~/~$31.7\%$~sheet~/~$35.2\%$~filament
~/~$21.3\%$~knot, while V-Web puts essentially the entire sample into
filament~(31.7\%) and cluster~(68.3\%), with only $3$~spirals total in
the V-Web void~$+$~wall classes. (The repeated $31.7\%$ is a
numerical coincidence, not a copy error: ASTRA sheet $= 7{,}980$ and
V-Web filament $= 7{,}972$ of the $25{,}186$-spiral overlap, per the
per-object artifact.) This reflects the survey-shell
density-grid systematic discussed in
\S\ref{sec:tweb_concurrent}: the $25.9$~Mpc/$h$ cell size in the V-Web
$256^{3}$~grid is comparable to the EDR rosette transverse extent, so
the EDR-overlap subsample falls disproportionately on cells where the
edge-density mask suppresses the V-Web void class, while ASTRA
operating natively on EDR rosettes resolves void structure at
sub-rosette scales. The two classifiers therefore disagree
substantially on the per-galaxy environment label.

Despite this strong classifier disagreement on \emph{per-galaxy
environment assignment}, the \emph{chirality-vs-environment headline}
is recovered identically by both: $f_{\rm CW}$ does not vary by more
than $\sim\!2$~pp across the four classes under any of the three
classifiers, and no class clears the Bonferroni-corrected significance
threshold. This is a supporting diagnostic consistency check: the P5 headline null
does not depend on which independent, published DESI environmental
classifier is applied to the EDR-overlap subsample (restricted to the
$\sim\!175$\,deg$^2$ EDR rosettes, $N_{\rm overlap}=25{,}186$ spirals),
and the deterministic single-class V-Web assignment reaches the same
conclusion as the probabilistic ASTRA classifier even though the two
classifiers assign vastly different per-galaxy class labels. Given the
small overlap size and the strong per-galaxy label disagreement, this
result is best read as a descriptor of consistency on the EDR subsample,
not as load-bearing robustness evidence.

We note that the ASTRA EDR
per-object overlap is restricted to the $\sim\!175$\,deg$^2$ EDR
rosettes ($N_{\rm overlap}=25{,}186$), and that V-Web and ASTRA argmax
disagree strongly on per-galaxy environment labels on this overlap;
the result is therefore best read as: the EDR overlap recovers the
same null result under either classifier despite per-galaxy label
disagreement, consistent with the catalog-monopole offset already
present at the catalog-wide level (Paper~IV), rather than as a new
independent detection. We list it alongside (i)~DESIVAST per-galaxy
cross-match, \S\ref{sec:desivast_xmatch}; (ii)~within-class density
quartile null, \S\ref{sec:results_within_class_density};
(iii)~DESIVAST-anchored void classifier;
(iv)~three-algorithm DESIVAST robustness;
(v)~catalog-native V2 GALZONE membership;
(vi)~MAXIMAL voids HEALPix sky-position stratification, with the
caveat that the ASTRA overlap is the smallest subsample in this
list and the V-Web/ASTRA per-galaxy label disagreement caps its
independent statistical weight.

\section{Systematics and null tests}\label{sec:systematics}

We run six classes of systematics tests:
chirality-label shuffle ($1{,}000$ permutations, applied per-bin in
the redshift, density, and HEALPix scans of
\S\ref{sec:results}, including the leg$\times$program-stratified
variants), sky-position
permutation, confidence-threshold sweep, match-radius sweep,
footprint split, and target-class split.
Table~\ref{tab:systematics_splits} reports the measured CW fraction
per split with its $N$ (artifacts in
\artifact{pipelines/p5\_desi\_chirality/results/analysis\_systematics/}).
The match-radius sweep is flat to $0.02$\,pp across
$\{0.5, 1, 2, 3, 5\}''$
(Table~\ref{tab:systematics_splits} prints the two extreme radii;
the intermediate-radius rows are carried in the committed artifact);
the footprint splits sit within $0.13$\,pp
of the global value; the confidence-threshold sweep drifts by at most
$-0.24$\,pp from the full-sample value at
$p_{\rm cls\_eq}^{\max} \ge 0.7$ (a ${\approx}2.1\sigma$ shift
against the $0.5/\sqrt{n}$ counting floor at the reduced
$n = 185{,}719$, below the $3\sigma$ bar applied throughout this
section); and the target-class split is the one structured
residual: BGS-bright $f_{\rm CW} = 0.4970$ vs LRG/ELG/QSO-dark
$0.5051$, a $0.81$\,pp difference ($|z| \approx 2.0$ two-sample) with
opposite-signed deviations from half. An earlier draft of this
summary stated the bright/dark split agreed ``within $\pm 0.001$'';
that statement was stale and is corrected here — the bright/dark
difference is the $\sim\!2\sigma$ residual structure analyzed at the
per-class level in \S\ref{sec:results_vweb}. No test produces a
$>3\sigma$ residual after
Paper~IV-monopole correction.

\begin{table}[ht]
\caption{Systematics splits: measured CW fraction per split
(full sample $f_{\rm CW} = 0.49719$, $n = 791{,}635$). Match-radius
rows count pre-dedup chirality-relevant matched rows ($828{,}457$ at
the primary $1.0''$ radius), so their $n$ exceeds the deduped full
sample; all other splits use the deduped one-row-per-\texttt{TARGETID}
convention.}
\label{tab:systematics_splits}
\begin{ruledtabular}
\begin{tabular}{llrr}
Test & Split & $n$ & $f_{\rm CW}$ \\
\hline
Match radius & $0.5''$ & $820{,}266$ & $0.4972$ \\
             & $5.0''$ & $868{,}165$ & $0.4972$ \\
Footprint & north (BASS+MzLS) & $253{,}821$ & $0.4971$ \\
          & south (DECaLS) & $535{,}890$ & $0.4972$ \\
          & DES-only & $1{,}924$ & $0.4958$ \\
Confidence & $\ge 0.4$ & $787{,}279$ & $0.4971$ \\
           & $\ge 0.6$ & $232{,}014$ & $0.4954$ \\
           & $\ge 0.7$ & $185{,}719$ & $0.4948$ \\
           & $\ge 0.8$ & $153{,}879$ & $0.4950$ \\
Program & bright (BGS) & $775{,}760$ & $0.4970$ \\
        & dark (LRG/ELG/QSO) & $14{,}782$ & $0.5051$ \\
        & backup $+$ other & $1{,}093$ & $0.5105$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

\section{Discussion}\label{sec:discussion}

\subsection{Interpretation}

The cosmic-web headline is most cleanly read as a null: the
per-environment CW fractions cluster at the catalog-wide $f_{\rm CW}
\!\approx\! 0.4974$ value, with signed $\sigma$ deviations driven by
sample size, not by environment. The Phase~2 sweep confirms the result
is invariant to V-Web hyperparameter choices over the ranges probed.
The redshift, density, and HEALPix tests also produce no $3\sigma$
deviations after look-elsewhere correction.

\subsection{Implications for bounce and inflation models}

No published model in either class (matter-bounce or inflation)
currently predicts an environment-dependent CW signature at the
sensitivity reached here, so the present null does not directly
discriminate between matter-bounce and inflation-class models. It
instead establishes an observational upper bound that any future
parity-violating model in the bounce-chirality coupling class
(Sec.~\ref{sec:p4}), proposing an environment-dependent chirality
signature at the $\gtrsim\!25\,\text{Mpc}/h$ V-Web smoothing scale,
must respect.
Paper~II~\cite{golden_fnl_2026} and Paper~III (both companion,
not-yet-published works by the same author) provide independent
discriminators (primordial $f_{\rm NL}$ and multi-survey anomaly
statistics); this null adds a clean negative result on the
spiral-chirality axis.

\subsection{Comparison to Shamir~2022 DESI Legacy}

Shamir~2022~\cite{Shamir2022DESI} reported a $\sim 2\!-\!4\%$
large-scale asymmetry on $\sim\!1.3\!\times\!10^6$ Ganalyzer-classified
galaxies. Paper~IV finds the catalog-wide CW-fraction offset is
$-0.26\%$ and the full-sky dipole amplitude $|A|\!<\!0.32\%$
($1\sigma$), about an order of magnitude smaller than the Shamir~2022
amplitude. The present paper's per-environment CW fractions sit at
$\sim 0.497$ with inter-class range $1.98$\,percentage points across the four
V-Web classes (per Table~\ref{tab:cw_vs_env} canonical run; across
the nine $(R_s, \lambda_{\rm th})$ Phase~2
sweep cells the per-class monopole-subtracted residuals stay below
$1.87\sigma$ and the filament/cluster fractions move by
only ${\sim}0.1$\,pp, \S\ref{sec:phase2}), leaving no room
for a residual environment-dependent chirality of the Shamir~2022
amplitude ($2$--$4$\,pp would be required in at least one well-populated
class). We emphasize the scope of this comparison: the present null
constrains an \emph{environment-conditioned} chirality asymmetry of
the Shamir~2022 amplitude; it does not by itself adjudicate
Shamir's \emph{global} (full-sky dipole) anisotropy claim, which is
addressed separately and directly by the Paper~IV dipole bound quoted
above.

\section{Limitations}\label{sec:limitations}

\begin{itemize}
\item The cross-match is selection-limited: the $8.47$\,M chirality
catalog is flux-limited at $r\!\le\!17.8$ in the DESI Legacy regime;
DESI spectroscopic targets extend much fainter.
\item Projected $k\!=\!5$ NN density is a coarse proxy for 3D density
and cannot distinguish chance line-of-sight associations from true
overdensities without spectroscopic confirmation across the
neighbourhood.
\item V-Web is one of several cosmic-web finders; we cross-validated
against the independent Tempel~\textit{et~al.}~2014 \cite{Tempel2014}
FoF group classifier (\S\ref{sec:tempel}). The like-for-like filament
class
concordance on the common overlap is $0.29$\,percentage points
(${\sim}0.5\sigma$, statistically indistinguishable); lower-$n$
classes scatter by counting statistics and
classifier-definition mismatch.
\item Imaging-leg systematics in chirality classification are tracked
in Paper~IV (per-leg $|\sigma|\!<\!3$); we propagate the per-leg split
here but do not re-derive the underlying bias.
\item No DESI value-added catalog assigning cosmic-web environments
at the full DR1 footprint is published at the time of writing.
The ASTRA-DESI EDR probabilistic environment catalog
\cite{ASTRADESI2026} restricted to the $\sim\!175$\,deg$^2$ EDR
rosettes is the closest published independent classifier and is
cross-matched per-galaxy against our V-Web run in
\S\ref{sec:astra_per_object}; a full-DR1-footprint published VAC
remains a desirable future input that would let us replace V-Web as
the canonical classifier rather than only cross-validate it in
the EDR-overlap subset.
\item Galaxy positions used for the V-Web tidal-tensor estimate are
in observed redshift space rather than corrected to real space.
\emph{The dominant RSD effect for a tidal-tensor classifier is
anisotropic eigenvalue deformation, not isotropic scalar displacement.}
The Kaiser
line-of-sight squashing and the finger-of-god elongation each
introduce a direction-dependent shift in the deformation-tensor
spectrum
(Hahn~\textit{et~al.}~2007~\cite{Hahn2007};
Hoffman~\textit{et~al.}~2012~\cite{Hoffman2012};
Cautun~\textit{et~al.}~2014~\cite{Cautun2014}): the eigenvalue
ordering ($\lambda_1 \ge \lambda_2 \ge \lambda_3$) is the
load-bearing quantity for V-Web class assignment, and anisotropic
flow contributions enter the off-diagonal Hessian components rather
than as a single scalar smearing length. At class-boundary edges
(filament$\leftrightarrow$wall, wall$\leftrightarrow$void) a small
fraction of pixels can shift class under RSD even when an isotropic
smoothing-length comparison would suggest immunity. A quantitative
RSD bound therefore requires a reconstructed-position re-classification
cross-check (Zel'dovich or BAO reconstruction), not the scalar
$\sigma_v/(aH)$ comparison alone.

As a \emph{secondary} indicative bound, the scalar-displacement
heuristic gives an order-of-magnitude floor: for the typical
pairwise velocity dispersion $\sigma_v \lesssim 400$\,km\,s$^{-1}$,
the Kaiser-plus-finger-of-god displacement is
$\sigma_v/(aH) \lesssim 5$--$8$\,Mpc/$h$ over $0.01 \le z \le 2$
(consistent with the $\lesssim 5$\,Mpc/$h$ figure quoted at
$z \lesssim 0.24$ in \S\ref{sec:desivast_primary}; the upper end of
the range applies at the high-$z$ end of the window),
a factor of $\sim 3$--$5$ smaller than the chosen Gaussian smoothing
scale $R_s = 25$\,Mpc/$h$. The implied per-class $\Delta f_{\rm CW}$
contamination is sub-percent ($\sim 0.2$\,pp), the
\emph{same order of magnitude} as the ${\sim}0.1$\,pp
filament/cluster class-fraction movement across the Phase~2 sweep
cells rather than negligible relative to it. This scalar bound is
necessary but \emph{not sufficient}: the anisotropic eigenvalue
deformation above is the dominant channel and is not separable from
the sweep-induced shift without a reconstructed-position rerun.
A reconstructed-position rerun is a natural follow-up if the smoothing
scale is ever pushed below $\sim 10$\,Mpc/$h$.

\emph{Order-of-magnitude boundary-crossing estimate.}
Using the V-Web eigenvalue field itself, the fraction of cells near
class boundaries (eigenvalues within $|\lambda - \lambda_{\rm th}|
\le \sigma_{\rm rsd}/R_s$ of the threshold) sets the maximum
boundary-crossing rate. At $\sigma_{\rm rsd} \sim 5$\,Mpc/$h$ (BGS
pairwise velocity dispersion projected through Hubble flow at
$z\!\sim\!0.2$) and $R_s\!=\!25$\,Mpc/$h$, the eigenvalue-shift
magnitude is $\sim 0.04\,\sigma_\lambda$, where $\sigma_\lambda$
denotes the rms width of the per-cell smoothed-eigenvalue
distribution (the per-cell eigenvalue histogram of the canonical
$R_s\!=\!25$\,Mpc/$h$ run). Based on that histogram,
the fraction of cells within that band of any threshold is
$\sim 3$--$5\%$. Galaxies in those cells are the maximum possible
class-flip population under RSD; with $\sim 800$k chirality-relevant
spirals this is at most $\sim 2$--$4\times 10^4$ galaxies, distributed
across all four class boundaries. The propagated contribution to
$\Delta f_{\rm CW}$ per class is therefore expected to be
sub-dominant at the current $\sim 10^{-3}$ precision, but we
explicitly do not quantify the propagated uncertainty in the
present paper:
a full quantification — distinguishing pure displacement from
anisotropic eigenvalue deformation — requires the proper
Zel'dovich-reconstructed re-classification, which we defer to a
companion follow-up. The headline null is reported at fixed-redshift-
space classification, with this caveat explicitly carried.
\end{itemize}

\section{Future LSST extension}\label{sec:lsst}

Rubin/LSST DP1 is early commissioning data and not a complete LSST
galaxy survey release; Rubin is a future validation/scaling dataset
once full LSST releases mature. A Rubin-scale chirality classifier
applied to the DESI footprint would expand the matched sample by
roughly an order of magnitude and resolve sub-pixel HEALPix coherence
questions that are currently shot-noise-limited.

\section{Conclusions}\label{sec:conclusions}

Spiral galaxy chirality shows no detectable dependence on large-scale
structure environment at current sensitivity within DESI Data
Release~1. The \emph{primary} result is the DESIVAST-anchored
void-vs-non-void contrast (\S\ref{sec:desivast_primary}):
$\Delta f_{\rm CW} = +0.0007$ at $n_{\rm void} = 56{,}981$
($z_\Delta = +0.31$, two-sided $p_\Delta = 0.76$, $95\%$ CI
$[-0.0036, +0.0050]$), robust across all three published DESIVAST
void-finding algorithms ($|z_\Delta| \leq 1.12$,
Table~\ref{tab:desivast_three_algo}) and the catalog-native zone
definitions. The secondary V-Web cross-check concurs:
the headline cosmic-web result, the Phase~2 sensitivity sweep, the
redshift and density tests, and the HEALPix regional-coherence scan
all return null at the $3\sigma$ level after look-elsewhere correction.
The CW fractions per V-Web class on the $812{,}793$ env-labeled spiral
rows (covering $783{,}820$ of the $791{,}635$ unique chirality-relevant
matched spirals; $7{,}815$ lack an environment row,
\S\ref{sec:results_vweb}) in the canonical run are
$\{f_{\rm CW}^{\rm void}, f_{\rm CW}^{\rm wall},
f_{\rm CW}^{\rm filament}, f_{\rm CW}^{\rm cluster}\} =
\{0.484, 0.503, 0.498, 0.496\}$, a range of $1.98$\,percentage points
dominated by counting statistics and the catalog-wide classifier
monopole reported in Paper~IV (omnibus homogeneity
$\chi^2 = 3.55$, $p = 0.31$; $\chi^2 = 0.11$, $p = 0.99$ on the
selection-corrected rebuild). The result is robust under nine
$(R_s, \lambda_{\rm th})$ Phase~2 sweep cells (all per-class
monopole-subtracted residuals $\le 1.87\sigma$; look-elsewhere
$p \ge 0.13$), under grid resolutions $128^3$--$384^3$, and under six
classes of systematics tests.

This null is consistent with the Paper~IV global parity-mixture null
and provides an observational upper bound that any future model in the
bounce-chirality coupling class (Sec.~\ref{sec:p4}) proposing an
environment-dependent parity signature at the $\gtrsim\!25\,\text{Mpc}/h$
V-Web smoothing scale must satisfy; it does not constrain the
bounce-vs.\
inflation discrimination program: neither model predicts an
environment-dependent chirality signature at DESI DR1 resolution at
this smoothing scale, and the data agree.

A schematic toy mapping of the observational bound to an
effective-field-theory operator is summarized in
Appendix~\ref{app:toy_eft} as a guide for future model-building
(not as a derived constraint).

\appendix
\section{Toy EFT mapping of the environmental bound}\label{app:toy_eft}

\emph{Mapping to a physical operator (toy parametrization).}
The observational bound from the main text can be cast in an
effective-field-theory language as an order-of-magnitude guide for
how a parity-violating coupling would map to V-Web class-to-class
$\Delta f_{\rm CW}$. We emphasize that the operator form and scaling
written below are a \emph{toy parametrization introduced in this
work}, inspired by but not derived from the cited
parity-violating-gravity literature: the specific operator
\(\mathcal{L}_{\rm parity}\supset g_\phi\,(\nabla_i\phi)\,
(\nabla^i\rho/\rho_{\rm bg})\,(\hat L\cdot\hat z)\) is not contained
in either Alexander \& Yunes~\cite{AlexanderYunes2009} (Chern--Simons
modified gravity review) or Lue--Wang--Kamionkowski~%
\cite{LueWangKamionkowski1999} (cosmological parity-violating
interactions); those works motivate the general class of
parity-violating couplings but not this specific
density-gradient form. We deliberately keep the parameterization
schematic. Consider an axion-like (or generic pseudoscalar) field
$\phi$ coupled to matter density via the above operator, where
$\hat L$ is the local angular-momentum unit vector and $\rho$ is the
total matter density. For $\nabla\phi$ aligned with the cosmic-web
gradient $\nabla\rho$, the induced chirality asymmetry scales as
$\Delta f_{\rm CW}^{\rm env}\!\propto\!g_\phi\,\nabla\phi\cdot
\nabla\rho/\rho_{\rm bg}$. With per-class $|\Delta f_{\rm CW}|\!<\!0.01$
on $n\!\gtrsim\!4\!\times\!10^5$ spirals, an order-of-magnitude bound
on the coupling $g_\phi\,|\nabla\phi|$ in $H_0$ units is
$|g_\phi\,(\nabla\phi)/H_0|\!\lesssim\!1\!\times\!10^{-2}/
\langle|\Delta\rho/\rho_{\rm bg}|\rangle$, where the cosmic-web
fractional contrast averages $\sim\!\mathcal O(1)$ across the
classes. \emph{This is an order-of-magnitude estimate only, not a
quantitative ALP-coupling exclusion}: a real exclusion would require
(i) a full transfer-function calculation mapping the high-redshift
$\phi$-field gradient to the low-redshift V-Web eigenvalue field, and
(ii) propagation of the per-class $\Delta f_{\rm CW}$ measurement
uncertainties through that transfer function. We do not claim either
calculation here; the parametrization is included as a guide for
future model-building, not as a derived constraint.

\emph{Rotational-invariance and gauge-invariance caveat.} Two distinct theoretical caveats apply to the toy
operator. \emph{First (rotational invariance):} the explicit
$(\hat L\cdot\hat z)$ factor breaks rotational invariance via the
fixed coordinate-system unit vector $\hat z$, and should be read as
shorthand for a rotationally-invariant pseudoscalar formed from
$\hat L$ and a physical direction set by the local cosmic-web
gradient (e.g.\ $\hat L\cdot\widehat{\nabla\rho}$ or $\hat L\cdot
\widehat{\nabla\phi}$ in the limit $\nabla\phi\!\parallel\!\nabla\rho$
adopted below); the literal $\hat z$ form is a coordinate-aligned
schematic, not a covariant operator. \emph{Second (gauge invariance):}
even with the rotational-invariance issue resolved, the
density-gradient factor $\nabla^i\rho/\rho_{\rm bg}$ and the
late-time angular-momentum direction $\hat L$ are quantities
defined in a chosen synchronous-comoving slicing on the V-Web
smoothing scale $R_s = 25$\,Mpc/$h$ and are not manifestly gauge
invariant under general coordinate transformations. The toy
operator above should therefore be read as a heuristic
parametrization in this specific slicing, not as a covariant
EFT operator in the sense of Weinberg's EFT-of-inflation or
the EFT-of-LSS literature. A fully gauge-invariant formulation
would require promoting $\nabla^i\rho/\rho_{\rm bg}$ to a
properly defined density-fluctuation gauge invariant (e.g.\
$\delta_{\rm com}$ on comoving slices) and $\hat L$ to a
late-time observable angular-momentum direction expressed in
the same slicing; we have not carried out that construction.
We flag this as an open theoretical limitation of the toy
mapping; it does not affect any of the empirical
$\Delta f_{\rm CW}$ bounds in the main text.

\section{Reference contingency tables for the V-Web $\chi^2$ tests}\label{app:contingency}

For independent recomputation of the V-Web class-homogeneity test
($\chi^2 = 3.55$, 3~d.o.f., $p = 0.31$) and the V-Web class~$\times$
target-program test ($\chi^2 = 4932$, 3~d.o.f., Cram\'er's $V = 0.078$)
quoted in the abstract and \S\ref{sec:results_vweb}, we tabulate
the $4{\times}2$ cell counts implied by the published per-class $n$,
$f_{\rm CW}$, and bright-fraction values.

\begin{table}[h]
\caption{$4{\times}2$ contingency table CW/CCW $\times$ V-Web class on
the $812{,}793$-row env-labeled parent. Cells derived from the abstract
per-class $(n,f_{\rm CW})$ pairs: $\mathrm{CW} = \mathrm{round}(n\,f_{\rm CW})$,
$\mathrm{CCW} = n - \mathrm{CW}$. Sum: $n = 812{,}793$. Row marginal CW = $404{,}075$;
CCW = $408{,}718$. Pearson $\chi^2 = 3.55$ ($3$ d.o.f., $p = 0.31$) on
the row-level parent under the homogeneity null.\label{tab:contingency_classCWCCW}}
\begin{ruledtabular}
\begin{tabular}{lcccc}
Class & $n$ & $f_{\rm CW}$ & CW & CCW \\
\colrule
Filament & $408{,}187$ & $0.4980$ & $203{,}277$ & $204{,}910$ \\
Cluster  & $397{,}505$ & $0.4963$ & $197{,}272$ & $200{,}233$ \\
Wall     & $6{,}673$   & $0.5034$ & $3{,}359$   & $3{,}314$   \\
Void     & $428$       & $0.4836$ & $207$       & $221$       \\
\end{tabular}
\end{ruledtabular}
\end{table}

\begin{table}[h]
\caption{$4{\times}2$ contingency table V-Web class $\times$ target
program (\texttt{bright}/\texttt{dark}) on the
$n_{\rm bright+dark} = 811{,}609$ bright$+$dark subset of the env-labeled
parent. Per-class bright fraction
$\{0.981, 0.962, 0.966, 0.989\}$ for \{void, wall, filament, cluster\}
(\S\ref{sec:results_vweb}). Cells: $\mathrm{bright} =
\mathrm{round}(n\,f_{\rm bright})$, $\mathrm{dark} = n - \mathrm{bright}$.
Pearson $\chi^2 = 4932$ ($3$ d.o.f., $\log_{10} p \approx -1069$), Cram\'er's
$V = \sqrt{\chi^2/n} = 0.078$.\label{tab:contingency_classProgram}}
\begin{ruledtabular}
\begin{tabular}{lcccc}
Class & $n$ & $f_{\rm bright}$ & bright & dark \\
\colrule
Filament & $408{,}187$ & $0.966$ & $394{,}309$ & $13{,}878$ \\
Cluster  & $397{,}505$ & $0.989$ & $393{,}132$ & $4{,}373$   \\
Wall     & $6{,}673$   & $0.962$ & $6{,}420$   & $253$       \\
Void     & $428$       & $0.981$ & $420$       & $8$         \\
\end{tabular}
\end{ruledtabular}
\end{table}

The per-class counts above match the abstract sample ledger to the
quoted precision; small ($<1$~row) discrepancies between table marginals
and the body-text $811{,}609$ total reflect rounding in the published
per-class bright fractions. Cell-level counts are computed against the
unrounded committed-artifact arrays in
\artifact{pipelines/p5\_desi\_chirality/outputs/} (drivers
\texttt{05}--\texttt{09}); the tabulated rounded cells are intended as
a referee-facing reproducibility anchor, not as the primary data
source.

\section{Data and code availability}\label{sec:data_code}

The companion data repository referenced throughout this paper is the
versioned GitHub repository
\href{https://github.com/Hubify-Projects/bigbounce}%
{\texttt{Hubify-Projects/bigbounce}}, directory
\artifact{pipelines/p5\_desi\_chirality/}; results quoted here
correspond to manuscript tag \texttt{\paperVersion}. All analysis
drivers are committed under
\artifact{pipelines/p5\_desi\_chirality/scripts/} (catalog fetch,
cross-match, per-analysis drivers \texttt{05}--\texttt{09}, z-shell
rebuild \texttt{16}, and the closure-wave recompute drivers
\texttt{17}--\texttt{18}, which include the DESIVAST point-in-sphere
membership driver), with the V-Web/T-Web grid pipeline and the Tempel
cross-validation under
\artifact{pipelines/p5\_desi\_chirality/env\_finder/}. Numeric
artifacts live under \texttt{outputs/}, \texttt{results/}, and
\texttt{env\_finder/reports/} in the same directory, each with
per-output provenance JSON. A DOI-minted archival snapshot of this
directory accompanies journal submission. The canonical chirality
catalog is mirrored on HuggingFace at
\href{https://huggingface.co/datasets/bamfai/galaxy-chirality-catalog}%
{bamfai/galaxy-chirality-catalog}. DESI Data Release~1 is available from
\url{https://data.desi.lbl.gov/public/dr1/}. The Phase~2 sweep
per-cell statistics are in
\artifact{pipelines/p5\_desi\_chirality/env\_finder/reports/02\_phase2\_sweep.csv}
(declared-parent recompute; the superseded unfiltered-join version is
retained alongside as \texttt{\_prefilter\_legacy}).

\section*{Reproducibility checklist}
\begin{itemize}
\item Single config file:
\artifact{pipelines/p5\_desi\_chirality/env\_finder/config.yaml}.
\item Per-output provenance metadata included in data release.
\item Deterministic seed: \texttt{20260515}.
\item All Phase~2 sweep cell configs persisted under
\href{https://github.com/Hubify-Projects/bigbounce/tree/main/pipelines/p5_desi_chirality/data/desi_env/phase2_sweep}{\nolinkurl{pipelines/p5\_desi\_chirality/data/desi\_env/phase2\_sweep/}}.
\end{itemize}

\begin{thebibliography}{99}

\bibitem{AlexanderYunes2009}
S.~Alexander and N.~Yunes,
\emph{Chern--Simons modified general relativity},
Phys.\ Rep.\ \textbf{480}, 1 (2009),
\href{https://doi.org/10.1016/j.physrep.2009.07.002}{doi:10.1016/j.physrep.2009.07.002},
arXiv:0907.2562.

\bibitem{LueWangKamionkowski1999}
A.~Lue, L.~Wang, and M.~Kamionkowski,
\emph{Cosmological signature of new parity-violating interactions},
Phys.\ Rev.\ Lett.\ \textbf{83}, 1506 (1999),
\href{https://doi.org/10.1103/PhysRevLett.83.1506}{doi:10.1103/PhysRevLett.83.1506},
arXiv:astro-ph/9812088.

\bibitem{golden_chirality_2026}
H.~Golden,
\emph{A Survey-Scale Chirality Catalog of 8.47M Galaxies (3.2M Spirals):
A Null Detection of Large-Scale Parity Violation at Sub-Percent
Sensitivity}, companion paper (Paper~IV), in preparation; manuscript in preparation.

\bibitem{golden_fnl_2026}
H.~Golden,
\emph{$f_{NL} = -35/8$ Forecast: SPHEREx Discrimination of Bounce vs.\
Inflation}, companion paper (Paper~II), in preparation; manuscript in preparation.

\bibitem{Hahn2007}
O.~Hahn, C.~M.~Carollo, C.~Porciani, and A.~Dekel,
``Properties of dark matter haloes in clusters, filaments, sheets and
voids,'' Mon.\ Not.\ Roy.\ Astron.\ Soc.\ \textbf{375}, 489 (2007),
arXiv:astro-ph/0610280.

\bibitem{Hoffman2012}
Y.~Hoffman, O.~Metuki, G.~Yepes, S.~Gottl\"ober, J.~E.~Forero-Romero,
N.~I.~Libeskind, and A.~Knebe,
``A kinematic classification of the cosmic web,''
Mon.\ Not.\ Roy.\ Astron.\ Soc.\ \textbf{425}, 2049 (2012),
arXiv:1201.3367.

\bibitem{Cautun2014}
M.~Cautun, R.~van de Weygaert, B.~J.~T.~Jones, and C.~S.~Frenk,
``Evolution of the cosmic web,''
Mon.\ Not.\ Roy.\ Astron.\ Soc.\ \textbf{441}, 2923 (2014),
arXiv:1401.7866.

\bibitem{Planck2018cosmoparams}
Planck Collaboration,
``Planck 2018 results. VI. Cosmological parameters,''
Astron.\ Astrophys.\ \textbf{641}, A6 (2020),
arXiv:1807.06209.

\bibitem{Shamir2022DESI}
L.~Shamir,
``Analysis of spin directions of galaxies in the DESI Legacy
Survey,''
Mon.\ Not.\ Roy.\ Astron.\ Soc.\ \textbf{516}, 2281 (2022),
doi:10.1093/mnras/stac2372, arXiv:2208.13866.

\bibitem{Tempel2014}
E.~Tempel, A.~Tamm, M.~Gramann, T.~Tuvikene, L.~J.~Liivam\"agi,
E.~Saar, P.~Hein\"am\"aki, P.~Nurmi, and J.~Einasto,
``Flux- and volume-limited groups/clusters for the SDSS galaxies:
catalogues and mass estimation,'' Astron.\ Astrophys.\ \textbf{566},
A1 (2014), arXiv:1402.1350.

\bibitem{TWebDESI2026}
H.~I.~Ullah, M.~Awais, T.~Matos, and J.~F.~Su\'arez-P\'erez,
``Cosmic-web quenching with DESI DR1: T-Web environments and
mass-dependent red/blue classification,''
preprint (2026), arXiv:2604.02463.

\bibitem{ASTRADESI2026}
D.~C.~Zapata-Zuluaga, S.~Guevara-Montoya, V.~Torres-Gomez,
J.~Hernandez, and J.~E.~Forero-Romero, ``The Cosmic Web in the DESI
Early Data Release: A Probabilistic Environment Catalog,'' (2026),
arXiv:2604.01456.

\bibitem{DESIVAST2025}
H.~Rinc\'on, S.~BenZvi, K.~A.~Douglass \textit{et al.},
``DESIVAST: Catalogs of Low-redshift Voids Using Data from the DESI
Data Release 1 Bright Galaxy Survey,'' Astrophys.\ J.\ \textbf{982},
38 (2025), doi:10.3847/1538-4357/adb559, arXiv:2411.00148.

\bibitem{Walmsley:2023}
M.~Walmsley \emph{et~al.}, Galaxy Zoo DESI: large-scale automated
morphology classification of 8.7 million galaxies in the DESI Legacy
Imaging Surveys, Mon.\ Not.\ R.\ Astron.\ Soc.\ \textbf{526}, 4768 (2023),
arXiv:2309.11425.

\end{thebibliography}

\end{document}
