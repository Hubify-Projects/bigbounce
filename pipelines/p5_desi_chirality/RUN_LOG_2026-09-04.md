# P5 parquet regeneration — RUN_LOG 2026-09-04

Task: p5_matched_chirality_desi.parquet is gitignored + missing everywhere
(no HF, no local, no sha256). Regenerate from committed recipe
(pipelines/p5_desi_chirality/scripts/{01,02,03}*.py per
reproducibility/manifests/experiments/p5-desi-dr1-crossmatch-build.json).

## Inputs found already local (no re-download needed)
- `data/p4_chirality.parquet` (952,115,239 bytes) + provenance sidecar:
  sha256 e8525ba5c98576f6361580e4a0aa7a86929ccc9f79b1423808774cfaaf313563,
  rows_observed=8,474,531 == expected_rows in p5_config.yaml. Source: HF
  bamfai/galaxy-chirality-catalog / catalog_production.parquet @ 1ba76bc7.
- `data/desi_zall.fits` (22,371,272,640 bytes = 22.37 GB) — raw DESI DR1
  zall-pix-iron.fits already downloaded (matches
  sources.desi_dr1.file_size_bytes in p5_config.yaml exactly). A prior
  desi_zall.parquet.provenance.json exists but the parquet itself is gone
  (gitignored + not retrievable) — the .fits survived so no re-download is
  needed, only the FITS->parquet conversion step (02_fetch_desi_dr1.py
  skips download when out_parquet missing but raw_fits present... actually
  script re-downloads if out_parquet missing regardless; see notes below).

## Environment check
- astropy 8.0.1, pyarrow 25.0.0, pandas 3.0.5, numpy 2.5.1 — all present.
- RAM: 24 GB. Disk free: 35 GB (desi_zall.fits already counted in Used).

## Plan
1. Run 02_fetch_desi_dr1.py pointed at existing local raw_fits (patch to
   skip download since file already present) -> data/desi_zall.parquet.
2. Run 03_crossmatch.py -> results/p5_matched_chirality_desi.parquet +
   summary.json + provenance sidecar.
3. Compare row count / schema to committed summary/provenance JSONs.
4. Backup 3x (HF, B2, local datasets dir), verify checksums.
5. Update 8 dependent manifests + build manifest with real locators/sha256,
   validate via tools/validate_repro_manifests.py.
6. Append restoration section to reproducibility/FULL_PASS_2026-09-04.md.

## Log
- (this file created; committing now as step 0)
- 02_fetch_desi_dr1.py had a real memory-blowup bug (Table.read() loaded
  all 136 columns before dropping unwanted ones, ~20GB+ RSS, thrashed this
  24GB host). Fixed: memmap column-selective + chunked streaming write
  (commits 7d0db6de, 4d188826 fixing a pyarrow byte-swap crash, 289e257a
  fixing a leftover-variable crash in provenance write). Final run:
  28,425,963 rows, 27 cols, matches 2026-05-16 provenance exactly.
  Disclosed: local desi_zall.fits's sha256 does not match the 2026-05-16
  provenance's raw_sha256 despite identical byte size -- row count parses
  identically so tabular content is very likely unchanged; not
  investigated further, disclosed honestly (see provenance sidecar).
- 03_crossmatch.py ran clean: matched_primary_deduped=2,232,212, exact
  match to every committed manifest/summary reference. sha256
  a0fa4725841c4f6b81233a8dc7d323e1de755ed6cfa0dc878b6af24674adc147.
  Committed fb93e904.
- 9 manifests (8 dependents + build manifest) updated with real
  locators/sha256, commit 9beeac85. Validator: 0 errors.
- 3-way backup in progress: local datasets dir DONE; HF
  (bamfai/bigbounce-aug-011-clean-rerun/p5/2026-09-04/) and B2
  (s3://bigbounce/p5/2026-09-04/) uploads running in background --
  network-bound, ~400-500 KB/s, ETA ~45-60 min for the 1.3GB file. Will
  verify checksums on completion.
- NOTE (repo hygiene): commit 289e257a accidentally swept in an unrelated
  staged change from a concurrent session (site/src/app/publish/publish.css,
  545 deletions) because this repo's git index is shared across
  concurrently-running sessions on this host. Not reverted (would destroy
  someone else's real in-progress work). All commits after that one used
  `git commit -- <exact paths>` to avoid repeating this.
