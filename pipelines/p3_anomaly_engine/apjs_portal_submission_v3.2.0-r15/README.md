# P3 ApJS portal staging package — v3.2.0-r15

Prepared 2026-08-03 for the ApJS submission portal. All uploadable files are at
this directory level; `paper3_apjs.tex` is a staging copy whose three figure
paths have been flattened. It compiles with the bundled official AASTeX 7.0.2
class (`aastex702.cls`) and includes required line numbers.

Contents:

- `paper3_apjs.tex`, `aastex702.cls`, and the three referenced PDF figures form
  the manuscript upload set.
- `tab3.tsv`, `ReadMe`, and `AAS_DIGITAL_ASSET_MANIFEST.json` are the existing
  181-row, 43-column machine-readable-table submission assets. Their journal
  digital-asset DOI remains pending.
- `SHA256SUMS` binds every file in this staging directory, including this note.

This directory is a local upload staging area, not an AAS submission record.
Before upload, Houston must complete the live portal metadata, data-asset, and
attestation choices recorded in `../APJS_PORTAL_SUBMISSION_KIT_v3.2.0-r15_2026-08-03.md`.
