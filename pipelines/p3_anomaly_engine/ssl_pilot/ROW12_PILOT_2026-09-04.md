# Row 12 pilot — self-supervised spectral model (2026-09-04)

**Ledger:** `project-context/NEXT_SCIENCE_LEDGER.md` row 12 — self-supervised
spectral model on DESI DR1 science-target spectra, 1M-spectrum pilot,
evaluated via the known-object recovery benchmark against the v2 catalogue.

## Plan
1. Preflight doc: selection rule, expected sky/non-science fraction (<0.5%),
   10k-slice composition check — `PREFLIGHT_2026-09-04.md`.
2. RunPod pod: GPU ≥24GB VRAM, ≥100GB disk, spend cap $50, balance logged
   before/after. Terminate + one retry if not SSH-reachable in 15 min.
3. Stage 1M science-target spectra from existing DR1 mirrors (HF
   `bamfai/bigbounce-aug-011-clean-rerun` / B2 / pod) — no fresh DR1 download.
4. Train small (<=20M param) masked-spectrum transformer or 1-D contrastive
   encoder, <=2h, committed script+config+seed. Save embeddings for 1M +
   the 1,244 v2-catalogue objects.
5. Anomaly score = reconstruction/embedding-density outlier score; run
   `benchmark_known_object_recovery.py` on pilot vs v2 reference classes.
6. Backup-3plus (HF `ssl_pilot/2026-09-04/`, B2, local) with checksums before
   pod stop.
7. Manifest (venue, cost, wall-clock) + result section + ledger row 12
   status update (<=5 lines).

## Status log
- 2026-09-04: plan created. RunPod balance confirmed reachable
  ($136.78 pre-spend). Proceeding to preflight doc (step 1).
- 2026-09-04 (relaunch): pod pipeline (pod_row12_pilot.sh + launch_row12_pilot.sh,
  stages 01-07) confirmed present, compiles clean, all referenced scripts/data
  present locally (flagship_sample_v2.parquet, recovery_refs_2026-09-02, gates,
  benchmark script). Prior pod `p2nmgd3g5buxeh` (killed by rate limit) was
  already terminated by the orchestrator and was NOT reused.
  - Attempt 1: `podFindAndDeployOnDemand` created pod `5axzi38imuye17`
    (RTX 4090, COMMUNITY). No SSH after 15 min (30x30s poll) -> terminated
    per contract.
  - Attempt 2 (one retry, other GPU type/region per contract):
    RTX A5000/SECURE returned `SUPPLY_CONSTRAINT` (no instances available);
    fell through to pod `fc0ub6lr8nqham` (RTX 3090, COMMUNITY). No SSH after
    15 min (30x30s poll) -> terminated per contract.
  - Both pods confirmed gone from `myself.pods` (not just EXITED — fully
    terminated) via a follow-up GraphQL query.
  - Balance: $136.4415 before attempt 1 -> $136.1681 after both terminations
    (~$0.27 total, community on-demand SSH-boot billing; nowhere near the
    $50 cap; no training/staging spend incurred).
  - Per task contract ("terminate and retry ONE other GPU type/region; then
    stop and report"): stopping here. No 1M-spectrum staging, training,
    embedding, benchmark, or backup ran this session — both pods died before
    stage 01 could start (SSH never came up).
  - Row 12 remains OPEN. Recommended next action for a future session:
    retry with a SECURE-cloud on-demand GPU (A5000/A4000 SECURE, not
    COMMUNITY) or a different region, since two consecutive COMMUNITY-cloud
    SSH-boot failures in one session suggests a COMMUNITY-pool availability/
    networking issue rather than a one-off fluke.
- 2026-09-04 (3rd attempt): fixed two real bugs found via a live GraphQL
  probe + `docs.runpod.io` before relaunching: (1) `Authorization: Bearer`
  returns HTTP 403 on the current RunPod API -- the working auth is the
  `?api_key=` query param; (2) `podFindAndDeployOnDemand` omitted
  `ports:"22/tcp"` and `supportPublicIp:true`, relying on `startSsh:true`
  alone. Reordered `GPU_CONFIGS` to SECURE cloud first (docs: SECURE always
  gets a public IP; COMMUNITY needs the flag and was flaky). Committed in
  `68b403a6`.
  - Attempt 1 (SECURE): pod `hi7gwl7xk1olnx` (RTX 3090, SECURE) created
    instantly but `runtime` stayed `null` for the full 15 min (30x30s
    poll) -- never reached a bootable state, not just a missing port.
    Terminated per contract; confirmed gone from `myself.pods`.
  - Attempt 2 (one retry, different SECURE GPU type per contract): pod
    `0o106zd78a8d5d` (RTX 4090, SECURE) -- same symptom, `runtime` null for
    the full 15 min. Terminated per contract; confirmed gone.
  - Balance: $135.5478 -> $135.2276 across both attempts (~$0.32 total,
    well under the $50 cap). No staging/training/embedding/benchmark/backup
    ran -- both pods died before SSH, let alone stage 01.
  - Per task contract ("one retry on a different SECURE GPU type; then stop
    and report"): stopping here. This is a THIRD consecutive session where
    RunPod pods for this specific pilot never reached SSH, now including
    SECURE cloud with the corrected schema fields -- the failure mode looks
    like an account/region/RunPod-side issue (not a pilot-script bug: the
    working phase-3 pod `8ofv5d4ynu7hku` was created via the RunPod web UI,
    not this GraphQL script). Row 12 remains OPEN. Recommended next action:
    try the RunPod web UI directly (bypassing `podFindAndDeployOnDemand`,
    per the precedent in `pipelines/p5_desi_chirality/env_finder/LAUNCH_POD.md`
    which notes the GraphQL schema "varies" and the UI is more reliable), or
    the `ssh.runpod.io` proxy against a manually-created pod, before
    spending further session budget on this GraphQL launch path.
