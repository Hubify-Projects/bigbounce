# Row 13 PILOT — 2026-09-04

## Status: IN PROGRESS (preflight committed; Part A + B to follow in this file)

## Preflight (Part A — image-level end-to-end injection)

- **Classifier**: `bamfai/galaxy-chirality-v2` (ViT-small/16 encoder + MLP
  head, 3-class CW/CCW/NS), checkpoint `chirality_model_v2_best.pt`,
  revision `237d021c451d75cf86a875e86d4de498b74e2f12`
  (`pipelines/p2_chirality/HF_MODEL_README.md`).
- **Cutout source**: Legacy Survey DR9 JPEG cutouts,
  `https://www.legacysurvey.org/viewer/jpeg-cutout?ra=..&dec=..&size=150&layer=ls-dr9`
  (`pipelines/p2_chirality/run_eq_dataloader.py:200-203`).
- **Existing label-level injection baseline**: catalog-level dipole injection
  on class labels (`pipelines/p2_chirality/scripts/full_catalog_injection_recovery.py`),
  reporting 50%-recovery-at-3sigma amplitude from re-running the MASTER
  ell=1 decoupling on injected label maps — NOT an image-level pixel
  injection. Row 13's question: does a true pixel-level parity injection
  (mirror-flip a fraction of raw cutouts, then re-run the classifier)
  recover consistently with the label-level curve, or is the label-level
  curve optimistic/conservative relative to what the actual image pipeline
  would show?
- **Declared pilot plan (full spec)**: 10,000 cutouts (5k CW / 5k CCW by
  catalog label, sky-uniform sample from `bamfai/galaxy-chirality-catalog`),
  mirror-flip (true pixel parity flip, left-right) a fraction f of cutouts
  at f in {0, 0.5%, 1%, 2%, 5%}, run the committed classifier end-to-end
  per cutout, measure the recovered CW-fraction dipole/asymmetry vs
  injected f, compare against the label-level curve.
- **Compute**: RunPod GPU authorized, spend cap $30, balance checked
  before/after; if no pod reachable within 15 min after one retry, fall
  back to a CPU-sized 2k-cutout local subset and disclose the reduction.
- **Backup-3plus**: any RunPod outputs mirrored to local + HF + B2 before
  stopping the pod (per `/backup-3plus`).

## Part A — result (to be filled after run)

## Part B — Euclid Q1 feasibility (to be filled after research)

## Ledger row 13 (<=5 lines)

- Row 13: image-level injection pilot (parity flip) vs label-level curve;
  Euclid Q1 feasibility. Status: preflight committed 2026-09-04, execution
  in progress same session.
