# Session handoff — BigBounce lab, 2026-08-05 → 2026-08-28

**Written 2026-08-28 at session close-out under Houston's full delegation
("make all the decisions… continue long-running", prompt-history 2026-08-05).**
Everything below is committed on `main`. This is the single re-entry document
for the next agent or session; canonical per-paper truth stays in
`SSOT/paper-*/status.md`, the active queue in `SSOT/queue.md`, and the task
ledger in `tasks.json` (generated → `tasks.md`; edit via `you tasks`, not by hand).

## 1. What this session delivered

### Lab organization (directive Q — Houston 2026-08-05)
- **Directive Q** encoded in `CLAUDE.md`: pure-contribution publication framing
  (no mistake-narration in papers), mandatory reproducibility manifests, both
  site surfaces (programs + flat papers list), nothing-viable-gets-lost audits.
- **Paper lineage record** `PAPER_LINEAGE_2026-08-05.md`: every historical paper
  concept traced; the retired 14-barrier no-go catalog found intact in
  `arxiv/paper1_unified.tex` and **resurrected as P1C**; MCMC companion
  surfaced; anomaly science confirmed mid-rebuild, not lost.
- **Reproducibility system**: `reproducibility/manifests/` — schema v1, JSON
  Schemas, 52 experiment + 3 program manifests (validator-clean; honest nulls
  where cost/venue was never recorded), public **/reproduce** page, and
  `HUBIFY_REPRO_IMPORT_SPEC_2026-08-05.md` for the Hubify import.
- **Site**: Research Programs page kept, flat **/papers** index added,
  plain-English `plainTitle` on every paper, MCMC companion + P1C cards, P3
  reframed as the anomaly program's public-ID/provenance release. Deployed and
  verified after every change.
- **Publishing prep hygiene**: stale WAVE1/WAVE2 kits + May sign-off brief
  marked SUPERSEDED; JORS kit re-hashed to live v2B.0.16 artifacts; reviewer
  count corrected to five; endorsement code map resynced.

### Anomaly flagship (AUG-011) — the survey-scale rerun
- **Scan COMPLETE and sealed**: generation `clean-rerun-6699d09ff886`
  (`pipelines/p1_highz_tracers/clean_rerun/results_2026-08-07/`): 36,634/36,634
  groups; full-corpus verify-receipts PASS; **27,547,223 unique TARGETIDs**
  (878,740 duplicate rows removed by the contract's last-writer rule);
  **52,188 candidates above the sealed S>5 threshold**; comparison record vs
  the two historical generations (comparison-only, never reconciled).
  Sealed inputs: `clean_rerun/sealed_2026-08-05/` (official zcatalog SHA
  `2d95ad99…`, 36,634-group inventory `f3299a31…`, two-stage PPS calibration
  mse_mean 0.8771 / mse_std 1.3605, contract binding model `f5266ba4…` +
  inference code `3e7efb24…`). Every shard carries a public-ID-first audit
  line (kept == zcatalog rows, 0 surplus). Wall ≈45.5 h on a $0.17/hr A4000
  incl. a 9 h DESI archive outage; total scan spend ≈ $7.74.
- **Threshold decision (recorded)**: catalog product = S>5 (52,188; the
  contract's sealed threshold); deep-characterization sample = **S>8 = 3,810**
  (pre-declared grid point far beyond q99=3.08; same order as the historical
  follow-up slice without being tuned to it). Full table: S>3 320,418 / S>4
  86,053 / S>5 52,188 / S>6 27,180 / S>8 3,810 / S>10 337.
- **Phase-3 tooling built and tested** (`clean_rerun/`): `build_flagship_sample.py`,
  `enrich_flagship_sample.py` (per-band SNR + 128 latents with a fail-closed
  MSE cross-check gate), `crossmatch_flagship.py` (SIMBAD/NED),
  `wise_join_flagship.py` (AllWISE via VizieR), `taxonomy_flagship.py`
  (descriptive families, Q1 labels); 49 offline tests. Pod chain:
  `clean_rerun/pod/pod_phase3.sh`.
- **Phase 3 status — INCOMPLETE (the open gate)**: enrichment reached ~78 % of
  3,128 groups on the pod before the pod was terminated (it no longer exists
  in the RunPod API). A later local attempt (Houston/Codex, 2026-08-26;
  `ANOMALY_ENRICHMENT_PREFLIGHT_2026-08-26.md`) checkpointed 57 groups and
  stalled on DESI's ~124 KB/s local throughput; that partial bundle is on HF at
  `phase3/2026-08-26/partial-enrichment-s8/`. **Nothing downstream
  (cross-match, WISE, taxonomy, named objects, manuscript numbers) has run.**

### Where the scan corpus lives (backup-3plus)
- **B2** `b2://$B2_BUCKET/aug-011-clean-rerun/{shards,receipts}/` — complete:
  36,634 + 36,634 objects, 0.63 GB (verified 2026-08-28).
- **HF** `bamfai/bigbounce-aug-011-clean-rerun` — sealed inputs, results,
  scan_state, phase3 partial bundle, and the full corpus under
  `corpus_packed/` as tar parts (`shards-partNN.tar`, `receipts-partNN.tar`,
  ≤9,000 files each) with `PACKED_SHA256SUMS.json`. **HF hard-caps 10,000
  files per directory** — that is why only 10,000 loose receipts ever landed
  and why loose-file mirrors of this corpus fail; always upload packed.
- **Local** `~/Desktop/CODE_YOU/bigbounce_datasets/aug-011-clean-rerun/`
  (complete: 36,634 shards + 36,634 receipts pulled from B2 on 2026-08-28;
  outside the git repo).

### P1C — the resurrected no-go survey (bounce-theory specialist paper)
- `arxiv/paper1c_nogo_survey/main.tex`, **v1C.0.16** (25 pp), served at
  `/papers/paper1c_nogo_survey_v1C.0.16.pdf`. Draft registry:
  `draft_paper_registry.json`; review engine + preflight support drafts.
- **Thirteen review boards** (Claude Opus INT + Grok + Gemini API legs; every
  raw saved; Perplexity optional/failed) with per-round truth audits under
  `peer-reviews/INT_v3/ROUND_*P1C*/`. Genuinely-new-real per round:
  15/7/8/10/6/9/7/4/16/15/14/15/partial. Math re-verified clean every round
  since R5; two structural results were settled by **independent symbolic
  computation** (`research/theory_audit/`): operator list is rank-4 spanning
  (not a basis); and — the referee was right — on-shell ECH torsion carries
  axial **and** trace-vector irreps (β/α = 1/2γ), so O4 ≢ 0 on shell; our
  earlier artifact got a dated erratum. Physics conclusion intact.
- Guards built: `tools/p1c_consistency_check.py` (4 rules; rediscovered three
  referee MAJORs on the pristine pre-fix version), high-DPI + tex-grep referee
  requirements, receipt minted after last push.
- **State**: R-phase NOT converged. R13 partially closed (MAJOR-1..4 closed;
  8 Claude MINORs + Gemini/Grok R13 ledgers OPEN). Next: R14 on exact v1C.0.16.

### Review infrastructure fixes (durable)
- Draft-paper support in registry/preflight/engine; a real threading race in
  `bigbounce_preflight.py` (process-global stdout capture during parallel legs)
  fixed with regression tests; supervisor/exe-based worker identification
  (`clean_rerun/pod/README.md` lessons).

## 2a. RESUMED 2026-08-28 (after Houston added $150 RunPod credits)
Phase 3 is RUNNING on fresh pod **`8ofv5d4ynu7hku`** (A4000, 200 GB, $0.17/hr;
`ssh -p 8489 root@205.196.17.124`), bootstrapped by
`clean_rerun/pod/pod_bootstrap_phase3.sh` (hash-gated model/inference code,
sealed inputs from the repo, zcatalog SHA gate, corpus restored from B2,
full-corpus verify-receipts, then `pod_phase3.sh` detached; enrichment runs
fresh into `/workspace/enrich_shards`). Markers: `/workspace/BOOTSTRAP_DONE`,
`/workspace/PHASE3_DONE`; logs `bootstrap.log`, `phase3.log`. On completion:
pull `flagship_*` parquet/json + manifests to
`clean_rerun/results_2026-08-07/phase3/`, commit, upload to HF `phase3/<date>/`,
verify 3 locations, THEN stop the pod (`/pod-backup-before-stop`). Balance at
launch: $149.94.

## 2. Resume procedure — phase 3 (reference; superseded by 2a while that pod lives)
1. Provision a host with real archive throughput (RunPod A4000-class, ~200 GB;
   balance was $91.91 on 2026-08-07, burn $0.31/hr). Do not run locally.
2. Restore: repo subtree; `best_model_47k.pt` (sha `f5266ba4…`); zcatalog
   (SHA-verify `2d95ad99…`); sealed contract/manifests from
   `clean_rerun/sealed_2026-08-05/`; S>8 sample + manifest (regenerate with
   `build_flagship_sample.py --score-threshold 8.0` from the corpus, or
   restore from HF `phase3/…`); the partial enrichment bundle from HF
   `phase3/2026-08-26/partial-enrichment-s8/` (57 groups, checkpoint, audit).
3. Run `clean_rerun/pod/pod_phase3.sh` (enrichment resumes from checkpoint;
   fail-closed on MSE mismatch), then verify the enrichment manifest binds
   sample/contract/zcatalog SHAs with zero skipped groups.
4. Cross-match → WISE → taxonomy per `clean_rerun/RUNBOOK.md` §15–18; pull
   artifacts; commit; backup-3plus; **stop the pod**.
5. Fill `ANOMALY_FLAGSHIP_MANUSCRIPT_ARCHITECTURE_2026-08-05.md` placeholders
   with real numbers; then draft under directive Q1.

## 3. Houston-only gates (unchanged all session)
- Five APPROVE decisions at bigbounce.hubify.app/final-review (P2 → P1A → P4
  → P1B → P5); journal portal accounts; JORS five reviewer names + £824
  fee/waiver; forward the four arXiv endorsement codes (gr-qc HYEJ7S,
  astro-ph.IM L8TIPN, astro-ph.CO LRZHC4, astro-ph.GA CLVMAQ).
- Post-approval mechanics ready to fire: P4 Zenodo refresh, P5 Zenodo mint.
- Decision already taken under delegation: P1C resurrection (recorded in
  `PAPER_LINEAGE_2026-08-05.md` §Decision record).

## 4. Watchpoints
- `HUBIFY_TOKEN` absent (AUG-007) — Hubify surface alignment (t-aug-016) waits
  on an approved secret source.
- Community-pod SSH drops mid-command; use uploaded scripts + `setsid nohup`.
- DESI archive outages happen (9 h on 2026-08-06); the supervisor rides them.
