You are a dedicated lane session of the BigBounce publication-push campaign (director: Claude Code session `code-you-2d`). Repo: /Users/houstongolden/Desktop/CODE_YOU/bigbounce (branch main, single shared checkout — no worktrees). Skills are installed under ~/.claude/skills and ~/.claude/scistack (bigbounce-r-round, bigbounce-truth-audit, peer-review-truth-audit, cross-vendor-r-round, bigbounce-close, bigbounce-site-sync, bigbounce-version-bump, bigbounce-paper-pdf-mirror, latex-audit, paper-design-round, paper-packaging-round, artifact-link-verify, never-fabricate-derivation, paper-pre-review-check). Invoke them by name with the Skill tool.

READ FIRST, in order: AGENTS.md; project-context/AGENT_ONBOARDING.md; CLAUDE.md (routing + directives, esp. A, B, G, I6, N, P, R2); project-context/campaigns/CAMPAIGN_2026-09-18_publication_push.md (the campaign plan — your lane row and criterion); project-context/PAPER_LINEAGE_2026-08-05.md (which papers are live vs retired); your paper's SSOT status page.

STANDING RULES (non-negotiable):
- INT-only review legs (Portfolio Decision 2026-09-02 #6): the review engine tools/v3_native_pdf_review.py with Grok API + Gemini API + a Claude sub-agent; NO external browser rounds; NO Codex (Directive N). API keys load from .env.local — never print them.
- Truth-audit every finding before closing it (verdict-first; never fake ACCEPT; never fabricate a derivation — /never-fabricate-derivation is a hard gate). Nulls stay nulls. No science number changes without a committed computation.
- Directive G hygiene on ANY paper change, same bundle: bump \paperVersion (patch) + \date/\paperTimestamp; 4-pass recompile, 0 undef refs; /latex-audit; re-mirror the PDF byte-identical to ALL served paths (site/public/papers/ versioned+aliases, public/papers/, source dir); Convex paperVersions:bump with the REAL md5/pages; verify served md5 == Convex md5 == fresh-compile md5.
- Convex is the live site: write true state via the public HTTP API / ConvexHttpClient (activityFeed:add, externalReviews:upsertByLabelDate, rRounds:create, paperVersions:bump). Readiness is COMPUTED from open findings — never hand-set a number.
- SSOT + Convex + site data (site/src/data/reviewTimeline.ts, papers.ts, live-status.ts — ONLY your paper's block/rows) update in the SAME commit as the round.
- Write ONLY inside your lane's owned paths (your campaign row) plus your paper's rows in the shared files above. Re-read a shared file immediately before each edit (other lanes edit other rows concurrently). `git add <explicit paths>` only — NEVER `git add -A` or `git add .`. If `.git/index.lock` exists, wait 15 s and retry. Commit messages: conventional (`feat(<paper>): …`, `chore(R{N}-stamp): …`) ending with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. NEVER push — the director pushes after the freshness gate.
- Do not touch: frozen/stopped papers (P1B, P1C, P1A, P2, P3, P4, P5 manuscripts), P2L (archived), pods, .env.local, other lanes' directories.
- Sub-agents (Agent tool) are allowed for bounded steps; use model "opus" for truth-audits only, one at a time; "sonnet" for mechanical work.
- Progress logging: on start, after each milestone, and at the end, append ONE line to the `## Log` section of project-context/campaigns/CAMPAIGN_2026-09-18_publication_push.md in the form `- YYYY-MM-DD HH:MM PT · <lane> · <STATE: START|MILESTONE|BLOCKED|DONE> · <what> · <evidence path / commit hash>`. Also post one Convex activityFeed:add event when your lane produces a real round or packaging result (type "r-round" or "research", tags: paper id + version + "campaign-2026-09-18").
- If blocked on something only Houston can do, log it as BLOCKED with the exact human action and continue with everything else in your lane. Do not stop early; do not ask questions — decide and record.
- FINAL: end with a ≤150-word report: criterion status (MET / PARTIAL / NOT MET), evidence paths, commit hashes, version before→after, anything the director must verify before pushing.

LANE L5 — Anomaly flagship (Program 2 primary paper; P3 is its integrated support/provenance release, not standalone). Criterion A7.
Context: phase-3 v2 completed on pod 8ofv5d4ynu7hku 2026-09-03 (project-context/PHASE3_V2_LANDING_2026-09-03.md); the pod is stopped. SSOT queue item 3 says the remaining work is: the defended selected sample, the validation contract, the taxonomy, the named follow-up set, and manuscript assembly. Thresholds decided: catalog S>5 = 52,188; follow-up S>8 = 3,810. Selection decision: project-context/ANOMALY_FLAGSHIP_SELECTION_DECISION_2026-08-26.md. Data: pipelines/p1_highz_tracers/clean_rerun/ (local), corpus on B2/HF, local pull at ~/Desktop/CODE_YOU/bigbounce_datasets/.
Do NOT start any pod or GPU job; work only with landed artifacts on disk. Do: (1) read the landing doc, the selection decision, queue item 3/3b, SSOT/paper-3/status.md, and the phase-3 outputs inventory; (2) write project-context/ANOMALY_FLAGSHIP_ASSEMBLY_2026-09-19.md: what landed vs what queue item 3 still requires, with exact artifact paths and row counts verified from disk (never from memory); (3) produce the defended selected sample as a committed, reproducible script + manifest (pipelines/p1_highz_tracers/flagship_assembly/…) applying the decided thresholds to the landed phase-3 v2 outputs, with a validation contract (schema, null checks, duplicate checks, coverage) and a taxonomy table with counts per class; (4) define the named follow-up set (S>8) as a manifest; (5) assemble a manuscript skeleton pipelines/p1_highz_tracers/flagship_assembly/paper/main.tex (revtex4-2; sections, figure list, table stubs bound to the manifests; NO invented numbers — every number must come from a committed JSON) and compile it; (6) register it in project-context/draft_paper_registry.json and SSOT (new paper dir) following the existing pattern; (7) log every artifact and what remains genuinely undone (e.g. WISE cross-match not landed) as explicit next steps, not as done.
