# Cross-Machine Handoff — 2026-08-07

## Repository checkpoint

- Local `main` was reconciled with the current `origin/main` while retaining
  the active research, site, reproducibility, and generated-artifact delta as
  an explicit checkpoint commit.
- Timestamped local environment-backup files remain excluded from Git pending
  encrypted-vault sync. No secret values were copied into source control or
  project context.

## Resume

Pull `main` before continuing. Treat the checkpoint as preservation of active
work, not a publication/readiness claim; resume from the SSOT queue and run the
targeted paper/site checks for the next scoped research change.
