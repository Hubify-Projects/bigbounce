# P4 v1.0.264 — Truth Audit (exact-board confirmation)

- **Date:** 2026-07-17 (PT)
- **Paper:** P4 — Galaxy Chirality Catalog · venue APJS-CATALOG-METHODS
- **Version:** v1.0.264 (ApJS, 27pp)
- **Exact PDF SHA-256:** `325b7ced736081a201fb5f1bc9c6283760c1296b5adcb1baf7cb503ce98cb862` (on-disk `shasum -a 256 chirality_catalog_paper.pdf` == board sha == **PASS**)
- **Source:** `pipelines/p2_chirality/chirality_catalog_paper.tex` (repo HEAD `6cfc4f08`; board review commit `104ebb86`)
- **Auditor stance:** fresh, skeptical, source-cited; genuinely-new real findings retained; every dismissal carries a citation; never fabricate; when in doubt → GENUINELY-NEW-REAL.
- **Prior audits cross-referenced:** `ROUND_2026-07-16-P4-v1.0.26{1,2,3}-.../P4_v1.0.26{1,2,3}_truth_audit.md` (all genuinely-new-real items from those rounds are closed: §3.2 gloss + delivered monopole map [261], App-B stability delta in main Results [262/263]).
- **Campaign context:** `project-context/SSOT/paper-4/COMPUTE_CAMPAIGN_2026-07-17.md` — G1 retrain queued (BLOCKED, needs pod smoke-test); G2 held-out GZ1 BLOCKED on G1 manifest; G3 joint covariance **COMPLETED locally this session** and **now integrated as the new Sec 4.5 + Table 9 in v1.0.264**; G4 monopole-mechanism injection SCOPED/queued.

## What changed in v1.0.264 (the delta under audit)

v1.0.264 adds **§4.5 Joint Estimator Covariance** (`\subsection{Joint Estimator Covariance}`, tex L1222, `\label{sec:joint_cov}`) and **Table 9** (`tab:joint_cov`, tex L1230) — the integrated G3 3×3 block-bootstrap joint covariance of the three NaMaster-independent estimators (real-space dipole, WLS dipole, monopole), computed on one shared NSIDE=8 superpixel bootstrap (N=2000, seed 42, primary HC N=949,584). The integrated numbers match the committed artifact `outputs/canonical_provenance/g3_joint_estimator_covariance.json` and the campaign doc **exactly**: monopole z=−6.57 (the only |z|>3 mode), real-space dipole z=+2.21, WLS z=+1.36; ρ(dipole,monopole)=−0.037, ρ(dipole,WLS)=+0.277, ρ(dipole,Cl1)=+0.846 (validity check). The prose is honest: it states the object is *newly computed* (L1225–1227), that the 3×3 is NaMaster-independent, and that the **full joint likelihood + the MASTER-decoupled ℓ=1 leg remain open** (L1227, L1430) and that it **"does not identify the monopole's causal mechanism, which remains a separate open gate"** (L1227). Abstract + §4.4 + Conclusions + Appendix all cross-reference it consistently.

## Board summary

| Leg | Model | Parsed verdict | Findings |
|-----|-------|----------------|----------|
| Claude (INT, Anthropic subagent) | claude-opus-4-8 | MAJOR REVISIONS | 7 MAJOR, 5 MINOR |
| Grok (INT, xAI API) | grok-4.3 | MAJOR REVISIONS | 3 MAJOR, 2 MINOR |
| Gemini (INT, Google API) | gemini-3.1-pro-preview | MINOR REVISIONS | 4 MINOR |

All three legs affirm the narrow central claim is supported (strict HC observed-label real-space dipole null `z_mom=+0.635`, one-sided add-one rank `p=0.23768`). Claude's report itself concedes the central claim "is not over-interpreted."

## Already-tracked gates (context)

SSOT/campaign open gates: **G1** training-realization/provenance (records conflict 26,616 vs 26,626 / 826 vs 846 / 93.6878% vs 92.10%; no object/split manifest); **G2** training-disjoint independent classifier validation; **G3** joint covariance (**integrated this round → Sec 4.5**); **G4** monopole causal mechanism (image-level injection + per-pixel confusion + generative null); spatially resolved physical transfer function; matched-footprint independent estimator; full joint likelihood; complete systematics-metadata sidecar; DOI-backed archive; external preregistration/blinding; human ApJS review. Readiness cap **80 HOLDS**.

---

## Numbered dispositions

### Claude leg (MAJOR REVISIONS)

**M1 [MAJOR] — Catalog classifier not exactly reproducible (three conflicting committed records; no object/split manifest or random-state).**
Verdict: **ALREADY-TRACKED-GATE** (G1 / training-realization-provenance).
Disclosed verbatim: §Data L847+, Table 12 `tab:training_provenance` ("Conflicting historical training-data records"; "Exact object/split manifest — not retained"), Abstract, open-gates list. Claude's own recommendation (foreground it in the abstract + provide a frozen inference environment that reproduces the *labels* bit-for-bit from the frozen checkpoint) — the label-reproducing overlay is already public and byte-verified at immutable HF rev `911316f3…` (L1437, L1706), while the *training* replay is exactly the G1 gate. Identical to v1.0.261 C1/G1, v1.0.262 C1/Gk1, v1.0.263 M1/Gk1. Not new.

**M2 [MAJOR] — Sole independent cross-check (GZ1) is overlap-contaminated.**
Verdict: **ALREADY-TRACKED-GATE** (G2 / independent-classifier-validation).
Disclosed: §Morphology Systematics L1565 ("does not contain an object-level anti-join against the 6,637 GZ1 training rows, and the historical training script did not retain stable training IDs; this evaluation must therefore be treated as overlap-contaminated rather than independent"; 69.91%/κ=0.40 "descriptive… do not establish a physical transfer function"), L1581, Intro. The clean held-out validation is blocked by the same not-retained training IDs that block G1; the anti-join is G2, itself blocked on the G1 manifest (campaign doc). The "soften the abstract 'corroborates independently' claim" sub-ask is a bounded wording variant on already-disclosed overlap contamination. Identical to v1.0.261 C2, v1.0.262 C5, v1.0.263 M3. Not new.

**M3 [MAJOR] — A null with no calibrated sensitivity floor / coverage-calibrated upper limit; the paper explicitly declines any bound. → THE KEY ADJUDICATION.**
Verdict: **GENUINELY-NEW-REAL** (bounded, science; observed-label upper limit) — split from an **ALREADY-TRACKED-GATE** (physical upper limit / transfer function).

*Explicit decline verified.* The paper does decline exactly as Claude says: **Table 1 (`tab:headline_summary`) row (vii)** labels the injection-recovery *"Finite-grid sensitivity of the primary real-space estimator; **not calibrated coverage or an upper limit**"* (tex L899); the Fisher scale `σ(A)≈9.7×10⁻⁴` is *"not a calibrated detection threshold"* (L1289); the injection sweep reports fractions *"rather than defining calibrated 50% or 95% recovery amplitudes"* (L1292) and the grids are *"insufficient for calibrated coverage"* (L1302); Table `tab:injection_recovery` values are *"not calibrated recovery thresholds or physical limits"* (L1410); the historical `fig:harmonic_completeness` diagnostic (L1443/L1458–1459) is explicitly *"retained only as sensitivity provenance; it is not a calibrated recovery threshold or physical limit"* and is on a **different field/mask/null** from the primary. The "95% upper limit ≈3.0×10⁻⁴" strings (L899/L918/L1272/L1612) are the **hemisphere look-elsewhere p-value precision floor**, NOT an amplitude bound — they do not satisfy M3.

*The honest split.* There are two distinct upper limits, and they adjudicate differently:

- **(a) Coverage-calibrated OBSERVED-LABEL upper limit on the primary real-space dipole amplitude `A_obs` — GENUINELY-NEW-REAL, computable from committed artifacts.** The paper already has an injection-recovery chain that runs through the **exact primary estimator** and the **exact fixed-occupancy null**: the Stage-B harness injects a dipole into the *observed hard-label* CW/CCW field and propagates it through per-pixel map-making, the uniform-pixel `healpy.fit_dipole` real-space estimator, and the per-pixel-shuffle score (L1298–1303, L1339–1358; artifact `stage_b_hybrid_image_field_recovery.py`), and the "does/does-not traverse" paragraph confirms this chain **does not require the ViT, the confidence cut, or the spatial confusion model** (L1300–1303). What is missing for a coverage-calibrated `A_95^obs` is *only scale*: the current grids are 20 deterministic Fibonacci axes / 100 injections, which the paper itself says lack "Angular-quadrature convergence and independent sky/noise realizations" (L1361). The committed `harmonic_completeness` diagnostic already runs **10³ injections/amplitude** — so the compute scale is demonstrably feasible; it simply has never been run **on the primary real-space channel with random-axis coverage counting and inverted to an amplitude**. That is pure CPU work on committed artifacts (primary estimator + fixed-occupancy null + injection harness), yields a defensible coverage-calibrated observed-label upper limit, is **not** on any tracked gate list (G1–G4, SSOT gates, or the four open scientific gates at L1430), and closes precisely what Claude flags — "the pipeline has [not] demonstrated that it can detect a dipole of any stated amplitude at stated coverage." Retained as GENUINELY-NEW-REAL per the round's scenario-(a) rule and the "when in doubt" tiebreaker; the prior audits folded "no calibrated recovery threshold" into the *physical*-transfer gate (v1.0.261 G3, v1.0.262 Gk4, v1.0.263 Gk4) and never separately dispositioned the self-contained observed-label coverage calibration, which needs no transfer function at all.

- **(b) Coverage-calibrated PHYSICAL upper limit on `A_phys` — ALREADY-TRACKED-GATE (transfer function).** Converting `A_obs` → `A_phys` requires the spatially resolved morphology transfer function; the scalar `g=0.398` is illustrative only (L1289, L1305, L1418). This is correctly a disclosed/tracked limitation and must **not** be claimed.

**Minimal honest fix (spec only — do NOT run; queue for the campaign):** scale the existing observed-label injection-recovery up on the **primary real-space estimator** — ≥10³ random-sky-axis injections per amplitude on a finer amplitude grid — count the fraction recovered above the primary fixed-occupancy per-pixel-shuffle null threshold at each amplitude, and invert for `A_95^obs`. Report it explicitly as an **observed-label** (not physical) sensitivity floor / upper limit on the primary dipole amplitude, with a one-line abstract statement that (i) this is the coverage-calibrated observed-label bound and (ii) no physical parity-amplitude bound is claimed pending the transfer function. Estimated compute: CPU, hours; no GPU, no pod, no ViT re-inference; all inputs committed. Changes no existing science number — it is *additive*, giving the null quantified scientific weight.

**M4 [MAJOR] — Primary sample definition (`raw_flip_qc_unsafe` quarantine) fixed post-unblinding.**
Verdict: **DISCLOSED-RE-FLAG** (post-hoc disclosure) + **SCOPE-VENUE-OPINION** (headline-ordering preference).
Disclosed verbatim: §Dipole Analysis L1094-region ("introduced and finalized during post-review closure… not preregistered or fixed before unblinding… a transparent confirmatory analysis, not a blinded confirmatory test") and §4.1 ("it replaces, rather than supplements, the unsafe-inclusive primary result"); the stability delta `z=+0.48` excluded vs `+0.52` baseline is now in main Results (the v1.0.263 closure, verified at L1547-region and §Dipole). The residual "present the inclusive number as primary" is the headline-placement preference dispositioned at v1.0.261 C5, v1.0.262 C3, v1.0.263 M2. Not new.

**M5 [MAJOR] — Single load-bearing result buried under a thicket of non-comparable z's.**
Verdict: **SCOPE-VENUE-OPINION** (presentation).
The primary IS foregrounded: it is the sole abstract result; the estimator hierarchy declares it "the single primary science sample" (L870-region); Table 1 is titled the "Primary-estimator summary" with an explicit non-comparability caption note (L908: "σ values in different rows… are not directly comparable across rows as detection significances"); §Conclusions opens "We report one primary result… z_mom=+0.635" (L1437). The paper already demotes the diagnostics and repeatedly warns against z→p conversion. Reorganizing further is a bounded readability preference on already-caveated content. Same family as v1.0.263 M7/Gk-notation. Not a correctness defect.

**M6 [MAJOR] — Released catalog carries a −9.47σ monopole; TTA causal role not established.**
Verdict: **ALREADY-TRACKED-GATE** (G4 / monopole causal mechanism) + **SCOPE-VENUE-OPINION** (abstract placement).
Disclosed prominently in main Results §Global CW Fraction L1080: the −9.5σ residual, three candidate mechanisms, the explicit statement that the 2.98× suppression "does not isolate TTA causally because the raw and equivariant products came from different inference passes and memberships," the local-monopole-renormalization instruction, "the single largest false-positive risk in any ℓ=0 chirality-parity study," AND the delivered per-region correction map (`monopole_correction_map_v1_0_262.json`). The matched-membership A-vs-C causal test = the G4 gate. Identical to v1.0.261 C4-residual, v1.0.262 C4 (whose "only in Data Availability" premise was FALSIFIED), v1.0.263 M5. Not new.

**M7 [MAJOR] — Fitness-for-use of the full 8.47M labels vs the HC science sample under-characterized.**
Verdict: **ALREADY-TRACKED-GATE** (G2 / independent-validation) + **DISCLOSED-RE-FLAG**.
Disclosed: §Catalog Statistics L1004 ("every p_eq quantity in this paper is an uncalibrated ranking score. The HC threshold selects a sample; it does not assert a per-object correctness probability"); the full-sample GZ1 error is in Table 15 `tab:gz1_confusion` (L1565: three-class 58.7%, chirality 69.91%/κ=0.40 on the full N=240,919 cross-match), and the science-cut improvement is the stratified confusion at p_eq>0.6 (L1581–1586); the purity-vs-confidence ladder is in main Results (L1252, L1606: the +3.29σ in the [0.5,0.6) 1.87M-galaxy bin "does not survive the sample-purity ladder… cutting to p_eq>0.6 gives −0.03σ"). A clean *per-confidence-bin chirality-error* table requires the training-disjoint held-out set = the G2 gate. Claude's ask is an early/prominent placement of already-disclosed content plus a number gated on G2. Not new.

**M8 [MINOR] — No minted Zenodo DOI or commit hashes at review time.**
Verdict: **ALREADY-TRACKED-GATE** (DOI-backed archive; Houston/submission-time).
Disclosed at Data Availability L1706 ("The DOI and commit hashes will be inserted here in place of this sentence at submission time"; "no placeholder DOI is claimed"). Identical to v1.0.261 C8, v1.0.263 M9. Not new.

**M9 [MINOR] — 35 catalog rows unexplained.**
Verdict: **DISCLOSED-RE-FLAG / ALREADY-TRACKED-GATE** (provenance).
Disclosed verbatim at §Catalog Statistics L1004: "The final catalog contains 8,474,531 galaxies, 35 fewer than the immutable 8,474,566-row Smith42 training split. The historical inference runner retained neither a per-object failure ledger nor reason codes, so the identities of those absent rows and their exact failure reasons are unavailable; we do not characterize them as having failed a specific quality criterion." Claude's sub-ask ("list the missing dr8_ids") is **explicitly impossible** — no per-object ledger was retained (the same not-retained-artifact class as G1). Not new.

**M10 [MINOR] — Unresolved D₄ rotational-equivariance instability (21.4% borderline argmax flips).**
Verdict: **DISCLOSED-RE-FLAG** (touches label-reliability / independent-validation).
Disclosed in **main Methods** L968 verbatim: "per-galaxy argmax labels flip in 21.4% of cases between Z₂ and D₄ on borderline galaxies with p_cw≈p_ccw≈0.4… a classifier-stability check… not a spatial-null or isotropy test," plus mean ⟨p_CW⟩ stable to |Δ|<0.0016; full detail App-B L1543. The 21.4% is scoped to *borderline* objects (p≈0.4), which the HC cut (p_eq>0.6) removes. Propagating it into a per-object confidence caveat is a bounded enhancement of a disclosed, already-scoped result. Not new.

**M11 [MINOR] — False precision on a null (z quoted to 7 sig figs).**
Verdict: **SCOPE-VENUE-OPINION** (editorial). The headline uses `z_mom=+0.635`; the 7-figure form appears only where the exact rank arithmetic is shown. Identical to v1.0.261 C6. Not a correctness defect.

**M12 [MINOR] — Abstract phrasing "supports no primordial-parity bound" ambiguous + hedge list.**
Verdict: **DISCLOSED-RE-FLAG / SCOPE-VENUE-OPINION.** The sentence is a disclaimer consistent with §Relation to Parity-Violating Sectors L1423 ("The ℓ=1 morphology observable is parity-even and cannot by itself constrain primordial parity violation"). The "five items remain open" list is the honest open-gates disclosure. Editorial rewording only. Identical to v1.0.261 G4, v1.0.262 C10, v1.0.263 M12. Not new.

### Grok leg (MAJOR REVISIONS)

**G-M1 [MAJOR] — Irreconcilable committed training records; no manifest/random-state → non-reproducible training realization.**
Verdict: **ALREADY-TRACKED-GATE** (G1). Identical to M1 / all prior boards; same §Data + Table 12 citations. Not new.

**G-M2 [MAJOR] — `raw_flip_qc_unsafe` predicate + strict primary sample finalized post-review; changes the declared hierarchy after unblinding.**
Verdict: **DISCLOSED-RE-FLAG.** Identical to M4; disclosed §4.3 L1094-region + §4.1. Not new.

**G-M3 [MAJOR] — Sec 3.2 & 4.5: joint covariance computed only after reviewer request; text does not demonstrate the primary is robust to reasonable variations in support / null family. → responds to the NEW Sec 4.5.**
Verdict: **DISCLOSED-RE-FLAG** + **ALREADY-TRACKED-GATE** (full joint likelihood open). This is the one Grok major that engages the new content, but it raises **no methodological defect in the joint-covariance computation itself**. The paper openly states §4.5 is *newly computed* (L1225, "a reviewer gate asked… so… we compute that object"), that the 3×3 is NaMaster-independent, and that the **full joint likelihood across all channels + the MASTER-decoupled ℓ=1 leg remain open** (L1227, L1430) — Grok's "computed only after reviewer request" is an accurate paraphrase of a disclosed fact, not an error. Robustness-to-support/null is handled by the declared single-primary hierarchy (L870-region) + Table 1's non-comparability note (L908) + the primary's own permutation/rank null; the paper explicitly does **not** treat the diagnostics as commensurable. The residual "demonstrate robustness to variations in support/null family" is the tracked full-joint-likelihood gate that §4.5 narrows but does not close (honestly stated). Not a genuine objection to the new method; not new.

**G-m1 [MINOR] — Literature comparisons qualitative only (no matched-footprint independent-estimator analysis); abstract/intro frame as a direct test.**
Verdict: **ALREADY-TRACKED-GATE** (matched-footprint estimator) / **SCOPE-VENUE-OPINION**. The matched-footprint independent estimator is named open at L1430; §Comparison (L1255+) is explicitly qualitative. Bounded framing preference on disclosed-open content. Not new.

**G-m2 [MINOR] — GZ1-overlap confusion contaminated; scalar transfer g=0.398 illustrative only; no spatially resolved directional confusion model.**
Verdict: **ALREADY-TRACKED-GATE** (transfer function) / **DISCLOSED-RE-FLAG**. Disclosed L1565, L1581, L1289/L1305 ("illustrative rather than an independent transfer calibration"). Identical to v1.0.261 G4/Gk4, v1.0.263 Gk4. Not new.

### Gemini leg (MINOR REVISIONS)

**Ge1 [MINOR] — "Audit"/pipeline-history/review-process language in the prose (§4.4, Table 13, and §4.5 L581-of-PDF: "a reviewer gate asked whether they instead share…").**
Verdict: **SCOPE-VENUE-OPINION** (recurring lab-notebook/audit-language) + **GENUINELY-NEW-REAL** (bounded editorial — the newly-introduced review-process narration clause in §4.5).
The recurring "conflicting committed historical records" / "corrects older audit language" flag is the standing lab-notebook-tone preference dispositioned at v1.0.261 Ge3, v1.0.262 Ge4, v1.0.263 Ge1 — SCOPE-VENUE-OPINION on accurate, load-bearing provenance prose. **However**, the specific clause Gemini cites in the **newly-added Sec 4.5** is genuine review-process narration in the manuscript body — verified verbatim at tex L1225: *"The estimators above are reported against separate, non-interchangeable nulls; **a reviewer gate asked** whether they instead share a single sampling covariance…"*. Narrating "a reviewer gate asked" inside a journal manuscript is a legitimate (if minor) editorial defect introduced by this round's new content; consistent with how prior rounds counted comparable one-sentence editorial items (v1.0.261 Ge2 gloss, v1.0.262/263 App-B pointer) as GENUINELY-NEW-REAL, it is retained as a bounded, lowest-priority editorial closure. **Minimal honest fix:** rephrase the clause to standard scientific prose, e.g. "To test whether the estimators instead share a single sampling covariance, we compute that object by evaluating the paper's committed estimators on one shared block bootstrap." Changes no science number.

**Ge2 [MINOR] — Inline SHA-256 hashes / file paths in main text hurt readability; move to footnotes / provenance table.**
Verdict: **SCOPE-VENUE-OPINION** (presentation). Recurring; the hashes/paths are accurate, load-bearing reproducibility artifacts. Identical to v1.0.261 Ge1, v1.0.262 Ge1/C9, v1.0.263 (Gemini). Not new.

**Ge3 [MINOR] — Fig 7/8 captions excessively long / contain interpretive methodology; move narrative to the body.**
Verdict: **SCOPE-VENUE-OPINION** (presentation). Legitimate but bounded caption-length style preference; the caption content is accurate. Not a correctness defect.

**Ge4 [MINOR] — Table 13 "conflicting historical training-data records": clarify the origin (prior paper / preprint / older git commit).**
Verdict: **SCOPE-VENUE-OPINION** (presentation) — same substance as the recurring provenance-context flag (v1.0.263 Ge1 family). The origin is the project's own committed pipeline records (§Data L847+, immutable rev pin); a one-line clarification is a bounded readability nicety. Not a correctness defect.

---

## Verdict tally

| Verdict | Count | Findings |
|---------|-------|----------|
| ALREADY-TRACKED-GATE | 8 | M1, M2, M6, M7, M8, M9, G-M1, G-m1 (M3-b, G-m2 dual) |
| DISCLOSED-RE-FLAG | 6 | M4, M9, M10, M12, G-M2, G-M3 (G-m2 dual) |
| SCOPE-VENUE-OPINION | 8 | M4(dual), M5, M6(dual), M11, M12(dual), Ge1(recurring), Ge2, Ge3, Ge4 |
| FALSIFIED | 0 | — |
| GENUINELY-NEW-REAL | 2 | **M3** (coverage-calibrated observed-label upper limit — science, bounded, computable), **Ge1** (review-process narration clause in new §4.5 — editorial, lowest) |

Total findings: **21** (Claude 12 + Grok 5 + Gemini 4). No finding requires any change to an existing scientific number; the narrow central null (`z_mom=+0.635`, `p=0.23768`) is unchanged and unanimously supported. No finding dismissed without a source-cited verdict; nothing fabricated.

## New Sec 4.5 / Table 9 methodological check

The computed G3 joint covariance drew **no methodological objection**. The two legs that touch §4.5 do not fault the method: Grok G-M3 restates the disclosed "computed after reviewer request" + open-full-likelihood facts (DISCLOSED-RE-FLAG), and Gemini Ge1 objects only to the "a reviewer gate asked" *phrasing* (editorial). The integrated numbers (z=−6.57 monopole, +2.21/+1.36 dipoles, ρ=−0.037/+0.277/+0.846) match the committed artifact and campaign doc exactly, and the prose honestly states §4.5 narrows but does not close the joint-covariance gate and does not identify the monopole mechanism.

---

## GENUINELY-NEW-REAL section

**Two items (one science-bounded, one editorial). Neither changes an existing number.**

1. **M3 — coverage-calibrated OBSERVED-LABEL injection-recovery upper limit on the primary real-space dipole amplitude (bounded science; computable from committed artifacts; spec, do NOT run).**
   The paper explicitly declines any calibrated coverage / amplitude upper limit (Table 1 row vii, L899; L1292; L1302; L1410). A coverage-calibrated **observed-label** limit `A_95^obs` is a real, bounded closure that runs entirely through the **exact primary estimator** (uniform-pixel `healpy.fit_dipole` real-space dipole) and the **exact fixed-occupancy per-pixel-shuffle null** — both committed — via the committed Stage-B injection harness, needing **no ViT, no transfer function, no pod** (the chain "does not traverse the ViT," L1300–1303). It is not on any tracked gate list. **Fix:** scale the existing injection-recovery on the primary channel to ≥10³ random-sky-axis injections/amplitude on a finer amplitude grid (the harmonic-completeness diagnostic already demonstrates 10³ injections/amplitude is feasible), count recovery above the primary null threshold, invert for `A_95^obs`, and report it as an explicitly **observed-label** (not physical) sensitivity floor, with a one-line abstract note that no physical parity-amplitude bound is claimed pending the transfer function. The **physical** `A_phys` upper limit stays a tracked/disclosed limitation (transfer-function gate — the scalar g=0.398 is illustrative only, L1289/L1305). CPU-only; additive; no existing number changes.

2. **Ge1 (partial) — remove review-process narration from the new §4.5 (editorial, lowest priority).**
   Rephrase tex L1225 "…a reviewer gate asked whether they instead share a single sampling covariance…" to standard scientific prose (e.g. "To test whether the estimators instead share a single sampling covariance, we compute…"). One-clause fix; changes no science.

### Convergence statement

On the exact-PDF-bound v1.0.264 board (Claude MAJOR, Grok MAJOR, Gemini MINOR — all affirming the central null), the truth audit finds **two genuinely-new-real items, both bounded and neither altering an existing number**: (1) M3, a coverage-calibrated **observed-label** dipole-amplitude upper limit that is computable now from the committed primary estimator + fixed-occupancy null + injection harness and is *not* on any tracked gate list (its **physical** counterpart correctly remains the tracked transfer-function gate); and (2) a one-clause editorial cleanup of review-process narration introduced by the new §4.5. Every other finding (16 of 18 substantive dispositions) is a source-cited re-flag of disclosed content, an already-tracked gate (G1 training provenance, G2 independent validation, G4 monopole mechanism, matched-footprint/transfer-function/joint-likelihood, DOI/archive), or a venue/presentation opinion. The v1.0.264 delta — the integrated G3 joint covariance (§4.5 + Table 9) — is numerically faithful to its committed artifact, honestly scoped as narrowing-not-closing the joint-covariance gate, and drew **no methodological objection**. Readiness cap **80 HOLDS**; no readiness uplift and no acceptance is claimed. The M3 observed-label upper-limit compute is the one text-adjacent real closure available without a pod (queue it alongside the blocked G1/G2/G4 campaign jobs); everything else remains real-compute or Houston-level.
