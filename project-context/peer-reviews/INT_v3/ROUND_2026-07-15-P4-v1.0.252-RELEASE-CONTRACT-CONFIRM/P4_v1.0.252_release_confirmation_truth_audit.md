# P4 v1.0.252 release-contract confirmation truth audit

Artifact reviewed: `pipelines/p2_chirality/chirality_catalog_paper.pdf`, 28 pages,
SHA-256 `a109f3d150ff02107bc10bc7dec576ad28b0157081b3e521da86e7c06ade3292`.
The confirmation reviewer used the source tree at review commit
`69226adbf3da02fff16f7fb65c45953c020b0966`; its three findings therefore
correctly describe the public-release state visible at that commit, before the
subsequent provider publications recorded below.

The leg ran through the authenticated Codex CLI/ChatGPT subscription route.
The authentication dry-run was observed in command output but was not retained
inside this review bundle, so it is not independently recoverable from these
files. The bundle's `manifest.jsonl` is the durable routing assertion: provider
`chatgpt-subscription-via-codex-cli`, `openai_api_used:false`, and
`anthropic_used:false`. This audit does not elevate that assertion into stronger
evidence than the retained manifest supports.

## Dispositions

### 1. Published morphology validator lacked immutable retrieval and derived-`b/a` contract

**VERIFIED REAL at review commit; now closed externally.** The reviewer correctly
distinguished the older 5,774-byte public validator from the corrected
10,733-byte repository validator. At `69226adb`, the latter had not yet been
published, so the manuscript's public executability claim was not supported by
the then-current immutable provider artifact.

The dataset publication is now evidenced by immutable Hugging Face commit
`2fc392e22b3155107f6f56a60aa8ac75f57c866b`. The byte/SHA receipt at
`pipelines/p2_chirality/outputs/canonical_provenance/p4_hf_contract_dataset_receipt_v1_0_252.json`
binds the published validator to 10,733 bytes and SHA-256
`31c663b274308f72e19bd694c7e401630cf44d7d2a1d02ac261525998d89347f` and
records that immutable downloads matched their declared byte counts and
SHA-256 digests. The published validator retrieves the two inputs at immutable
revisions, verifies them before parsing, enforces the one-to-one join, and
implements the declared deterministic derived-`b/a` rule without claiming that
`b/a` is a stored column.

### 2. Corrected dataset card was not present at the cited immutable revision

**VERIFIED REAL at review commit; now closed externally.** At `69226adb`, the
provider receipt still bound the public `README.md` to the older v1.0.251 card,
while the corrected v1.0.252 card and its immutable revision pins existed only
locally. The reviewer was therefore right that the manuscript's public-card
claim was premature.

The same immutable dataset commit
`2fc392e22b3155107f6f56a60aa8ac75f57c866b` now closes the discrepancy. The
receipt file above binds the published `README.md` to 5,954 bytes and SHA-256
`58e6bac01648537409ca2a7e73105fbe06e7188141606458be90f777648fc293`, alongside
the published manifest, schema, and validator. This is provider publication
evidence for the corrected pinned card, not merely evidence of a local edit.

### 3. Model card contained incorrect significance, suppression factor, and checkpoint loader

**VERIFIED REAL; now closed locally and externally.** The confirmation review
correctly recomputed the catalog imbalance as approximately `-9.47 sigma`,
identified that the card's `3.86x` statement did not match the manuscript's
reported `1.576% / 0.529% = 2.98x` comparison, and showed that the safetensors
example was incompatible with the audited `chirality_model_v2_best.pt`
checkpoint.

The corrected model card now states the proper statistical scope, removes the
incorrect `~1 sigma` and `3.86x` claims, downloads the pinned `.pt` checkpoint,
loads its `enc` and `head` state dictionaries with the matching architecture,
and preserves the unresolved historical-training limitations. Publication is
evidenced by immutable Hugging Face model commit
`3baeab8635a0c854166ecafaa03d83f9a94cea4b`. The byte/SHA receipt at
`pipelines/p2_chirality/outputs/canonical_provenance/p4_hf_contract_model_receipt_v1_0_252.json`
binds the published `README.md` to 8,607 bytes and SHA-256
`494e349b96be9aa7a0929d282aafcab19a36d7c9b23de2e6c490817e5e5088f9` and records
immutable-download verification.

## Conclusion

All three confirmation findings were genuine public-release defects, not
central-result failures. Each now has local closure plus immutable provider
publication evidence. The narrow central claim remains supported: the declared
high-confidence observed-label statistic is null-consistent under its stated
fixed-occupancy randomization; these release fixes neither strengthen that
claim nor close the paper's standing scientific gates.

Readiness therefore remains **80**. The next evidentiary step is an exact
v1.0.253 re-review against the newly compiled and published artifact; this
truth audit alone does not establish a clean confirmation verdict or justify a
readiness uplift.
