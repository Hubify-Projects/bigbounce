# Truth Audit — P2 v1.7.125 exact board (INT_v3, CLAUDESTACK-CONFIRM round)

- **Auditor stance:** fresh skeptical truth-audit; verdict-first; never dismiss without a source-cited citation; skeptical in BOTH directions (inflate nothing, bury nothing); when in doubt → GENUINELY-NEW-REAL; never fabricate.
- **Paper:** P2 v1.7.125, PRD Research Article. Tex: `research/focused_paper_source_integration/02_full_draft.tex` (`\preprint{v1.7.125}`, `\date{July 18, 2026, 12:00 AM PDT}`).
- **PDF sha256 (exact binding):** `45846f4ac780ac4663ef24a6e5d48c4924e4691b057a0cf7683399f48d534490` — carried identically by all three leg headers **and** both `manifest.jsonl` rows (`pdf_sha256`). Claude header carries `version: v1.7.125`; Grok/Gemini headers/manifest carry the same 45846f4a bytes. All three legs reviewed the identical v1.7.125 PDF. Review commit `a4c5dbaff377…`.
- **Board under audit:**
  - `API_P2_claude.md` (claude-opus-4-8) — **MAJOR REVISIONS**, 4 MAJOR (M1–M4) + 4 MINOR (m5–m8).
  - `API_P2_grok.md` (grok-4.3) — **MAJOR REVISIONS**, 2 MAJOR + 2 MINOR. Closing sentence (3) AFFIRMS the core: "The central algebraic claim of an exact −35/16 amplitude from the four-vertex sum is supported by the internal consistency checks shown." Grok's MAJOR↔MINOR oscillation on unchanged/adjacent P2 content is the documented pattern-066 band (M20/M23/M28/M31/M34/M37/M41/M43; DP2-24).
  - `API_P2_gemini.md` (gemini-3.1-pro-preview) — **MINOR REVISIONS**, 3 MINOR (0 MAJOR). Closing AFFIRMS: "The central claim resolving the historical factor-of-two discrepancy … is thoroughly derived and algebraically supported."
- **All three legs independently affirm the load-bearing core** `(3,1,−9,5,−33,9)` four-vertex sum → `f_NL^local = −35/16` (equilateral `−255/128`, folded `−9/8`). Zero math/arithmetic defect flagged anywhere.

---

## What changed v1.7.124 → v1.7.125 (and why it matters to this board)

v1.7.125 is **not** a review-driven presentation bump — it is a **directive-L OPEN-COMPUTE closure of the standing DP2-13 gate** (assumption (d), cubic transmission). The assumption-(d) paragraph (§`sec:assumptions`, L1143) was **upgraded from an order-of-magnitude residual to a COMPUTED, scheme-specific dressed-metric result**:
- dressed-metric prescription (`z̃∝a`, bounded `a''/a`, `c_s=1`; AAN quantum-mass term explicitly **not** included, disclosed): Weinberg conserved mode is transparent, `T_c(k)=1` identically (measured `|T_c−1| = 2.7×10⁻¹⁰ … 2.2×10⁻⁵` over `kη_B=0.002–0.03`), giving `|δf_NL| ≤ 6.8×10⁻⁸` — **>4 orders below the earlier ~10⁻³ OOM residual**;
- scheme-specificity **demonstrated, not asserted**: the effective-fluid basis integral `K=∫z²dη` diverges as `d_cut^{−1/2}` at the `H=0` pole (fitted `−0.4998` vs analytic `−1/2`) → no finite coefficient there;
- disclosed-open **kept honest**: exact AAN `U(η)` for quasi-dust not implemented (parity argument given), deformed-algebra scheme, and "a fully nonlinear third-order branch calculation remains open" (L1143). Numbers verbatim from committed `research/cubic_bounce_transmission/g1_dressedmetric_ic_close.{py,json}` (present on disk, verified).

Net effect on this board: the exact surface Claude M4 and Grok M2 re-flag (assumption (d) "only linear order, third-order open") was **just strengthened** in the reviewed PDF, with the residual it names explicitly foregrounded. This is an honest science closure of a standing open gate, not fabrication to make a finding disappear. `−35/16` untouched.

---

## Counts

| Verdict | Count | Findings |
|---|---|---|
| **GENUINELY-NEW-REAL** | **0** | — |
| DISCLOSED-RE-FLAG | 11 | Claude M1, M2, M4, m6, m8; Grok M1, m1; Gemini m1, m2, m3; (Claude m5 premise falsified) |
| SCOPE-VENUE-OPINION | 4 | Claude M3, m7; Grok M2, m2 |
| FALSIFIED (premise) | 2 | Claude M1 ("mutually incompatible"), Claude m5 ("bound" not relabeled) |
| ALREADY-TRACKED-GATE | (overlaps) | DP2-01/-02/-03/-04/-13/-16/-17/-22/-25/-26/-27/-29/-30/-34 |

**Net: 0 genuinely-new real findings.** Every line-item is a source-cited re-flag of already-disclosed/closed content, an honestly-disclosed open gate, a venue/presentation opinion, or a premise the reviewed v1.7.125 text refutes.

---

## THE FOUR CLAUDE MAJORS — adjudicated

### M1 — "factor-of-2 vs additive-term mutually incompatible and unreconciled" → **DISCLOSED-RE-FLAG (DP2-01/-02/-03/-25); the "mutually incompatible" PREMISE is FALSIFIED**

**M1's citation is accurate as to what the paper says.** Table I caption (L1131) does state the corrected benchmarks are "exactly one-half the amplitudes published in Cai et al. (`−35/8`, `−255/64`, `−9/4`)," and separately notes Cai's "transcribed printed polynomial carries a `−(99/128)Σk_i³` discrepancy relative to the exact vertex sum." So M1 quotes the paper correctly.

**But M1's central inference — that these are two mutually-incompatible mechanisms the paper juxtaposes to explain the SAME discrepancy — is a category error, and the arithmetic proves it.** There are **three distinct objects** in the squeezed limit, not two:

| Object | Squeezed value (÷64) |
|---|---|
| exact vertex sum (certified) | `−35/16 = −140/64` |
| Cai's transcribed **printed polynomial** `A_T` | `−305/64` |
| Cai's **separately-stated** literature value | `−35/8 = −280/64` |

Verified arithmetic (`fractions`, exact):
- additive term contributes `(10/3)·(−99/128) = −165/64` to `f_NL`, constant across configurations (M1's own arithmetic here is correct);
- `−35/16 + (−165/64) = −305/64` ✓ — the additive term bridges **vertex sum ↔ PRINTED polynomial**;
- `−35/16 × 2 = −35/8` ✓ — the factor-of-2 bridges **vertex sum ↔ Cai's STATED value**;
- `−305/64 ≠ −35/8` (`−305/64 = −4.766` vs `−280/64 = −4.375`) — the two relationships terminate on **different objects**.

The additive `−(99/128)Σk_i³` term therefore was **never** claimed by the paper to produce the factor of 2 — it produces `−305/64`, a value that is neither `−35/16` nor `−35/8`. The paper states this explicitly and repeatedly: L1314 ("The transcribed final polynomial does not reproduce either the vertex sum or Cai et al.'s separately stated `−35/8`"); L1328 (the printed polynomial "reduces to `−305/64` … consistent with the `−(99/128)Σk_i³` term shifting the correct `−35/16` by `−(10/3)(99/128)≈−2.58` to `−305/64`" and this term is "**one identified discrepancy** … **not** by itself the full mechanism of Cai's separately-published `−35/8`"); L1390 ("not to Cai's stated `−35/8`; not a naive additive shift"). The requested-index arithmetic in the task (`−305/64 + 165/64 = −35/16`) confirms exactly this bridge.

**M1's proposed alternative** — "the uniform 2× is the signature of a normalization / f_NL-definition convention difference, so maybe `−35/8` is right" — is **explicitly considered and rejected in-text with a source-cited reason** at L1392 ("The discrepancy is not a local-template normalization change: Cai et al. and Li et al. use the **same** `f_NL=10A/(3Σk_i³)` convention and the same squeezed limit") and L1328 (the one convention that WOULD move a factor of two — treating the `(5,2,2)` orbit as three distinct monomials — "drops a factor of two from only **one** orbit and breaks the equal forms of Cai's vertex expressions," i.e. shape-selective, not the uniform effect M1 posits).

So: M1's **"unreconciled"** observation is **TRUE and honestly disclosed** — it is the standing open gate **DP2-25** (the full mechanism of Cai's stated `−35/8` is unreconstructed; retained only as an erroneous literature value, L1314/1316/1328/1390). M1's **"mutually incompatible"** framing is **FALSIFIED**: the paper does not offer the additive term as a rival explanation of the factor of 2; it offers it as the explanation of the *printed-polynomial* discrepancy (`−305/64`), and it explicitly labels the factor-of-2-to-stated-value gap as unclosed. Maps to standing DP2-01/-02/-03 + DP2-25. This is a **sharper arithmetic re-framing of v1.7.124-M1/DP2-25 — not a new defect.** The paper's disclosure structure fully pre-empts it. **Not genuinely-new.**

### M2 — "independence overstated (four ways = one computation); Li reduction asserted not shown" → **DISCLOSED-RE-FLAG (DP2-02/-16/-17)**

M2's structural point is **correct and the paper agrees with it.** That the vertex-sum polynomial (Eq. `collapsed_vertexsum` L1370), Cai's ε-order intermediates (Eq. `order_grouped` L1382), and the per-vertex Table V column sums (L1354) are the *same* computation re-grouped on Cai's transcribed inputs is stated by the paper itself: L1390 "the correction is **internal to Cai's own paper**"; L1316 explicitly **bounds the provenance** ("Cai et al.'s four source-level vertex expressions … are **trusted inputs**"). The paper does not overstate — it labels the single independent anchor (Li) as the independent one.

The **"Li reduction asserted not shown"** sub-claim is **FALSIFIED at arithmetic level**: Li Eq.(5.1) `f_NL^local = −165/16 + 65/(8c_s²)` → at `c_s=1`: `−165/16 + 65/8 = −165/16 + 130/16 = −35/16` (verified exactly; displayed at L1323, L1328, L1390). The `cai_vertices.py` / `cai_conv.py` / `p2_vertex_check.py` certification scripts are **committed** (`research/focused_paper_source_integration/scripts/` and `.../scripts/caili_certification/`, confirmed on disk). M2's narrower demand — that Li's *general-c_s construction* "reduces exactly to Cai's standard matter bounce at c_s=1 (same operator content, same background)" — is a claim the paper **does not make**: it uses Li's **closed-form formula** at `c_s=1` "by a wholly separate in-in method" as a numeric cross-check, and honestly discloses the trusted-input conditionality (L1316, "deliberately bounded"). This is exactly the standing DP2-16 (App is not a self-contained from-Lagrangian in-in re-derivation, RE-FLAG-DISCLOSED) + DP2-02/-17 surface — same as v1.7.124-M2/M3, Grok/ChatGPT across M10–M43. The one genuinely-open residual (a per-vertex print loop + an independent from-Lagrangian check) is the disclosed non-blocking **DP2-27/-29**. **Not genuinely-new.**

### M3 — "observational half yields no defensible number (0.42σ→3.5σ ladder)" → **SCOPE-VENUE-OPINION (DP2-04/-22/-26/-29/-30)**

The σ-ladder is real content but **explicitly framed as illustrative-conditional, not a headline**: the **title itself** carries "…Four-Vertex Derivation and **Conditional** Large-Scale-Structure Mapping"; §III/IV label the ladder illustrative conditional diagnostics tied to nuisance treatment (Table III). The wide spread is the *point* of a disclosed nuisance ladder, not a hidden inconsistency; the unavailability of the true per-triangle SPHEREx `Cov_B` is the tracked external-data gate **DP2-26** (Heinrich covariance not publicly released; `INT_v3/DATA_UNLOCK_2026-07-05.md`), and an independent multi-tracer forecast is the venue barrier **DP2-29**. "Trim to a single bounded statement" is a **venue/editorial judgment (DP2-30)**, structurally pre-empted by the Conditional title + recast framing. Identical in class to v1.7.124-M4 and Grok-m2. Prior-audit consensus: venue opinion. **Not genuinely-new.**

### M4 — "load-bearing assumption (d): linear-only, single scheme, third-order open → mapping rests on an unproven scheme-dependent assumption" → **DISCLOSED-RE-FLAG (DP2-13); the reviewed v1.7.125 STRENGTHENED this surface**

M4 accurately describes the disclosed state — and it re-flags **exactly what v1.7.125 just upgraded and continues to foreground.** L1143 now (a) computes the dressed-metric transmission (`|δf_NL| ≤ 6.8×10⁻⁸`, >4 orders below the prior OOM), (b) **demonstrates** scheme-specificity (effective-fluid basis integral diverges → no finite coefficient), and (c) discloses verbatim that "the deformed-algebra scheme carries a different subleading structure, and a fully nonlinear third-order branch calculation remains open … Every late-time sensitivity in this paper accordingly remains conditional on faithful cubic transmission." M4's own request — "foreground this in the abstract/intro" — is met by the title's "Conditional" and the L1137 cubic-transfer-gate paragraph ("the algebraic coefficient and the cubic-transfer gate are separate claims: the first is certified here, while the second remains a completion-dependent condition"). This is the load-bearing caveat (★) DP2-13, honestly disclosed and now partially closed. **Not genuinely-new** — the paper concedes precisely the limitation M4 names, and the reviewed version reduced it.

---

## THE FOUR CLAUDE MINORS

- **m5 (torsion Eq. 5 "bound" is a heuristic estimate — relabel as estimate)** → **FALSIFIED / STALE.** Already reworded in the reviewed text: L1141 "assumption (f) … now bounded explicitly"; L1147 "an **asserted energy-density transfer proxy** … **not a derived in-in propagation**" and "supported by an explicit **order-of-magnitude parametric estimate**." This was the v1.7.123 GNR-1 closure, verified present. The word M4 wants ("estimate") is already the paper's word. Not new.
- **m6 (abstract "local-type f_NL=−35/16" vs config-dependent full shape — note it's the squeezed limit)** → **DISCLOSED-RE-FLAG.** Config-dependence is in Table I (equilateral `−255/128`, folded `−9/8`, L1125–1127) and §III; standard `f_NL^local` convention. Optional one-clause sharpening (DP2-30). Not new.
- **m7 (three numerical objects −35/16 / −305/64 / −35/8 coexist — add a reconciliation table)** → **SCOPE-VENUE-OPINION (DP2-30).** All three values + both discrepancies are already displayed across App B (L1314/1328/1390); the paper openly declines to reconstruct `−35/8` (DP2-25). Pure organization suggestion; identical to v1.7.124-m8. Not new.
- **m8 (certification entirely symbolic on transcribed inputs; no independent from-Lagrangian re-derivation)** → **DISCLOSED-RE-FLAG (DP2-16/-27/-29).** This is the trusted-input provenance bound the paper itself states (L1316). An independent from-Lagrangian check is the disclosed open item DP2-29; the per-vertex print loop is DP2-27. Honestly-disclosed conditionality, not a defect. Not new.

---

## GROK v1.7.125 board (MAJOR REVISIONS — pattern-066 band; core AFFIRMED)

- **Grok M1** (Sec II.A/App B: exact four-vertex sum presented as a correction but supported only by "internal script references and order-grouped intermediates," not a "fully self-contained, referee-reproducible symbolic expansion … verified without external artifacts") → **DISCLOSED-RE-FLAG (DP2-16/-27).** The reduction **is** displayed hand-checkably in App B: `eq:collapsed_vertexsum` (degree-9 polynomial, L1370), `eq:order_grouped` (`−5/2 + 5/16 + 0 = −35/16`, L1382), `tab:vertices` (four source vertices, L1331), `tab:vertexwalk` (per-vertex limits whose column sums = `−35/16` / `−255/128`, L1354). The "without external artifacts" residual is the disclosed non-blocking DP2-27 (per-vertex print loop). Same surface as Claude M2. Not new.
- **Grok M2** (Sec IV/VII: SPHEREx `2.63σ`, `3.5σ–0.4σ` ladder given as explicit numbers from an in-house surrogate covariance while conditional on unverified cubic transmission / missing per-triangle Cov_B / free nuisances; PRD theory-vs-forecast separation) → **DISCLOSED-RE-FLAG (DP2-04/-13/-22/-26/-34).** The values are labeled illustrative conditional diagnostics; surrogate non-substitutability and the Cov_B gate (DP2-26) are disclosed. Same as Claude M3/M4. Not new.
- **Grok m1** (assumptions (d)–(f) load-bearing yet scheme-specific/parametric — elevate to a dedicated subsection with explicit model-dependence caveats) → **DISCLOSED-RE-FLAG (DP2-13).** Already a dedicated §Assumptions (L1139) with (d) flagged "the load-bearing limitation" (L1143) and (f) bounded (L1147). Not new.
- **Grok m2** (private GitHub paths / .py/.json / research/ dirs inappropriate for a journal; numerical claims must be reproducible from published text + supplement alone) → **SCOPE-VENUE-OPINION (DP2-30/-27).** Presentation hygiene; identical to Gemini m2. Not new.

## GEMINI v1.7.125 board (MINOR REVISIONS — core AFFIRMED)

- **Gemini m1** (Sec IV/Table III: channel-native surrogate covariance introduced with little math description — give a brief mathematical definition of the surrogate Fisher matrix elements) → **DISCLOSED-RE-FLAG (DP2-22/-26/-34).** The surrogate is disclosed as a heavily-caveated illustrative diagnostic that "does not replace" the real covariance; a fuller in-text Fisher-element definition is optional polish (DP2-30). Adjacent to the real disclosed external gate DP2-26. Not new.
- **Gemini m2** (raw repo filenames embedded in prose — move to footnotes / Data & Code Availability) → **SCOPE-VENUE-OPINION (DP2-30/-27).** Same class as Grok m2. Not new.
- **Gemini m3** (Sec II.C: torsion spin-coherent proxy `⟨J_5²⟩≲n_ψ²` — comment on physical circumstances of maximal alignment) → **DISCLOSED-RE-FLAG (DP2-13-adjacent).** The proxy is disclosed at L1147 ("the **assumed** spin-coherent proxy `⟨J_5²⟩≲n_ψ²`; thermal or incoherent spin distributions only reduce this"). Optional one-clause physical-context addition; the direction of the inequality is already stated conservative. Not new.

---

## FALSIFIED

- **Claude M1 premise ("factor-of-2 vs additive-term mutually incompatible")** — the additive `−(99/128)Σk_i³` term bridges vertex-sum ↔ **printed polynomial** (`−35/16 + (−165/64) = −305/64`, verified), while the factor-of-2 bridges vertex-sum ↔ Cai's **stated** `−35/8` (`−35/16 × 2 = −35/8`); `−305/64 ≠ −35/8`. Two relationships on different objects, not two rival explanations of one. The paper never uses the additive term to explain the factor of 2 (L1314/1328/1390) and explicitly rejects the convention-difference alternative (L1328/1392). The "unreconciled" half is the disclosed DP2-25 gate. Recorded as DISCLOSED-RE-FLAG with premise falsified.
- **Claude m5 ("bound" not relabeled as estimate)** — false for the reviewed v1.7.125: L1147 already reads "an asserted energy-density transfer proxy … not a derived in-in propagation" and "order-of-magnitude parametric estimate" (v1.7.123 GNR-1 closure).

No board finding is a true, un-disclosed defect.

---

## One-paragraph state assessment (incl. directive-H-refined convergence)

v1.7.125's exact board is a **variance board, not a signal board**: all three legs re-affirm the load-bearing core `(3,1,−9,5,−33,9) → f_NL^local = −35/16` (Grok and Gemini both credit it verbatim in their closings; Gemini returns MINOR REVISIONS with zero majors). The one substantive change in this version is an **honest open-compute closure** — the directive-L G1 dressed-metric cubic-transmission computation that upgraded the DP2-13 assumption-(d) caveat from a `~10⁻³` OOM residual to a computed `|δf_NL| ≤ 6.8×10⁻⁸` with scheme-specificity *demonstrated* and the third-order branch *explicitly kept open* (L1143, numbers from committed `g1_dressedmetric_ic_close.{py,json}`) — so the exact surface Claude M4 and Grok M2 re-flag was **strengthened, not weakened**, in the reviewed PDF, with `−35/16` untouched. The audit finds **0 genuinely-new real findings**. The sharpest item, **Claude M1**, is a crisper arithmetic re-framing of the standing Cai/Li surface whose "mutually incompatible" premise is **falsified** by the paper's own three-object disclosure (vertex sum `−35/16` ↔ additive term ↔ printed polynomial `−305/64`; a *separate* factor-of-2 ↔ Cai's stated `−35/8`, honestly left unreconstructed at DP2-25) — the requested check `−305/64 + 165/64 = −35/16` confirms the additive term bridges vertex↔printed, never vertex↔stated, and the convention-difference alternative M1 raises is rejected in-text at L1328/L1392. **Claude M2** (independence overstated / Li "asserted not shown") is the standing DP2-16/-17 trusted-inputs surface — Li Eq.(5.1) at `c_s=1` = `−35/16` is displayed and verified, the certification scripts are committed, and the paper openly bounds its provenance. **Claude M3 / Grok M2** are the conditional-forecast venue opinion pre-empted by the title's own "Conditional" qualifier (DP2-04/-26/-29/-30). **Claude M4 / Grok m1** are the disclosed load-bearing assumption (d) (DP2-13, just partially closed). **Claude m5** is falsified (already reworded to "estimate/proxy"); m6/m7/m8 and every Grok/Gemini minor fingerprint-match a standing DP2 disposition or an explicit in-paper paragraph. **Directive-H-refined convergence (the honesty gate, "0 genuinely-new real findings across all reviewers on truth-audit") HOLDS for P2 at v1.7.125** — every MAJOR/minor is a source-cited re-flag, an honestly-disclosed open gate, a venue opinion, or a falsified premise, with the core untouched. **The literal all-ACCEPT verdict grid (directive-M terminal criterion) does NOT hold** (Grok MAJOR + Claude MAJOR + Gemini MINOR — the documented harsh-referee floor, DP2-24), so the drive-to-100 loop continues; the honest remaining levers are the external/Houston-gated gates DP2-25 (trace Cai's `−35/8` from his original derivation source), DP2-26/-29 (obtain the Heinrich per-triangle `Cov_B` / build an independent multi-tracer forecast), and DP2-30/-27 (presentation-streamlining, repo-path relocation, real Zenodo DOI at camera-ready). **Recommendation: no v1.7.126 content bump is warranted on review grounds** (0 genuinely-new real findings; every number verified; `−35/16` triple-affirmed and quadruple-certified) — v1.7.125 stands; record this as a clean re-flag wave. No faked accept, no un-sourced dismissal, no fabrication.
