# P5 v0.1.138 residual-confirmation truth audit

Exact PDF: SHA-256 `3c47ccf75da20653c463557fc54fff50da01e1e6bde43a225f61c46cd50baaf0`, 41 pages.
Review commit: `68d7e2541aa48de03c15133f7aa9f3b8e1cce5ac`.

## Board

- Grok 4.3: MINOR REVISIONS
- Gemini 3.1 Pro: MINOR REVISIONS
- Codex subscription, GPT-5.6-sol/high: MINOR REVISIONS

The board is unanimously minor-only and all three reviewers support the
catalog-specific non-detection. Readiness still depends on finding-level closure
and the standing release/human gates.

## Verified findings

1. **Systematics prose denied an interaction calculation that now exists —
   VERIFIED MINOR.** A43--A44 do calculate program-by-void interactions. The
   supported conclusion is narrower: no robust interaction evidence, with weak
   bounds from sparse DARK/other-program strata. Corrected in the next source
   candidate.
2. **Same-sample monopole residuals omitted shared-reference covariance —
   VERIFIED MINOR.** Treating the pooled P5 fraction as fixed gives the printed
   approximate values; pooled class-versus-rest recomputation gives about
   `+1.40` for filaments and `-1.56` for clusters. The omnibus conclusion is
   unchanged. Corrected and explicitly qualified in the next source candidate.
3. **T-Web void-count explanation confused the full parent with the low-z
   subset — VERIFIED MINOR.** The full T-Web void parent is 428 objects; the
   `z<=0.24` cut leaves six. The depth cut therefore cannot explain the quoted
   428 count. Corrected in the next source candidate.
4. **Seed-only determinism claim omitted stable tie-breaking — VERIFIED
   MINOR.** The retained tie-breaking audit shows an unstable sort changes 22
   rows and moves the focal contrast by `3.09e-5`. The next source candidate
   restricts byte-identical determinism to frozen artifacts and requires the
   committed stable ordering for reconstruction.

## Standing or editorial findings

- The post-review hierarchy change, fixed-geometry RSD sensitivity limitation,
  public artifact archive, Paper IV availability, and immutable DOI/tag are
  already disclosed standing submission gates.
- Inline artifact IDs, T-Web section length, and the defensive unit-conversion
  footnote are editorial streamlining requests, not new scientific defects.

## Readiness consequence

P5 remains **74** until the four verified minor fixes are recompiled and
confirmed and the public-release, companion-paper, narrative, and human AJ gates
are closed. The unanimous minor-only board is positive evidence, but it does not
by itself erase those gates or prove acceptance.
