# P3 v3.2.0-r6 exact-PDF ApJS panel — normalized truth audit

## Immutable review target

- P3 closure commit: `064b06bdbc2a5844837dbd92d5fafbc58c44328b`
- Full review-packet commit: `c6277107cb8d705d6b2c1b675269f507fa54ab9a`
- PDF: `pipelines/p3_anomaly_engine/paper3_apjs.pdf`
- PDF SHA-256: `a16c217930a31ba799b68a88b2477b020ad5e309ff79626e49b2b69a715fdd5a`
- Version/page count: `v3.2.0-r6`, 15 pages
- Registry profile: `APJS-CATALOG`; venue: *The Astrophysical Journal Supplement Series*
- Dispatch: native-PDF, three non-Anthropic vendors, all first attempt

| Vendor/model | Raw verdict | Hidden issue tags | Latency | Packet key |
|---|---|---:|---:|---|
| OpenAI `gpt-5.5` | **MAJOR REVISIONS** | 5 MAJOR + 6 MINOR | 59.7 s | `3ab042051bdcc7ded3cad1a683f6247eec61f0fa845ada819fd871562dffbebf` |
| Gemini `gemini-3.1-pro-preview` | **MINOR REVISIONS** | 3 MINOR | 21.2 s | `b0bf5397610722069f378000cc6b819d3f25ead664838b34fcf5356537a38d84` |
| xAI `grok-4.3` | **ACCEPT** | 4 MINOR | 19.7 s | `b9783eca34f6b19c4f137670c9b982db98b2070f259badd42e5f12e42694e978` |

All three reviewers say the central computational claim is supported: the frozen historical-coordinate list can be deterministically rejoined to public DESI DR1 `TARGETID` rows under the declared positional and quality rules. OpenAI correctly bounds this to a positional rejoin, not secure physical anomaly identification or uniformly secure identity for all 181 rows.

The raw panel is **not ACCEPT/minor-only evidence** because OpenAI remains MAJOR. Truth adjudication confirms two acceptance-relevant contract/workflow issues (170-core versus 11-tail semantics and one definitive submission bundle) plus a bounded coordinate-lineage issue. It does not identify a wrong row count, failed exact rejoin, fabricated physical result, or corrupted release table.

## Direct evidence for the four r6 controls

1. **Chance/local-shift control — implemented and numerically verified.** The committed script evaluates the observed cluster positions plus 16 fixed great-circle shifts (60/120 arcsec, eight position angles), streams all 28,425,963 FITS rows, and separately evaluates the 20,299,155-row science-bit parent and warning-free global-primary target population. At one arcsecond the parent is 2,468 observed versus `86.6875 +/- 14.4209` shifted (66--109), and strict is 181 versus `76.1875 +/- 13.3027` (61--103). At 0.1 arcsec strict is 170 versus 0.625 shifted. Evidence: manuscript lines 319--349 and `p3_apjs_r6_science_controls.json`.

2. **Warning-population comparison — implemented against exact products.** The script reads the frozen 181-row primary Parquet (SHA-256 `25f06752...ce988b03`) and 2,267-row warned auxiliary (SHA-256 `e370f0ae...ea1e57c`) and reports exact medians/quartiles for `z`, `original_score`, separation, and `deltachi2`, plus Redrock class counts. Accepted/warned medians are `z=1.108423/0.886051`, `original_score=5.324423/5.841820`, separation `0.001269/0.001350` arcsec, and `DELTACHI2=61.063386/4.919479`. The prior r5 truth-audit value `6.211950` for warned “historical score” is not reproduced by either `original_score` or `cluster_best_score`; the committed exact-product value is 5.841820. The paper correctly uses `original_score` and labels the comparison descriptive, not a bias correction.

3. **170-core/11-tail control — implemented, with a residual catalog-contract gate.** The release already has a two-valued `match_quality_tier`; the paper states that users can reproduce the 170-row high-coordinate-consistency subset (lines 275--284), lists all 11 tail rows, reports 11 observed versus `75.5625 +/- 13.0127` shifted in the 0.1--1 arcsec annulus, and explicitly says the tail is not secure candidate-level identity (lines 329--339). This closes the missing-control defect. OpenAI's remaining concern is **partially confirmed and acceptance-relevant**: the title/headline and primary-release contract still group all 181 under “Public-ID Recovery.” Closure should preserve the 181-row evidence but either (a) make 170 the high-confidence recovery tier and 11 an explicitly lower-confidence positional-association tier in the submission contract, or (b) use association-only language consistently. No tail row should be silently deleted or declared physically false.

4. **Original-member sensitivity — closed.** The deterministic expression `original_member_separation_arcsec <= 1.0` is published for all 181 rows. It retains 180 and removes only `P3-DESI-000030`, whose target-to-original-member separation is `1.979009213974` arcsec; the target-to-cluster separation remains `0.979656` arcsec. Evidence: manuscript lines 153--163, sensitivity CSV, and JSON. OpenAI's earlier r5 request for the counterfactual is satisfied.

## Finding-level adjudication

### OpenAI (`MAJOR REVISIONS`)

1. **181 headline versus 170 core/11 tail — PARTIALLY CONFIRMED, MAJOR CONTRACT/PRESENTATION.** The paper already publishes the tier and explicitly denies secure tail identity, so the claimed absence of tiering is stale. The remaining title/primary-contract ambiguity is real and should be closed as described above.
2. **Chance control not integrated / 76 expected backgrounds — PARTIALLY CONFIRMED, MINOR WORDING AFTER CONTROL.** Exact tier curves and metadata already exist, and the paper explicitly refuses a background-subtracted purity. The 76.2 shifted mean is a null association count over all shifted cluster positions, not a justified estimate that exactly 76 of the observed 181 are contaminants; the shifts are correlated and the paper says so. Add one plain sentence that the full one-arcsecond list is not purity controlled, but do not manufacture a corrected purity.
3. **Release versioning / definitive frozen bundle — CONFIRMED, MAJOR WORKFLOW GATE.** The exact components are individually frozen and auditable, but the submitted record spans primary r2, viewer r3, AAS table r4, warned auxiliary r5, and manuscript/control r6. Before submission, produce one definitive manifest-led submission bundle that binds the primary and optional auxiliary assets, scripts, dictionaries, exact checksums, and their relationship to the machine-readable AAS table. The AAS DOI remains honestly unassigned.
4. **Historical scores insufficiently labeled — RE-FLAG / ALREADY CLOSED.** The title says historical list; the abstract and Sections 2.1, 5.1, 7, dictionary, and JSON repeatedly call `original_score` frozen/legacy, uncalibrated ranking metadata with unavailable preprocessing and no physical significance. Do not add claims about physical features.
5. **`TARGET_RA/TARGET_DEC` coordinate semantics — CONFIRMED, BOUNDED SCIENCE-PROVENANCE GATE.** The source identifies the public fields and states that historical positions are frozen ICRS cluster means (lines 138--140, 167--180), but it does not establish whether the historical DESI member coordinates originated as target, fiber, coadd, or another spectral-product coordinate, nor why `TARGET_RA/TARGET_DEC` is the semantically matching public coordinate. Recover immutable upstream lineage if available; otherwise state the lineage gap explicitly and bound the result to coordinate association.
6. **Define warning-free — FALSE/ALREADY CLOSED.** The abstract defines it by “requiring `ZWARN=0`” before the headline 181 count; Sections 3.3 and 4.1 repeat the exact public Redrock-row predicate.
7. **Bit typography parsed as 21/22/211 — PDF-EXTRACTION ARTIFACT.** The exact rendered PDF visually shows superscript powers and the prose independently says bits 1, 2, and 11. No numerical ambiguity exists in the executable mask.
8. **Effective versus deterministic duplicate rules — ALREADY CLOSED.** Section 3.3 says no final tie-break was needed, all keys were already unique, and the unsafe-order counterfactual loses zero rows.
9. **Viewer audit evidence hierarchy — ALREADY BOUNDED.** Section 6.2 states that viewer capture proves retrieval/metadata only, not visual classification, novelty, anomaly origin, or physical identity. FITS-row exact rejoin is separately described as authoritative.
10. **Positive-redshift Boolean — OPTIONAL MINOR.** The paper gives the deterministic `z>0` consumer rule and exact resulting 179 rows. A sidecar Boolean would be convenience, not a correctness closure.
11. **Final DESI references/acknowledgments — CONFIRMED SUBMISSION CHECK.** Re-run the current required DESI/AAS wording check at final submission; no present scientific result depends on it.

### Gemini (`MINOR REVISIONS`)

1. **Name the historical dataset earlier — CONFIRMED PRESENTATION MINOR.** Ref. 5 and Section 2.1 identify it, but spelling out “BigBounce Multi-Survey Autoencoder Anomaly Catalog” in the introduction would help readers.
2. **Hardware context for 97.1 s — OPTIONAL, NOT REPRODUCIBILITY-CRITICAL.** The manuscript explicitly labels runtime non-portable and records software/chunk inputs. Hardware can be added if preserved, but must not be guessed.
3. **Interpret negative redshifts — RE-FLAG / SHOULD NOT INFER.** The paper intentionally retains exact Redrock metadata and explicitly declines to call the values template artifacts without physical analysis. Keep that boundary unless a source-backed DESI interpretation is added.

### Grok (`ACCEPT`, four hidden MINOR tags)

1. **SPECTYPE descriptive status — ALREADY CLOSED.** The abstract says spectral type was not a selection cut; the composition section calls it descriptive metadata.
2. **Residual/ZWARN correlation — OPTIONAL FUTURE ANALYSIS.** The exact warned comparison includes historical score and fit metadata. Residual-specific correlation could be useful but is not required to support positional recovery and would not repair the unavailable upstream sensitivity function.
3. **Restate example-row selection in table caption — CONFIRMED PRESENTATION MINOR.** Section 4.4 gives the deterministic rule; duplicating it compactly in the caption would improve extraction.
4. **Shell backslashes will not paste — FALSE.** The verbatim block uses standard POSIX line continuations. The split SHA assignment deliberately uses a trailing backslash to concatenate the literal and is explained immediately below the command.

## Normalized verdict and closure gates

**Normalized verdict: MAJOR REVISIONS, bounded to catalog semantics/provenance and submission packaging.** The core computation and all four requested r6 controls are supported. The round cannot honestly be called ACCEPT/minor-only while one vendor remains MAJOR and the following gates remain:

1. Resolve the 170-core/11-tail contract without deleting or upgrading the tail evidence.
2. Produce one definitive frozen, checksum-bound ApJS submission bundle; keep the journal DOI explicitly pending until assigned.
3. Recover or explicitly bound the historical-to-public coordinate semantic lineage and justify `TARGET_RA/TARGET_DEC`.
4. Apply the small confirmed presentation items, then rerun exact-PDF non-Anthropic confirmation.

No SSOT, tracker, site, Convex, source/PDF, or readiness state is changed by this audit.
