# P3 v3.2.0-r3 exact-PDF audit

## Frozen proof

- Manuscript commit: `82fa4d26`
- PDF SHA-256: `9ed38c3c54fce23bafbcd9cbfcf783d535cf27a92fe06da8df16618e5513e929`
- Source SHA-256: `7ca653eb8c4be6d038136c5a06bd07eca987a98bc124811002f0b2bbc54bfe17`
- Pages: 12, US Letter
- PDF bytes: 197,069
- Data release audited: `p3-v3.2.0-r2`, immutable Hugging Face commit
  `1a9e85ee004894956665444b4f110111f1090b79`

The proof copies in `proof/` are byte-identical to the committed manuscript source and PDF.

## Compile and structural checks

- Built in an isolated temporary directory with the official AASTeX 7.0.1 class downloaded from
  the AAS Journals distribution.
- Tectonic completed after its normal reference rerun.
- Log scan: no TeX error, undefined control sequence, undefined reference/citation, overfull box,
  or underfull box. The only class warning says the requested `aastex701` class identifies itself
  internally as `aastex7`; this is the official distributed class.
- `pdffonts`: every listed font is embedded. The Type 3 DejaVu subsets originate in the supplied
  PDF figures and are embedded; there is no missing external font.
- `pdftotext`: no unresolved-reference marker or local `/Users/...` path.

## Visual overflow audit

All 12 pages were rendered at 144 dpi and inspected in the contact sheet. Pages 6, 8, 11, and 12
were also inspected at full rendered resolution because they contain the representative table,
wide executable command, long URLs, column dictionaries, and final audit matrix. No text crosses
a page or column boundary, no table overlaps adjacent content, and no URL or raw path escapes its
intended region. The wide command on page 8 remains inside the full-width `widetext` block.

## Link audit

- DESI DR1 zcatalog directory: HTTP 200.
- Hugging Face dataset and both pinned historical Parquets: HTTP 200 after redirect.
- AAS data guide and digital-asset DOI policy: HTTP 200.
- DESI institutions page: HTTP 200.
- DESI DR1 DOI: resolves to IOP, HTTP 200.
- Nicolaou MNRAS DOI: resolves correctly to OUP; OUP rejects the automated GET with HTTP 403,
  while the Crossref work record returns HTTP 200. This is bot policy, not a broken DOI.
- Repository artifact links name files committed in `d2b698d1`; they become public when the
  orchestrator pushes the repository. No push was performed in this lane.

## r3 scientific-integrity closure checks

- Order/counterfactual audit: PASS; 2,468 / 2,448 / 181, exact released identity set, zero rows
  lost by the deliberately unsafe counterfactual for these data.
- Positional ambiguity audit: PASS; all 20,299,155 eligible rows scanned; no competitor within
  1 arcsec; one candidate has one competing eligible row within 2 arcsec (at 1.0498 arcsec).
- Public viewer audit: PASS for 20/20 unique targets, comprising all 11 separation-tail rows and
  the 12-row representative set with overlap. Returned metadata agree and B/R/Z arrays are served.
- Governed cmux browser captures: 20/20 target pages rendered a spectrum and coordinate marker.
  The individual captures and two contact sheets are preserved under `audit/viewer/`; no physical
  or novelty classification is inferred.
- AAS machine-readable package: PASS for 181 rows by 43 columns, no nulls, exact typed TSV
  round-trip. No journal asset DOI is yet assigned or claimed.

## Review status

This directory freezes the r3 exact PDF for orchestrator-dispatched review. It contains no r3
model verdict yet. A local compile/audit PASS must not be reported as external acceptance.
