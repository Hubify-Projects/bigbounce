# P4 v1.0.248 exact-board truth audit

Exact artifact: commit `5bc450274afa05cf7d6d3a14ca54d66279c83176`; 26-page PDF SHA-256 `1b1e2497f5a4083d41b2d56fb627cbced60b8a9ec95da595d86c8d244052d937`.

Board: Grok 4.3 direct API **MAJOR REVISIONS**; Gemini 3.1 Pro Preview direct API **MINOR REVISIONS**; GPT-5.6 Sol/high through ChatGPT-subscription Codex CLI **MAJOR REVISIONS**. The manifest records `openai_api_used=false` and `anthropic_used=false`.

## Verified findings

- **REAL — second support-audit regression.** v1.0.248 incorrectly called six diagnostics FS-C-consistent. The boundary artifact records `f_sky=0.740926` and 35,438 shell pixels; the leg proxy records `f_sky=0.493774` (24,270 pixels); quartile tests construct four different masks; and the cross-spectrum artifact does not record a mask/pixel count. Only the apodized-mask and multipole-spectrum diagnostics are bound to the 24,087-pixel FS-C base support. v1.0.249 restricts the synthesis accordingly; no statistic is recomputed or harmonized post hoc.
- **REAL, standing gate — training/model provenance.** Exact training objects, split, random state, run receipt, and a clean training anti-join are unavailable, and committed historical records conflict. TTA enforces flip-equivariance of the output protocol but does not make the unknown training realization irrelevant. This remains an open submission gate, not a wording-only closure.
- **REAL, standing gate — immutable archive/DOI.** The manuscript still points many artifact links at mutable `main` and explicitly promises a frozen paper/source/figure/code archive later. This remains open; no placeholder DOI or acceptance is claimed.
- **REAL, presentation — AASTeX/ApJS pass.** The manuscript remains REVTeX-formatted and overly audit-dense. A venue-format/presentation pass is still required.
- **REAL, minor — stale release ordering and causal overstatement.** Data Availability presents the superseded HF binding before the authoritative correction, and one release note over-attributes the CW monopole to a single cause. These are slated for the v1.0.249 presentation closure.

## Not verified as new defects

- Grok's claim that the public bundle lacks a machine-readable schema, checksums, validation, and executable reproducer is **FALSE** for authoritative HF data commit `db11023306ab4eed1d7727670bd78e127b7af17a` / receipt commit `e535b26247c892971963be6029435544cf29d19b`; the published receipt and manifest enumerate these files. The distinct training-replay gap remains real.
- Gemini's proposed statement that TTA makes the training realization irrelevant is **scientifically unsafe** and is rejected. The manuscript correctly states the opposite.
- Injection score fractions are already labeled descriptive rather than calibrated coverage or a physical bound. No numerical promotion is warranted.

## Disposition

The narrow HC observed-label null remains supported and unchanged: `z=0.7053169638`, one-sided rank `p=0.2246775322`. The exact v1.0.248 board is not an acceptance board. Readiness remains 80 pending v1.0.249 exact review, training/model provenance, spatial transfer/covariance, immutable DOI archive, venue presentation, and human review.
