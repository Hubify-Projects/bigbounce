# P2 v1.7.122 — normalized truth audit of the exact-PDF PRD confirmation panel

## Artifact and routing provenance

- Paper: P2, *The Exact Matter-Contraction Non-Gaussian Amplitude: Four-Vertex Derivation and Conditional Large-Scale-Structure Mapping*
- Review profile: Physical Review D research article (`PRD-RESEARCH`)
- Source and dispatch commit: `3e4a8cbf206fa8f15f69eb350d3e4ad61568ab52`
- Source SHA-256: `9144e1be05ba38e37271f8ffbb44bf9d52b73235b352f6672abbbcdaf56aaf1a`
- PDF SHA-256: `4097bac5a9930df7fa73e4a4567a7c60156f6cadb4321e51146dd237e13225c9`
- PDF length: 10 pages
- Source and PDF were clean at dispatch. Each API packet failed closed on a commit or PDF-hash mismatch and used the canonical `PRD-RESEARCH` registry entry.

The valid confirmation board is:

| Valid route | Model | Verdict | Hidden severity tags | Time |
|---|---|---:|---:|---:|
| ChatGPT-subscription Codex CLI | `gpt-5.6-sol`, high | **ACCEPT** | 0 MAJOR / 0 MINOR | 351 s |
| Gemini native PDF | `gemini-3.1-pro-preview` | **ACCEPT** | 0 MAJOR / 0 MINOR | 41.5 s |
| Grok native PDF | `grok-4.3` | **ACCEPT** | 0 MAJOR / 0 MINOR | 22.5 s |

An OpenAI API `gpt-5.5` call also returned ACCEPT with one `[MINOR]` tag that merely restated the four disclosed external gates. That call started before the routing correction prohibiting OpenAI API use for BigBounce. It is preserved as `API_P2_openai.md`, typed **NONCONFORMING / DIAGNOSTIC-ONLY**, and excluded from every acceptance/readiness conclusion. It was replaced blind—before reading or synthesizing any vendor raw—by the ChatGPT-subscription Codex CLI leg. Anthropic/Claude was not dispatched.

## Executive adjudication

The bounded v1.7.122 clarity wave is confirmed closed. All three valid reviewers return ACCEPT, contain no hidden MAJOR/MINOR tags, and support the central contraction-phase algebraic claim. The subscription reviewer independently recomputed the symbolic certificate: the four vertices sum to ordered-basis coefficients `(3,1,-9,5,-33,9)`, the squeezed limit is `-35/16`, the first correction is `35 k_1^2/(64 k^2)`, and the equilateral/folded values are `-255/128` and `-9/8`.

This board does **not** authorize a readiness uplift or the word “accepted” in the journal sense. It confirms that the exact contraction-phase result and its presentation now pass these automated PRD-style checks. The direct cubic transfer, external survey covariance, model-specific torsion bound, archive/DOI, and actual human/editorial decision remain open typed gates.

## v1.7.121 finding closure ledger

| Prior finding | v1.7.122 evidence | Adjudication |
|---|---|---|
| SPHEREx and “Testing” dominated the title. | Title now leads with the exact matter-contraction amplitude and labels LSS conditional. | **CLOSED** |
| Degree-nine `P` was confusable with power spectra. | Momentum polynomial is now `K_9`; `P_zeta` and `P_Phi` remain power spectra. Coefficients are unchanged. | **CLOSED** |
| Ordered sums were defined too late. | Six ordered pairs and six ordered all-distinct triples are defined immediately before Eq. (3). | **CLOSED** |
| Table IV's product notation was not self-contained. | Caption defines ordered sums and `Pi k^2=k_1^2 k_2^2 k_3^2`. | **CLOSED** |
| Table IV claimed epsilon was substituted while rows retained epsilon. | Caption/header now state general epsilon; `epsilon=3/2` is applied only to evaluated limits. | **CLOSED** |
| Primordial-to-LSS convention bridge was implicit. | Sec. III A displays `B_zeta^loc=(6/5) f_NL[P_zeta P_zeta+cyc]`, `Phi=(3/5)zeta`, and `B_Phi^loc=2 f_NL[P_Phi P_Phi+cyc]`; the exact factors cancel, so no extra `3/5` is applied. | **CLOSED** |
| General `b_phi` response was disconnected from Eq. (5). | Eqs. (5)–(6) give `Delta b=f_NL b_phi/M` and `b_phi^UMF=2 delta_c(b_1-1)`, while the text explains the free-response case. | **CLOSED** |
| Surrogate prose was over-precise. | Narrative uses 3.5, 3.1, 2.3, and 0.4 sigma; Table III and the generating artifact retain 3.47, 3.14, 2.32, and 0.42. | **CLOSED** |

## Central-claim truth check

The valid board agrees that no algebraic, normalization, sign, convention, or multiplicity defect is present in the certified result. The exact four source-level vertex rows, per-vertex squeezed/equilateral values, collapsed polynomial, epsilon-order grouping, and Li `c_s=1` cross-check remain mutually consistent. The central claim supported by this panel is deliberately narrow: for the stated matter-contraction background and cubic action, before an uncomputed nonsingular-transition transfer, the exact four-vertex sum gives `f_NL^local=-35/16`.

## External gates kept separate

1. **Direct third-order/cubic bounce transfer — open, adequately disclosed.** Blocks promotion from a contraction-phase coefficient to a full late-time bounce prediction; does not invalidate the algebra.
2. **Actual SPHEREx per-triangle covariance/likelihood — open, adequately disclosed.** Blocks a survey-level forecast/likelihood claim; the paper labels current mappings illustrative diagnostics.
3. **Model-specific fermion/torsion bound — open, adequately disclosed.** Required for fermion-populated completions; does not alter the scalar-only certified vertex sum.
4. **Immutable archive/DOI — open, adequately disclosed.** Camera-ready/submission packaging gate.
5. **Human PRD review/editorial decision — not run.** Automated ACCEPT verdicts cannot be represented as journal acceptance.

## Latency and process acceleration

Gemini and Grok were dispatched concurrently against immutable snapshots. Their valid runtimes sum to 64.0 s and the critical path was 41.5 s, saving 22.5 s (35.2%) versus serial execution. The OpenAI API diagnostic ran in the same wave but is excluded from valid-board metrics. After the routing correction, the blind subscription replacement ran separately for 351 s; the elapsed interval from initial API-wave start to replacement completion was 557 s. No aggregate concurrency speedup is claimed across that deliberate sequential correction.

The repeatable acceleration is methodological: content-addressed packets fail closed on dirty/wrong artifacts; a seven-item closure prompt prevents stale findings from triggering broad rewrites; exact tag counts expose verdict/tag mismatches; and the routing ledger makes a nonconforming leg impossible to launder into the acceptance board. The 6.1 MB Codex tool-telemetry transcript was hashed and moved to local review cache, while its 1.9 KB raw final referee response plus auth/model/session/SHA receipt are committed.
