# P4 v1.0.245 local closure claim-sync audit

**Scope:** local v1.0.245 post-review closure of findings from the exact
v1.0.244 review. This record does not claim that the
reviewed PDF contained these changes, does not update SSOT/site readiness, and
does not close the immutable archive/DOI gate. The next versioned exact PDF must
be reviewed before any readiness uplift.

## Quantitative and categorical claim changes

| Claim family | Reviewed v1.0.244 claim | Local closure claim | Evidence/status |
|---|---|---|---|
| Primary HC null | Pixel-asymmetry permutation: $z=+0.5491202$, one-sided rank $p=0.2651735$ | Fixed-occupancy support-sample label randomization: $A=0.0045970743$, $z=+0.705316964$, $k=2246$, $p=(k+1)/(10000+1)=0.224677532$ | Exact 10,000-draw PCG64 array, seed 20260715, SHA-256 `f6360f4bec22669097cee3e2fad8b176291d3ecbfbfbb9a9290d0bce3d5152c0`; old pixel permutation retained only as robustness. |
| Null interpretation | Pixel asymmetries treated as exchangeable while pixel occupancies/weights stayed in place | Count-level distribution induced by uniformly randomizing the fixed support sample's CW/CCW labels while holding galaxy positions, support membership, total CW count, pixel occupancies, and estimator fixed | Generator and receipt are repository-relative and record Python, NumPy, healpy, and PyArrow versions. |
| Training rows | 6,637 GZ1 + 17,153 CE spiral + 2,000 synthetic = 25,790; CE fraction 66.5%; flip augmentation described as adding rows | 6,637 GZ1 + 17,153 CE spiral + 826 CE nonspiral + 2,000 synthetic = 26,616; CE-derived fraction 67.5%; flips are on-the-fly consistency transforms and add no rows | `train_v2_fast.py` construction audited. Historical CE-nonspiral sampling, synthetic generation, split, and epoch shuffle were not all seeded; exact object/split manifest was not retained, so exact historical training reproduction is explicitly disclaimed. |
| Calibration | Numerical ECE lower bounds combined catalog-wide confidence and GZ1 accuracies | Numerical ECE bounds withdrawn; the object sets differ and no same-object reliability artifact is retained | Scores remain uncalibrated ranking outputs; downstream roadmap requires matched held-out labels/simulations, spatial transfer calibration, and propagation through the same selection/estimator. |
| GZ1 agreement | “Independent/disjoint” 234,282-object cross-match, 69.91% | Full-overlap cross-match $N=240,919$; chirality-evaluable subset $N=117,205$, agreement 69.91%, $\kappa=0.40$; overlap-contaminated and descriptive | No retained object-level anti-join can exclude the 6,637 GZ1 training rows. |
| Reader language | Internal `DP4-*` codes and dense literal artifact paths appeared in prose | Zero reader-visible `DP4-*` codes; bounded path cleanup; limitations named descriptively | Historical source comments and review records remain preserved as provenance. |

## Source-to-claim search

The closure search covered current `*.tex`, `*.html`, `*.tsx`, `*.md`, and
`*.py` surfaces using the old/new values and category terms above. Current P4
manuscript-body checks show zero reader-visible `DP4-*` hits and no live
`234,282`, `disjoint`, `independent GZ1`, mismatched-sample ECE-bound, `25,790`,
or `66.5%` claim. The old $z=0.549$, $p=0.2652$ pair remains once, explicitly
labeled as the former pixel-permutation robustness diagnostic. Literal
`\artifact{}` calls in the manuscript body were reduced to 32 and retained
where they carry material provenance.

## Deliberately deferred surfaces

- `project-context/SSOT/paper-4/status.md` still records the reviewed-PDF
  primary value. It must change only with the final versioned PDF/review/site
  synchronization commit.
- Site state was searched and has no matching stale claim in the scoped
  `*.html`, `*.tsx`, or `*.md` surfaces; no site edit is warranted before the
  final review round.
- `pipelines/p5_desi_chirality/paper/p5_desi_chirality.tex` still repeats the
  unsupported P4 training/GZ1 provenance. That cross-paper correction belongs
  to the active P5 owner and was not edited here to avoid overlapping work.
- Historical review snapshots, disposition prose, and version-history comments
  retain the claims that were true of their reviewed source state. They are
  evidence, not current scientific assertions.
- The local release bundle must be regenerated after the version bump so its
  filenames, manifest, reproduction note, and PDF all bind the final source.

The isolated regeneration now binds `paper_version=v1.0.245` separately from
`catalog_payload_version=v1.0.244`; the latter remains unchanged because the
paper closure changes no catalog row or column. The paper title no longer calls
the exact catalog “Public” while the content-addressed upload/archive gate is
open.

## Validation contract

The fail-closed claim validator checks the receipt fields and independently
hashes the retained `.npy` array. The reproducer must match the exact primary
amplitude, null moments, $z$, exceedance count, and rank $p$; release tests must
pass; the manuscript must compile and pass the required log/all-page visual
audit. No immutable archive or DOI exists yet, and no publication or acceptance
claim is authorized by this local closure.
