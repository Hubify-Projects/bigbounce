# P5 v0.1.135 exact-PDF confirmation truth audit

Manuscript of record: commit `0197358b17570309ba217070e43b56b55e840e23`,
PDF SHA-256 `7223afcce95bd735ddbd0efc05745738ba699949aa8f6536271a97aca394bdc8`
(39 pages).

## Findings and dispositions

1. **Codex C1 — valid MAJOR; closed in source for the next version.** The
   78-column focal fit had more fitted columns than the 50 coarse-sky clusters,
   and its CR1-normal interval was rank-fragile. The 13-column model on the
   identical sample, exposure, outcome, measured covariates, and cluster unit is
   now focal: CR1 contrast `+0.00145442`, SE `0.00331502`, 95% CI
   `[-0.00504290,+0.00795174]`, normal `p=0.66085`; the null-imposed
   99,999-draw wild-cluster efficient-score test gives `p=0.67345` (seed
   `20260715`). The 78-column fit is explicitly a flexible sensitivity.
2. **Codex C2 — valid MAJOR; wording closed, release gate remains.** P5
   incorrectly attributed the 25,790-to-26,616 difference to flip augmentation.
   It now reproduces Paper IV's honest account: 26,616 rows include 2,826
   not-spirals (826 CE-selected plus 2,000 synthetic), while flips are generated
   on the fly and add no rows. Conflicting historical totals/accuracy and the
   missing split manifest, seed, run receipt, and training-exclusion proof remain
   disclosed. Final-label re-verification against the frozen Paper IV release
   remains mandatory.
3. **Codex C3 — valid MAJOR; stale identifiers removed, immutable-release gate
   remains.** Unsupported `v2026.04`, stale `v0.1.132`, and the claim that
   `e2e842d...` regenerates later A41/A42 additions are removed. The text now
   cites acquisition revision `1ba76bc7` and binds results to the current
   checksummed release-candidate bundle without pretending a public tag or DOI
   exists.
4. **Codex C4 — valid MINOR; closed.** The Figure 5 generator legend now uses
   `sigma_pred = 2 Delta f_CW sqrt(N)`, consistent with the equation, negative
   `Delta f_CW`, and plotted negative predictions.
5. **Codex C5 — valid MINOR; closed.** The Figure 9 generator labels the T-Web
   panel as 812,793 environment-labelled rows, matching its displayed class
   counts. Both figure generators now derive the repository root dynamically.
6. **Gemini G1 — valid standing MAJOR.** Paper IV must be independently
   reviewable and final P5 labels/monopole inputs must be reverified against its
   frozen release before submission.
7. **Gemini G2/G3 — valid editorial MINORs.** The manuscript remains unusually
   dense and repetitive. A later venue-editing pass should move nonessential
   artifact bookkeeping out of the narrative and consolidate repeated caveats,
   without weakening scientific limitations.
8. **Gemini G4 — unsupported as stated.** The manuscript date is the actual
   review date, 2026 references are not inherently synthetic, and no specific
   false reference was demonstrated. Final bibliography/date verification
   remains a normal submission gate.
9. **Grok leg — no finding.** The direct-provider leg failed first with HTTP
   503 and again on an isolated retry with HTTP 503/429 provider-capacity
   errors. Both failed receipts remain in the manifest, but the retry overwrote
   the rolling `API_P5_grok.md` path, so the first failure has no distinct raw
   body. This archive-completeness gap is explicit; no verdict is inferred.

These closures do not make the confirmation PDF clean: they alter source and
figures for v0.1.136, which still requires compile, visual PDF audit, exact-PDF
re-review, immutable release packaging, Paper IV release coordination, and
human AJ review.
