# EXT3 P1B — External Truth-Audit (Round EXT3, in-thread delta)

**Paper**: `arxiv/paper1b_mcmc_companion.tex` · v1B.0.57 (18 pp., compiled PDF dated June 10, 2026, v1B.0.57 banner verified)
**Reports audited**:
- `EXT3_P1B_ChatGPT.md` — GPT Pro Extended — **MAJOR REVISIONS** (3rd consecutive MAJOR; shrinking: 2 fresh "blockers", 3 fresh majors, 4 minors; 8 of 9 prior majors CLOSED by its own table)
- `EXT3_P1B_Gemini.md` — Gemini 3.5 Thinking — **MAJOR REVISIONS** (held from EXT2) — 2 NOT-ADDRESSED closure calls, 1 fresh major, 2 minor clusters
- `EXT3_P1B_Grok.md` — Grok Heavy — **ACCEPT** (held from EXT2) — all 4 prior minors CLOSED, 4 fresh editorial minors

**Audit date**: 2026-06-11 · **Protocol**: feedback_peer_review_truth_audit_protocol (recompute machine-readable claims; extraction-artifact claims checked against SOURCE + visual render; DOI-minting/release-policy = HOUSTON-DECISION)

---

## Verdict table — fresh findings + disputed closures

| # | Reviewer | Sev | Finding | Verdict | Evidence |
|---|----------|-----|---------|---------|----------|
| F1 | GPT F-B1 | BLOCKER | `planck_bao_sn_20260312_1954/diagnostics/parameter_summary_CORRECTED.json` is invalid JSON (raw newline inside `_provenance`) | **FALSIFIED — reviewer-side soft-wrap** | Recompute: `python3 json.load()` parses the file cleanly BOTH locally and at `origin/main` (blob from commit `63931207`, pushed 06-10 23:15, well before the ~02:50 harvest). `_provenance` is a single 300+-char line; ChatGPT's browser soft-wrapped it and it read the wrap as a literal newline. Values match Table I (H₀=67.7845, ΔN_eff=0.0578, S₈ present in this file). Its proposed CI check (`python -m json.tool` over all JSON artifacts) is still cheap and worth adopting defensively. |
| F2 | GPT F-B2 | BLOCKER | Paper claims HuggingFace "DOI links … listed below in Appendix A"; Appendix A lists no DOIs/URLs; CHANGELOG says DOI assignment pending | **VERIFIED (wording) + HOUSTON-DECISION (minting) — closure-introduced regression** | tex L2069–2071: "HuggingFace dataset DOIs … are listed below in Appendix~A under `HuggingFace datasets'"; Appendix A (L2155–2165) lists only three dataset *descriptions* and points back to CHANGELOG.md; root `CHANGELOG.md` L45–51: all three "pending DOI assignment; live at `https://huggingface.co/datasets/Hubify/p1b-…`". The EXT2-F1 closure created the CHANGELOG honestly but wrote a cross-reference into the tex that the Appendix does not satisfy. Fix the sentence + paste the three live URLs into Appendix A (same-day); actual DOI/Zenodo minting at submission = HOUSTON-DECISION per protocol. |
| F3 | GPT F-M1 | MAJOR | The named "authoritative corrected summary" (`full_tension …/parameter_summary_CORRECTED.json`) omits S₈, one of the seven Table I parameters | **VERIFIED — genuinely new (closure-introduced by the EXT2 F2 artifact wave)** | Recompute: keys = `[H0, delta_neff, tau, sigma8, omegam, ns]` — no S₈. `freeze_diagnostics_CORRECTED.json` `param_stats` DOES contain S₈ (verified), but that is not the file the Data-Availability correction paragraph names. ChatGPT's `table1_reproduction.json` proposal (all 7 params × both datasets + counts + R̂/ESS) is the right consolidation. |
| F4 | GPT F-M2 + Gemini Fresh-M1 | MAJOR | w₀wₐ overlap-uncorrected product likelihood still headlined as "Quintom-B empirical anchor"/"empirical test" in the conclusion | **VERIFIED residual / PARTIAL — standing EXT2 C1, compute-queued** | tex L2047: conclusion paragraph still titled "\textit{Quintom-B empirical anchor.}" and calls the chain "an empirical test of the quintom-B scenario". §III caveat (e) (L1184–1205) is honest and full (20% overlap, double-weighting direction-of-bias, "not demonstrated quantitatively", two control chains "queued on the dedicated MPI pod"). Gemini's fresh MAJOR is a re-raise of this exact standing item — its proposed fix IS the already-queued control chains, disclosed in the very text it quotes. Residual action: demote the conclusion heading until the control chains land (one-line edit), per EXT2 action 1's own fallback. |
| F5 | GPT F-M3 | MAJOR | Abstract footnote "PR4/NPIPE … is what the ALP-MCMC re-runs actually use" conflicts with App C "all fits use a Gaussian summary likelihood on β=0.342°±0.094°" | **VERIFIED (minor-grade wording; new site of the PR3/PR4 theme)** | tex L770–772 (fn:eskilt_pr3_pr4) vs L2267 ("All fits use a Gaussian summary likelihood…") and L1833 ("not a re-analysis of the EB spectra"). If the ALP MCMC consumes only the scalar β summary, the PR4/NPIPE dataset label is provenance of the likelihood code, not what the fits "use". ChatGPT's replacement sentence is exactly right; one-footnote fix. |
| F6 | GPT minor | MINOR | Post-burn sample-count variants (123,129 / 123,368 / 216,432 / 93,064 / 93,066) | **PARTIAL** | The 123,129-vs-123,368 and 216,432 figures are *already reconciled in-text* (tex L943–963 + `COUNT_EXPLANATION.md` pointer) — over-called there. Real residual: tex L957 says 93,064 post-burnin while `parameter_summary_CORRECTED.json` says `post_burnin_samples: 93066` (2-sample chain-end-truncation delta, unexplained for this pair). One convention sentence. |
| F7 | GPT minor | MINOR | First overlap sentence cites [14] DES-SN5YR instead of [16] Vincenzi | **VERIFIED (one-token; residual of the EXT2 F3 closure)** | tex L1186–1189: "share approximately 20%… corrections applied by the respective collaborations~\cite{DES2024SN5YR}". Vincenzi (`Vincenzi2025SNcompare`) was added (EXT2 F3 closed) but attached only at the later "Users requiring…" sentence (L1206–1209). Move/add the cite at the 20% claim. |
| F8 | GPT minor | MINOR | Root reproducibility README still Paper I(a)-oriented; "No CMB map analysis" misleading for P1B | **VERIFIED (minor)** | `reproducibility/README.md` L3: "**Paper:** … (Paper I A)"; L82 "No CMB map analysis". A "Paper I(b) quick links" block or `Paper1B_README.md` is the cheap fix. |
| F9 | GPT minor | MINOR | PACS numbers remain | **OPINION — 3rd-round re-raise (EXT1 F14, EXT2 F11)** | Journal-dependent. |
| G1 | Gemini M1 closure | "NOT ADDRESSED" | Pairing-bias (NPIPE high-ℓ + 2018 low-ℓ) swap test still not run | **VERIFIED-UNCHANGED / closure dispute — 3rd-round re-raise of a deliberately-parked item** | tex L912: disclosure verbatim ("have not run a release-pairing swap test … unquantified at the quoted precision"). EXT1 truth-audit F27 ruled the labeled-limitation disclosure adequate for a companion paper; no patch was ever scheduled. Gemini itself concedes "acceptable as an explicitly labeled limitation". No new fact. |
| G2 | Gemini M2 closure | "NOT ADDRESSED" | Live unconverged Planck-only chain still referenced (abstract/§III/conclusions) | **VERIFIED-UNCHANGED / closure dispute — 3rd-round re-raise of a deliberately-parked item** | tex L966, L2012 (+abstract parenthetical): all sites explicitly exclude it from tables/headlines. EXT1 F28 ruled disclosure adequate; EXT2 F8 same. Gemini concedes it serves "only as a status indicator". Optional one-line abstract trim remains on the menu (EXT2 action 9), HOUSTON-DECISION. |
| G3 | Gemini minor | MINOR | App C "Configuration (iii) — model-independent firee fit" typo | **FALSIFIED — 2nd-time re-raise (EXT2 F14 FALSIFIED)** | grep `firee` in tex: 0 hits; source reads "model-independent $\beta_{\rm free}$ fit". PDF-extraction rendering of the β subscript. |
| G4 | Gemini minor | MINOR | Table II glitches: "H₀ I₀" duplicate, "(marg.-tailfn; . a)", Cyrillic "Хомв", footnote letters out of sequence | **FALSIFIED — extraction artifacts (2nd re-raise of EXT2 F15/F16 + one new same-class claim)** | Source: `$H_0$ [km\,s$^{-1}$\,Mpc$^{-1}$]`, `(marg.-tail; fn.~\ref{fn:wcaveat})`, `$\chi^2_{\rm CMB}$` — no Cyrillic anywhere in tex (grep 0 hits). **Visual render of compiled p. 16 at 250 dpi** (the check EXT2 F16 left open): all columns clean, "−3.6σ from 0 (marg.-tail; fn. a)" renders correctly, χ² labels Latin, footnote markers sequential (a, b, c) with block starting at "a". EXT2 F16's deferred visual check is hereby CLOSED — no real defect. |
| K1 | Grok closure 3 | CLOSED claim | "Appendix A … HuggingFace DOIs pinned to v1B.0.57. CLOSED" / fresh minor 2: "The three DOIs are now listed" | **FALSIFIED CLOSURE — repeat over-credit (same failure mode as EXT2 F17)** | Direct contradiction with F2: Appendix A lists zero DOIs/URLs; CHANGELOG says "pending DOI assignment". Second consecutive round Grok credits a release/DOI claim by reading the sentence instead of the artifact. Its ACCEPT again rests partly on over-credit. Grok's other three CLOSED calls (burn-in footnote, recomputed-from-chains statement, overlap softening) audit accurate against tex L943–963, L2030s, L1184–1205. |
| K2 | Grok minors 1–4 | MINOR | JSON-bug sentence rewording; "DOIs pinned" caveat sentence; one-sided ΔN_eff supplementary clause; Table IV caption commit tie | **OPINION (editorial)** | All four are wording-polish proposals on accurate text; minor 2 carries a false premise (DOIs don't exist — see K1) and should be implemented as URL-pinning language instead. Table IV caption already states "pending tagged release" (tex L2168–2175), which Grok under-read. |

## Closure-verification claims audit

- **ChatGPT's table**: M1–M8 CLOSED calls all consistent with on-disk state (sample-count language, two-sided ΔN_eff, proxy framing, S₈ prior disclosure, NaMaster scoping, under-recovery language, SNR demotion, ALP summary-likelihood labeling — each verified present in v1B.0.57 tex). B1 "PARTIAL/REGRESSION" is half-right: the 176,240 resync and planck_bao_sn CORRECTED artifact are real and correct (recomputed), but its "invalid JSON" regression component is falsified (F1) — net over-call. B2/B3/M9 PARTIALs accurate.
- **Grok's table**: 3 of 4 CLOSED accurate; 1 of 4 (DOI pinning) is a falsified closure (K1) — the EXT2 F17 lesson did not stick. ACCEPT direction is still defensible once K1 is discounted, because nothing Grok over-credited is physics-bearing.
- **Gemini's table**: CLOSED rows (sample count, column-permutation disclosure, drafting markers) accurate; both NOT-ADDRESSED rows factually true but are re-raises of items the EXT1/EXT2 audits explicitly parked as adequate-disclosure (G1, G2).

## Consensus

| Theme | Reviewers | Verdict |
|---|---|---|
| SN-overlap product likelihood needs control chains / demotion in conclusion | GPT F-M2, Gemini Fresh-M1 (+EXT2 C1 standing) | VERIFIED residual; chains already queued; demote heading now |
| Release identity (DOIs/tag) wording vs reality | GPT F-B2; (Grok's CLOSED falsified) | VERIFIED wording + HOUSTON-DECISION minting |

## Action plan (VERIFIED/PARTIAL, hardest first)

1. **[F4] Demote the conclusion heading** — tex L2047: "\textit{Quintom-B empirical anchor.}" → "\textit{Exploratory $w_0w_a$ cross-check.}"; "an empirical test of the quintom-B scenario" → "an exploratory, overlap-uncorrected test … provisional pending the queued SN-overlap control chains". Keep §III caveat (e) as-is (it is already correct). When the queued MPI control chains converge, restore stronger language with the $(w_0,w_a)$ shifts shown.
2. **[F3] Regenerate the artifact** — add S₈ to `reproducibility/cosmology/frozen/full_tension_20260311_1728/diagnostics/parameter_summary_CORRECTED.json` (regenerate from chains, don't hand-edit), or better: emit ChatGPT's `table1_reproduction.json` (both datasets × all 7 Table I params + counts + worst R̂−1 + min ESS) and cite it in Data Availability. Add the cheap `python -m json.tool` CI sweep over all `*_CORRECTED.json` while there.
3. **[F2] Fix the DOI cross-reference** — tex L2069–2071 + Appendix A: paste the three live HF URLs into Appendix A and replace "DOI links" with "dataset URLs; DOI assignment pending (identifiers will be inserted at submission)". DOI/Zenodo minting timing = HOUSTON-DECISION.
4. **[F5] Reconcile the PR4/NPIPE footnote** — tex L770–772: "the ALP MCMC uses only the scalar Gaussian summary β=0.342°±0.094°; it does not depend on the PR3/PR4 map-level distinction except through the provenance of that summary value."
5. **[F7+F6] Minor pass** — move `\cite{Vincenzi2025SNcompare}` onto the 20%-overlap sentence (L1187–1189, keep [14] for the likelihood); add one sentence reconciling 93,064 (tex) vs 93,066 (JSON post-burnin convention).
6. **[F8] Add a "Paper I(b) quick links" block** at the top of `reproducibility/README.md` (or `Paper1B_README.md`) so the program-wide README stops reading as P1A-only; fix the "No CMB map analysis" line to point at the P1B NaMaster artifacts.

## GAP METRIC

- **(a) Genuinely new: 2 solid + 1 closure-regression ≈ 3** (vs EXT2's 4) — F3 (S₈ omitted from the named authoritative artifact — introduced by the EXT2-F2 CORRECTED-file wave), F5 (footnote/App-C contradiction, new in kind), F2 (Appendix-A-lists-DOIs cross-reference written false by the EXT2-F1 closure). Minor-new: F6's 93,064/93,066 pair. Both substantive (a)-items are again **closure-introduced** — third consecutive round in which the reproducibility fix wave creates the next round's findings. Trend: 4 → ~3, converging but artifact-layer churn is the bottleneck.
- **(b) Re-raises of audited-FALSIFIED items: 2 clusters (4 claims)** — G3 ("firee", 2nd), G4 (Table II garbles, 2nd + new same-class Cyrillic claim; the one open EXT2 sub-item — footnote-marker sequence — is now visually CLOSED). Plus OPINION re-raise F9 (PACS, 3rd).
- **(c) Policy/HOUSTON-DECISION + parked residue: 5** — DOI minting + immutable tagged release (paper now honestly says "pending"; mint at submission), G1 pairing-swap (parked, adequate disclosure ×3 rounds), G2 live Planck-only chain mentions (parked ×3; optional abstract trim), SN control chains (compute-queued on MPI pod — the single hard item, already in flight), abstract f_a∼M_Pl framing (EXT1-F3 lineage, accommodation-vs-prediction wording, Houston call).

Pattern notes for `r-round-pattern-mine`: (1) Grok over-credited a release artifact two rounds running — auto-flag any Grok CLOSED on tags/DOIs/releases for artifact-level verification. (2) ChatGPT's "invalid JSON" blocker shows browser soft-wrap can fabricate artifact corruption — recompute every machine-readability claim locally before crediting. (3) Gemini spent its entire fresh-finding budget on one standing item + extraction artifacts.

## EXIT-CRITERION ASSESSMENT

**ChatGPT MAJOR classification**: F-B1 falsified; F-B2 = wording + policy; F-M1 real but same-day artifact regen; F-M2 = standing compute-queued item + one-line demotion; F-M3 = one-footnote fix. **No substantive physics/statistics blocker is newly identified.** Gemini's MAJOR adds nothing beyond the standing SN-overlap item and parked disclosures. Grok's ACCEPT survives modulo its repeat DOI over-credit.

**Is P1B externally clean modulo HOUSTON-DECISION/policy?** **Yes, modulo ONE compute-bound item.** After Actions 1–6 (same-day), the only substantive residual is the SN-overlap control-chain pair already queued on the MPI pod (EXT2 deferred-queue) — a robustness demonstration, not a defect discovery. Everything else open is HOUSTON-DECISION (DOI/tag minting, parked disclosures, abstract framing). Recommend: execute the same-day wave now, hold the final clean call until the control chains converge and their $(w_0,w_a)$ shifts are folded into §III/Table II.
