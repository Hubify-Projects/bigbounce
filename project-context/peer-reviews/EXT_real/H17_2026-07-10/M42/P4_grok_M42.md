VERDICT: MINOR REVISIONS

ISSUES:

[MINOR] Abstract and Sec. I (Introduction): The unit mapping between Shamir’s reported 1.7%–4.0% asymmetry and the paper’s Ap = 2(fCW − ½) convention is stated correctly but appears only once; repeating the explicit equivalence (“already in Ap units directly — no factor-of-two rescaling”) immediately adjacent to the z ≈ −7.6 template-disfavor statement would eliminate any risk of reader confusion on amplitude comparison.
[MINOR] Sec. IV D (Monopole+Mask Leakage Generative Null) and referenced Appendix D: The forward-modeling result that imaging + morphology templates account for only ~52–54% of the observed post-MASTER ℓ = 1 residual amplitude (leaving an explicit ~47% open item) is correctly bounded below the real-space A95 threshold; however, the main text should contain a single forward-reference sentence to the planned per-galaxy DR8 morphology → classifier-purity map computation so that readers see the residual is not left unaddressed.
[MINOR] Sec. VI A (Pseudo-label independence…): The fully model-independent GZ1-human-only dipole test (z = −0.54σ at N ≈ 4.6 × 10^4) is a valuable cross-check, but its sensitivity (A95 ≈ 4.5–6.8%) is ~4–5× coarser than the headline HC sample; the text should explicitly note (in one sentence) that this test corroborates the absence of large-scale inherited bias at the few-percent level while the sub-percent regime is constrained by the template-regression and block-bootstrap anchors.
[MINOR] Sec. III B (Declared Analysis Hierarchy) and Table I: The pre-registration of the peq > 0.6 cut via a specific git commit hash is documented and the confidence-cut sweep demonstrates robustness; for journal standards, a brief parenthetical statement that the exact selection predicate in the public generator script is immutable in the committed history would strengthen reproducibility claims without altering any result.

One sentence: The central claim — that the large-scale chirality dipole is consistent with null at sub-percent sensitivity and that a genuine Shamir-scale (A ≈ 1.7%) signal would have been detected — is supported by the primary real-space dipole estimator on the pre-specified high-confidence subsample (+0.41σ, empirical rank p = 0.31 against the isotropic pixel-permutation null), the injection-recovery calibration (A50 ≈ 0.75%, A95 ∈ (1.0%, 1.5%]), the block-bootstrap WLS template disfavor of a clean 1.7% dipole, and the independent GZ1 human-label null cross-check.
