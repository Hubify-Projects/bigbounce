VERDICT: MINOR REVISIONS

ISSUES:

[MAJOR] Sec. I (Introduction) and Sec. IV (SPHEREx forecast): The headline sensitivity envelope (∼1.3–2.75σ) and abstract mix three distinct procedures (noise-weighted template recast with r = 0.84, GR-proxy floor using transferred ρ = −0.868 from the SDB channel, and channel-native surrogate Fisher giving ρ ≈ −0.42 and ∼2.3σ); while the text discloses the distinctions and retains the proxy as conservative, the primary quoted range and abstract must explicitly prioritize and justify one procedure (or present all three side-by-side in a table) so readers can assess the claim without ambiguity.
[MAJOR] Sec. II A (The Prediction) and referenced Appendix A: The central correction of Cai et al.’s f_NL = −35/8 to −35/16 (via the −(99/128) ∑_i k_i^3 term in the transcribed polynomial) is supported by vertex re-summation, Li et al. cross-check, and benchmark matching, but the explicit printed vs. corrected degree-9 polynomial coefficients (or at minimum the discrepant monomial term) must appear in the main text or a dedicated table; reliance on an unreproduced appendix transcription error weakens immediate verifiability of the factor-of-two claim that underpins every subsequent significance.
[MINOR] Sec. IV (independent Fisher validation) and Sec. VII (systematics): The tree-level Gaussian multi-tracer covariance surrogate reproduces Heinrich et al.’s σ(f_NL) ≈ 0.7 to 2–11% and yields reff ≈ 0.99, but the validation fixes b2/bs2 at fiducial values and omits non-Gaussian covariance contributions; the text must quantify (even approximately) how marginalizing b2 or including the connected six-point terms would shift the reported reff and the channel-native GR–fNL correlation before the surrogate can be presented as confirming the recast.
[MINOR] Sec. V (MegaMapper forecast) and Sec. VI (Bayesian comparison): Both the MegaMapper 1.5–3.5σ design-uncertainty envelope and the BF ≈ 9–14 range are labeled illustrative/prior-dependent, yet the abstract and Table V (implied) present them as quantitative outlook targets; the text must add a one-paragraph “limitations of the projection” subsection and a explicit prior-sensitivity table (or plot) showing BF variation with σ_theory and competitor width so the discriminatory-power claim is not overstated.
[MINOR] Throughout (notation and presentation): Six distinct r-like symbols (r, r_cos, r_eff, r from null-space scan, etc.) plus multiple σ floors are defined inline; a compact notation table at the end of Sec. I would eliminate reader confusion without lengthening the manuscript.

One sentence: The central claim—that the corrected matter-bounce f_NL = −35/16 yields a realistic SPHEREx sensitivity of order 2–3σ (template-corrected) that preserves qualitative discriminatory power against single-field inflation after a documented systematic budget—is supported by the explicit template-overlap calculation, the Heinrich et al. recast, the independent surrogate Fisher validation, and the closed-form Bayes-factor cross-checks, provided the presentation clarifications above are implemented.
