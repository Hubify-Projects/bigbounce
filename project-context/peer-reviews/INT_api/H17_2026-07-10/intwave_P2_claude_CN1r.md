I have everything I need. Let me confirm the transfer-convention mismatch is real: the primordial density bispectrum carries `M(k1)M(k2)M(k3)` (c13 line 264, applied in c15 line 208 as `Dprim = bABC * M123 * Bpr`), while the GR template `gr_reduced` (c15 lines 142–150) uses bare `P_phi` legs with **no** `M123`. That is the source of `F_22 ≈ 2.8e-18` (18 orders below `F_00`).

---

## Referee Report — Paper P2, v1.7.114 (channel-native Fisher re-test)

**(1) VERDICT: MAJOR REVISIONS**

**Numbers verified — all reproduce.** I checked every c15 figure the paper quotes against `outputs/c15_channel_native_fisher.json` and re-derived them from `F_3x3`:
- ρ(f_NL,A_GR)₂ₓ₂ = −0.00091 ≈ **−0.001** ✓ (JSON L91); ρ(f_NL,b_φ) = +0.9903 ≈ **+0.99** ✓ (L89); α = 0.9916 ≈ **0.99**, Fisher cosine 0.99983 ≈ **1.00** ✓ (L29–30).
- σ_marg = **0.880** ✓: bounce 2×2 {f_NL,b_φ} with 30% prior `F[1,1]+=1/0.09` → (F⁻¹)₀₀ = 21.998/(2.5070·21.998−5.1734²) = 0.7750, √ = 0.8803 (JSON L87). Significance |−2.1875|/0.8803 = **2.485 → 2.5σ** ✓ (L102). Proxy floor 0.7/√(1−0.868²)=1.410 → 1.30σ ✓ (L115–116). The prose at `02_full_draft.tex:1302` transcribes the JSON faithfully; there is no transcription error.

**(2) ISSUES**

**[MAJOR] The channel-native GR result is a normalization artifact — the section does not actually close DP2.** `02_full_draft.tex:1302` asserts as a finding: *"ρ(f_NL,A_GR)≈−0.001 (near-orthogonal)… The GR channel is therefore far less degenerate with f_NL than either the transferred ρ=−0.868 SDB proxy or the uniform-measure |ρ|≈0.95 shape overlap suggested—both proxies overstated the GR marginalization loss."* This claim is not supported by the artifact:
- The GR template is built in **primordial-potential space**: `gr_reduced` uses bare `P_phi(ka)·P_phi(kb)` with the (k_s/k_l) factor and **no matter transfer** (`c15_channel_native_fisher.py:142–150`). Every other object in the joint Fisher is in **density/observed space**: the f_NL primordial derivative carries `M123 = M(k1)M(k2)M(k3)` (`c15:208`, per the c13 convention `B_δ^prim = M(k1)M(k2)M(k3) B_φ`, `c13_independent_bounce_fisher.py:264`), the SDB term uses the density matter bispectrum `B_matter_grav`, and the covariance `Pgg` is density-space.
- Consequence: A_GR carries essentially **zero** Fisher weight — `F_22 = 2.81e-18` vs `F_00 = 2.507` (JSON L65–80), a suppression of ~M123² exactly as expected from the missing transfer. The GR marginalization is therefore **numerically vacuous**: the 2×2 {f_NL,A_GR} σ = 0.63158 is identical to the all-nuisance-fixed σ = 0.63158 (JSON L83, L85) — marginalizing over A_GR changes σ(f_NL) at the 7th digit.
- Because ρ(f_NL,A_GR) is computed in the wrong metric (the missing `M123` is strongly k-dependent, not a constant, so it does **not** cancel out of the normalized ρ), the ≈0 value is not the physical channel-native correlation. That it overturns **two** independent in-repo estimates (c8 ρ=−0.868, c12 |ρ|≈0.95) via a template carrying 10⁻¹⁸ of the Fisher weight is a red flag that the c15 GR normalization — not the priors — is wrong. **Fix:** either promote S_GR to the consistent observed/density space (multiply by `M123`, matching the local template) and recompute ρ, or demote the GR result to "the potential-space GR template carries negligible bispectrum Fisher weight; a consistent observed-space GR marginalization is deferred" and **delete the "both proxies overstated the GR loss" sentence.** *Mitigation:* the paper conservatively retains the ρ=−0.868 proxy as the quoted 1.3σ headline floor and states "no headline number is loosened by this computation" (`:1302`), so no reported significance is inflated by the bug — which is why this is fixable, not a reject.

**[MINOR] The 2.5σ "channel-native floor" is entirely a b_φ-prior result, not a GR-marginalization result.** Since GR contributes nothing (above), the 0.88 → 2.5σ number comes solely from the 30% Gaussian prior on the A_SDB=f_NL·b_φ nuisance. This is defensible and consistent with the paper's Barreira-motivated 20–30% prior, but the section is framed as closing the *GR* proxy objection (DP2) while the operative degeneracy it actually marginalizes is b_φ. The framing should say so plainly, and should note the strong prior-sensitivity already present in the JSON (20% → 2.90σ, 30% → 2.48σ, 50% → 1.84σ, free → 0.48σ; JSON L97–99, L103).

**[MINOR] Wording error, `:1302`:** "local-template self-consistency: σ_marg^local ≈ 0.87, ~25% of the Heinrich 0.7." 0.87 is 25% *above* 0.7 (JSON ratio = 1.246, L110), not 25% *of* it. Should read "within ~25% of." Relatedly, the surrogate's all-fixed σ=0.626 sits ~10% *below* 0.7 (optimistic); consistent with the disclosed "2–11%" band but worth stating as optimistic-leaning.

**(3)** The central claim — a matter-bounce f_NL^local = −35/16 testable by SPHEREx at a conservative ~1.3σ (proxy) up to ~2.75σ — **remains supported and is not inflated by the new computation** (the paper wisely keeps the conservative proxy as its headline); however, the specific v1.7.114 assertion that the channel-native Fisher shows the GR proxies "overstated the loss" and thereby closes DP2 is **not** supported, because the GR template is mis-normalized (potential vs density space) and its marginalization is vacuous.
