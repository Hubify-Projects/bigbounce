# P3 v3.2.0-r7 normalized truth audit

**Frozen review artifact:** commit `ba2f5b93a30de41122dc2ed31b543ac0da9a37c8`; source SHA-256 `01cb68b1d52d411c1f4b181d6504f2f1344bc45d1f0ad3793d74b58a5d7e75d8`; PDF SHA-256 `761e35ec840e93599163d68c6b4db9b8d75293545e49c45c978dc0be0f38cb2b`; 16 pages.

**Countable board:** Codex CLI authenticated through the ChatGPT subscription (`gpt-5.6-sol`, high) — **MAJOR REVISIONS**; xAI direct API (`grok-4.3`) — **ACCEPT**; Google direct API (`gemini-3.1-pro-preview`) — **MINOR REVISIONS**. The provider receipts are preserved in the raw reports and `manifest.jsonl`. No OpenAI API review is counted; no Anthropic/Claude route was used.

Failed attempts are evidence, not votes. The first Codex subscription attempt exceeded its resource budget after attempting to hash the 21 GB DESI FITS input. Two later API launches failed closed before dispatch because of an exact-HEAD mismatch and a registry check. Their logs and typed failure receipts remain in this directory and contribute neither findings nor zero-count verdicts.

## Finding dispositions

| Source | Finding | Truth disposition | Evidence / action |
|---|---|---|---|
| Codex #1 | Three manifest-listed Parquet payloads were absent from the frozen Git tree, so the advertised clean-checkout bundle could not validate. | **REAL — CLOSED IN r8** | Commit `d155eb27488b271be12942b1a1be8b3c39dd24f4` tracks the historical anomaly, 181-row primary, and 2,267-row warned-auxiliary Parquet payloads. The exact-tree validator reports all 38 manifest payloads and all 41 bundle files present with matching sizes and SHA-256 values. |
| Codex #2 | The provenance of the 0.1-arcsec quality boundary was unstated. | **REAL — CLOSED IN r8** | r8 source lines 301–303 explicitly call 0.1 arcsec post hoc and descriptive, not a predeclared selection cut; the fixed 1-arcsec membership and all 181 rows remain unchanged. |
| Codex #3 | Candidate-only data cannot test model discrimination, calibration, completeness, or false-positive rate without a representative control sample. | **ALREADY CLOSED** | The appropriate-use language states that model comparison requires a representative control sample (source lines 576–591) and rejects occurrence-rate inference. |
| Codex #4 | The artifact must remain framed as public-ID recovery, not a reproducible anomaly detector or physically validated anomaly catalog. | **ALREADY CLOSED** | The abstract states that production normalization and physical-feature sensitivity are unavailable and that the 181 rows are not validated detections (source lines 37–64). The limitations repeat that historical scores are not reproduced and no physical-anomaly or novelty claim is made (lines 681–703). |
| Codex #5 | Pages 13–15 are underfilled and Table 2 has awkward flow. | **OPTIONAL PRESENTATION** | The reviewer found all 16 pages legible and free of clipping or overlap. This does not obscure content or invalidate a claim; journal production can improve the flow. |
| Gemini #1 | Add typical physical/spectral features of the historical anomalies. | **DECLINED — UNSUPPORTED / OUT OF SCOPE** | Production normalization, resampling, object-to-spectrum mapping, and physical-feature sensitivity are unavailable (lines 37–64 and 681–686). Inventing a typical physical class would fabricate evidence and contradict the declared recovery-only scope. |
| Gemini #2 | Physically classify the highest-scoring deterministic example targets. | **DECLINED — NEW SCIENCE REQUIRED** | The 12-row table is a deterministic illustrative set and explicitly not a novelty or importance ranking (lines 512–519). Physical interpretation requires calibrated spectra, masks, observing conditions, repeat observations, and object-level review (lines 576–581 and 700–703). |
| Grok | No findings. | **ACCEPT** | The reviewer found the 181-row public-ID recovery, quality tiers, warned auxiliary table, and shift controls supported and ApJS-appropriate. |

## Exact r8 closure confirmation

The bounded r8 confirmation reviewed commit `d155eb27488b271be12942b1a1be8b3c39dd24f4`, source SHA-256 `2b9a5fd356e49ae7a9939cbf8e9197379bef71b1f66a0e364e0de41ae416d10b`, and PDF SHA-256 `b5f254f92b10bda43b687f07c5f58b828a6f7dc70d98c08f9e9b609edbba08b0` (16 pages). A read-only, ephemeral Codex CLI run authenticated by `codex login` through the ChatGPT subscription returned **ACCEPT** with zero in-scope blockers after checking the 41-file package, the post-hoc 0.1-arcsec statement, and the `181 = 170 + 11` contract. It used no API, no Anthropic/Claude route, no network, and did not rebuild the paper or catalog.

This closes the bounded r7 manuscript/release defects. It does **not** justify a readiness increase, journal acceptance, a DOI claim, or a new physical interpretation. P3 remains capped at 56 until the separately typed human/editorial and immutable-release gates are satisfied.
