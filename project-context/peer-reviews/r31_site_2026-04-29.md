# Round 31 — Site QA Sweep (2026-04-29)

**Scope:** Static-link / image / nav / responsive / freshness sweep across all
public-site HTML pages (root + `articles/` + `research/project_master_dossier/`).
Conducted via grep + Python file-existence checks (no live browser).

**Constraint envelope:** No SSOT touches. No PDF recompiles. No redesign — only
fix what is broken or stale.

## Findings (8 categories swept)

| ID | Category | Page(s) | Verdict | Resolution |
|----|----------|---------|---------|------------|
| WT-1 | Broken image | `articles/program-visual-guide.html` | BROKEN | FIXED — removed `<img>` tag for missing `images/beyond_big_bounce_infographic.png` and reworded section 2 + 2.4 + 2.5 to read cohesively from prose alone (the section already explained the panel content in 2.1–2.5). Commit `6e28477`. |
| WT-R31-1 | Broken JS image link | `anomaly-explorer.html` | BROKEN | FIXED — JS template built filenames as `spectrum_rank###_tid######.png` but actual files in `pipelines/p1_highz_tracers/outputs/desi_dr1/spectra/` follow `rank###_tid######_score##.#.png` (per-rank score). Replaced string concat with a static rank → filename map (50 entries) extracted from `extraction_metadata.json`. Commit `ab8571c`. |
| WT-2 | Broken internal links | all `*.html` | CLEAN | After WT-R31-1 fix: 0 broken `href` refs site-wide (Python sweep). |
| WT-3 | Broken images (other) | all `*.html` | CLEAN | After WT-1 fix: 0 broken `<img src>` refs site-wide. |
| WT-4 | Nav consistency | all `*.html` | CLEAN | All non-redirect pages include `nav.js`. The 5 pages flagged as missing nav.js (`arxiv-preview.html`, `data-comparison.html`, `findings.html`, `review.html`, `team.html`) are intentional `<meta http-equiv="refresh">` redirect stubs; no nav needed. |
| WT-5 | Inline dark-mode bleed | all `*.html` | CLEAN | One `style="background:#fffbeb"` highlight row in `ssot.html` L611, paired with explicit `color:#92400e` on inner text — readable in both themes. All other inline `background:#fff` occurrences pair `color:#fff` (badges); no orphaned bleed. |
| WT-6 | Mobile responsive | `style.css` | CLEAN | Breakpoints at `max-width: 900px` (tablet/mobile collapse, sidebar drawer, table horizontal scroll, single-column grids) and `max-width: 480px` (phone-tight typography). No fixed-width elements that overflow at <480px. |
| WT-7 | Data-explorer datasets | `data-explorer.html` | CLEAN | All 15 datasets are inline arrays (no fetch paths). No `TODO`, no truncation markers, no obvious shape inconsistencies. |
| WT-8 | Activity freshness | `activity.html` | STALE → FIXED | Most recent entry was 2026-04-22 (fire #189). Today is 2026-04-29; 14 commits since (R26–R30 adversarial peer review + 4-paper PDF recompile). Added a 2026-04-29 timeline entry summarizing R26–R30 + Round-31 site sweep. |
| WT-9 | paper.html ↔ SSOT readiness | `paper.html` | DRIFT → FIXED | Paper 1 was 99% on site vs 100% in SSOT `index.md` line 78. Site PDF date stamps were Apr 27/28; actual on-disk PDFs are all Apr 29 with refreshed sizes (P1 988 KB, P2 680 KB, P3 28 MB, P4 25 MB). Updated all four cards. |

## Sweep methodology

```python
# /tmp/qa_sweep.py — verifies <img src>, <a href>, <link href>, <script src>
# for every site HTML page under /Users/houstongolden/Desktop/CODE_2025/bigbounce/
# - resolves relative paths against the page's directory
# - skips http(s)://, mailto:, anchors, javascript:, data:
# - skips dynamic template literals (${...}, '" + ... + "')
# - reports any path that doesn't resolve to an on-disk file
```

After all R31 fixes: `python3 /tmp/qa_sweep.py` returns `CLEAN — no broken refs`.

## Commits (this round)

1. `6e28477` — `fix(WT-1): handle missing infographic in beyond_big_bounce article`
2. `ab8571c` — `fix(WT-R31-1): correct broken spectrum image links in anomaly-explorer`
3. (queued) — `chore(WT-8 + WT-9): activity feed + paper.html refresh`

## Queued for follow-up

Nothing blocking. Potential v1.1 hardening:
- Add a CI step that runs `qa_sweep.py` on PRs (would have caught both WT-1 and WT-R31-1 at author time).
- Tighten `articles/program-visual-guide.html` § 2.1–2.5 prose now that the visual reference is gone (current rewrite is minimal).
