# P3 R29 — Brutal-honesty PRD referee with full PDF access (figures, tables, equations)

**Reviewer**: `Claude_brutal`
**Model**: `claude-opus-4-7 (in-session)`
**Input PDF**: `/Users/houstongolden/Desktop/CODE_2025/bigbounce/site/public/papers/paper3_anomaly_catalog_v3.1.88.pdf` md5=bbc52726 pages=26
**Input format**: NATIVE PDF + repo-side .tex/artifact verification (in-session, replaces failed API leg)
**Reviewer**: Houston-directed in-session sweep; covers upgraded EXT1-closure regressions

---

## Scope and method

API leg failed on credits. This leg replaces it: full read of `pipelines/p3_anomaly_engine/paper3_draft.tex` (v3.1.88, 1182 lines), cross-checked against the committed companion artifacts (`DATA_RELEASE_MANIFEST.md`, `r24conf_erosita_axis_sweep.json`, `r24conf_pod_session_batch.json`, `pathc_dedup/r23conf_dedup_audits.json`) and the upgraded EXT1-closure list in the v3.1.88 version-note block. Sweeps (1)–(14) standard; (15) abstract-last drift; (16) provenance/manifest reconciliation; (17) uncomputed claims; (18) standalone-reader; (19) effect sizes; plus pattern-008 closure-introduced-regression scan and the four named EXT1 closures (NANOGrav SMBHB caveat in both sections, Liang2023 ApJL 956 L6, "High-z QSO Candidates" heading rename, Appendix C "Legacy ... Superseded" labels).

Findings are tagged `P3-R29-CB-{ESS|E|M|m|n}-NN`, mapped to the upgraded pattern catalog where possible.


## ESSENTIAL findings

### P3-R29-CB-ESS-01 — eROSITA section-prose still quotes $S_{\rm BigAE}=1.084$ quantitatively after Table III caption declares the axis non-science
**Pattern**: pattern-008 (closure-introduced regression / partial-fix); pattern-002 (claim/caption mismatch within same section).
**Reference**: `paper3_draft.tex` line 453, §III.E (eROSITA DR1).
**Quote**: "the top anomaly (1eRASS~J053856.1$-$640457, $S = 1.084$) is near the LMC with no SIMBAD counterpart".
**Problem**: The EXT1 F1 BLOCKER closure upgraded the Table~\ref{tab:erosita_top} caption to "**Do not use $S_{\rm BigAE}$ as a continuous science data product or for score-weighted analysis; the committed, reproducible selection is the $n=298$ membership list**" (line 456). The same section's prose then headlines an *individual numerical* $S_{\rm BigAE}$ value ($1.084$) one paragraph earlier, presenting it as if it were a scientifically meaningful score. This is exactly the failure mode the caption was upgraded to prevent — and it is in the *same subsection*. Worse, the abstract block and the §III.E opening paragraph also quote the production threshold $0.259$ in headline numerical form. A reader who lands on §III.E first will treat $S=1.084$ as a published anomaly score before reaching the warning attached to the table. The fix should make §III.E prose mention the source by IAU name + "the rank-1 entry of the $n=298$ membership list" without printing the irreproducible $S_{\rm BigAE}$ value as a headline number, or print it parenthetically with "(see Table~\ref{tab:erosita_top} caption: $S_{\rm BigAE}$ is not a science data product)".
**Fix**: rewrite line 453 as `Headline finding: the rank-1 entry of the $n=298$ membership list (1eRASS~J053856.1$-$640457; $S_{\rm BigAE}$ irreproducible per Table~\ref{tab:erosita_top} caption) is near the LMC with no SIMBAD counterpart;`. Optionally drop the threshold value $0.259$ from §III.E and Table~\ref{tab:survey_summary} headline copy and replace with "fixed top-298 cap". This is the highest-leverage fix in the round because Table III was the EXT1 BLOCKER and the prose now contradicts the closure.

### P3-R29-CB-ESS-02 — abstract uses $S=1.084$-style reasoning by transitivity (eROSITA "298" headline) without flagging the non-science axis
**Pattern**: pattern-002 (abstract/body mismatch); pattern-015 (headline number without disclosure).
**Reference**: abstract (line 203) + Conclusions item 1 (line 739) + Table~\ref{tab:survey_summary} (line 330) + Table~\ref{tab:processing} (line 785).
**Quote (abstract)**: "the catalog contains $\mathbf{378{,}280}$ unique anomalies: $\mathbf{378{,}080}$ point-source object detections from six photometric/spectroscopic surveys plus $\mathbf{200}$ Planck CMB map-patch sky regions."
**Problem**: 298 eROSITA detections are folded into the 378,280 headline without the abstract ever disclosing that the eROSITA score axis is irreproducible and the EXT1 F1 BLOCKER reclassified the selection as "membership-only". An external reader reading only the abstract has no idea that the eROSITA tier's score axis is non-science. This is a *standalone-reader* regression (sweep 18) directly downstream of the Table III caption fix.
**Fix**: add to the abstract a half-sentence parenthetical: "(eROSITA tier released as a $n=298$ membership list only; per-object score axis non-reproducible; \S\ref{sec:erosita})". Mirror the same in Conclusions item 1.

### P3-R29-CB-ESS-03 — Companion-repo artifact path mismatch: `r23conf_dedup_audits.json` lives in `pathc_dedup/` subdir; paper text implies repo-root co-location
**Pattern**: pattern-016 (artifact path drift between paper and repo); /artifact-link-verify-class.
**Reference**: lines 517, 571 (text references `\texttt{r23conf\_dedup\_audits.json}` "in the companion repository" / "audit artifact \texttt{r23conf\_dedup\_audits.json}").
**On-disk verification**: file resolves only at `pipelines/p3_anomaly_engine/pathc_dedup/r23conf_dedup_audits.json` — not at `pipelines/p3_anomaly_engine/r23conf_dedup_audits.json`. By contrast, the paper's eROSITA + pod-session-batch artifacts are correctly co-located at `pipelines/p3_anomaly_engine/` root (verified).
**Problem**: a reader following the in-text reference to grep the companion repo will not find the file at the implied location. The text uses `\texttt{...}` rather than `\artifact{...}`, so `/artifact-link-verify` does not catch this — a hidden failure mode of the macro convention. Three sites (line 517, line 571 twice via "same artifact") all carry this drift.
**Fix**: replace the three `\texttt{r23conf\_dedup\_audits.json}` occurrences with `\artifact{pipelines/p3\_anomaly\_engine/pathc\_dedup/r23conf\_dedup\_audits.json}`. This both fixes the path and routes the URL through the verified-on-main hyperlink.


## MAJOR findings

### P3-R29-CB-E-04 — Abstract reorder is correct in spirit but loses the *upper-edge clipping* disclosure that justifies the "exactly returns single-tracer baseline" claim
**Pattern**: pattern-007 (math/derivation collapse during abstract compression); pattern-036 (math-mode caveat omitted in summary).
**Reference**: abstract (line 203) vs §V (line 633).
**Quote (abstract)**: "the de-biased point estimate returns the single-tracer baseline $\sigfnl^{\rm std} = 8.98$ exactly (\emph{no multi-tracer improvement at current S/N}); inserting the noisy $\hat\alpha$ into the Fisher-positivity-respecting form $1/\sigma^2(f_{\rm NL}) = F_0 + c\,\alpha^2$ gives a central forecast $\sigfnl = 8.14$ with $1\sigma$ envelope $[3.92, 8.98]$".
**Problem**: The reorder now leads with "exactly" returns the baseline, then quotes the envelope upper edge $8.98$ — but the abstract never tells the reader *why* the upper edge is $8.98$ rather than something larger. §V (line 633) explains: "the upper edge is $\hat\alpha - \sigma_\alpha = -0.46$ clipped at $\alpha = 0$, which returns the single-tracer baseline $8.98$." Without that one sentence the abstract's $[3.92, 8.98]$ envelope reads as a coincidence rather than as the structural consequence of the positivity-respecting form. A referee reading only the abstract will ask "why does the envelope cap at the baseline?" — and that question is the exact $\alpha^2$-positivity argument the abstract is trying to convey.
**Fix**: append to the abstract envelope clause: "(upper edge clipped at $\sigfnl^{\rm std}$ by the $\alpha^2$-positivity form when $\hat\alpha - \sigma_\alpha$ crosses zero)" — one parenthetical, 15 words, restores the structural logic.

### P3-R29-CB-E-05 — Abstract still calls 9.4% improvement a "forecast" while the de-bias result says it is *exactly zero*
**Pattern**: pattern-024 (two competing summary numbers in same paragraph without explicit precedence); pattern-014 (forecast language for null result).
**Reference**: abstract (line 203).
**Quote**: "the de-biased point estimate returns the single-tracer baseline ... exactly (\emph{no multi-tracer improvement at current S/N}); ... gives a central forecast $\sigfnl = 8.14$ ... (the central 9.4\% improvement is a noise-driven forecast pending higher-S/N follow-up, not a detection)".
**Problem**: This is internally consistent at the technical level (the de-bias is exact null; the convex-bias central forecast is $9.4\%$ "pending follow-up"), but the abstract reorder leaves the reader holding two summary numbers ($0\%$ improvement and $9.4\%$ improvement) without an explicit precedence statement on which is the headline. The reorder fixed the *ordering* but did not fix the *number that goes in a headline news-graf*. A press release writer reading this abstract will print "9.4% improvement, central forecast" because that is the only positive number quoted. Conclusions item 5 (line 747) has the same structural problem.
**Fix**: replace "the central 9.4\% improvement is a noise-driven forecast pending higher-S/N follow-up, not a detection" with "the de-biased result (no improvement) is the headline; the $9.4\%$ central-value figure is a convex-bias artifact of inserting a noisy $\hat\alpha$ into a positive form and is not a multi-tracer detection." This makes the precedence explicit. Mirror in Conclusions item 5.

### P3-R29-CB-E-06 — DATA_RELEASE_MANIFEST.md lists 13 parquet files but Data Availability text claims only 12 categories; one file is the ACT-quarantine block which is then claimed "archived separately"
**Pattern**: pattern-016 (provenance/manifest reconciliation, sweep 16); pattern-002 (Data Availability claim vs manifest mismatch).
**Reference**: `pipelines/p3_anomaly_engine/DATA_RELEASE_MANIFEST.md` (13 parquet entries verified by `grep -c parquet`) vs §Acknowledgments Data Availability paragraph (line 763).
**On-disk verification**: 13 parquet files in manifest: `pathc_unique_objects_no_act` + `pathc_unique_objects` + `pathc_multi_survey_matches_no_act` + `pathc_multi_survey_matches` + `cmb_native_anomalies` + `gaia_dr3_anomalies` + `lamost_dr10_pathc_native` + `neowise_anomalies` + `neowise_pathc_masked_anomalies` + `planck_cmb_anomalies` + `sdss_dr18_pathc_native` + `act_dr6_cross_transfer_anomalies` + `erosita_dr1_anomalies`. Two are explicit ACT-quarantine / cross-transfer ("act_dr6_cross_transfer_anomalies", "planck_cmb_anomalies" which is "cross-transfer baseline (200 patches; diagnostic only)"). Two more are "sensitivity check" with-ACT variants.
**Problem**: Data Availability says the Path-C catalog "is staged on HuggingFace" and "The $319{,}443$-anomaly cross-transfer baseline is preserved as an archival comparison artifact". But the manifest stages the cross-transfer baseline *parquet files alongside the Path-C primary files in the same dataset*, and §sec:act_appendix line 958 says ACT-with-dedup "is preserved as a sensitivity-check artifact in the companion data repository" — the manifest puts it *next to* the headline (`pathc_unique_objects.parquet`, sensitivity 378,480) rather than archived separately. A reader who pulls the HF dataset will see both files at the same directory level and could trivially mis-pull the 378,480 sensitivity file as the canonical one. The naming convention (`_no_act` suffix on canonical, *no* suffix on sensitivity) is brittle and inverts the usual "primary file has the unmarked name" expectation.
**Fix**: either rename the sensitivity files (e.g., add `_sensitivity_with_act_` prefix) in the HF stage *before* arXiv flip, OR amend Data Availability to make the file-naming convention explicit ("the canonical Path-C catalog is `pathc_unique_objects_no_act.parquet`; the unsuffixed `pathc_unique_objects.parquet` is the 378,480-object 8-way-with-ACT sensitivity check, not the headline"). Also note SHA-256 collision on lines for `pathc_multi_survey_matches_no_act` and `pathc_multi_survey_matches` (both `3605b16a939b1dc44c4cb76e96dcbb7411a6eeb5917d12567c4fbc35fc85e784`) — either the two files are byte-identical (in which case why are both staged?) or the manifest has a copy-paste error. Both possibilities are publication-blockers.

### P3-R29-CB-E-07 — Conclusions item 5 still references "SPHEREx 3–5σ detection" as a *projection* without inheriting the "depends on future execution" qualifier into the headline
**Pattern**: pattern-014 (forecast→detection language drift); pattern-017 (conclusion overruns abstract caveats).
**Reference**: line 747 (Conclusions item 5).
**Quote**: "A SPHEREx $3$--$5\sigma$ detection of $\fnl = -35/8$ is projected under the multi-tracer methodology of Heinrich \etal~\cite{Heinrich2023}; this forecast depends on future survey execution and anomaly-tracer calibration."
**Problem**: The "this forecast depends on future survey execution" closure-introduced qualifier is present, but it follows a leading sentence that uses "is projected" as if it were a scheduled outcome rather than a conditional forecast. Houston's EXT1 directive (per the v3.1.88 version note: "SPHEREx forecast labeled dependent on future execution") was followed in the abstract (no SPHEREx mention there at all — clean), but in Conclusions item 5 the "is projected" verb still reads as confident. Compare to §V.fnl text which uses "yields $3$--$5\sigma$ detection significance" (line 661) — same drift. A skim-reading press release writer pulls the "3–5σ" detection number and the conditional clause does not survive copy-paste.
**Fix**: replace "A SPHEREx 3–5σ detection ... is projected" with "Under the Heinrich \etal multi-tracer methodology, a SPHEREx forecast for $\fnl = -35/8$ at $3$--$5\sigma$ significance is conditional on future SPHEREx survey execution and on the empirical $\alpha$ being upgraded from the present $<1\sigma$-null to a measured value." The verb "projected" becomes "conditional on".

### P3-R29-CB-E-08 — eROSITA "membership-only" framing is asserted at Table III but the rate column in Table~\ref{tab:survey_summary} (line 330) still prints "0.03" as if it were a measured rate
**Pattern**: pattern-002 (caption-vs-table mismatch); pattern-008 (closure-introduced regression).
**Reference**: Table I (`tab:survey_summary`) row "eROSITA DR1 ... 298 ... 0.03 ... 68" (line 330) plus Note: "for three surveys (Planck, Gaia DR3, NEOWISE), the anomaly count reflects a fixed top-1% selection threshold rather than a data-driven detection rate; these surveys contribute predetermined counts to the overall catalog, and their 1.00% anomaly rates should not be interpreted as independent measurements".
**Problem**: The caption Note explicitly disclaims rate-interpretation for Planck/Gaia/NEOWISE (predetermined fixed-top-1%). eROSITA is now *also* a predetermined fixed-count (n=298 membership list, score axis irreproducible per EXT1 F1 closure). The Note still leaves "0.03" eROSITA rate untouched, implying it is a measured detection rate, while Table III caption simultaneously says the score axis is non-science. A reader scanning Table I sees four numbers in the Rate column (0.87, 3.38, 0.39, 0.03, 1.00, 1.00, 1.00) with no distinction between "measured" (DESI/SDSS/LAMOST/eROSITA-implied) and "imposed" (Planck/Gaia/NEOWISE-disclosed-in-note). eROSITA should be in the imposed category after the EXT1 closure.
**Fix**: amend the Table~\ref{tab:survey_summary} Note "for three surveys ... anomaly count reflects a fixed top-1% selection" to "for **four** surveys (Planck, Gaia DR3, NEOWISE, *and eROSITA DR1 post-EXT1-membership-only reclassification*), the anomaly count reflects a fixed-count or fixed-percentile selection..." Replace the bare "0.03" rate column entry with "0.03$^\S$" routed to a footnote that mirrors the Table III caption's membership-only language.


### P3-R29-CB-E-09 — "High-z QSO Candidates" heading rename is correct, but body text still claims "12 candidates" without acknowledging "Confirmed→Candidates" downgrade in the *narrative*
**Pattern**: pattern-009 (heading-vs-body residual after rename); pattern-008 (closure-introduced regression).
**Reference**: §sec:highz (line 395, "High-$z$ QSO Candidates" — heading correctly de-Confirmed per EXT1 F18) vs body lines 397–399.
**Quote (body)**: "Applying these three cuts to the full 195,829 DESI anomaly catalog yields 12 candidates with $z = 6.0$--$6.23$." ... "The highest-Z-arm-dominance objects (TARGETID 39633191367084936, $z = 6.20$, $r_Z = 5.30$; TARGETID 39628507709444221, $z = 6.22$, $r_Z = 5.18$) show compact point-source morphology consistent with unresolved quasars."
**Problem**: The heading was changed from "Confirmed High-z QSO Candidates" to "High-z QSO Candidates" because no independent follow-up spectroscopy exists (per the v3.1.88 closure note F18: "internal evidence only, no independent follow-up spectroscopy"). The body text still asserts redshift values ($z = 6.0$–$6.23$, $z = 6.20$, $z = 6.22$) as *measured* numbers without acknowledging that those redshifts are *anomaly-pipeline-inferred* rather than spectroscopically confirmed by an independent reduction. A reader still treats "z = 6.20" as a confirmed measurement. The heading rename without body refit is a half-closure.
**Fix**: amend the first sentence to "Applying these three cuts ... yields 12 *anomaly-pipeline-inferred* candidates at *pipeline-redshift* $z = 6.0$–$6.23$; no independent follow-up spectroscopy has been performed, and these redshifts should be read as anomaly-pipeline outputs pending spectroscopic confirmation." Mirror in the TARGETID sentence.

### P3-R29-CB-E-10 — "Appendix C" Legacy/Superseded label is correct on §A.3 heading + Table~\ref{tab:sensitivity} caption, but Fig.~\ref{fig:fnl_improvement} (§V, line 638) still labels itself "fixed-α=0.15 reference" without "superseded" qualifier
**Pattern**: pattern-009 (multi-surface rename incomplete); pattern-011 (figure caption autonomy).
**Reference**: heading line 823 ("Legacy Fixed-$\alpha=0.15$ Sensitivity Reference (Superseded)") and Table~\ref{tab:sensitivity} caption line 836 ("Legacy fixed-$\alpha$ reference (superseded by empirical $\alpha_{\rm jk}$ result of §V)") — both correctly labeled. But Fig.~\ref{fig:fnl_improvement} caption line 638 reads "Per-redshift-bin decomposition of the fixed-$\alpha = 0.15$ \emph{reference} Fisher forecast (Appendix~\ref{app:sensitivity})" — *reference* not *legacy reference (superseded)*.
**Problem**: The figure is the most-shared standalone-asset (e.g., on a slide deck or a press summary) and lacks the "superseded" qualifier that §A.3 and Table~\ref{tab:sensitivity} both carry. A figure pulled out of the paper for a talk will read as if the fixed-α forecast is the primary result. This is the standalone-reader (sweep 18) failure mode for figures.
**Fix**: change Fig.~\ref{fig:fnl_improvement} caption opening to "Per-redshift-bin decomposition of the **legacy** fixed-$\alpha = 0.15$ reference Fisher forecast (Appendix~\ref{app:sensitivity}; **superseded** by the empirical $\alpha_{\rm jk}$ result of \S\ref{sec:fnl})". Two words, one fix.

### P3-R29-CB-E-11 — NANOGrav SMBHB environmental caveat is present in both §nanograv and §bounce_implications, but the abstract's $B_{\rm MB/SMBHB} = 7.14 \times 10^3$ "decisive" claim is qualified as "prior-sensitive and population-mean reference" only, *not* environmental-flattening
**Pattern**: pattern-014 (caveat present in body, missing from abstract); pattern-002 (abstract/body mismatch).
**Reference**: abstract (line 203, end) vs §nanograv (line 666–667, "SMBHB environmental caveat") vs §bounce_implications (line 729).
**Quote (abstract)**: "Savage-Dickey $B_{\rm MB/SMBHB} = 7.14\!\times\!10^3$ under the flat $\gamma\in[0,7]$ prior; prior-sensitive by construction, and the SMBHB $\gamma=4.33$ is a population-mean reference value rather than a sharp prediction".
**Problem**: The abstract qualifier addresses prior-sensitivity + population-mean, but does *not* mention environmental flattening (stellar scattering, eccentric binaries) that the EXT1 F24/F8 closure added to both §nanograv and §bounce_implications. The single most-cited physical objection to a decisive Bayes factor against SMBHB is environmental flattening to $\gamma \sim 2.5$–$3$ — which is exactly the recovered range. The abstract reads "decisive vs population-mean SMBHB" when the body reads "decisive only vs idealized circular-orbit SMBHB; environmentally flattened SMBHB can produce the same $\gamma$ we recover." A press release / arXiv summary writer pulls the $7.14 \times 10^3$ number and the environmental caveat does not survive copy-paste.
**Fix**: append to the abstract Bayes-factor parenthetical: "(decisive only against the idealized circular-orbit reference; environmentally flattened SMBHB models with eccentric binaries or stellar scattering can produce $\gamma \sim 2.5$–$3$ overlapping the recovered range — see \S\ref{sec:nanograv})". ~25 words; mirrors the §bounce_implications text.


## MINOR findings

### P3-R29-CB-M-12 — Effect-size disclosure: $\sim 17.8\%$ "genuine novelty fraction" stratum is 178/1,000 on the *DESI top-1,000* score stratum; the abstract qualifier is correct but the body table (Limitations item 6) re-uses the bare 17.8% in a way that can extrapolate
**Pattern**: pattern-018 (point-estimate-with-CI without N exposed at every site); sweep (19) effect sizes.
**Reference**: abstract (line 203); §discussion line 685 (Limitations item 6); Conclusions item 2 (line 741); §sec:simbad (line 514).
**Quote (Limitations 6)**: "the genuine novelty fraction ($\sim\!17.8\%$) is a single-sample point estimate at the top-$1{,}000$ score stratum carrying a Wilson 68\% binomial sampling interval of $\pm 1.2\%$ only".
**Problem**: The "$\pm 1.2\%$ only" is a *sampling* interval at fixed $N=1{,}000$. The text never quantifies the *extrapolation* envelope from the top-1,000 score stratum to the full $195{,}829$-object DESI catalog or the $378{,}080$-object point-source tier. Effect-size literature would expect a sensitivity bound: "if the novelty fraction at rank-$N$ scales as a power law over the score axis, the full-catalog novelty bound is $\sim X\%$–$Y\%$." Without that, every downstream forecast that multiplies "17.8% × 378,080" implicitly extrapolates.
**Fix**: add one sentence to Limitations item 6: "the full-catalog extrapolation is empirically untested; if the novelty fraction is monotone in score, $17.8\%$ is an upper bound and the full-catalog mean would be lower." Then in the abstract clause "full-catalog rate empirically untested" — explicitly state the direction of the bound (upper).

### P3-R29-CB-M-13 — "abstract-last drift" sweep (15): the abstract reorder preserved every f_NL number but introduced a new ordering hazard with the NANOGrav $\gamma$-MCMC block placed *after* the f_NL block
**Pattern**: pattern-019 (cross-block ordering); sweep (15) abstract-last drift.
**Reference**: abstract (line 203).
**Problem**: The reorder put the de-biased null first, then the convex-bias central $8.14$ envelope, then the NANOGrav $\gamma$-MCMC, then "catalog, model weights, and reproducibility scripts will be publicly released". The reader's last impression is "data release" — but the previous-to-last impression is the NANOGrav $\gamma$ Bayes factor $7.14 \times 10^3$. The strongest *positive*-looking number in the abstract is now NANOGrav's $7.14 \times 10^3$, not the de-biased null or the $8.14$ central. This is a soft-but-real audience-perception effect that the reorder did not anticipate.
**Fix**: optional — add a single transition phrase "Both cosmological applications (§V f_NL and the NANOGrav GWB consistency below) are presented as illustrative tracer applications rather than detections." Place it just before the NANOGrav block.

### P3-R29-CB-M-14 — Sweep (17) uncomputed claims: the dedup-radius sweep $\{3'', 5'', 7''\}$ → $\{378{,}604 / 378{,}280 / 378{,}145\}$ is reported but the *0.086\%* maximum variation is not propagated into the abstract or Conclusions item 1
**Pattern**: pattern-020 (sensitivity-computed-but-not-quoted-in-summary); sweep (17) uncomputed-vs-uncited.
**Reference**: §sec:crossmatches line 569 ("maximum unique-count variation of $0.086\%$ relative to the canonical $5''$ result"); abstract / Conclusions item 1.
**Problem**: This is a genuine R-round-closed sensitivity calculation that strengthens the headline (the 378,280 count is robust to dedup-radius choice at $<0.1\%$). It is correctly computed and lives in §crossmatches but never surfaces in the abstract or Conclusions, where it would close the "but how robust is your dedup?" reviewer question pre-emptively. This is the inverse of pattern-014: a *strengthening* caveat that should be quoted but isn't.
**Fix**: append to Conclusions item 4 a half-sentence: "Unique-object headline robust to dedup-radius choice: $378{,}604 / 378{,}280 / 378{,}145$ at $\{3'', 5'', 7''\}$, $\Delta_{\max} = 0.086\%$."

### P3-R29-CB-M-15 — Pattern-008 closure-introduced-regression scan: F19 Liang2023 ApJL 956 L6 update is correctly applied (line 1022), but the EXT1 wave introduced a math-mode whitespace artifact in the abstract's `F_0 + c\,\alpha^2` form
**Pattern**: pattern-008 (closure-introduced regression); pattern-021 (math-mode spacing).
**Reference**: abstract (line 203, "Fisher-positivity-respecting form $1/\sigma^2(f_{\rm NL}) = F_0 + c\,\alpha^2$"); §V (line 633, identical form); EXT1 wave note: "F11/F7: conclusions item 5 reordered ... abstract f_NL sentence reordered".
**Problem**: The abstract math now uses `f_{\rm NL}` inside $\sigma^2(\cdot)$ while §V uses `\fnl` (the custom command). Both render identically in revtex but a `grep` for "fnl" misses the abstract instance — a tooling consistency drift introduced by the reorder. Low risk for the paper but a real maintenance hazard for downstream grep-based claim-replacement (`/bigbounce-claims-table-sync`).
**Fix**: standardize on `\fnl` everywhere (replace 3 `f_{\rm NL}` instances in abstract). Not a science change; tooling consistency only.

### P3-R29-CB-M-16 — Sweep (18) standalone-reader: Fig.~\ref{fig:injection_recovery} (line 716) caption reuses "PASS" three times including for NEOWISE, which the in-text §sec:neowise and §sec:pathc_caveats(ii) explicitly downgrade to "mask-geometry QA check, passes by construction"
**Pattern**: pattern-022 (figure-vs-text PASS terminology drift).
**Reference**: line 718 figure caption ("Three surveys PASS the gate at 5σ: SDSS DR18 continuum-dip (PASS, 64%), Planck CMB native (PASS, 500/500 = 100% at 5σ), and NEOWISE ecliptic-pole mask (PASS, 1000/1000 = 100%...)").
**Problem**: The caption does include the parenthetical "this gate passes by construction and is a masking-geometry QA check rather than a detector-sensitivity test", but the headline structure "Three surveys PASS the gate" sells NEOWISE as a detector-PASS at first glance. A figure exported standalone (poster, slide, news graphic) loses the parenthetical and keeps the headline. Consistent with the abstract's framing of "3 PASS" but exposes the same standalone-reader regression.
**Fix**: change figure caption opening to "**Two surveys PASS the detector-sensitivity gate at 5σ + one mask-geometry QA gate passes by construction:** SDSS DR18 continuum-dip (detector PASS, 64%), Planck CMB native (detector PASS, 100%), and NEOWISE ecliptic-pole mask (geometry-QA PASS by construction)". This makes the 2+1 decomposition explicit in the headline rather than parenthetical.

### P3-R29-CB-M-17 — DATA_RELEASE_MANIFEST.md SHA-256 collision flag (sub-finding of E-06)
**Pattern**: pattern-016 (provenance manifest integrity).
**Reference**: `DATA_RELEASE_MANIFEST.md` lines for `pathc_multi_survey_matches_no_act.parquet` and `pathc_multi_survey_matches.parquet`.
**On-disk verification**: both lines list SHA-256 `3605b16a939b1dc44c4cb76e96dcbb7411a6eeb5917d12567c4fbc35fc85e784`.
**Problem**: identical SHA-256 means the files are byte-identical. If they are intended to be byte-identical (i.e., the with-ACT 637-cluster multi-survey set has zero positional ACT overlaps per §planck_act_null), the manifest should say "ACT contributes zero multi-survey overlaps; the no-act and with-act multi-survey-matches parquets are byte-identical and we stage both for naming-consistency only". If they are not intended to be identical, this is a manifest bug. Either way the manifest needs one disambiguating line.
**Fix**: amend manifest to either drop the duplicate or annotate "byte-identical to {other}; ACT zero-overlap by §planck_act_null".


## NITS

### P3-R29-CB-n-18 — Abstract bolding: `\mathbf{378{,}280}`, `\mathbf{378{,}080}`, `\mathbf{200}` are bolded but `\mathbf{269{,}000}` / `\mathbf{269{,}317}` / `\mathbf{269{,}117}` / `\mathbf{108{,}963}` are not — readers will treat unbolded numbers as secondary.
**Fix**: either bold all four catalog-grade sub-counts or remove the bold from the first three. Consistency only.

### P3-R29-CB-n-19 — Line 723 (§Comparison with Prior Work) opening "Our DESI anomaly rate of 0.87% is consistent with the 1.07% rate reported by Liang et al." quotes "1.07%" — the Liang2023 entry is correctly updated to ApJL 956 L6 but the *rate value* 1.07% should be re-verified against the corrected journal entry (ApJL 956 L6, not MNRAS 525,1078).
**Fix**: re-verify the 1.07% rate against the ApJL version. Low-risk but a one-line cross-check.

### P3-R29-CB-n-20 — Conclusions item 4 line 745 "Planck×ACT cross-correlation: null (largely expected from disjoint footprints; §planck_act_null)" — phrasing "largely expected" is softer than §planck_act_null which says "largely guaranteed by footprint geometry alone". Use the stronger body language in the conclusion.

### P3-R29-CB-n-21 — Eq.~\ref{eq:score_raw} label "MSE(x) = (1/N) sum (x_i - x̂_i)^2" — N is documented as "input dimensionality" but the abstract and §sec:training also refer to N as the validation-pool size. Tooling/readability nit; not ambiguous in context.

## Summary recommendation

**Counts**: 3 ESSENTIAL (ESS-01, ESS-02, ESS-03) + 8 MAJOR (E-04 through E-11) + 6 MINOR (M-12 through M-17) + 4 NIT (n-18 through n-21) = **21 open findings on v3.1.88**.

**Round verdict**: **NOT CLEAN — minor revision before any external journal submission**.

**Justification**:

1. **The EXT1 closure wave is structurally correct but partially regresses at three high-leverage sites** — the eROSITA Table III "membership-only" caption is contradicted by §III.E body text that still headlines $S = 1.084$ (ESS-01); the abstract folds eROSITA into the 378,280 headline without disclosing the non-science axis (ESS-02); and the SHA-256-collided manifest entry (E-06 / M-17) is a publication-blocker on the companion data product. These three findings are all *direct downstream* of EXT1 closures that were applied in *one* place but not propagated to the *other* surfaces the same physical claim appears on. They are exactly pattern-008 (closure-introduced regression) — which the EXT1 wave should have caught at the `/bigbounce-post-bump-sync` stage.

2. **The four named EXT1 closures verify against on-disk evidence**: Liang2023 → ApJL 956 L6 confirmed at line 1022 (bibitem); NANOGrav SMBHB environmental caveat present in both §nanograv (lines 666–667) and §bounce_implications (line 729); "High-z QSO Candidates" heading rename present at line 395 (no "Confirmed"); Appendix C "Legacy ... Superseded" label present at line 823 and Table~\ref{tab:sensitivity} caption at line 836. These four are all clean. But the *body text* downstream of three of those four still carries the pre-closure framing (E-09 high-z body still uses "confirmed-style" z = 6.20; E-10 Fig.~\ref{fig:fnl_improvement} caption lacks "superseded"; E-11 abstract lacks environmental caveat for the Bayes factor). The closures hit the headings/captions/bib but not all the body sites.

3. **No publication-blocker science errors**: every quantitative claim I cross-checked against the artifacts (378,280 / 269,317 / 0.086% radius variation / $\bar J = 0.862$ / $\sigfnl = 8.14$ with envelope [3.92, 8.98] / $\gamma = 2.567 \pm 0.382$ / $B_{\rm MB/SMBHB} = 7.14 \times 10^3$ / $\alpha_{\rm jk} = 0.19 \pm 0.65$) is internally consistent and matches the upgraded EXT1-closure version note. The de-bias argument, the Fisher-positivity-respecting $\alpha^2$-form, the envelope-clipping derivation, and the cluster-accounting reconciliation ($9{,}553$ multi-member clusters, $\sum(\text{size}-1) = 10{,}213$, $637$ multi-survey) all check out arithmetically. The novel value-add of v3.1.88 over v3.1.87 is the Table III + §III.E + abstract eROSITA reframing and the NANOGrav SMBHB environmental caveat; both are real improvements and would be fully clean with the ESS-01 / ESS-02 / E-11 single-paragraph fixes.

4. **Recommendation for the v3.1.89 patch wave**: address ESS-01 / ESS-02 / ESS-03 + E-04 / E-06 / E-08 / E-11 in the same atomic commit (these are the lowest-cost, highest-leverage fixes), defer the remaining MAJORs (E-05 abstract precedence, E-07 Conclusions "projected", E-09 highz body language, E-10 Fig caption "superseded") to the v3.1.90 wave if a 2-stage close is preferred, and treat the MINORs + NITs as polish for the pre-arXiv-flip pass. With those 7 fixes the paper would survive a hostile external R-round and is publishable to PRD as a methods + multi-archive catalog paper. The forecast / NANOGrav blocks are correctly framed as illustrative tracer applications, not detections — that framing has held across three R-rounds and is the right scope for this submission.

**Round classification (per /readiness-cap-99)**: Stage-2 cross-vendor R-round result. Paper readiness should *oscillate backward* from the post-EXT1 high-water mark by the ESS-01 / ESS-02 magnitude (≈ −3pp) until ESS-01–03 + E-06 close — consistent with the "readiness must oscillate forward/backward" standing directive. After the v3.1.89 patch wave (7 fixes above), readiness should re-rise to the prior level; an additional cross-vendor round at v3.1.89 is required before any 99% award.
