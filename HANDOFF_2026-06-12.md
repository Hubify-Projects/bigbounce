# Handoff — 2026-06-12 ~03:30 PT

**Read this first.** Then `cat CLAUDE.md project-context/SSOT/index.md`, then resume.

## State of the world

Six-paper publish push, mid-loop. **Loop: external review (browser) → harvest → truth-audit → close → restamp → internal confirmation round → close → repeat.** Five external rounds done, one in flight.

**Current versions (live, mirrored, Convex-bumped on both deployments, in `site/public/papers/`):**

- P1A `v1A.0.65` · arxiv/paper1a_ech_nogo.tex · md5 `418777c6`
- P1B `v1B.0.62` · arxiv/paper1b_mcmc_companion.tex · md5 `e0066b42`
- P2  `v1.7.57`  · research/focused_paper_source_integration/02_full_draft.tex · md5 `cb95f253`
- P3  `v3.1.100` · pipelines/p3_anomaly_engine/paper3_draft.tex · md5 `39c00ff6`
- P4  `v1.0.179` · pipelines/p2_chirality/chirality_catalog_paper.tex · md5 `3ba688c1`
- P5  `v0.1.69`  · pipelines/p5_desi_chirality/paper/p5_desi_chirality.tex · md5 `8a6e800f`

All six 0 errors / 0 undef refs. Tarballs `paper*_arxiv_v*.tar.gz` co-located with each tex. Site verified live (`curl -I https://bigbounce.hubify.app/papers/<file>.pdf` → 200 on every versioned mirror).

**External verdict ladder, EXT1→EXT5:**
- Grok Heavy: 6/6 ACCEPT × 3 consecutive rounds
- Gemini Thinking: posted its first ACCEPTs at EXT5 (P2, P4)
- ChatGPT Pro Extended: MAJOR ×6 with shrinking substance — its real catches now are mostly regressions in our own closure waves

**Gap series (genuinely-new substantive findings per round):** 60 → 32 → 27 → 13 → 19 (the EXT5 uptick was ~5 self-inflicted; chains-recomputed numbers entered all subsequent fixes).

## What is in flight RIGHT NOW

**EXT6 submitted and waiting to be harvested.** Submission 02:35–03:03 PT 2026-06-12.

All 18 chat threads contain a delta-prompt + the v1A.0.65 / v1B.0.62 / v1.7.57 / v3.1.100 / v1.0.179 / v0.1.69 PDFs. Gemini P3 was moved to a **fresh thread** (full referee prompt, not delta) after three rounds of reading stale v3.1.91 content. Manifest: `project-context/peer-reviews/EXT6_BROWSER_MANIFEST.md`.

The vendors are now generating reports on their own servers (not your tokens). Open the chats from ~03:35 PT onward.

## How to resume in a new session

```
cd /Users/houstongolden/Desktop/CODE_2025/bigbounce
# 1. Read the handoff + project rules
cat HANDOFF_2026-06-12.md
cat CLAUDE.md
cat project-context/SSOT/index.md

# 2. (Optional) Skim the loop's home skill
cat ~/.claude/scistack/astrostack/external-review-browser-loop/SKILL.md
```

Then say to the new Claude/Codex session: *"Resume from HANDOFF_2026-06-12.md — harvest EXT6."*

## Next concrete step — EXT6 harvest

Chat URLs are identical to EXT1–EXT5 manifests (`project-context/peer-reviews/EXT{1,2,3,4,5}_BROWSER_MANIFEST.md`). One exception: Gemini P3 fresh thread = `https://gemini.google.com/app/2b33106610ec2401`.

Harvest pattern (the bash flow that has worked all night):

```bash
B=$HOME/.claude/skills/gstack/browse/dist/browse
# ChatGPT (last assistant message):
$B goto "<chat-url>"; sleep 11
$B js 'const m=[...document.querySelectorAll("[data-message-author-role=\"assistant\"]")]; m[m.length-1].innerText'
# Grok:
$B js 'const m=[...document.querySelectorAll("[class*=\"message-bubble\"]")]; m[m.length-1].innerText'
# Gemini:
$B js 'const m=[...document.querySelectorAll("model-response")]; m[m.length-1].innerText'
```

**MANDATORY before every scrape: verify `$B js 'location.href'` matches the target chat URL.** Cross-chat contamination happened once at EXT4.

Save each report verbatim to `project-context/peer-reviews/EXT6_<paper>_<Provider>.md` with a header citing chat URL + PDF + harvest timestamp.

After harvest: write per-paper truth-audits (the standard verdict schema VERIFIED/PARTIAL/OPINION/STALE/FALSIFIED/HOUSTON-DECISION + auto-falsify rules — see `EXT5_P*_TRUTH_AUDIT.md` for templates), then closures, then restamp bundle, then internal confirmation round (`tools/v3_native_pdf_review.py`), repeat.

## Hard-won protocols (DO NOT skip)

1. **Chains and counts are the only truth.** If a number can be recomputed from a committed chain/parquet/json, recompute it instead of trusting an audit. The R35conf wave caught two audit estimates that way (ΔNeff 0.27 → 0.40; P5 2.7% → 3.56%).
2. **Closure-agent mandatory verification.** Every closure-wave sub-agent must run `git diff --stat` + grep each inserted phrase + grep each "old phrase gone" check. The persistence-failure class (changelog says applied, body still has old text) bit us at EXT5 — rule encoded.
3. **Pattern-052 (re-raise vindication).** A reviewer re-raising a previously FALSIFIED finding only auto-falsifies if the prior falsification cited primary tex/artifact evidence. If the prior dismissal was assumption-based, re-verify.
4. **Fisher F₀ = 1/8.98² is a known PDF extraction artifact.** "1/8.98²" extracts as "1/8.982". Auto-falsify; the tex always wins. (Falsified 6 rounds running; R35conf was the first round nobody raised it.)
5. **Version-decimal collisions** ("z=−18.1.34" etc.) are renderer artifacts — verify against tex source.
6. **June 2026 IS the current month.** arXiv 25xx/26xx IDs are valid. HD-6 (correction notes) and HD-11 (DOI placeholders) are submission-day decisions, not findings.
7. **PDF browser-automation recipe** (battle-tested, encoded in `~/.claude/scistack/astrostack/external-review-browser-loop/SKILL.md`): use the logged-in "Google Chrome for Testing" profile via gstack `$B = ~/.claude/skills/gstack/browse/dist/browse`. ChatGPT: `#prompt-textarea` + `input[type="file"]:not([accept*="image"])` + `[data-testid="send-button"]`. Grok: `Heavy` effort verified, `@e` refs from `snapshot -i`. Gemini: NO DOM file input — native osascript dialog with FRONTMOST GUARD (`if frontApp is not "Google Chrome for Testing" then return "ABORT"`) + Escape-first + `Cmd+Shift+G` + path + Enter ×2. Wait ≥12s post-goto before any ChatGPT upload. Verify chip rendered AND sheet count = 0 after Gemini dialog.

## Other watchers that were running

**c15 pod MCMC** (P1B w₀wₐ addendum): RunPod pod ID `5i2td3deu3hojr`, currently SSH `root@194.26.196.210 -p 34435` (port drifts on restart — re-query via GraphQL: `curl -X POST https://api.runpod.io/graphql -H "Authorization: Bearer $RUNPOD_API_KEY" -d '{"query":"query{pod(input:{podId:\"5i2td3deu3hojr\"}){runtime{ports{ip publicPort privatePort}}}}"}'`). Latest checkpoint: `R−1 ≈ 0.050` (not converged; needs < 0.015). When converged: retrieve chains, write P1B addendum, run `/pod-backup-before-stop`, stop pod. Compute spec: `project-context/SSOT/compute-queue.md` + `project-context/COMPUTE_QUEUE.md` §3.

**Queued compute (not running):**
- COMPUTE_QUEUE §6: P3 Planck top-200 held-out re-scoring (needs Planck native checkpoint reproduced on A100 pod, ~2h; no paper claim depends on it).
- COMPUTE_QUEUE §2 NEOWISE/Gaia legs of FM1 (pod-side feature tables only; eROSITA leg already done).

## For your own context

- **Anthropic API credits exhausted** earlier in the run — the v3_native_pdf_review Claude leg has been failing on every round and routing to fallback. Top up if you want a 5th internal vendor. Direct keys for all 5 vendors are in `youmd/.env.local` / `bigbounce/.env.local` (Anthropic, OpenAI, Gemini, Grok/xAI, Perplexity).
- **GitHub PAT rotation pending** — the security audit (`project-context/SECURITY_BACKUP_AUDIT_2026-06-11.md`) flagged a live PAT in local `.git/config` for ceo-agents/lead-agents/ui-agents repos. PATs stripped from remotes already; the actual token should be rotated at github.com/settings/tokens. 2-minute action.
- **Sign-off package**: `project-context/SSOT/SIGNOFF_PACKAGE_2026-06-11.md` has the per-paper checkbox list with versions/audit links. P2 now has effective 3-vendor ACCEPT/MINOR support after the v1.7.57 Chaussidon fix; P4 had Gemini ACCEPT at EXT5. Worth re-reading after EXT6 truth-audits land.

## Backups

All instructions/skills/CLAUDE.md/memory backed up to private `houstongolden/agent-stack-backup` repo via `~/agent-stack-backup/sync-backup.sh`. Scistack also pushed (was 14 ahead during this run). Bigbounce repo is fully public-history-clean — no secrets in 2,224 commits.

## Cost note for the new session

The cheapest way to continue this loop is:
1. Have the next session do harvest + truth-audit + closure ONE PAPER AT A TIME (not 6 parallel agents).
2. Run the internal confirmation rounds via `tools/v3_native_pdf_review.py` (vendor APIs, not Claude session tokens) — those cost ~$0.30/paper at full 5-vendor (now 4 since Anthropic is out).
3. Skip the Claude in-session Opus fallback leg — pattern-052 + the 3 remaining vendors cover the gap.

The closure waves themselves are cheap. The expensive thing is paging huge audit reports + closure agents + truth-audits through the main Claude conversation. A new session should harvest → truth-audit → close in tight slices, not megapassed.

---

Good luck. Everything is on disk; the loop knows what it's doing.
